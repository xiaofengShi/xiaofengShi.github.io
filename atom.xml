<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>挖掘机成长日记</title>
  
  <subtitle>LearningClub</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://xiaofengshi.com/"/>
  <updated>2020-02-25T09:41:20.918Z</updated>
  <id>http://xiaofengshi.com/</id>
  
  <author>
    <name>ShiXiaofeng</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>AwesomesErrors</title>
    <link href="http://xiaofengshi.com/2020/02/25/AwesomesErrors/"/>
    <id>http://xiaofengshi.com/2020/02/25/AwesomesErrors/</id>
    <published>2020-02-25T09:13:32.000Z</published>
    <updated>2020-02-25T09:41:20.918Z</updated>
    
    <content type="html"><![CDATA[<p>本篇主要记录深度学习过程中遇到的各种bug，error以及对应的解决方法。</p><a id="more"></a><h3 id="WARNING-root-NaN-or-Inf-found-in-input-tensor"><a href="#WARNING-root-NaN-or-Inf-found-in-input-tensor" class="headerlink" title="WARNING:root:NaN or Inf found in input tensor."></a>WARNING:root:NaN or Inf found in input tensor.</h3><p>这个报错一版搜索的时候出现的是网络训练的时候Loss变为Nan的解决办法，通用的有四种</p><ol><li>降低学习率</li><li>加入正则化</li><li>进行梯度截断</li><li>检查数据</li></ol><p>我遇到的这个问题的场景是在训练一个模型<code>vlbert</code>,输入是数据包含图像和对应的图像描述，并且我的模型中包含正则化和梯度截断，因此大概率是数据的原因，下面是我进行debug的过程</p><ol><li><p>固定shuffle的种子，检查数据是否出现为nan的情况，在pytorch中，使用如下程序进行对dataset的数据进行检查</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">check_tensor</span><span class="params">(self, vector, name=None)</span>:</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">if</span> isinstance(vector, torch.Tensor):</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> np.any(np.isnan(vector.cpu().detach().numpy())):</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">            print(<span class="string">"[] is false"</span>.format(name))</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line">        <span class="keyword">elif</span> isinstance(vector, np.ndarray):</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> np.any(np.isnan(vector)):</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">            print(<span class="string">"[] is false"</span>.format(name))</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line">        <span class="keyword">elif</span> isinstance(vector, list):</span><br><span class="line">            vector = np.asarray(vector, dtype=np.float32)</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> np.any(np.isnan(vector)):</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">            print(<span class="string">"[] is false"</span>.format(name))</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> ex:</span><br><span class="line">        logger.error(traceback.format_exc())</span><br><span class="line">        logger.error(<span class="string">"name is [&#123;&#125;] value is &#123;&#125;"</span>.format(name, vector))</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">False</span></span><br></pre></td></tr></table></figure><p>加入了数据check部分，使用同样的参数配置进行再次训练，发现在上次同样的迭代次数的时候又出来了nan的错误，但是这一个部分的输入数据并没有触发nan错误</p></li><li><p>检查模型输出数据是否为nan</p><p>在模型数据输出部分，该部分数据还没有进行loss计算，只检查这部分数据是否为nan，检查发现，在这个迭代次数出现nan错误的时候，模型的输出为nan</p></li><li><p>调低学习率</p><p>降低学习率，实际上这个时候的学习率是很低的1e-7，调低到1e-10，这个错误还是没有解决</p></li><li><p>确信以及肯定是数据的问题，但是就是没找到问题所在</p><ol><li><p>在dataset的<code>__geiitem__</code>程序中记录当前的<code>index</code>，并将这个数传递到后续的模型计算过程中，这样再出现错误的时候就能根据<code>index</code>取到原始数据，再次同样参数运行程序，找到了出现问题的的这一组batch中的index是谁，因为我是使用的单机多卡训练的，因此为了保险起见，我把出现错误的前面四个batch的数据全部记录下来</p></li><li><p>check数据，这一组记录的数据进行check，设置为batch=1，发现里面有一个数据会触发nan的错误，检查这个原始数据，在原始数据中记录的是这个样本的图片的url以及对应的图片描述，为了以防万一，打开了图片链接查看，惊喜出现了 <a href="https://sf1-ttcdn-tos.pstatp.com/obj/image-pair-videos/image_text_cc_dataset_BM5B_10A2Pglobal001jpglargewid1250_157795348409_jrlVRNYLF" target="_blank" rel="noopener">https://sf1-ttcdn-tos.pstatp.com/obj/image-pair-videos/image_text_cc_dataset_BM5B_10A2Pglobal001jpglargewid1250_157795348409_jrlVRNYLF</a> 这是这个样本的链接，发现这是一张不正常的图片，查看这个图片记录的其他信息为宽度=0，高度=0，终于找到问题了，如果图片的宽度或者高度为0，那么后续的处理过程中，会使得模型中某个数据变为0，这个数据碰巧又在分母的位置上，导致了nan的错误，把这个数据删除之后一切正常了，天啦撸，太难了</p></li></ol></li><li><p>这个问题debug了一天多，因为出发nan的时候在一个epoch的中间位置，一开始没有相应的记录，很难找到出错的样本，实际上针对梯度爆炸或者梯度小时的问题，开始在模型设计的时候就把该加的都加了经过这个事情，应该学会一下几点</p><ol><li>数据要清洗，针对图像数据，就要看图像是否正常，针对文本数据就要看文本是否为空</li><li>dataset中最好记录index，能进行反向追溯</li></ol></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本篇主要记录深度学习过程中遇到的各种bug，error以及对应的解决方法。&lt;/p&gt;
    
    </summary>
    
      <category term="Awesomes" scheme="http://xiaofengshi.com/categories/Awesomes/"/>
    
    
  </entry>
  
  <entry>
    <title>深度学习-ConvNet-SENet</title>
    <link href="http://xiaofengshi.com/2020/02/17/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-ConvNet-SENet/"/>
    <id>http://xiaofengshi.com/2020/02/17/深度学习-ConvNet-SENet/</id>
    <published>2020-02-17T14:05:43.000Z</published>
    <updated>2020-02-17T14:37:45.613Z</updated>
    
    <content type="html"><![CDATA[<img src="/2020/02/17/深度学习-ConvNet-SENet/SE-pipeline.jpg"><p>本篇主要记录一下2017年ILSVR竞赛的冠军-SENet，这是一种网络的block结构，可以有效的结合到InceptionNet，Resnet中。</p><a id="more"></a><h2 id="Block结构"><a href="#Block结构" class="headerlink" title="Block结构"></a>Block结构</h2><p>网络结构如上图所示，图中参数解释：</p><ol><li>$F_{tr}$—标准卷积操作</li><li>$F_{sq}(.)$—sequeeze操作，这一步进对输入的特征图进行GlobalPooling，特征图由$H \times W \times C$变为$1 \times 1\times C$</li><li>$F_{ex}(.)$—excitation操作，这一步包含两个fc全连接操作<ol><li>第一步FC-压缩维度数量，维度数由$C$变为$C/r$，Relu激活层</li><li>第二步FC-扩展维度数量，维度数由$C/r$变为$C$，Sigmoid激活层</li></ol></li><li>$F_{scale}(.)$—类似通道注意力，将得到的C维度的值作为权重乘到输入的特征图的维度上。</li></ol><h2 id="为什么有效"><a href="#为什么有效" class="headerlink" title="为什么有效"></a>为什么有效</h2><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><ol><li>官方repo <a href="https://github.com/hujie-frank/SENet" target="_blank" rel="noopener">https://github.com/hujie-frank/SENet</a></li><li></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;img src=&quot;/2020/02/17/深度学习-ConvNet-SENet/SE-pipeline.jpg&quot;&gt;
&lt;p&gt;本篇主要记录一下2017年ILSVR竞赛的冠军-SENet，这是一种网络的block结构，可以有效的结合到InceptionNet，Resnet中。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/CV/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="ConvNet" scheme="http://xiaofengshi.com/tags/ConvNet/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/tags/CV/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-ConvNet-Shufflenet</title>
    <link href="http://xiaofengshi.com/2020/02/09/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-ConvNet-ShuffleNet/"/>
    <id>http://xiaofengshi.com/2020/02/09/深度学习-ConvNet-ShuffleNet/</id>
    <published>2020-02-09T07:06:18.000Z</published>
    <updated>2020-02-16T13:49:26.737Z</updated>
    
    <content type="html"><![CDATA[<p>本篇主要分析轻量化Conv-ShuffleNet</p><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/shufflenetv2-block.png"> <a id="more"></a><h2 id="ShuffleNetV1"><a href="#ShuffleNetV1" class="headerlink" title="ShuffleNetV1"></a>ShuffleNetV1</h2><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/shufflenet-mobilenet.png"><p><code>ShuffleNet</code>是$2017$年由<code>Facebook</code>提出的，它的主要竞争对手是<code>Google</code>提出的<code>MobilenetV1</code>，上图作者在论文中与<code>MobileNetV1</code>在<code>ImageNet</code>的的对比。<hr></p><h3 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h3><p>ShuffleNet网络主要是针对的<code>Xception</code>和<code>ResNet</code>，由于$1 \times 1$卷积部分的计算开销在小型网络中不够理想的问题。受<code>depthwise</code>卷积的启发，每个通道都与一个卷积核进行计算，那么在$1 \times 1$这部分卷积中，一个很自然的方法就是，对当前的<code>featuremap</code>的通道进行分组计算，但是如果直接分组的话，就造成了不同特征的隔离，不同的组之间的特征无法交互，这样做显然会造成数据特征的丢失，效果不好。<code>ShuffleNet</code>就是针对这个问题提出的解决方案，对<code>featuremap</code>的通道进行分组进行<strong>group convolution</strong>，然后每组输出的<code>featuremap</code>进行重排<strong>channel shuffle</strong>，这样不同组的的信息由此得到交换，并没有完全隔离，能在较小计算量的情况下保持较好的精度。</p><p><hr> 不同featuremap分组方式对比</p><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/comparison.png"><p>图$a$，是将<code>featuremap</code>的通道进行分组，显然这样会造成特征之间的隔离，输出只有一部分输入决定；图b/c在第一步卷积后对分组的通道进行重排，这样下层的输入就会接收到上层所有<code>channel</code>的数据，效果相比图$a$更好。<hr></p><p>在图c中可以看到，channelshuffle层，在进行group convolution之后，对得到的通道进行shuffle，这里的shuffle不是随机的，而是均匀的打乱。在程序上的实现方法很简单：假设将输入层分成g组，每组的数量为n，那么总的通道数为$g \times n$，将通道拆成两个维度$(g,n)$，然后将这两个维度进行转置$(n,g)$，最后reshape成一个维度，这样得到的维度就是均匀shuffle之后的channel了，也就是图c中的channel shuffle的样子。</p><p>代码实现如下</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ShuffleBlock</span><span class="params">(nn.Module)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, groups)</span>:</span></span><br><span class="line">        super(ShuffleBlock, self).__init__()</span><br><span class="line">        self.groups = groups</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self, x)</span>:</span></span><br><span class="line">        <span class="string">'''Channel shuffle: [N,C,H,W] -&gt; [N,g,C/g,H,W] -&gt; [N,C/g,g,H,w] -&gt; [N,C,H,W]'''</span></span><br><span class="line">        N,C,H,W = x.size()</span><br><span class="line">        g = self.groups</span><br><span class="line">        <span class="keyword">return</span> x.view(N,g,C//g,H,W).permute(<span class="number">0</span>,<span class="number">2</span>,<span class="number">1</span>,<span class="number">3</span>,<span class="number">4</span>).reshape(N,C,H,W)</span><br></pre></td></tr></table></figure><h3 id="网络结构"><a href="#网络结构" class="headerlink" title="网络结构"></a>网络结构</h3><p>在<code>ShuffleNet</code>中，也是和<code>ResNet</code>等模型相似，使用的是<code>Block</code>，结构如下所示。</p><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/shufflenet-unite.png"><p>图$a$是<code>ResNet</code>中的<code>Block</code>结构，图$b$是<code>ShuffleNet</code>中使用的<code>Block</code>，先经过$1 \times 1$通道分组卷积，然后通道重排，在$3 \times 3$空间上的depthwise convolution，然后再经过一个$1 \times 1$的通道分组卷积，图$c$是要进行降维时候的结构$stride=2$，旁边的shortcut也变为$stride=2$的平均池化。</p><p>可以看到shufflenet中的blcok是从残差网络的block中改造得到的。在resnet中block模块的设计是：$1 \times 1$的卷积—&gt;$3\times 3$的深度卷积，并且在这一步数据通道减少，可以理解成一个bottleneck—&gt;$1 \times 1$卷积，最后是一个短路连接，将输入直接加到输出上。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1x1 Conv --&gt; 3x3 DWConv--&gt; 1x1 Conv --------&gt;</span><br><span class="line"> |                                     |</span><br><span class="line"> |-------------------------------------|</span><br></pre></td></tr></table></figure><p>在shufflenet中对resnet的block进行了改进设计，将$1 \times 1$的普通卷积换成了组卷积，并且在后面加入了channel shuffle操作。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1x1 Group convolution --&gt; channel shuffle --&gt; 3x3 DWConv--&gt; 1x1 Group convolution --------&gt;</span><br><span class="line"> |                                                                                    |</span><br><span class="line"> |------------------------------------------------------------------------------------|</span><br></pre></td></tr></table></figure><p>ShuffleNet的block设计代码如下所示</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bottleneck</span><span class="params">(nn.Module)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, in_planes, out_planes, stride, groups)</span>:</span></span><br><span class="line">        super(Bottleneck, self).__init__()</span><br><span class="line">        self.stride = stride</span><br><span class="line"></span><br><span class="line">        mid_planes = out_planes/<span class="number">4</span></span><br><span class="line">        g = <span class="number">1</span> <span class="keyword">if</span> in_planes==<span class="number">24</span> <span class="keyword">else</span> groups</span><br><span class="line">        self.conv1 = nn.Conv2d(in_planes, mid_planes, kernel_size=<span class="number">1</span>, groups=g, bias=<span class="keyword">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(mid_planes)</span><br><span class="line">        self.shuffle1 = ShuffleBlock(groups=g)</span><br><span class="line">        self.conv2 = nn.Conv2d(mid_planes, mid_planes, kernel_size=<span class="number">3</span>, stride=stride, padding=<span class="number">1</span>, groups=mid_planes, bias=<span class="keyword">False</span>)</span><br><span class="line">        self.bn2 = nn.BatchNorm2d(mid_planes)</span><br><span class="line">        self.conv3 = nn.Conv2d(mid_planes, out_planes, kernel_size=<span class="number">1</span>, groups=groups, bias=<span class="keyword">False</span>)</span><br><span class="line">        self.bn3 = nn.BatchNorm2d(out_planes)</span><br><span class="line"></span><br><span class="line">        self.shortcut = nn.Sequential()</span><br><span class="line">        <span class="keyword">if</span> stride == <span class="number">2</span>:</span><br><span class="line">            self.shortcut = nn.Sequential(nn.AvgPool2d(<span class="number">3</span>, stride=<span class="number">2</span>, padding=<span class="number">1</span>))</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self, x)</span>:</span></span><br><span class="line">      <span class="comment"># group convolution and bn, relu </span></span><br><span class="line">        out = F.relu(self.bn1(self.conv1(x)))</span><br><span class="line">        <span class="comment"># channel shuffle</span></span><br><span class="line">        out = self.shuffle1(out)</span><br><span class="line">        <span class="comment"># depthwise convolution and output channel num is out/4</span></span><br><span class="line">        out = F.relu(self.bn2(self.conv2(out)))</span><br><span class="line">        <span class="comment"># group convolution and bn, relu</span></span><br><span class="line">        out = self.bn3(self.conv3(out))</span><br><span class="line">        <span class="comment"># shortcut</span></span><br><span class="line">        res = self.shortcut(x)</span><br><span class="line">        <span class="comment"># relu </span></span><br><span class="line">        out = F.relu(torch.cat([out,res], <span class="number">1</span>)) <span class="keyword">if</span> self.stride==<span class="number">2</span> <span class="keyword">else</span> F.relu(out+res)</span><br><span class="line">        <span class="keyword">return</span> out</span><br></pre></td></tr></table></figure><p><hr>下图是shufflenet的模型结构示意图</p><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/shufflenet-architecture.png"><p>开始是普通的$3 \times 3$的卷积，和maxpool层，然后是3个stage，每个stage都是重复堆叠shufflenet中的block结构，在每个stage中，第一个基本单元采用的是$stride=2$，这样特征图width和height各降低一半，而通道数增加1倍。后面的基本单元都是$stride=1$，特征图和通道数都保持不变。对于基本单元来说，其中瓶颈层，就是3x3卷积层的通道数为输出通道数的1/4，这里和resnet中的残差单元设计是一致的。<hr></p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>实际上就像开头说的那样，shufflenet的设计的竞标对象是mobilenetv1，在论文中也可以看到作者的实验中对比对象是MobileNetV1。</p><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/comparison.png"><p>可以看出来，shufflenet在计算复杂度和模型准确率上都是由于mobilenet的。</p><h2 id="ShuffleNetV2"><a href="#ShuffleNetV2" class="headerlink" title="ShuffleNetV2"></a>ShuffleNetV2</h2><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/shufflenetv2-conv-time.png"><p>在<code>ShuffleNet</code>中，作者使用<code>FLOPS</code>来作为模型效率的一个评价指标，但是在在<code>ShuffleNetV2</code>的论文中作者指出这个指标是间接的，因为一个模型实际的运行时间除了要把计算操作算进去之外，还有例如内存读写，<code>GPU</code>并行性，文件<code>IO</code>等也应该考虑进去。最直接的方案还应该回归到最原始的策略，即直接在同一个硬件上观察每个模型的运行时间。如上图所示，在整个模型的计算周期中，<code>Conv</code>耗时仅占$50 \%$左右，如果我们能优化另外$50 \%$，我们就能够在不损失计算量的前提下进一步提高模型的效率。</p><p>在ShuffleNetV2中作者从内存访问代价（Memory Access Cost，MAC）和GPU并行性的方向分析了网络应该怎么设计才能进一步减少运行时间，提高模型的效率。</p><h3 id="高效模型的设计原则"><a href="#高效模型的设计原则" class="headerlink" title="高效模型的设计原则"></a>高效模型的设计原则</h3><h4 id="相同输入输出情况下MAC最小"><a href="#相同输入输出情况下MAC最小" class="headerlink" title="相同输入输出情况下MAC最小"></a>相同输入输出情况下MAC最小</h4><p>对于一个featuremap，假设尺寸为$h \times w \times c_{1}$，经过卷积之后输出的featuremap为$h \times w \times c_{2}$，那么卷积操作需要的算力FLOPs为$B=hwc_{1}c_{2}$，在计算过程中占用的内存，输入featuremap为$hwc_{1}$，输出featuremap为$hwc_{2}$，卷积核为$c_{1}c_{2}$，也就是$MAC=hw(c_{1}+c_{2})+c_{1}c_{2}$</p><p>有如下的推导公式</p><script type="math/tex; mode=display">\begin{aligned}M A C&=h w\left(c_{1}+c_{2}\right)+c_{1} c_{2}\\&=\sqrt{\left(h w\left(c_{1}+c_{2}\right)\right)^{2}}+\frac{B}{h w}\\&=\sqrt{(h w)^{2} \cdot\left(c_{1}+c_{2}\right)^{2}}+\frac{B}{h w}\\&\geq \sqrt{(h w)^{2} \cdot 4 c_{1} c_{2}}+\frac{B}{h w}\\&=s \sqrt{h w \cdot\left(h w c_{1} c_{2}\right)}+\frac{B}{h w}\\&=2 \sqrt{h w B}+\frac{B}{h w}\end{aligned}</script><p>当满足$c_{1}=c_{2}$的时候，上式可以取到等号，也就是说，当所需算力确定的前提下，$c_{1}=c_{2}$时所需的<code>MAC</code>最小，此时的模型效率最高。</p><h4 id="MAC与group-convolution的分组数量成正比"><a href="#MAC与group-convolution的分组数量成正比" class="headerlink" title="MAC与group convolution的分组数量成正比"></a>MAC与group convolution的分组数量成正比</h4><p>在group convolution中，FLOPs为$B = hwc_{1}c_{2}/g$，对应的<code>MAC</code>计算公式为</p><script type="math/tex; mode=display">\begin{aligned}M A C &=h w\left(c_{1}+c_{2}\right)+\frac{c_{1}}{g} \frac{c_{2}}{g} g \\&=h w\left(c_{1}+c_{2}\right)+\frac{c_{1} c_{2}}{g} \\&=B g\left(\frac{1}{c_{1}}+\frac{1}{c_{2}}\right)+\frac{B}{h w}\end{aligned}</script><p>也就是在使用group convolution的时候，group的分组数量不要太大。</p><h4 id="模型的分支会降低网络并行能力"><a href="#模型的分支会降低网络并行能力" class="headerlink" title="模型的分支会降低网络并行能力"></a>模型的分支会降低网络并行能力</h4><p>在比较典型的模型结构中，<a href="https://towardsdatascience.com/a-simple-guide-to-the-versions-of-the-inception-network-7fc52b863202" target="_blank" rel="noopener">InceptionNet</a>和<code>NasNet</code>是分支比较多的，在论文里，作者对比了几种不同的网络结构，通过控制卷积的通道数来使5组对照试验的<code>FLOPs</code>相同，通过实验我们发现它们按效率从高到低排列依次是$a&gt;b&gt;d&gt;c&gt;e$</p><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/shuffnetv2-branches.png" title="Branches"><hr><p>可以明显的看到，模型的效率和模型的分支数量是成反比的。</p><h4 id="ElementWise操作是很昂贵的"><a href="#ElementWise操作是很昂贵的" class="headerlink" title="ElementWise操作是很昂贵的"></a>ElementWise操作是很昂贵的</h4><p>所谓的<code>ElementWise</code>是指在模型网络中的激活函数，bias_add等操作，这些操作在计算FLOPs时被忽略掉了，但是根据上面论文中作者提供的<code>shufflenetv1</code>和<code>mobilebnetv2</code>的计算时间分布饼图，可以知道<code>ElementWise</code>实际上所占的比例一点都不小。</p><h4 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h4><p>既然有了上面四个设计准则，那么要想设计更高效的网络就有了入手点</p><ol><li>使用输入通道和输出通道相同的卷积操作</li><li>谨慎使用分组卷积</li><li>减少网络分支数</li><li>减少<code>ElementWise</code>操作</li></ol><p>在shufflenetv1中设计的block中有两个分组卷积层存在，并且每个分组卷积层group数量一般为3，这显然就不满足第二个准则了，回顾MobileNetV2的结构借鉴了resnet的设计，包含深度可分离卷积，扩展层和映射层以及大量的Relu6操作，显然不太符合条件3，4。</p><h3 id="网络结构-1"><a href="#网络结构-1" class="headerlink" title="网络结构"></a>网络结构</h3><p>上面说了一大堆，主要是作者在阐述shufflenetv2的设计思路，那么网络结构到底啥样子</p><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/shufflenetv2-block.png" title="Shufflenet Block"><hr><p>左边两个是第一节中所说的V1的结构，右边是需要重点说的V2的结构。</p><p>对比发现</p><ol><li>$c$中加入了通道分割操作(channel split)，具体的操作方法是：将c个输入的feature分成$c-c^{1}$和$c^{1}$两组，并且一般情况下$c^{1}=\frac{c}{2}$，这样是为了满足设计准则的第三点，尽量控制模型的分支数量</li><li>进行通道分割之后左侧的分支直接映射，右侧是一个输入通道数和输出通道数相等的深度可分离卷积，这样是为了满足条件1</li><li>右侧的分支中只使用了$1\times1$卷积，并没有使用V1的分组卷积，这样是为了满足条件2</li><li>在最后合并的时候都是使用的concat操作，并没有使用Add，是为了满足条件4</li><li>在堆叠<code>ShuffleNetv2</code>的时候，concat，channel-split和channel-shuffle合并成1个element-wise操作，也是为了满足条件4。</li></ol><p>图$d$是降采样操作，不进行channel-split操作，每个分支直接copy一份输入，每个分支进行stride=2的下采样，最后concat在一起，这样featuremap减半，但是通道数量加倍。</p><p>其他的地方就和V1模型相似了，通过多个block堆叠，组成ShuffleNetV2模型，模型结构如下</p><img src="/2020/02/09/深度学习-ConvNet-ShuffleNet/shufflenetv2-architecture.png" title="ShuffleNetV2 Architecture"><p>V2相比V1多了一个conv5层，除了这个外就没什么不一样的了。</p><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ol><li><p>论文地址：<a href="https://arxiv.org/abs/1707.01083" target="_blank" rel="noopener">https://arxiv.org/abs/1707.01083</a></p></li><li><p>V1代码-Pytorch：<a href="https://github.com/kuangliu/pytorch-cifar/blob/master/models/shufflenet.py" target="_blank" rel="noopener">https://github.com/kuangliu/pytorch-cifar/blob/master/models/shufflenet.py</a></p></li><li><p>V2代码-Pytorch：<a href="https://pytorch.org/hub/pytorch_vision_shufflenet_v2/" target="_blank" rel="noopener">https://pytorch.org/hub/pytorch_vision_shufflenet_v2/</a></p></li><li><p>V2代码-TensorFlow：<a href="https://github.com/tensorpack/tensorpack/blob/master/examples/ImageNetModels/shufflenet.py" target="_blank" rel="noopener">https://github.com/tensorpack/tensorpack/blob/master/examples/ImageNetModels/shufflenet.py</a></p></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本篇主要分析轻量化Conv-ShuffleNet&lt;/p&gt;
&lt;img src=&quot;/2020/02/09/深度学习-ConvNet-ShuffleNet/shufflenetv2-block.png&quot;&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/CV/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="ConvNet" scheme="http://xiaofengshi.com/tags/ConvNet/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/tags/CV/"/>
    
      <category term="轻量化Conv" scheme="http://xiaofengshi.com/tags/%E8%BD%BB%E9%87%8F%E5%8C%96Conv/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-ConvNet-MnasNet</title>
    <link href="http://xiaofengshi.com/2020/02/09/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-ConvNet-MnasNet/"/>
    <id>http://xiaofengshi.com/2020/02/09/深度学习-ConvNet-MnasNet/</id>
    <published>2020-02-09T06:56:52.000Z</published>
    <updated>2020-02-09T07:01:33.870Z</updated>
    
    <summary type="html">
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/CV/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="ConvNet" scheme="http://xiaofengshi.com/tags/ConvNet/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/tags/CV/"/>
    
      <category term="轻量化CNN" scheme="http://xiaofengshi.com/tags/%E8%BD%BB%E9%87%8F%E5%8C%96CNN/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-ConvNet-EfficientNet</title>
    <link href="http://xiaofengshi.com/2020/02/09/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-ConvNet-EfficientNet/"/>
    <id>http://xiaofengshi.com/2020/02/09/深度学习-ConvNet-EfficientNet/</id>
    <published>2020-02-09T03:48:15.000Z</published>
    <updated>2020-02-10T05:45:52.151Z</updated>
    
    <content type="html"><![CDATA[<img src="/2020/02/09/深度学习-ConvNet-EfficientNet/efficientnet-params-acc.png"><p>上图为不同模型的参数量和模型ACC曲线，可以看到和EfficientNet对比的网络包含ResNet，SENet，GPipe，Inception，NASNet，DenseNet等目前的主流网络，并且处于领先位置。</p><a id="more"></a><h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><p>开篇明义，整篇论文一直围绕着下面这张图在进行的</p><p><hr> Flops-Acc曲线</p><img src="/2020/02/09/深度学习-ConvNet-EfficientNet/efficient-flops-acc.png"><p>横坐标为模型所需要的算力<code>FLOPS，</code>纵坐标为ACC。在相同的算力需求下，不同的模型策略对应的模型准确率曲线，联合多维度模型优化的策略是优于单个维度优化的策略的。 <hr></p><p>前几年的发展，在模型设计上为了提高模型的准确率，研究学者从模型的宽度，深度和输入分辨率来进行设计研究。在这个论文中，作者提出了一个思考：能否找到一个规范化的神经网络扩展方法可以同时提高网络的准确率和效率。要实现这点，一个很关键的步骤便是如何平衡<strong>宽度、深度和分辨率</strong>这三个维度。从Alexnet到2018年的GPipe，人们不断探寻更高效的模型。</p><div class="table-container"><table><thead><tr><th style="text-align:left">Architecture</th><th style="text-align:left">Year</th><th style="text-align:left">Accuracy</th><th style="text-align:left">Parameters</th></tr></thead><tbody><tr><td style="text-align:left">AlexNet</td><td style="text-align:left">2012</td><td style="text-align:left">56.55%</td><td style="text-align:left">62M</td></tr><tr><td style="text-align:left">GoogleNet</td><td style="text-align:left">2014</td><td style="text-align:left">74.8%</td><td style="text-align:left">6.8M</td></tr><tr><td style="text-align:left">SENet</td><td style="text-align:left">2017</td><td style="text-align:left">82.7%</td><td style="text-align:left">145M</td></tr><tr><td style="text-align:left">GPipe</td><td style="text-align:left">2018</td><td style="text-align:left">84.3%</td><td style="text-align:left">557M</td></tr></tbody></table></div><h2 id="Model-Acc-vs-Model-Size"><a href="#Model-Acc-vs-Model-Size" class="headerlink" title="Model Acc vs Model Size"></a>Model Acc vs Model Size</h2><p>很显然，为了提升模型的准确率，往往会带来更多的模型参数，因此我们在选择模型的时候要考虑模型准确率和模型参数里的trade-off，在图表中可以说GoogleNet是全面碾压AlexNet的，因为不论从准确率还是参数里上，GoogleNet都是更优的，目前我们设计<strong>ConvNet</strong>网络的几个准则</p><h3 id="模型压缩"><a href="#模型压缩" class="headerlink" title="模型压缩"></a>模型压缩</h3><ol><li><p>模型剪枝 </p><p>去掉无法提升模型效率的部分</p></li><li><p>量化 </p><p>从数据类型的角度，比如将<strong>float</strong>转化为<strong>int</strong>，将<strong>float32</strong>转化为<strong>float16</strong>或者<strong>float8</strong>，可以成倍的缩小模型大小</p></li></ol><h3 id="网络搜索"><a href="#网络搜索" class="headerlink" title="网络搜索"></a>网络搜索</h3><p>以<code>MnasNet</code>为例，自动搜索的方法找到最佳的网络深度，卷积内核的宽度，大小等。</p><h3 id="模型缩放"><a href="#模型缩放" class="headerlink" title="模型缩放"></a>模型缩放</h3><p>仍然使用使用标准CNN网络，例如GoogleNet或ResNet，并通过更改网络的宽度和/或深度或网络的大小来放大（即使用较大的参数）或缩小（即使用较少的参数）。EfficientNet也是采用的这种方式。</p><h2 id="模型设计"><a href="#模型设计" class="headerlink" title="模型设计"></a>模型设计</h2><p>为了提高模型的效率。扩大网络宽度，深度或分辨率的任何尺寸都可以提高精度，但是对于较大的模型，精度增益会降低。为了追求更好的准确性和效率，在<strong>ConvNet</strong>扩展过程中平衡网络宽度，深度和分辨率的所有维度至关重要。</p><p>关键点就在于：是如何选择应该使网络更深或更宽的范围，或者增加图像分辨率的程度。</p><h3 id="Compound-Scaling复合模型缩放"><a href="#Compound-Scaling复合模型缩放" class="headerlink" title="Compound Scaling复合模型缩放"></a>Compound Scaling复合模型缩放</h3><p>这个是在<code>EfficientNet</code>中提出来的，使用复合系数来原则上均匀地缩放宽度，深度和分辨率。在论文中，为了将三个维度结合起来，作者设计了一个公式</p><script type="math/tex; mode=display">\begin{array}{l}{\text { depth: } d=\alpha^{\phi}} \\{\text { width: } w=\beta^{\phi}} \\{\text { resolution: } r=\gamma^{\phi}} \\s.t. (\alpha \cdot \beta^{2} \cdot \gamma^{2} \approx 2) \\\alpha \geq 1, \beta \geq 1, \gamma \geq 1\end{array}</script><p>按照论文所述</p><blockquote><p>使用$d$表示模型的深度，$w$表示模型宽度，$\gamma$表示模型的输入分辨率对模型准确性和效率的影响。$\alpha, \beta, \gamma$是由网格搜索的方法得到的三个常数，$\phi$是用户指定的参数，用于控制有多少资源可用于模型缩放，而$\alpha, \beta, \gamma$指定如何分别将这些额外资源分配给网络宽度，深度和分辨率。常规卷积运算的<strong>FLOPS</strong>与$d$，$w^2$，$\gamma ^2$成正比，即网络深度加倍将使<strong>FLOPS</strong>加倍，但网络宽度或分辨率加倍将使<strong>FLOPS</strong>增加四倍。 由于卷积运算通常在<strong>ConvNet</strong>中占主导地位，因此按照上述公式符合缩放<strong>ConvNet</strong>将使总<strong>FLOPS</strong>大约增加$\left(\alpha \cdot \beta^{2} \cdot \gamma^{2}\right)^{\phi}$。在论文中建立约束条件，$\alpha \cdot \beta^{2} \cdot \gamma^{2} \approx 2$，这样对于任意网格搜索得到的参数$\alpha, \beta, \gamma$对需要的<strong>FLOPS</strong>算力的影响被限制为$2^{\phi}$。</p></blockquote><h2 id="模型训练"><a href="#模型训练" class="headerlink" title="模型训练"></a>模型训练</h2><p>在EfficientNet中使用的是MnasNet的训练方法，使用的是和MnasNet相同的搜索空间，优化目标为</p><script type="math/tex; mode=display">A C C(m) \times[F L O P S(m) / T]^{w}</script><p>其中$A C C(m)$对应着模型的准确率，$[F L O P S(m) / T]^{w}$对应着模型需要的算力，$T$对应着我们一开始设置的需要的算力值，$w=-0.07$是控制准确率和算力之间平衡的超参数</p><p>区别于MnasNet的是这里优化的是FLOPS而不是延迟，这是因为模型没有针对专门的设备进行设计，因此使用算力这个值更加合理。经过这中方法，搜索得到模型<strong>EfficientNet-B0</strong>。</p><p><hr> 模型结构如下图所示</p><img src="/2020/02/09/深度学习-ConvNet-EfficientNet/efficientnet-b0.png"><hr><p>图中的MBConv对应着MobilenetV2中提出的翻转残差模块。可以看到经过MnasNet搜索得到的网络结构跟我们手工设计的网络的确不太一样。</p><p>在固定上面公式的$\phi=1$，在约束条件下，经过网格搜索得到最优参数$\alpha=1.2，\beta=1.1,\gamma=1.15$，实际上网格搜索是最暴力的搜索方法，这里不得不说要有强大的计算资源才能进行如此计算。</p><p>个人看这个论文的时候被一张图吸引</p><p><hr>不同的模型缩放方式对应的激活图</p><img src="/2020/02/09/深度学习-ConvNet-EfficientNet/efficientNet-activation-map.png"><hr><p>这张图展示了在不同的模型策略下，对应的激活图，可以看到</p><ul><li><p>基础模型，区域信息加大，包含很多噪音特征</p></li><li><p>模型加深之后，特征变得稀疏，会集中在相对更明显的物体上</p></li><li>模型加宽之后，特征值变得较大，但是区域仍然包含较多噪音</li><li>增大输入分辨率，特征会集中在单个实体上，但是特征值很少</li><li>符合缩放模型，特征值具有了三者的优势，特征值变大，并且集中在单个实体上。</li></ul><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul><li><p>论文链接<a href="https://arxiv.org/abs/1905.11946" target="_blank" rel="noopener">“EfficientNet: Rethinking Model Scaling for Convolutional Neural Networks”</a></p></li><li><p>官方代码 <a href="https://github.com/tensorflow/tpu/blob/master/models/official/efficientnet/efficientnet_builder.py" target="_blank" rel="noopener">https://github.com/tensorflow/tpu/blob/master/models/official/efficientnet/efficientnet_builder.py</a></p></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;img src=&quot;/2020/02/09/深度学习-ConvNet-EfficientNet/efficientnet-params-acc.png&quot;&gt;
&lt;p&gt;上图为不同模型的参数量和模型ACC曲线，可以看到和EfficientNet对比的网络包含ResNet，SENet，GPipe，Inception，NASNet，DenseNet等目前的主流网络，并且处于领先位置。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/CV/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="ConvNet" scheme="http://xiaofengshi.com/tags/ConvNet/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/tags/CV/"/>
    
      <category term="轻量化Conv" scheme="http://xiaofengshi.com/tags/%E8%BD%BB%E9%87%8F%E5%8C%96Conv/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-Convolution-Summary</title>
    <link href="http://xiaofengshi.com/2020/02/05/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-Convolution-Summary/"/>
    <id>http://xiaofengshi.com/2020/02/05/深度学习-Convolution-Summary/</id>
    <published>2020-02-05T13:56:11.000Z</published>
    <updated>2020-02-09T03:25:15.775Z</updated>
    
    <content type="html"><![CDATA[<img src="/2020/02/05/深度学习-Convolution-Summary/convs.png" title="convolution"><p>本篇主要总结一下卷积的不同的方式以特点，包括普通卷积，反卷积，空洞卷积，深度可分离卷积，可变形卷积等，如果有更新的方式出来了或者我看到了，继续更新在此。</p><a id="more"></a><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ol><li><a href="https://zhuanlan.zhihu.com/p/29367273" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/29367273</a></li><li><a href="https://blog.csdn.net/imPlok/article/details/93143897" target="_blank" rel="noopener">https://blog.csdn.net/imPlok/article/details/93143897</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;img src=&quot;/2020/02/05/深度学习-Convolution-Summary/convs.png&quot; title=&quot;convolution&quot;&gt;
&lt;p&gt;本篇主要总结一下卷积的不同的方式以特点，包括普通卷积，反卷积，空洞卷积，深度可分离卷积，可变形卷积等，如果有更新的方式出来了或者我看到了，继续更新在此。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="ConvNet" scheme="http://xiaofengshi.com/tags/ConvNet/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-ConvNet-MobileNetV1-V3</title>
    <link href="http://xiaofengshi.com/2020/02/03/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-ConvNet-MobileNetV1-V3/"/>
    <id>http://xiaofengshi.com/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/</id>
    <published>2020-02-03T13:16:01.000Z</published>
    <updated>2020-02-16T14:16:57.402Z</updated>
    
    <content type="html"><![CDATA[<img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/pose_estimation.gif" title="Pose estimation"><p>本篇主要对目前流行的移动端轻量化网络MobileNet进行分析。</p><p>最近一段时间由于工作需要，想要将服务端的模型迁移到移动端，突然发现一个尴尬的现实，服务端的大模型在移动端是很难跑起来的。随着5G技术的到来，端上设备越来越强悍，将模型做到移动端是一个很显然的大趋势。目前的移动端模型 ( <a href="https://www.tensorflow.org/lite/models/" target="_blank" rel="noopener">https://www.tensorflow.org/lite/models/</a> )，主要跑的是CV相关方面的识别，检测模型，姿态预估等相对简单的模型，并且目前的模型很多是基于<code>MobileNet</code>这一网络的，本篇就主要针对<code>Mobilenet</code>进行研究分析。</p><a id="more"></a><h2 id="MobileNetV1"><a href="#MobileNetV1" class="headerlink" title="MobileNetV1"></a>MobileNetV1</h2><p>同样是针对移动端进行优化，使用<code>mobilenet</code>替代常规的卷积方式，由于<code>Mobilenet</code>的深度可分离卷积结构设计，目前，在工业界<code>Mobilenet</code>是移动端部署使用最广泛的，到2019年已经公布了MobilenetV3版本，该网络被广泛用于移动端的图像识别，检测等任务。</p><p><a href="https://arxiv.org/pdf/1704.04861.pdf" target="_blank" rel="noopener">论文链接🔥 MobileNets: Efficient Convolutional Neural Networks for Mobile Vision</a></p><h3 id="设计思路"><a href="#设计思路" class="headerlink" title="设计思路"></a>设计思路</h3><p>在进行小型高效的神经网络时，一般两种思路:</p><ul><li><p>压缩模型</p><p>获得小型网络的一个办法是减小、分解或压缩预训练网络，例如量化压缩(product quantization)、哈希(hashing )、剪枝(pruning)、矢量编码( vector quantization)和霍夫曼编码(Huffman coding)等；此外还有各种分解因子(various factorizations )用来加速预训练网络；还有一种训练小型网络的方法叫蒸馏(distillation )，使用大型网络指导小型网络，例如上面的<code>squeezenet</code>。</p></li><li><p>直接训练小模型</p><p>例如<code>Flattened networks</code>利用完全的因式分解的卷积网络构建模型，显示出完全分解网络的潜力；<code>Factorized Networks</code>引入了类似的分解卷积以及拓扑连接的使用；<code>Xception network</code>显示了如何扩展深度可分离卷积到<code>Inception V3 networks</code>；<code>Squeezenet</code>使用一个bottleneck用于构建小型网络。</p></li></ul><h3 id="深度可分离卷积"><a href="#深度可分离卷积" class="headerlink" title="深度可分离卷积"></a>深度可分离卷积</h3><p><code>Mobilenet</code>主要注重于优化延迟的同时考虑小型的模型，从深度可分离卷积的角度构建模型。</p><p><strong><code>mobilenet</code>基于深度可分离卷积，将标准卷积分解成深度卷积(<code>depthwise convolution</code>)和逐点卷积(<code>pointwise convolution</code>)，通过此举大幅度降低参数量和计算量</strong></p><hr><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/mobilenet_Struct.png"><p>图中的浅色代表的是下一个<code>block</code>的开始，并且注意方块代表的是特征图，而红色映射才是卷积或者<code>ReLU</code>操作。$a$表示的是普通卷积方式，$b$表示的就是在<code>MobileNet</code>中广泛使用的深度可分离卷积(<code>Depthwise Separable Convolution</code>)，先进行<code>Depthwise</code>再进行<code>Pointwise</code><hr></p><ul><li><p>普通卷积计算</p><p>假设输入的<code>featuremap</code>为$12 \times 12 \times 4$，卷积为<code>conv_3-2</code>,则对应的计算量为$12\times12\times2\times(3\times3\times4)=10368$，参数量为(卷积核带来的参数) $3\times3\times4\times2=72$，如下图所示</p><p><hr> image comes from <a href="https://blog.csdn.net/huachao1001/article/details/79171447" target="_blank" rel="noopener">https://blog.csdn.net/huachao1001/article/details/79171447</a></p>  <img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/mobilenet_conv.png" title="mobilenet convolution"><hr></li></ul><ul><li><p>深度可分离卷积</p><p>在<code>Mobilenet</code>中将卷积操作进行了分解，分解为深度卷积和逐点卷积</p><p><hr></p><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/mobilenetV1_block.png"><hr><p><strong>深度卷积<code>DepthwiseConvolution</code>与传统的卷积方式不同</strong></p><ul><li><p>对于传统的卷积，卷积核在输入特征图的所有通道上共享参数，输出的通道数由卷积核的数量决定</p></li><li><p>深度卷积对输入的特征图的每个通道上使用不同的卷积核，一个卷积核对应一个输入通道</p></li></ul><p>所以可以说<code>DepthwiseConvolution</code>是depth级别的操作。</p><p>对于这一步的深度卷积有一篇回答特别棒 <a href="https://stackoverflow.com/questions/45369920/tensorflow-what-exactly-does-depthwise-convolution-do" target="_blank" rel="noopener">https://stackoverflow.com/questions/45369920/tensorflow-what-exactly-does-depthwise-convolution-do</a></p><p>为防止链接挂掉，引用代码如下</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="comment"># batch of 2 inputs of 13x13 pixels with 3 channels each.</span></span><br><span class="line"><span class="comment"># Four 5x5 filters applied to each channel, so 12 total channels output</span></span><br><span class="line">inputs_np = np.ones((<span class="number">2</span>, <span class="number">13</span>, <span class="number">13</span>, <span class="number">3</span>))</span><br><span class="line">inputs = tf.constant(inputs_np)</span><br><span class="line"><span class="comment"># Build the filters so that their behavior is easier to understand.  For these filters</span></span><br><span class="line"><span class="comment"># which are 5x5, I set the middle pixel (location 2,2) to some value and leave</span></span><br><span class="line"><span class="comment"># the rest of the pixels at zero</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># <span class="doctag">NOTE:</span> 5x5 filters for 3 inputs and applying 4 such filters to each one. here '3' equal with the input tensor's channel num S</span></span><br><span class="line">filters_np = np.zeros((<span class="number">5</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">4</span>)) </span><br><span class="line"></span><br><span class="line"><span class="comment"># <span class="doctag">NOTE:</span> for betweer display, fileter is zeros, except bellow positions </span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">0</span>, <span class="number">0</span>] = <span class="number">2.0</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">0</span>, <span class="number">1</span>] = <span class="number">2.1</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">0</span>, <span class="number">2</span>] = <span class="number">2.2</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">0</span>, <span class="number">3</span>] = <span class="number">2.3</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">1</span>, <span class="number">0</span>] = <span class="number">3.0</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">1</span>, <span class="number">1</span>] = <span class="number">3.1</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">1</span>, <span class="number">2</span>] = <span class="number">3.2</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">1</span>, <span class="number">3</span>] = <span class="number">3.3</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">0</span>] = <span class="number">4.0</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">1</span>] = <span class="number">4.1</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>] = <span class="number">4.2</span></span><br><span class="line">filters_np[<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">3</span>] = <span class="number">4.3</span></span><br><span class="line">filters = tf.constant(filters_np)</span><br><span class="line">out = tf.nn.depthwise_conv2d(</span><br><span class="line">      inputs,</span><br><span class="line">      filters,</span><br><span class="line">      strides=[<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>],</span><br><span class="line">      padding=<span class="string">'SAME'</span>)</span><br><span class="line"><span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">    sess.run(tf.global_variables_initializer())</span><br><span class="line">    out_val = out.eval()</span><br><span class="line"></span><br><span class="line">print(<span class="string">"output dim is &#123;&#125;"</span>.format(out_val.shape))</span><br><span class="line">print(<span class="string">"output cases 0 and 1 identical? &#123;&#125;"</span>.format(np.all(out_val[<span class="number">0</span>]==out_val[<span class="number">1</span>])))</span><br><span class="line">print(<span class="string">"One of the pixels for each of the 12 output &#123;&#125;"</span>.format(out_val[<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>]))</span><br><span class="line"><span class="comment"># Output:</span></span><br><span class="line"><span class="comment"># output dim is (2, 13, 13, 12)</span></span><br><span class="line"><span class="comment"># output cases 0 and 1 identical? True</span></span><br><span class="line"><span class="comment"># One of the pixels for each of the 12 output [2.  2.1 2.2 2.3 3.  3.1 3.2 3.3 4.  4.1 4.2 4.3]</span></span><br></pre></td></tr></table></figure><p><strong><code>PointwiseConvolution</code>其实就是普通的卷积，只不过采用1x1的卷积核</strong>。假设输入为<code>M*N*C</code>，原来的卷积为<code>conv3-k</code>，对应着<code>k</code>个<code>3x3</code>的卷积核，输出为<code>M*N*k</code>，现在将其进行改变成两步进行</p><ul><li>第一步：depthwise conv—输入为<code>M*N*C</code>，卷积为<code>conv3-1</code>，输出为<code>M*N*C</code></li><li>第二步：pointwise conv —输入为<code>M*N*C</code>，卷积为<code>conv1-k</code>，输出为<code>M*N*k</code></li></ul><p>最终输出的<code>feature map</code>尺寸相同，但是会很大程度减少参数量。</p><p>使用和普通卷积同样的参数进行演示，如下所示，输入的<code>feature map</code>的尺寸为$12 \times 12 \times 4$</p><ul><li><p>第一步<code>DepthwiseConv</code>—卷积为<code>conv3-1</code>,</p><ul><li><p>计算量：$12 \times 12 \times 1\times  3 \times 3 \times 4 =  5184$</p></li><li><p>参数量：$1\times 3 \times 3 \times 4=36$</p><p>对$4$个通道每个通道都与一个$3\times3$的卷积核计算</p></li><li><p>输出的特征图大小为$12\times12\times4$</p></li></ul></li><li><p>第二步<code>PointwiseConv</code>-卷积为<code>conv1-2</code>，输入的尺寸为$12 \times 12 \times 4$</p><ul><li>计算量：$12 \times 12 \times 2 \times (1 \times 1 \times 4)=1152$</li><li>参数量： $2 \times (1 \times 1 \times 4)= 8$</li><li>输出特征图大小为$12 \times 12 \times 2$</li></ul></li></ul><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/mobilenet_cov2.png" title="mobilenet"><p>比较普通卷积计算方式和<code>mobilenet</code>的计算方式，可以看到参数量由$72$较少到了$44$；计算量由$10368$减少到了$6336$。</p><p>深度可分离卷积可以看做是<code>InceptionNet</code>中的结构的极致设计，详细分析在这里<a href="https://zhuanlan.zhihu.com/p/29367273" target="_blank" rel="noopener">👉👉👉</a></p></li></ul><h3 id="网络参数"><a href="#网络参数" class="headerlink" title="网络参数"></a>网络参数</h3><p>对<code>Mobilenet</code>的网络参数如下图所示</p><hr>  <img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/mobile_param.png" title="mobilenet params"><hr><p>  除了在最后的全连接层没有使用非线性激活函数，其他层均使用<code>BN+Relu6</code>策略。</p><h2 id="MobileNetV2"><a href="#MobileNetV2" class="headerlink" title="MobileNetV2"></a>MobileNetV2</h2><p><a href="https://arxiv.org/abs/1801.04381" target="_blank" rel="noopener">论文链接🔥 MobileNetV2: Inverted Residuals and Linear Bottlenecks</a></p><p>参考链接 </p><ul><li><a href="https://machinethink.net/blog/mobilenet-v2/" target="_blank" rel="noopener">https://machinethink.net/blog/mobilenet-v2/</a> </li><li><a href="https://www.zybuluo.com/Team/note/1350161" target="_blank" rel="noopener">https://www.zybuluo.com/Team/note/1350161</a></li></ul><h3 id="V2相比于V1版本的升级"><a href="#V2相比于V1版本的升级" class="headerlink" title="V2相比于V1版本的升级"></a>V2相比于V1版本的升级</h3><p>针对<code>MobileNet</code>进行升级，开发了<code>V2</code>版本，在<code>V1</code>版本中核心点是深度可分离卷积，在<code>MobileNetV2</code>中仍然采用的是深度可分离卷积，不同的是在此基础上又引入了残差结构以及<code>bottleneck</code>瓶颈层，<code>MobileNetV2</code>的可以看成一系列<strong>Bottleneck residual block</strong>的结构的堆叠，如下所示</p><hr><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/mobilenetv2-block.png" title="MobileNetV2 Block"><hr><p>不同的点这里说明一下：</p><p>自上而下对<code>MobileNetV2</code>中使用的<code>Block</code>进行分析</p><ol><li><p><code>ExpansionLayer</code></p><p><strong>扩展层</strong>，这一部分使用的是$1 \times 1$ 的卷积，作用是为了扩充输入当前Block的特征图的通道数量，因此，扩展层始终具有比输入通道更多的输出通道。通道数量的扩展量由扩展因子决定。这是一个超参数。默认扩展因子为6，也就是扩展通道数$6$倍。</p></li><li><p>DepthwiseLayer</p><p>这里的深度卷积与<code>MobileNetV1</code>中是相同的，不变换通道的数量，这里主要是为了参数量。</p></li><li><p>ProjectionLayer</p><p>投影层。在这一层可以理解成pointwise，使用的是和<code>MobileNetV1</code>中相同的$1 \times 1$卷积，这里区别于MobileNetV1的地方：</p><ol><li>在MobileNetV1中，pointwise的输出通道数量保持不变或者加倍</li><li>在MobileNetV2中，输出通道数量减少</li></ol><p>这也是为什么在<code>MobileNetV2</code>中这一层被称作投影层。在这一层，通道数量减少，可以理解成创建了一个高纬度到低纬度的映射。</p></li></ol><p>每个Block的输出都是将输出的维度进行从高纬度到低纬度的变换，这也是每个block叫做<strong>bottleneck residual block</strong>的原因</p><p><hr>下图为MobileNetV2中Bottleneck residual block示意图。</p><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/MobilenetV2-ExpandProject.png" title="MobileNetV2 Bottleneck"><p>假设输入的通道数为24，经过<code>expansion</code>层之后，扩展$6$倍，变成$144$层，经过<code>depthwise</code>层，通道层数不变，经过<code>projection</code>层,通道数变为$24$，使用<code>ResNet</code>相同的方式连接<code>Block</code>的输入和输出，也就是进出模块的的特征图是低纬度的，在<code>Block</code>内部则是高纬度的，这就是<code>MobileNetV2</code>中广泛使用的结构。<hr></p><p>在每个<code>Bllock</code>中，每个层都是<code>BN+Relu6</code>，除了最后一步的<code>ProjectionLayer</code>，在<code>ProjectionLayer</code>中没有使用激活函数，由于这一层的作用是进行从高通道数向低通道数的映射，使用非线性激活会使得有效信息丢失，在这里模型中使用的直接就是线性映射，没有使用激活函数。</p><p>整个<code>MobileNetV2</code>使用了$17$个<code>Block</code>结构进行串联叠加（第1个Block稍有不同，它使用具有32个通道的常规$3 \times 3$卷积代替扩展层中的$1 \times 1$卷积)，最后就是标准的卷积核全局池化和分类层（这个是为适配图片识别任务，将MobileNetV2当做其他任务的基础网络的时候，这一部分不需要。）</p><h3 id="V2升级的动机"><a href="#V2升级的动机" class="headerlink" title="V2升级的动机"></a>V2升级的动机</h3><p>在V1中，模型设计的出发点是减少卷积的参数量并且减少计算量，即便使用更多的层数也没关系，因此在V1中引入了深度可分离卷积，事实证明这个方法是可行的，在V2中，相比与V1的变化主要在借鉴ResNet的残差连接和Block中的ExpansionLayer和ProjectionLayer。</p><p>分析MobileNetV2的结构可以看到，在整个模型中各个Block之间数据的通道数都是很小的</p><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/MobileNetV2-LowDimensionality.png"><p>由于ProjectionLayer的存在，使得相邻Block之间传递的数据维度很小，这很大程度减少了模型的计算量。但是只使用低纬度的数据特征，无法有效的提高模型的准确性，这个问题由ExpansionLayer来解决，该层的设计使得在每个Block中扩大输入当前特征图通道的数量，并且由于残差模块的存在，该层能够显著的提高模型的表达能力。</p><p>一个很有意思的解释：ProjectionLayer可以理解成为了减少计算量对数据进行的压缩，ExpansionLayer可以理解成为为了更好的表达特征图，对压缩过的数据进行解压缩，流经Depthwise的数据可以看做是真实的数据维度。</p><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/MobileNetV2-Compression.png"><p>从图中可以看出，在每个<code>Blcok</code>模块中首尾链接，内部对数据解压缩(<code>expansion</code>)，进行<code>depthwise</code>卷积操作，然后对数据进行压缩(<code>projection</code>)，传递到下一个<code>block</code>中。</p><hr><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/mobilnetv2-params.png"><p>如上所示便是MobileNetV2的模型结构，t表示的是expansion层的扩展系数，c是输出的通道数，n表示当前block重复了几次，s表示步长<hr></p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>先上图，这是MobileNetV2和其他移动端模型的对比。</p><hr><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/comparison.png"><hr><p>MobileNetV2是在V1的基础上升级而来，当然它的竞品是其他的轻量卷积模型shufflenet，nasnet这些。</p><hr><img src="/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/mobilenetv2-compare.png"><hr><p>在对比指标中可以看出来，MobileNetV2相比于几个网络时更有优势的。</p><h2 id="MobilenetV3"><a href="#MobilenetV3" class="headerlink" title="MobilenetV3"></a>MobilenetV3</h2><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ol><li>官方代码 <a href="https://github.com/tensorflow/models/tree/master/research/slim/nets/mobilenet" target="_blank" rel="noopener">https://github.com/tensorflow/models/tree/master/research/slim/nets/mobilenet</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;img src=&quot;/2020/02/03/深度学习-ConvNet-MobileNetV1-V3/pose_estimation.gif&quot; title=&quot;Pose estimation&quot;&gt;
&lt;p&gt;本篇主要对目前流行的移动端轻量化网络MobileNet进行分析。&lt;/p&gt;
&lt;p&gt;最近一段时间由于工作需要，想要将服务端的模型迁移到移动端，突然发现一个尴尬的现实，服务端的大模型在移动端是很难跑起来的。随着5G技术的到来，端上设备越来越强悍，将模型做到移动端是一个很显然的大趋势。目前的移动端模型 ( &lt;a href=&quot;https://www.tensorflow.org/lite/models/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;https://www.tensorflow.org/lite/models/&lt;/a&gt; )，主要跑的是CV相关方面的识别，检测模型，姿态预估等相对简单的模型，并且目前的模型很多是基于&lt;code&gt;MobileNet&lt;/code&gt;这一网络的，本篇就主要针对&lt;code&gt;Mobilenet&lt;/code&gt;进行研究分析。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/CV/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="ConvNet" scheme="http://xiaofengshi.com/tags/ConvNet/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/tags/CV/"/>
    
      <category term="轻量化Conv" scheme="http://xiaofengshi.com/tags/%E8%BD%BB%E9%87%8F%E5%8C%96Conv/"/>
    
  </entry>
  
  <entry>
    <title>Awesomes-Python</title>
    <link href="http://xiaofengshi.com/2020/01/29/Awesomes-Python/"/>
    <id>http://xiaofengshi.com/2020/01/29/Awesomes-Python/</id>
    <published>2020-01-29T05:22:42.000Z</published>
    <updated>2020-02-25T09:43:37.374Z</updated>
    
    <content type="html"><![CDATA[<p>本篇涉及平时工作中使用的一些<strong>Python</strong>相关轮子，在此记录，不断补充更新……</p><img src="/2020/01/29/Awesomes-Python/python.png" title="Python"><a id="more"></a><h2 id="文件及路径"><a href="#文件及路径" class="headerlink" title="文件及路径"></a>文件及路径</h2><h3 id="遍历读取文件夹下所有目录"><a href="#遍历读取文件夹下所有目录" class="headerlink" title="遍历读取文件夹下所有目录"></a>遍历读取文件夹下所有目录</h3><p>在<code>Python</code>中使用<code>os.path.lisdir()</code>可以实现读取给定路径下的所有文件，并返回文件名列表，在给定的路径下，有可能存在着子目录，这个时候我们想要读取子目录下的文件又该如何处理呢</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># encoding:utf-8</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">readDir</span><span class="params">(dirPath)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> dirPath[<span class="number">-1</span>] == <span class="string">'/'</span>:</span><br><span class="line">        print( <span class="string">u'文件夹路径末尾不能加/'</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    allFiles = []</span><br><span class="line">    <span class="comment"># 判断给定的路径是否是一个路径</span></span><br><span class="line">    <span class="keyword">if</span> os.path.isdir(dirPath):</span><br><span class="line">      <span class="comment"># 获取当前路径下所有的所有文件</span></span><br><span class="line">        fileList = os.listdir(dirPath)</span><br><span class="line">        <span class="keyword">for</span> f <span class="keyword">in</span> fileList:</span><br><span class="line">            full_path = os.path.join(dirPath, f)</span><br><span class="line">            <span class="keyword">if</span> os.path.isdir(full_path):</span><br><span class="line">                <span class="comment"># 如果存在子目录，递归调用读取函数</span></span><br><span class="line">                subFiles = readDir(full_path)</span><br><span class="line">                allFiles = subFiles + allFiles <span class="comment">#合并当前目录与子目录的所有文件路径</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                allFiles.append(full_path)</span><br><span class="line">        <span class="keyword">return</span> allFiles</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">'Error,not a dir'</span></span><br></pre></td></tr></table></figure><p>如上是对文件进行读取，当读取文件之后，如果文件是<code>.txt</code>文件，那么读取这个文件的内容并打印</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># encoding:utf-8</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">readTxt</span> <span class="params">(filePath)</span>:</span></span><br><span class="line">    <span class="keyword">with</span> open(filePath) <span class="keyword">as</span> f:</span><br><span class="line">        lines = f.readlines()</span><br><span class="line">        <span class="keyword">for</span> line <span class="keyword">in</span> lines:</span><br><span class="line">            data = line.strip().split(<span class="string">'\001'</span>) <span class="comment">#以列表形式返回分割了的行数据</span></span><br><span class="line">                <span class="keyword">for</span> i <span class="keyword">in</span> data:</span><br><span class="line">                <span class="keyword">print</span> i</span><br><span class="line">             </span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    dirPath = <span class="string">'xxxx'</span></span><br><span class="line">    fileList=ReadDirFiles.readDir(dirPath)</span><br><span class="line">    <span class="keyword">for</span> f <span class="keyword">in</span> fileList:</span><br><span class="line">    <span class="keyword">if</span> f.split(<span class="string">'.'</span>)[<span class="number">-1</span>] <span class="keyword">in</span> [<span class="string">'txt'</span>]:</span><br><span class="line">    readTxt(f)</span><br></pre></td></tr></table></figure><h2 id="PANDAS"><a href="#PANDAS" class="headerlink" title="PANDAS"></a>PANDAS</h2><h3 id="CSV2LIST"><a href="#CSV2LIST" class="headerlink" title="CSV2LIST"></a>CSV2LIST</h3><p><code>csv</code>是很常用的数据存储方式，推荐使用<code>pandas</code>读取，<code>pandas</code>真是一个非常棒的数据分析工具，说回本例，数据存储在<code>csv</code>文件中，读取里面的一列或者某几列存储为列表格式</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line">csv_file=<span class="string">'Pandas_example_read.csv'</span></span><br><span class="line"><span class="comment"># 默认使用第一行作为列的名称</span></span><br><span class="line">df_example=pd.read_csv(filepath,header=<span class="number">0</span>)</span><br><span class="line"><span class="comment"># csv存在标题但是想换成自己的标题</span></span><br><span class="line">df_example = pd.read_csv(<span class="string">'Pandas_example_read.csv'</span>，names=[<span class="string">'A'</span>,<span class="string">'B'</span>,<span class="string">'C'</span>]) </span><br><span class="line"></span><br><span class="line"><span class="comment"># CSV没有标题，第一行就是数据，这样读取的时候要设置header</span></span><br><span class="line">df_example_noCols = pd.read_csv(<span class="string">'Pandas_example_read_withoutCols.csv'</span>, header=<span class="keyword">None</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># csv没有标题，读的时候加上标题，操作方法和有标题相同</span></span><br><span class="line">df_example_noCols = pd.read_csv(<span class="string">'Pandas_example_read_withoutCols.csv'</span>, names=[<span class="string">'A'</span>, <span class="string">'B'</span>,<span class="string">'C'</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># key 对应的是要提取的列的header</span></span><br><span class="line">select=df_example[<span class="string">'key'</span>].tolist()</span><br></pre></td></tr></table></figure><h2 id="图片处理"><a href="#图片处理" class="headerlink" title="图片处理"></a>图片处理</h2><h3 id="Canny边缘检测"><a href="#Canny边缘检测" class="headerlink" title="Canny边缘检测"></a>Canny边缘检测</h3><p>参考：<strong><a href="https://mp.weixin.qq.com/s/ejwnsPkrpSJieCPssU8Caw" target="_blank" rel="noopener">https://mp.weixin.qq.com/s/ejwnsPkrpSJieCPssU8Caw</a></strong></p><p>主要涉及如下几个操作</p><ul><li>降噪</li><li>梯度计算</li><li>非最大抑制</li><li>双重阈值</li><li>滞后边缘跟踪</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy <span class="keyword">import</span> ndimage</span><br><span class="line"><span class="keyword">from</span> scipy.ndimage.filters <span class="keyword">import</span> convolve</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> scipy <span class="keyword">import</span> misc</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">cannyEdgeDetector</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, imgs, sigma=<span class="number">1</span>, kernel_size=<span class="number">5</span>, weak_pixel=<span class="number">75</span>, strong_pixel=<span class="number">255</span>, lowthreshold=<span class="number">0.05</span>, highthreshold=<span class="number">0.15</span>)</span>:</span></span><br><span class="line">        self.imgs = imgs</span><br><span class="line">        self.imgs_final = []</span><br><span class="line">        self.img_smoothed = <span class="keyword">None</span></span><br><span class="line">        self.gradientMat = <span class="keyword">None</span></span><br><span class="line">        self.thetaMat = <span class="keyword">None</span> </span><br><span class="line">        self.nonMaxImg = <span class="keyword">None</span></span><br><span class="line">        self.thresholdImg = <span class="keyword">None</span></span><br><span class="line">        self.weak_pixel = weak_pixel</span><br><span class="line">        self.strong_pixel = strong_pixel</span><br><span class="line">        self.sigma = sigma</span><br><span class="line">        self.kernel_size = kernel_size</span><br><span class="line">        self.lowThreshold = lowthreshold</span><br><span class="line">        self.highThreshold = highthreshold</span><br><span class="line">        <span class="keyword">return</span> </span><br><span class="line">    <span class="comment"># 创建高斯核函数</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">gaussian_kernel</span><span class="params">(self, size, sigma=<span class="number">1</span>)</span>:</span></span><br><span class="line">        size = int(size) // <span class="number">2</span></span><br><span class="line">        x, y = np.mgrid[-size:size+<span class="number">1</span>, -size:size+<span class="number">1</span>]</span><br><span class="line">        normal = <span class="number">1</span> / (<span class="number">2.0</span> * np.pi * sigma**<span class="number">2</span>)</span><br><span class="line">        g =  np.exp(-((x**<span class="number">2</span> + y**<span class="number">2</span>) / (<span class="number">2.0</span>*sigma**<span class="number">2</span>))) * normal</span><br><span class="line">        <span class="keyword">return</span> g</span><br><span class="line">    <span class="comment"># 梯度计算步骤通过使用边缘检测算子计算图像的梯度来检测边缘强度和方向。</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">sobel_filters</span><span class="params">(self, img)</span>:</span></span><br><span class="line">        Kx = np.array([[<span class="number">-1</span>, <span class="number">0</span>, <span class="number">1</span>], [<span class="number">-2</span>, <span class="number">0</span>, <span class="number">2</span>], [<span class="number">-1</span>, <span class="number">0</span>, <span class="number">1</span>]], np.float32)</span><br><span class="line">        Ky = np.array([[<span class="number">1</span>, <span class="number">2</span>, <span class="number">1</span>], [<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>], [<span class="number">-1</span>, <span class="number">-2</span>, <span class="number">-1</span>]], np.float32)</span><br><span class="line"></span><br><span class="line">        Ix = ndimage.filters.convolve(img, Kx)</span><br><span class="line">        Iy = ndimage.filters.convolve(img, Ky)</span><br><span class="line"></span><br><span class="line">        G = np.hypot(Ix, Iy)</span><br><span class="line">        G = G / G.max() * <span class="number">255</span></span><br><span class="line">        theta = np.arctan2(Iy, Ix)</span><br><span class="line">        <span class="keyword">return</span> (G, theta)</span><br><span class="line">    </span><br><span class="line"><span class="comment"># 非最大抑制步骤是：</span></span><br><span class="line">    <span class="comment"># 创建一个初始化为0的矩阵，该矩阵与原始梯度强度矩阵的大小相同;</span></span><br><span class="line">    <span class="comment"># 根据角度矩阵的角度值识别边缘方向;</span></span><br><span class="line">    <span class="comment"># 检查相同方向的像素是否具有比当前处理的像素更高的强度;</span></span><br><span class="line">    <span class="comment"># 返回使用非最大抑制算法处理的图像。</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">non_max_suppression</span><span class="params">(self, img, D)</span>:</span></span><br><span class="line">        M, N = img.shape</span><br><span class="line">        Z = np.zeros((M,N), dtype=np.int32)</span><br><span class="line">        angle = D * <span class="number">180.</span> / np.pi</span><br><span class="line">        angle[angle &lt; <span class="number">0</span>] += <span class="number">180</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,M<span class="number">-1</span>):</span><br><span class="line">            <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>,N<span class="number">-1</span>):</span><br><span class="line">                <span class="keyword">try</span>:</span><br><span class="line">                    q = <span class="number">255</span></span><br><span class="line">                    r = <span class="number">255</span></span><br><span class="line"></span><br><span class="line">                   <span class="comment">#angle 0</span></span><br><span class="line">                    <span class="keyword">if</span> (<span class="number">0</span> &lt;= angle[i,j] &lt; <span class="number">22.5</span>) <span class="keyword">or</span> (<span class="number">157.5</span> &lt;= angle[i,j] &lt;= <span class="number">180</span>):</span><br><span class="line">                        q = img[i, j+<span class="number">1</span>]</span><br><span class="line">                        r = img[i, j<span class="number">-1</span>]</span><br><span class="line">                    <span class="comment">#angle 45</span></span><br><span class="line">                    <span class="keyword">elif</span> (<span class="number">22.5</span> &lt;= angle[i,j] &lt; <span class="number">67.5</span>):</span><br><span class="line">                        q = img[i+<span class="number">1</span>, j<span class="number">-1</span>]</span><br><span class="line">                        r = img[i<span class="number">-1</span>, j+<span class="number">1</span>]</span><br><span class="line">                    <span class="comment">#angle 90</span></span><br><span class="line">                    <span class="keyword">elif</span> (<span class="number">67.5</span> &lt;= angle[i,j] &lt; <span class="number">112.5</span>):</span><br><span class="line">                        q = img[i+<span class="number">1</span>, j]</span><br><span class="line">                        r = img[i<span class="number">-1</span>, j]</span><br><span class="line">                    <span class="comment">#angle 135</span></span><br><span class="line">                    <span class="keyword">elif</span> (<span class="number">112.5</span> &lt;= angle[i,j] &lt; <span class="number">157.5</span>):</span><br><span class="line">                        q = img[i<span class="number">-1</span>, j<span class="number">-1</span>]</span><br><span class="line">                        r = img[i+<span class="number">1</span>, j+<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">if</span> (img[i,j] &gt;= q) <span class="keyword">and</span> (img[i,j] &gt;= r):</span><br><span class="line">                        Z[i,j] = img[i,j]</span><br><span class="line">                    <span class="keyword">else</span>:</span><br><span class="line">                        Z[i,j] = <span class="number">0</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">                <span class="keyword">except</span> IndexError <span class="keyword">as</span> e:</span><br><span class="line">                    <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> Z</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 双阈值步骤旨在识别3种像素：强，弱和不相关：</span></span><br><span class="line">    <span class="comment"># 强像素是强度如此之高的像素，我们确信它们有助于最终边缘。</span></span><br><span class="line">    <span class="comment"># 弱像素是具有不足以被视为强的强度值的像素，但是还不足以被认为与边缘检测不相关。其他像素被认为与边缘无关。</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">threshold</span><span class="params">(self, img)</span>:</span></span><br><span class="line"></span><br><span class="line">        highThreshold = img.max() * self.highThreshold;</span><br><span class="line">        lowThreshold = highThreshold * self.lowThreshold;</span><br><span class="line"></span><br><span class="line">        M, N = img.shape</span><br><span class="line">        res = np.zeros((M,N), dtype=np.int32)</span><br><span class="line"></span><br><span class="line">        weak = np.int32(self.weak_pixel)</span><br><span class="line">        strong = np.int32(self.strong_pixel)</span><br><span class="line"></span><br><span class="line">        strong_i, strong_j = np.where(img &gt;= highThreshold)</span><br><span class="line">        zeros_i, zeros_j = np.where(img &lt; lowThreshold)</span><br><span class="line"></span><br><span class="line">        weak_i, weak_j = np.where((img &lt;= highThreshold) &amp; (img &gt;= lowThreshold))</span><br><span class="line"></span><br><span class="line">        res[strong_i, strong_j] = strong</span><br><span class="line">        res[weak_i, weak_j] = weak</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> (res)</span><br><span class="line"><span class="comment"># 滞后边缘跟踪</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">hysteresis</span><span class="params">(self, img)</span>:</span></span><br><span class="line"></span><br><span class="line">        M, N = img.shape</span><br><span class="line">        weak = self.weak_pixel</span><br><span class="line">        strong = self.strong_pixel</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, M<span class="number">-1</span>):</span><br><span class="line">            <span class="keyword">for</span> j <span class="keyword">in</span> range(<span class="number">1</span>, N<span class="number">-1</span>):</span><br><span class="line">                <span class="keyword">if</span> (img[i,j] == weak):</span><br><span class="line">                    <span class="keyword">try</span>:</span><br><span class="line">                        <span class="keyword">if</span> ((img[i+<span class="number">1</span>, j<span class="number">-1</span>] == strong) <span class="keyword">or</span> (img[i+<span class="number">1</span>, j] == strong) <span class="keyword">or</span> (img[i+<span class="number">1</span>, j+<span class="number">1</span>] == strong)</span><br><span class="line">                            <span class="keyword">or</span> (img[i, j<span class="number">-1</span>] == strong) <span class="keyword">or</span> (img[i, j+<span class="number">1</span>] == strong)</span><br><span class="line">                            <span class="keyword">or</span> (img[i<span class="number">-1</span>, j<span class="number">-1</span>] == strong) <span class="keyword">or</span> (img[i<span class="number">-1</span>, j] == strong) <span class="keyword">or</span> (img[i<span class="number">-1</span>, j+<span class="number">1</span>] == strong)):</span><br><span class="line">                            img[i, j] = strong</span><br><span class="line">                        <span class="keyword">else</span>:</span><br><span class="line">                            img[i, j] = <span class="number">0</span></span><br><span class="line">                    <span class="keyword">except</span> IndexError <span class="keyword">as</span> e:</span><br><span class="line">                        <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> img</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">detect</span><span class="params">(self)</span>:</span></span><br><span class="line">        imgs_final = []</span><br><span class="line">        <span class="keyword">for</span> i, img <span class="keyword">in</span> enumerate(self.imgs):    </span><br><span class="line">            self.img_smoothed = convolve(img, self.gaussian_kernel(self.kernel_size, self.sigma))</span><br><span class="line">            self.gradientMat, self.thetaMat = self.sobel_filters(self.img_smoothed)</span><br><span class="line">            self.nonMaxImg = self.non_max_suppression(self.gradientMat, self.thetaMat)</span><br><span class="line">            self.thresholdImg = self.threshold(self.nonMaxImg)</span><br><span class="line">            img_final = self.hysteresis(self.thresholdImg)</span><br><span class="line">            self.imgs_final.append(img_final)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> self.imgs_final</span><br></pre></td></tr></table></figure><h3 id="降噪"><a href="#降噪" class="headerlink" title="降噪"></a>降噪</h3><p>由于场景背后涉及的数学主要基于导数（参见步骤2：梯度计算），边缘检测结果对图像噪声高度敏感。摆脱图像噪声的一种方法是使用高斯模糊来平滑它。为此图像卷积技术应用高斯核（3x3,5x5,7x7等…）。内核大小取决于预期的模糊效果。基本上内核最小，模糊不太明显。</p><h3 id="判断图像是否模糊"><a href="#判断图像是否模糊" class="headerlink" title="判断图像是否模糊"></a>判断图像是否模糊</h3><p>Reference:<a href="https://juejin.im/post/5b76df76f265da43330c3f50" target="_blank" rel="noopener">https://juejin.im/post/5b76df76f265da43330c3f50</a></p><p>将图像转换成灰度图，之后使用拉普拉斯算子进行滤波，得到的是图像的高频图像，会将低频图像去掉，计算滤波之后的图像的方差，如果方差较大，认为滤波之后的图像具有更广阔的频率范围，认为是一个清晰的图像； 如果滤波之后方差较小，可以认为，经过滤波得到的图像，频率范围很窄，可以在一定程度上认为这是一个模糊的图像。总而言之，是通过拉普拉斯滤波之后的图像的方差的值进行判断。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> cv2</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">getImageVar</span><span class="params">(imgPath)</span>:</span></span><br><span class="line">image = cv2.imread(imgPath);</span><br><span class="line">img2gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)</span><br><span class="line">imageVar = cv2.Laplacian(img2gray, cv2.CV_64F).var()</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> imageVar</span><br></pre></td></tr></table></figure><p><strong>但是会存在一个问题，那就是对于远景虚化或者远景模糊的图像，经过滤波之后，得到的图像会将虚化部分过滤掉，这样相比于正常的清晰照片，得到的图像的方差较低，有可能会被误杀。</strong></p><h2 id="IOU的计算"><a href="#IOU的计算" class="headerlink" title="IOU的计算"></a>IOU的计算</h2><p>在<code>ObjectDetection</code>任务中，在后处理阶段一般要进行<code>NMS</code>处理，也就是需要计算两个<code>box</code>之间的<code>IOU</code>，顾明思议，<code>IOU</code>就是两个矩形交集面积与并集面积的比值，比值的范围为$[0,1]$，比值越大，表示两个矩形越相同。</p><p>代码实现</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#coding=utf-8</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_cal_iou</span><span class="params">(query_box, store_box)</span>:</span></span><br><span class="line">  <span class="comment"># @query_box is a list, contains the cordinates for the leftup and rightdown corner</span></span><br><span class="line">    <span class="comment"># left_x,left_y,right_x,right_y=query_box</span></span><br><span class="line">    <span class="comment"># @store_box is a list, each element is a box</span></span><br><span class="line">    <span class="comment"># return，ratios between query and store boxes</span></span><br><span class="line">    x1, y1, x2, y2 = query_box</span><br><span class="line">    w1 = x2 - x1</span><br><span class="line">    h1 = y2 - y1</span><br><span class="line">    query_area = w1 * h1</span><br><span class="line">    ratios = []</span><br><span class="line">    <span class="keyword">for</span> box <span class="keyword">in</span> store_box:</span><br><span class="line">        x_11, y_11, x_22, y_22 = box</span><br><span class="line">        w_11 = x_22 - x_11</span><br><span class="line">        h_11 = y_22 - y_11</span><br><span class="line">        store_area = w_11*h_11</span><br><span class="line">        </span><br><span class="line">        endx = max(x2, x_22)</span><br><span class="line">        startx = min(x1, x_11)</span><br><span class="line">        width = w1 + w_11 - (endx - startx)</span><br><span class="line">        </span><br><span class="line">        endy = max(y2, y_22)</span><br><span class="line">        starty = min(y1, y_11)</span><br><span class="line">        height = h1 + h_11 - (endy - starty)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> width &lt;= <span class="number">0</span> <span class="keyword">or</span> height &lt;= <span class="number">0</span>:</span><br><span class="line">            ratio = <span class="number">0</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            inter_area = width * height</span><br><span class="line">            ratio = inter_area / (query_area + store_area - inter_area)</span><br><span class="line">        ratios.append(ratio)</span><br><span class="line">    <span class="keyword">return</span> ratios</span><br></pre></td></tr></table></figure><h2 id="数据转换Convertation"><a href="#数据转换Convertation" class="headerlink" title="数据转换Convertation"></a>数据转换Convertation</h2><h3 id="String-amp-List"><a href="#String-amp-List" class="headerlink" title="String&amp;List"></a>String&amp;List</h3><p>该程序涉及将字符串转换为<code>list</code>，这里的字符串不是简单地字符串，是具有数据格式的字符串，如下所示</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">demo=<span class="string">'[&#123;"label":0,"dots":[&#123;"x":109.60000000000001,"y":7.466666666666667&#125;,&#123;"x":311.2,"y":7.466666666666667&#125;,&#123;"x":311.2,"y":197.33333333333334&#125;,&#123;"x":109.60000000000001,"y":197.33333333333334&#125;]&#125;]'</span></span><br></pre></td></tr></table></figure><p>可以看出来，虽然整体是一个<code>string</code>的格式，但是内部实际上是列表，并且列表内是一个字典，那么如何将这个字符串转换成列表，并且能够读取列表中的字典呢？</p><p>很简单，使用依赖包<code>ast</code></p><p>处理代码如下所示</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">## import package</span></span><br><span class="line"><span class="keyword">from</span> ast <span class="keyword">import</span> literal_eval</span><br><span class="line"><span class="comment">##<span class="doctag">NOTE:</span> turn string to list</span></span><br><span class="line">mlist = literal_eval(demo)</span><br><span class="line"><span class="keyword">assert</span> isinstance(mlist,list)， <span class="string">"After literal_eval, must return a list"</span></span><br><span class="line">out=[]</span><br><span class="line"><span class="keyword">for</span> dic <span class="keyword">in</span> mlist:</span><br><span class="line">    want=dic[<span class="string">'dots'</span>]</span><br><span class="line">    keys=[<span class="string">'x'</span>,<span class="string">'y'</span>]</span><br><span class="line">    <span class="keyword">for</span> each_points <span class="keyword">in</span> want:</span><br><span class="line">        <span class="keyword">for</span> key <span class="keyword">in</span> keys:</span><br><span class="line">            out.append(each_points[key])</span><br><span class="line"><span class="keyword">print</span> out</span><br></pre></td></tr></table></figure><p>经过如上程序，就可以得到列表</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[109.60000000000001, 7.466666666666667, 311.2, 7.466666666666667, 311.2, 197.33333333333334, 109.60000000000001, 197.33333333333334]</span><br></pre></td></tr></table></figure><p>对应的<code>jupyter</code>程序截图如下</p><img src="/2020/01/29/Awesomes-Python/string_to_list.png" title="string to image"><h3 id="Dict-amp-Object"><a href="#Dict-amp-Object" class="headerlink" title="Dict&amp;Object"></a>Dict&amp;Object</h3><p>在<code>Object</code>中直接调用<code>object.__dict__</code>就可以得到当前object的dictionary形式</p><img src="/2020/01/29/Awesomes-Python/object_dict.png" title="Get dictionary from object"><p>当然了，还要有反向操作，就是将<code>dictionary</code>转换成<code>struct</code>形式，通过一个转换方程</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Struct</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, d)</span>:</span></span><br><span class="line">        <span class="keyword">for</span> a, b <span class="keyword">in</span> d.items():</span><br><span class="line">            <span class="keyword">if</span> isinstance(b, (list, tuple)):</span><br><span class="line">               setattr(self, a, [Struct(x) <span class="keyword">if</span> isinstance(x, dict) <span class="keyword">else</span> x <span class="keyword">for</span> x <span class="keyword">in</span> b])</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">               setattr(self, a, Struct(b) <span class="keyword">if</span> isinstance(b, dict) <span class="keyword">else</span> b)</span><br></pre></td></tr></table></figure><p>对应的<code>jupyter</code>测试程序如下所示↓↓↓↓</p><img src="/2020/01/29/Awesomes-Python/dict_obj.png" title="Convet dictionary to struct"><h3 id="数据序列化"><a href="#数据序列化" class="headerlink" title="数据序列化"></a>数据序列化</h3><p>在进行传输的过程中，一般对数据进行序列化，比如在图片转化成base64格式，tcp传输使用pb或protobuf格式等，根据自己工作的经验，对这方面进行一下总结记录。</p><h4 id="pickle-amp-cPickle"><a href="#pickle-amp-cPickle" class="headerlink" title="pickle&amp;cPickle"></a>pickle&amp;cPickle</h4><p>使用<code>pickle.dumps</code>和<code>pickle.loads</code>的方式进行序列化和反序列化</p><p><code>Python</code>中提供了<code>pickle</code>和<code>cPickle</code>两个模块，<code>cPickle</code>顾名思义使用C编译的，运行速度更快，但是cPickle模块中定义的类型不能被继承（其实大多数时候，我们不需要从这些类型中继承，推荐使用cPickle）。<code>cPickle</code>和<code>pickle</code>的序列化/反序列化规则是一样的，使用<code>pickle</code>序列化一个对象，可以使用<code>cPickle</code>来反序列化。同时，这两个模块在处理自引用类型时会变得更加“聪明”，它不会无限制的递归序列化自引用对象，对于同一对象的多次引用，它只会序列化一次。</p><p>在操作的时候有一个参数需要注意<code>protocol</code>这个表示代表了序列化模式(pickle协议)，<u><strong>在python2.X版本默认值为0，在python3.X本默认值为3</strong></u>。简而言之，不同的python版本对应着不同的最高协议，同时protocol值越大，代表了所用的协议版本越高，<strong>protocol值越大，dump的速度越快，并且支持的数据类型更多，保存下来的文件占用空间更小</strong></p><h4 id="Json"><a href="#Json" class="headerlink" title="Json"></a>Json</h4><p>使用<code>json.dumps</code>和<code>json.loads</code>对字典数据进行序列化和反序列化</p><p>在<code>Python</code>中包含多个<code>json</code>处理方式</p><div class="table-container"><table><thead><tr><th>依赖库</th><th>序列化耗时</th><th>反序列化耗时</th></tr></thead><tbody><tr><td>yajl</td><td>1.910</td><td>1.970</td></tr><tr><td>cjson</td><td>3.305</td><td>1.328</td></tr><tr><td>simplejson</td><td>10.279</td><td>4.658</td></tr><tr><td>stdlib json</td><td>7.013</td><td>8.579</td></tr><tr><td>ujson</td><td>2.084</td><td>1.157</td></tr></tbody></table></div><p>一般情况下使用<code>ujson</code>来也就是<code>import ujson as json</code>使用方法还是<code>json.dumps</code>和<code>json.loads</code></p><p>显而易见，<code>json</code>类型的数据保存了数据中元素的数据类型和数据结构等信息，比如<code>json</code>中保存多维数组数据，只需要在使用<code>vector.to_list()</code>将其赋值给字典中的元素即可使用<code>json.dumps</code>的方式保存，但是<code>json</code>方式带来的问题就是序列化的时间很慢，尤其是带序列化的数据越大，所需时间大量增加。</p><h4 id="msgpack"><a href="#msgpack" class="headerlink" title="msgpack"></a>msgpack</h4><p>和<code>json</code>类似，<a href="https://msgpack.org/" target="_blank" rel="noopener"><code>msgpack</code></a>的口号就是<code>It&#39;s like JSON. but fast and small.</code>目前我已经不再使用<code>json</code>而使用<code>msgpack</code>了，序列化和反序列化时间相比<code>usjon</code>更快，也是使用<code>msgpack.loads</code>和<code>msgpack.dumps</code>的方式。</p><h4 id="struct"><a href="#struct" class="headerlink" title="struct"></a>struct</h4><p>将字符串转化为二进制字节流，这样缺点是最造成数据的结构丢失，但是好处是会最大程度减小数据的大小。在<code>pack</code>的时候，只能处理<code>float</code>的数据，也就是如果数据是多维的，那么要对数据进行铺平才能进行二进制自己留转化。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> struct</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">data = np.random.uniform(size=(<span class="number">10</span>, <span class="number">4</span>, <span class="number">2</span>))</span><br><span class="line">data_size = data.size</span><br><span class="line">data_shape = data.shape</span><br><span class="line"><span class="comment"># pack data</span></span><br><span class="line">packed_data = struct.pack(<span class="string">'=%sf'</span> % data.size, *data.flatten(<span class="string">'F'</span>))</span><br><span class="line"><span class="comment"># unpack data</span></span><br><span class="line">unpacked_data = struct.unpack(<span class="string">'=%sf'</span> % data_size, packed_data)</span><br><span class="line"><span class="comment"># reshape data to original shape</span></span><br><span class="line">original_data = np.reshape(np.asarray(unpacked_data), data_shape,order=<span class="string">'F'</span>)</span><br><span class="line">print(<span class="string">"original_data shape is &#123;&#125;"</span>.format(original_data.shape))</span><br></pre></td></tr></table></figure><h4 id="thrift"><a href="#thrift" class="headerlink" title="thrift"></a>thrift</h4><p>thrift一般用于定义数据格式，用来进行服务之间的tcp传递。一般是两种处理方式，一个是将字典转化成<code>struct</code>的形式，也就是先使用<a href="https://en.wikipedia.org/wiki/Apache_Thrift" target="_blank" rel="noopener"><strong>thrift</strong></a>定义结构体，然后再将字典数据赋值到对应的结构体上</p><ol><li><p>安装<code>thrift</code>  <a href="https://thrift.apache.org/docs/install/debian" target="_blank" rel="noopener">https://thrift.apache.org/docs/install/debian</a></p></li><li><p>编写<code>thrift</code>文件定义<code>struct</code>和<code>tcp</code>函数的接口</p><p>假设如下文件名为<code>service.thrift</code></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">!/usr/<span class="built_in">local</span>/bin/thrift --gen cpp --gen py:new_style -o . -r</span></span><br><span class="line">include "base.thrift" // thrift dependicy</span><br><span class="line">namespace py Service // namespace, the folder name you want generate by thrift</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> define request</span></span><br><span class="line">struct ServiceRequest&#123;</span><br><span class="line">  1: binary image_binary, // image binary </span><br><span class="line">  2: i32 req_id, // request id</span><br><span class="line">  255: optional base.Base Base // base caller</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> define response</span></span><br><span class="line">struct ServiceResponse &#123;</span><br><span class="line">  1: binary output, </span><br><span class="line">  255: optional base.BaseResp BaseResp // base response</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> define service</span></span><br><span class="line">service Service &#123;</span><br><span class="line">    ServiceResponse forward(1: ServiceRequest request),</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>实际上按照上面的步骤，导入需要的依赖，一般是通用项，用来保存常用的请求和返回的status，msg等信息，对于上面的<code>base.thrift</code>我这里是这么定义的 ↓↓↓↓↓</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">   #</span><span class="bash"> defination</span></span><br><span class="line">   namespace py base</span><br><span class="line">   namespace go base</span><br><span class="line">   namespace java com.bytedance.thrift.base</span><br><span class="line">   </span><br><span class="line">   struct TrafficEnv &#123;</span><br><span class="line">       1: bool Open = false,</span><br><span class="line">       2: string Env = "",</span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line"><span class="meta">   #</span><span class="bash"> define the base request</span></span><br><span class="line">   struct Base &#123;</span><br><span class="line">       1: string LogID = "",</span><br><span class="line">       2: string Caller = "",</span><br><span class="line">       3: string Addr = "",</span><br><span class="line">       4: string Client = "",</span><br><span class="line">       5: optional TrafficEnv TrafficEnv,</span><br><span class="line">       6: optional map&lt;string, string&gt; Extra,</span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line"><span class="meta">   #</span><span class="bash"> define the base response</span></span><br><span class="line">   struct BaseResp &#123;</span><br><span class="line">       1: string StatusMessage = "",</span><br><span class="line">       2: i32 StatusCode = 0,</span><br><span class="line">       3: optional map&lt;string, string&gt; Extra,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>定义好这两个<code>thrift</code>文件之后那么使用安装的<code>thrift</code>命令生成对应的可读取文件， 如果想要生成<code>python</code>文件，那么可以直接执行命令行命令</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> !/usr/bin/env bash</span></span><br><span class="line"></span><br><span class="line">echo "Step1: thrift deploy"</span><br><span class="line">thrift -r --gen py:new_style -o . service.thrift</span><br><span class="line"></span><br><span class="line">echo "Step2: remove gen-py detail output to root"</span><br><span class="line">mv gen-py gen_py</span><br></pre></td></tr></table></figure></li></ol><p>   如此就会生成文件夹<code>gen_py</code>在该文件夹下包含了两个目录<code>service</code> 和<code>base</code>，在这个目录下包含着对应的接口。</p><h2 id="数据处理"><a href="#数据处理" class="headerlink" title="数据处理"></a>数据处理</h2><h3 id="去重并保持原有顺序"><a href="#去重并保持原有顺序" class="headerlink" title="去重并保持原有顺序"></a>去重并保持原有顺序</h3><p>以前一直使用set来进行去重，但是set这种方式第一是只能够去除hash类型的数据的重复，并且去重之后无法保证原来的排序，如下所示</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">x=[<span class="number">1</span>, <span class="number">5</span>, <span class="number">2</span>, <span class="number">1</span>, <span class="number">9</span>, <span class="number">1</span>, <span class="number">5</span>, <span class="number">10</span>]</span><br><span class="line">y=list(set(x))</span><br><span class="line"><span class="comment"># y=[1, 2, 10, 5, 9]</span></span><br></pre></td></tr></table></figure><p>在python-cookbook一书中介绍了一种方法，可以在去重的时候保持原数据的顺序</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># define function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">dedupe</span><span class="params">(items)</span>:</span> </span><br><span class="line">    seen = set()</span><br><span class="line">    <span class="keyword">for</span> item <span class="keyword">in</span> items: </span><br><span class="line">        <span class="keyword">if</span> item <span class="keyword">not</span> <span class="keyword">in</span> seen: </span><br><span class="line">            <span class="keyword">yield</span> item </span><br><span class="line">            seen.add(item)</span><br><span class="line">x=[<span class="number">1</span>, <span class="number">5</span>, <span class="number">2</span>, <span class="number">1</span>, <span class="number">9</span>, <span class="number">1</span>, <span class="number">5</span>, <span class="number">10</span>]</span><br><span class="line">y=list(dedupe(x))</span><br><span class="line"><span class="comment"># y=[1, 5, 2, 9, 10]</span></span><br></pre></td></tr></table></figure><h3 id="命名切片"><a href="#命名切片" class="headerlink" title="命名切片"></a>命名切片</h3><p>在<code>Python</code>中切片非常好用，但是有的时候数据边长，或者刚接触的代码很难搞清楚切片的含义，建议使用命名切片</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">record = <span class="string">'....................100 .......513.25 ..........'</span> </span><br><span class="line">SHARES = slice(<span class="number">20</span>, <span class="number">23</span>) </span><br><span class="line">PRICE = slice(<span class="number">31</span>, <span class="number">37</span>) </span><br><span class="line">cost_ori = int(record[<span class="number">20</span>:<span class="number">23</span>]) * float(record[<span class="number">31</span>:<span class="number">37</span>])</span><br><span class="line">cost_imp = int(record[SHARES]) * float(record[PRICE])</span><br><span class="line"><span class="comment"># cost=51325.0</span></span><br></pre></td></tr></table></figure><p>代码中<code>cost_ori</code>和<code>cost_imp</code>是等价的，很显然使用命名切片的方式更容易理解代码含义。</p><h2 id="随机数"><a href="#随机数" class="headerlink" title="随机数"></a>随机数</h2><h3 id="random"><a href="#random" class="headerlink" title="random"></a>random</h3><p>一般常用的功能如下</p><ol><li>random.choice() 随机选择一个元素</li><li>random.sample() 随机选择N个元素组成列表</li><li>random.shuffle() 打乱数据</li><li>random.seed() 随机种子，在shuffle之前使用，直生效一次</li><li>random.randint() 生成随机整数</li><li>random.random() 生成0-1之间随机浮点数，用来控制概率</li><li>random.uniform() 计算均匀分布随机数</li><li>random.gauss() 计算正态分布随机数</li></ol><p>使用的时候可以使用<strong>random.Random(random_seed)</strong>来创建全局变量，这样<code>seed</code>可以全局生效</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> random</span><br><span class="line"></span><br><span class="line">random_seed=<span class="number">123456</span></span><br><span class="line">rng=random.Random(random_seed)</span><br><span class="line">samples=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>]</span><br><span class="line">rng.shuffle(all_samples)</span><br><span class="line"><span class="keyword">if</span> rng.random()&lt;<span class="number">0.5</span>:</span><br><span class="line"><span class="keyword">print</span> (<span class="string">"rate is little than 0.5"</span>)</span><br></pre></td></tr></table></figure><h2 id="数据运算加速"><a href="#数据运算加速" class="headerlink" title="数据运算加速"></a>数据运算加速</h2><h3 id="Numba加速"><a href="#Numba加速" class="headerlink" title="Numba加速"></a>Numba加速</h3><p><code>numpy</code>底层使用的<code>c++</code>实现的，实际上已经很快了，但是由于<code>Python</code>是一种动态编译的方法，拖累<code>numpy</code>速度的不是<code>numpy</code>的运算，而是<code>Python</code>的动态编译造成的，为了解决这一问题，显而易见就是把动态编译改成静态编译，<a href="http://numba.pydata.org/" target="_blank" rel="noopener">numba</a>就是用来做这个的，正如官方所说<strong>Numba makes Python code fast</strong>.</p><p><strong>按照官网所述：当程序中包含大量运算，用到了大量的numpy运算或者包含大量的循环结构，这个时候可以考虑使用numba</strong></p><p>使用的时候是按照修饰器的方法</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> numba <span class="keyword">import</span> jit</span><br><span class="line"><span class="keyword">import</span> random</span><br><span class="line"></span><br><span class="line"><span class="meta">@jit(nopython=True)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">monte_carlo_pi</span><span class="params">(nsamples)</span>:</span></span><br><span class="line">    acc = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(nsamples):</span><br><span class="line">        x = random.random()</span><br><span class="line">        y = random.random()</span><br><span class="line">        <span class="keyword">if</span> (x ** <span class="number">2</span> + y ** <span class="number">2</span>) &lt; <span class="number">1.0</span>:</span><br><span class="line">            acc += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">4.0</span> * acc / nsamples</span><br></pre></td></tr></table></figure><p>进行测试的结果如图所示</p><img src="/2020/01/29/Awesomes-Python/numba.png" title="Numba&Numpy"><p>可以看到经过<code>numba</code>加速之后，速度提升了大约30倍。</p><p>按照官方文档，最好使用<code>nopython=True</code>。<code>nopython</code>编译模式的行为是实质上编译装饰后的函数，以便其完全运行而无需<code>Python</code>解释器的参与，这样可以提升代码的运行速度。</p><h2 id="获取设备信息"><a href="#获取设备信息" class="headerlink" title="获取设备信息"></a>获取设备信息</h2><p>一般使用的是<a href="https://psutil.readthedocs.io/en/latest/" target="_blank" rel="noopener">psutil</a>模块，获取当前设备的cpu，memory，disk等使用情况</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#psutil:获取系统信息模块，可以获取CPU，内存，磁盘等的使用情况</span></span><br><span class="line"><span class="keyword">import</span> psutil</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">from</span> datetime <span class="keyword">import</span> datetime</span><br><span class="line"></span><br><span class="line"><span class="comment">#logfile：监测信息写入文件</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">MonitorSystem</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment">#获取cpu使用情况</span></span><br><span class="line">    cpuper = psutil.cpu_percent()</span><br><span class="line">    <span class="comment">#获取内存使用情况：系统内存大小，使用内存，有效内存，内存使用率</span></span><br><span class="line">    mem = psutil.virtual_memory()</span><br><span class="line">    <span class="comment">#内存使用率</span></span><br><span class="line">    memper = mem.percent</span><br><span class="line">    <span class="comment">#获取当前时间</span></span><br><span class="line">    now = datetime.now()</span><br><span class="line">    ts = now.strftime(<span class="string">'%Y-%m-%d %H:%M:%S'</span>)</span><br><span class="line">    line = <span class="string">f'<span class="subst">&#123;ts&#125;</span> cpu:<span class="subst">&#123;cpuper&#125;</span>%, mem:<span class="subst">&#123;memper&#125;</span>%'</span></span><br><span class="line">    print(line)</span><br></pre></td></tr></table></figure><h2 id="时间-日期-定时任务"><a href="#时间-日期-定时任务" class="headerlink" title="时间/日期/定时任务"></a>时间/日期/定时任务</h2><h3 id="获取当前时间"><a href="#获取当前时间" class="headerlink" title="获取当前时间"></a>获取当前时间</h3><p>下面的程序获取当前时刻并打印输出为<code>string</code>格式</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">GetTime</span><span class="params">()</span>:</span></span><br><span class="line">    time_now=time.strftime(<span class="string">'%Y-%m-%d-%H-%M-%S'</span>,time.localtime(time.time()))</span><br><span class="line">    <span class="keyword">return</span> time_now</span><br></pre></td></tr></table></figure><h3 id="获取当前时间戳"><a href="#获取当前时间戳" class="headerlink" title="获取当前时间戳"></a>获取当前时间戳</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">time.time()</span><br></pre></td></tr></table></figure><h3 id="定时任务"><a href="#定时任务" class="headerlink" title="定时任务"></a>定时任务</h3><h4 id="sleep方式"><a href="#sleep方式" class="headerlink" title="sleep方式"></a>sleep方式</h4><p>最简单的方法time.sleep()但是只能控制任务的间隔无法控制任务具体启动的时刻</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#psutil:获取系统信息模块，可以获取CPU，内存，磁盘等的使用情况</span></span><br><span class="line"><span class="keyword">import</span> psutil</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">import</span> datetime</span><br><span class="line"><span class="comment">#logfile：监测信息写入文件</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">MonitorSystem</span><span class="params">(logfile = None)</span>:</span></span><br><span class="line">    <span class="comment">#获取cpu使用情况</span></span><br><span class="line">    cpuper = psutil.cpu_percent()</span><br><span class="line">    <span class="comment">#获取内存使用情况：系统内存大小，使用内存，有效内存，内存使用率</span></span><br><span class="line">    mem = psutil.virtual_memory()</span><br><span class="line">    <span class="comment">#内存使用率</span></span><br><span class="line">    memper = mem.percent</span><br><span class="line">    <span class="comment">#获取当前时间</span></span><br><span class="line">    now = datetime.datetime.now()</span><br><span class="line">    ts = now.strftime(<span class="string">'%Y-%m-%d %H:%M:%S'</span>)</span><br><span class="line">    line = <span class="string">f'<span class="subst">&#123;ts&#125;</span> cpu:<span class="subst">&#123;cpuper&#125;</span>%, mem:<span class="subst">&#123;memper&#125;</span>%'</span></span><br><span class="line">    print(line)</span><br><span class="line">    <span class="keyword">if</span> logfile:</span><br><span class="line">        logfile.write(line)</span><br><span class="line">        </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">loopMonitor</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="keyword">True</span>:</span><br><span class="line">        MonitorSystem()</span><br><span class="line">        <span class="comment">#2s检查一次</span></span><br><span class="line">        time.sleep(<span class="number">2</span>)</span><br><span class="line">loopMonitor()</span><br></pre></td></tr></table></figure><h4 id="Timer模块"><a href="#Timer模块" class="headerlink" title="Timer模块"></a>Timer模块</h4><p>在使用多线程编程，为了线程安全，优先使用该方法。</p><p>定时器只能执行一次，如果需要重复执行，需要重新添加任务；在执行的时候携程递归的方式可以保证函数是重复执行的。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Timer</span><br><span class="line"><span class="keyword">import</span> psutil</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">from</span> datetime <span class="keyword">import</span> datetime</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">MonitorSystem</span><span class="params">(logfile = None)</span>:</span></span><br><span class="line">    cpuper = psutil.cpu_percent()</span><br><span class="line">    mem = psutil.virtual_memory()</span><br><span class="line">    memper = mem.percent</span><br><span class="line">    now = datetime.now()</span><br><span class="line">    ts = now.strftime(<span class="string">'%Y-%m-%d %H:%M:%S'</span>)</span><br><span class="line">    line = <span class="string">f'<span class="subst">&#123;ts&#125;</span> cpu:<span class="subst">&#123;cpuper&#125;</span>%, mem:<span class="subst">&#123;memper&#125;</span>%'</span></span><br><span class="line">    print(line)</span><br><span class="line">    <span class="keyword">if</span> logfile:</span><br><span class="line">        logfile.write(line)</span><br><span class="line">    <span class="comment">#启动定时器任务，每三秒执行一次</span></span><br><span class="line">    Timer(<span class="number">3</span>, MonitorSystem).start()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">MonitorNetWork</span><span class="params">(logfile = None)</span>:</span></span><br><span class="line">    netinfo = psutil.net_io_counters()</span><br><span class="line">    now = datetime.datetime.now()</span><br><span class="line">    ts = now.strftime(<span class="string">'%Y-%m-%d %H:%M:%S'</span>)</span><br><span class="line">    line = <span class="string">f'<span class="subst">&#123;ts&#125;</span> bytessent=<span class="subst">&#123;netinfo.bytes_sent&#125;</span>, bytesrecv=<span class="subst">&#123;netinfo.bytes_recv&#125;</span>'</span></span><br><span class="line">    print(line)</span><br><span class="line">    <span class="keyword">if</span> logfile:</span><br><span class="line">        logfile.write(line)</span><br><span class="line">    <span class="comment">#启动定时器任务，每秒执行一次</span></span><br><span class="line">    Timer(<span class="number">1</span>, MonitorNetWork).start()</span><br><span class="line">    </span><br><span class="line">MonitorSystem()</span><br><span class="line">MonitorNetWork()</span><br></pre></td></tr></table></figure><ol><li>任务框架APScheduler</li></ol><h2 id="多进程"><a href="#多进程" class="headerlink" title="多进程"></a>多进程</h2><p>所谓多进程可以理解成，独立运行多个程序，可以看做是一种并发环境；与之对应的是多线程，多线程是在一个进程中的，每个进程处理一部分的数据或操作。</p><p>在机器学习和深度学习中，当模型训练好之后，一般使用多进程进行<strong>Inference</strong>操作，但是有一点需要注意，要在每个进程中单独Load模型。</p><p>一般的操作方法是</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># !/usr/bin/env python</span></span><br><span class="line"><span class="comment"># -*- coding:utf-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> __future__ <span class="keyword">import</span> division, print_function</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"></span><br><span class="line"><span class="comment"># handler function to exit the process</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">exit_signal_handler</span><span class="params">(sig=None, frame=None)</span>:</span></span><br><span class="line">    t1 = time.time()</span><br><span class="line">    <span class="keyword">while</span>(multiprocessing.active_children()):</span><br><span class="line">        <span class="keyword">for</span> p <span class="keyword">in</span> multiprocessing.active_children():</span><br><span class="line">            t2 = time.time()</span><br><span class="line">            <span class="keyword">if</span> (t2 - t1 &gt; <span class="number">5</span>):</span><br><span class="line">                os.kill(p.pid, signal.SIGKILL)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                p.terminate()</span><br><span class="line">        time.sleep(<span class="number">1</span>)</span><br><span class="line">    os._exit(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># receive keyboard information</span></span><br><span class="line">signal.signal(signal.SIGINT, exit_signal_handler)</span><br><span class="line">signal.signal(signal.SIGTERM, exit_signal_handler)</span><br><span class="line">signal.signal(signal.SIGHUP, exit_signal_handler)</span><br><span class="line"></span><br><span class="line">processes=[]</span><br><span class="line">process_num=<span class="number">10</span></span><br><span class="line"><span class="keyword">for</span> process_idx <span class="keyword">in</span> range(process_num):</span><br><span class="line">  <span class="comment"># if there only one parmeter, the args should add "," </span></span><br><span class="line">  consumer = Process(target=consumer, args=(process_idx,))</span><br><span class="line">    processes.append(consumer)</span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">  <span class="comment"># start the process and protect the process</span></span><br><span class="line">    <span class="keyword">for</span> p <span class="keyword">in</span> processes:</span><br><span class="line">        p.daemon = <span class="keyword">True</span></span><br><span class="line">        p.start()</span><br><span class="line">    <span class="keyword">for</span> p <span class="keyword">in</span> processes:</span><br><span class="line">        p.join()</span><br><span class="line"><span class="comment"># recevie the keyboard input to terminate the process</span></span><br><span class="line"><span class="keyword">except</span> KeyboardInterrupt:</span><br><span class="line">    self.logger.info(<span class="string">'Parent received ctrl-c'</span>)</span><br><span class="line">    <span class="keyword">for</span> p <span class="keyword">in</span> processes:</span><br><span class="line">        p.terminate()</span><br><span class="line">        p.join()</span><br></pre></td></tr></table></figure><p>需要注意，在process的<code>args</code>参数，要是一个可迭代的对象，可以是列表，元组等</p><h3 id="多进程数据同步"><a href="#多进程数据同步" class="headerlink" title="多进程数据同步"></a>多进程数据同步</h3><p>在多进程中，如果需要进行数据同步，一般使用队列来进行进程数据管理，比如一个进程写入数据，其余进程读取数据，<strong>那么这里的进程要使用multiprocessing下面的Queue，不能使用默认的Queue。</strong></p><h2 id="logging"><a href="#logging" class="headerlink" title="logging"></a>logging</h2><h3 id="简单版"><a href="#简单版" class="headerlink" title="简单版"></a>简单版</h3><p>只使用log代替print显示，并不写入文件</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> logging</span><br><span class="line">logger = logging.getLogger(<span class="string">'IntenseModel'</span>)</span><br><span class="line">logger.setLevel(logging.DEBUG)</span><br><span class="line"></span><br><span class="line"><span class="comment"># create console handler and set level to debug</span></span><br><span class="line">ch = logging.StreamHandler()</span><br><span class="line">ch.setLevel(logging.DEBUG)</span><br><span class="line"></span><br><span class="line"><span class="comment"># create formatter</span></span><br><span class="line">formatter = logging.Formatter(<span class="string">'%(asctime)s - %(name)s - %(levelname)s - %(message)s'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># add formatter to ch</span></span><br><span class="line">ch.setFormatter(formatter)</span><br><span class="line"></span><br><span class="line"><span class="comment"># add ch to logger</span></span><br><span class="line">logger.addHandler(ch)</span><br></pre></td></tr></table></figure><h3 id="进阶版"><a href="#进阶版" class="headerlink" title="进阶版"></a>进阶版</h3><p>日志打印模块写入文件，把一个轮子记录在此，直接使用</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> logging</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_logger</span><span class="params">(_loggerDir, log_filename, logger_name)</span>:</span></span><br><span class="line">    <span class="string">"""[summary]</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    Arguments:</span></span><br><span class="line"><span class="string">        _loggerDir &#123;[type]&#125; -- [directory of the Logger]</span></span><br><span class="line"><span class="string">        log_filename &#123;[type]&#125; -- [Logger name ]</span></span><br><span class="line"><span class="string">        logger_name &#123;[type]&#125; -- [Logger file name]</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    Returns:</span></span><br><span class="line"><span class="string">        [type] -- [description]</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    _LogFile = os.path.join(_loggerDir, log_filename)</span><br><span class="line"></span><br><span class="line">    logger = logging.getLogger(logger_name)</span><br><span class="line">    logger.setLevel(logging.INFO)</span><br><span class="line">    <span class="comment"># default_value is True, it will send mesaage to the root, which will print twice for one mesaage</span></span><br><span class="line">    logger.propagate=<span class="keyword">False</span></span><br><span class="line">    <span class="comment"># ccreate file handler which logs even debug messages</span></span><br><span class="line">    fh = logging.FileHandler(_LogFile)</span><br><span class="line">    fh.setLevel(logging.INFO)</span><br><span class="line">    <span class="comment"># create console handler with a higher log level</span></span><br><span class="line">    console = logging.StreamHandler()</span><br><span class="line">    console.setLevel(logging.INFO)</span><br><span class="line">    <span class="comment"># create formatter and add it to the handlers</span></span><br><span class="line">    _LogFormat = logging.Formatter(<span class="string">"%(asctime)2s -%(name)-12s:  %(levelname)-10s - %(message)s"</span>)</span><br><span class="line"></span><br><span class="line">    fh.setFormatter(_LogFormat)</span><br><span class="line">    console.setFormatter(_LogFormat)</span><br><span class="line">    <span class="comment"># add the handlers to logger</span></span><br><span class="line">    logger.addHandler(fh)</span><br><span class="line">    logger.addHandler(console)</span><br><span class="line">    <span class="keyword">return</span> logger</span><br></pre></td></tr></table></figure><h2 id="Opencv-amp-PIL"><a href="#Opencv-amp-PIL" class="headerlink" title="Opencv&amp;PIL"></a>Opencv&amp;PIL</h2><p><code>Opencv</code>默认读取的图片格式为<strong>BGR</strong>通道顺序，写入的时候默认也是<strong>BGR</strong>的格式，，如果使用OpenCV读写，那么不需要进行通道转换，但是如果使用OpenCV读用PIL 写，那么在读取的时候要进行图片通道转换。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> cv2</span><br><span class="line"><span class="keyword">from</span> PIL <span class="keyword">import</span> Image</span><br><span class="line"></span><br><span class="line"><span class="comment"># opencv read and write</span></span><br><span class="line">image_file=<span class="string">''</span></span><br><span class="line">img_data=cv2.imread(image_file)</span><br><span class="line">img_crop=img_data[y1:y2,x1:x2]</span><br><span class="line">cv2.imwrite(crop_file_dir,img_crop)</span><br><span class="line"></span><br><span class="line"><span class="comment"># oepncv read, PIL write</span></span><br><span class="line">img_data=cv2.imread(image_file)</span><br><span class="line">img_data=img_data[:,:,::<span class="number">-1</span>]</span><br><span class="line">img=Image.fromarray(img_data)</span><br><span class="line">img.save(<span class="string">'xxxx'</span>)</span><br></pre></td></tr></table></figure><h2 id="出错重试"><a href="#出错重试" class="headerlink" title="出错重试"></a>出错重试</h2><h3 id="Tenacity"><a href="#Tenacity" class="headerlink" title="Tenacity"></a>Tenacity</h3><p>在爬虫或者下载的时候很容易出现错误，报错的时候进行自动重试</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> tenacity <span class="keyword">import</span> retry,stop_after_delay,stop_after_attempt,wait_fixed</span><br><span class="line"></span><br><span class="line"><span class="comment"># 重试5次，每次间隔10秒，重试前等待2秒</span></span><br><span class="line"><span class="meta">@retry(stop=(stop_after_delay(10) | stop_after_attempt(5)), wait=wait_fixed(2))</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">stop_after_10_s_or_5_retries</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"Stopping after 10 seconds or 5 retries"</span>)</span><br><span class="line">    <span class="string">"""other progress"""</span></span><br><span class="line">    <span class="keyword">raise</span> Exception</span><br></pre></td></tr></table></figure><h2 id="报错解决汇总"><a href="#报错解决汇总" class="headerlink" title="报错解决汇总"></a>报错解决汇总</h2><h3 id="locale-Error-unsupported-locale-setting"><a href="#locale-Error-unsupported-locale-setting" class="headerlink" title="locale.Error: unsupported locale setting"></a>locale.Error: unsupported locale setting</h3><p>解决方案</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> LC_ALL=C</span><br></pre></td></tr></table></figure><p>链接： <a href="https://stackoverflow.com/questions/36394101/pip-install-locale-error-unsupported-locale-setting" target="_blank" rel="noopener">https://stackoverflow.com/questions/36394101/pip-install-locale-error-unsupported-locale-setting</a></p><h3 id="UnicodeDecodeError"><a href="#UnicodeDecodeError" class="headerlink" title="UnicodeDecodeError"></a>UnicodeDecodeError</h3><p>这个是一大类的错误，顾名思义就是编码错误了，一版情况下是读取数据的时候读取错误造成的，比如说使用py2生成的文件，使用py3读取，因为py2的默认编码方式是unicode，而py3的默认编码方式是utf8，就很可能造成这个错误，解决方案</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">with</span> open(filepath,<span class="string">"rb"</span>) <span class="keyword">as</span> fi:</span><br><span class="line">   xxx</span><br></pre></td></tr></table></figure><p>关键就是使用<code>rb</code>方式打开文件，如果不添加打开方式或者使用`r的方式打开，会产生<strong>‘utf-8’ codec can’t decode byte 0x87 in position 0: invalid start byte</strong>类似的错误。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本篇涉及平时工作中使用的一些&lt;strong&gt;Python&lt;/strong&gt;相关轮子，在此记录，不断补充更新……&lt;/p&gt;
&lt;img src=&quot;/2020/01/29/Awesomes-Python/python.png&quot; title=&quot;Python&quot;&gt;
    
    </summary>
    
      <category term="Awesomes" scheme="http://xiaofengshi.com/categories/Awesomes/"/>
    
      <category term="Python" scheme="http://xiaofengshi.com/categories/Awesomes/Python/"/>
    
    
      <category term="Python" scheme="http://xiaofengshi.com/tags/Python/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-NLP-BERT</title>
    <link href="http://xiaofengshi.com/2019/12/10/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-NLP-BERT/"/>
    <id>http://xiaofengshi.com/2019/12/10/深度学习-NLP-BERT/</id>
    <published>2019-12-10T06:42:56.000Z</published>
    <updated>2020-02-03T13:30:36.975Z</updated>
    
    <content type="html"><![CDATA[<img src="/2019/12/10/深度学习-NLP-BERT/bert.png" title="BERT"><p><strong>BERT</strong>全名为(<strong>Bidirectional Encoder Representations from Transformers</strong>), 在<strong>NLP</strong>领域作为<strong>Word2vec</strong>的替代者，将NLP中的$11$项任务精度向前大大推进，作为一名算法工程师，非常有必要好好研究一下。</p><a id="more"></a><h2 id="BERT的背景和特点"><a href="#BERT的背景和特点" class="headerlink" title="BERT的背景和特点"></a>BERT的背景和特点</h2><h3 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h3><p>​    在<code>CV</code>领域，我们一般会使用基于<strong>Imagenet</strong>预训练的模型权重作为其他任务的basenet，这样不仅能够提高模型的泛化能力并且可以减少训练时间，使训练过程更加平稳，这是预训练带来的好处。在<code>NLP</code>领域，BERT就是要做这样的事情，在通用数据上学习词向量，并能够迁移到其他下游子任务。</p><h3 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h3><ol><li>对<strong>Transformer</strong>结构的极致使用</li><li>在预训练时，使用<code>MLM(Mask Language Model)</code>和<code>NSP(Next Sentence Prediction)</code>作为训练目标捕获词和句的关系，句和句的关系<ol><li><code>MLM</code>：随机mask一些词进行预测，类似于完形填空，该任务主要捕获序列不同词之间的关系</li><li><code>NSP</code>：预测两个句子是否是连续前后句关系，给定两个句子，预测后面的句子是否是前面的句子的下一句，该任务主要捕获不同序列之间的关系</li></ol></li><li>使用海量的数据进行训练，可以进行分布式训练</li><li>可以方便用于其他任务的迁移和微调，例如句子相关性，机器翻译，语音识别，关键词提取等NLP相关任务</li></ol><p>BERT有可能存在的问题</p><ol><li>由于<code>transformer</code>的利用，使用<code>position embding</code>来捕获序列中元素的位置关系，但是在巨量数据中，<code>transformer</code>对元素位置的信息表征不强</li></ol><h2 id="BERT如何工作"><a href="#BERT如何工作" class="headerlink" title="BERT如何工作"></a>BERT如何工作</h2><p>Bert使用了transformer结构，一种google提出的注意力学习机制，用以学习文本中不同token之间的上下文关系。在transformer中包含encoder和decoder部分，由于BERT的目的是生成语言模型，因此只需要transformer中的encoder部分。关于transformer，相关详细讲解以前有写过<a href="http://xiaofengshi.com/2019/02/15/NLP-Attention_is_all_you_need/">👉👉👉</a>。</p><p> transformer改变了原来使用RNN的方式，在RNN中为了捕获当前词语和其左右两侧词语的关系一般使用BiRNN设计，并且RNN不能进行并行设计，无法利用GPU加速运算。在Transformer中输入不再入RNN相同的是单个token而是整个sequence，并且加入输入的sequence中每个token的位置信息，这样每个token自然可以接收到它的上下文信息，并且这种设计可以使用GPU进行加速运算。</p><img src="/2019/12/10/深度学习-NLP-BERT/transformer.png" title="[TransformerBlock]"><h3 id="MLM"><a href="#MLM" class="headerlink" title="MLM"></a>MLM</h3><p>如开始所述，<code>MLM</code>任务类似于我们做过的完形填空，是为了学习<code>token</code>的上下文关系。在输入的序列中选择所有<code>token</code>的$15\%$的比例进行<code>mask</code>处理，在这$15\%$中选择$80\%$使用<code>MASK</code>进行替换，$15 \%$从此表中随机选择其它<code>token</code>替换，$10\%$保持原来的<code>token</code>不变，这样设计的<code>MLM</code>任务，既保证了当前的模型见过真正的词是什么，又见过随机替换的次和空白词，有助于模型能够学习到根据其余词推断当前词的能力。</p><p>在网络设计上，在模型输出的最后一层接一个全连接层将输出特征的维度转化为词表大小，然后在使用softmax和交叉熵损失来进行设计，和通用的分类网络最后部分相同，在计算损失的时候，只计算数据中被MASK的部分的损失。</p><h3 id="NSP"><a href="#NSP" class="headerlink" title="NSP"></a>NSP</h3><p><strong>NSP</strong>是为了学习句子之间上下文关系，通俗的将就是，给你两个句子，你来判断这两个句子是不是挨着的，如果是挨着的那么就是1，否则就是0。在<code>BERT</code>中模型接收的是成对的句子，并学习预测成对的第二个句子是否是第一个句子在原始文档中的后续句子。 在数据制作时，第二个句子有$50\%$的概率是真是的第一个句子的后续句子，$50\%$的概率是从其余的文档中随机抽选的句子，并且有一个先验就是，随机选择的句子和第一个句子是没啥关系的。为了让模型能够顺利学习两个句子之间的上下文关系，在数据原始数据中要进行一些处理，</p><ol><li>在输入的序列的头部插入入<code>CLS</code>这个<code>token</code>，在第一个句子结束后插入<code>SEP</code>，如果两个句子满足上下文关系那么$CLS=1$，否则为$0$；</li><li>在输入的向量中加入<code>token type</code>的<code>embeding</code>，标识当前的<code>token</code>归属于第一个句子还是第二个句子。</li><li>加入位置向量，用来表征输入的<code>token</code>的位置信息。</li></ol><img src="/2019/12/10/深度学习-NLP-BERT/bert_preprocess.png" title="Bert feature input"><p>在网络设计上，在模型输出的最后一层，取第一个位置进行全连接转化维度为$2$，进行一个0/1分类的损失计算。</p><h2 id="BERT代码研究"><a href="#BERT代码研究" class="headerlink" title="BERT代码研究"></a>BERT代码研究</h2><p>下面结合官方代码进行分析 <a href="https://github.com/google-research/bert" target="_blank" rel="noopener">https://github.com/google-research/bert</a></p><img src="/2019/12/10/深度学习-NLP-BERT/bert_structure.png" title="[BertStructure]"><p>图中<code>Trm</code>对应一个Transformer Block模块。在Tansformer中，position embeding将序列中任意两个点之间的距离都转化成了1，因此在BERT中，得益于transformer的应用，可以捕获所有层中的左右两侧的语义信息。</p><h3 id="数据处理"><a href="#数据处理" class="headerlink" title="数据处理"></a>数据处理</h3><p>在<code>BERT</code>中，输入的编码向量由三部分组成</p><h4 id="Tokens"><a href="#Tokens" class="headerlink" title="Tokens"></a>Tokens</h4><p>将输入的单词划分成一组有限的公共子词单元，也就是“拆词”，这个在英文在试有必要的，这样能够能在单词的有效性和字符的灵活性之间取得一个折中的平衡；但是在中文中，最小的单位就是一个字，不需要再进行拆分。官方代码位置 <a href="https://github.com/google-research/bert/blob/master/tokenization.py#L161，我对其中进行了部分修改，" target="_blank" rel="noopener">https://github.com/google-research/bert/blob/master/tokenization.py#L161，我对其中进行了部分修改，</a></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br><span class="line">212</span><br><span class="line">213</span><br><span class="line">214</span><br><span class="line">215</span><br><span class="line">216</span><br><span class="line">217</span><br><span class="line">218</span><br><span class="line">219</span><br><span class="line">220</span><br><span class="line">221</span><br><span class="line">222</span><br><span class="line">223</span><br><span class="line">224</span><br><span class="line">225</span><br><span class="line">226</span><br><span class="line">227</span><br><span class="line">228</span><br><span class="line">229</span><br><span class="line">230</span><br><span class="line">231</span><br><span class="line">232</span><br><span class="line">233</span><br><span class="line">234</span><br><span class="line">235</span><br><span class="line">236</span><br><span class="line">237</span><br><span class="line">238</span><br><span class="line">239</span><br><span class="line">240</span><br><span class="line">241</span><br><span class="line">242</span><br><span class="line">243</span><br><span class="line">244</span><br><span class="line">245</span><br><span class="line">246</span><br><span class="line">247</span><br><span class="line">248</span><br><span class="line">249</span><br><span class="line">250</span><br><span class="line">251</span><br><span class="line">252</span><br><span class="line">253</span><br><span class="line">254</span><br><span class="line">255</span><br><span class="line">256</span><br><span class="line">257</span><br><span class="line">258</span><br><span class="line">259</span><br><span class="line">260</span><br><span class="line">261</span><br><span class="line">262</span><br><span class="line">263</span><br><span class="line">264</span><br><span class="line">265</span><br><span class="line">266</span><br><span class="line">267</span><br><span class="line">268</span><br><span class="line">269</span><br><span class="line">270</span><br><span class="line">271</span><br><span class="line">272</span><br><span class="line">273</span><br><span class="line">274</span><br><span class="line">275</span><br><span class="line">276</span><br><span class="line">277</span><br><span class="line">278</span><br><span class="line">279</span><br><span class="line">280</span><br><span class="line">281</span><br><span class="line">282</span><br><span class="line">283</span><br><span class="line">284</span><br><span class="line">285</span><br><span class="line">286</span><br><span class="line">287</span><br><span class="line">288</span><br><span class="line">289</span><br><span class="line">290</span><br><span class="line">291</span><br><span class="line">292</span><br><span class="line">293</span><br><span class="line">294</span><br><span class="line">295</span><br><span class="line">296</span><br><span class="line">297</span><br><span class="line">298</span><br><span class="line">299</span><br><span class="line">300</span><br><span class="line">301</span><br><span class="line">302</span><br><span class="line">303</span><br><span class="line">304</span><br><span class="line">305</span><br><span class="line">306</span><br><span class="line">307</span><br><span class="line">308</span><br><span class="line">309</span><br><span class="line">310</span><br><span class="line">311</span><br><span class="line">312</span><br><span class="line">313</span><br><span class="line">314</span><br><span class="line">315</span><br><span class="line">316</span><br><span class="line">317</span><br><span class="line">318</span><br><span class="line">319</span><br><span class="line">320</span><br><span class="line">321</span><br><span class="line">322</span><br><span class="line">323</span><br><span class="line">324</span><br><span class="line">325</span><br><span class="line">326</span><br><span class="line">327</span><br><span class="line">328</span><br><span class="line">329</span><br><span class="line">330</span><br><span class="line">331</span><br><span class="line">332</span><br><span class="line">333</span><br><span class="line">334</span><br><span class="line">335</span><br><span class="line">336</span><br><span class="line">337</span><br><span class="line">338</span><br><span class="line">339</span><br><span class="line">340</span><br><span class="line">341</span><br><span class="line">342</span><br><span class="line">343</span><br><span class="line">344</span><br><span class="line">345</span><br><span class="line">346</span><br><span class="line">347</span><br><span class="line">348</span><br><span class="line">349</span><br><span class="line">350</span><br><span class="line">351</span><br><span class="line">352</span><br><span class="line">353</span><br><span class="line">354</span><br><span class="line">355</span><br><span class="line">356</span><br><span class="line">357</span><br><span class="line">358</span><br><span class="line">359</span><br><span class="line">360</span><br><span class="line">361</span><br><span class="line">362</span><br><span class="line">363</span><br><span class="line">364</span><br><span class="line">365</span><br><span class="line">366</span><br><span class="line">367</span><br><span class="line">368</span><br><span class="line">369</span><br><span class="line">370</span><br><span class="line">371</span><br><span class="line">372</span><br><span class="line">373</span><br><span class="line">374</span><br><span class="line">375</span><br><span class="line">376</span><br><span class="line">377</span><br><span class="line">378</span><br><span class="line">379</span><br><span class="line">380</span><br><span class="line">381</span><br><span class="line">382</span><br><span class="line">383</span><br><span class="line">384</span><br><span class="line">385</span><br><span class="line">386</span><br><span class="line">387</span><br><span class="line">388</span><br><span class="line">389</span><br><span class="line">390</span><br><span class="line">391</span><br><span class="line">392</span><br><span class="line">393</span><br><span class="line">394</span><br><span class="line">395</span><br><span class="line">396</span><br><span class="line">397</span><br><span class="line">398</span><br><span class="line">399</span><br><span class="line">400</span><br><span class="line">401</span><br><span class="line">402</span><br><span class="line">403</span><br><span class="line">404</span><br><span class="line">405</span><br><span class="line">406</span><br><span class="line">407</span><br><span class="line">408</span><br><span class="line">409</span><br><span class="line">410</span><br><span class="line">411</span><br><span class="line">412</span><br><span class="line">413</span><br><span class="line">414</span><br><span class="line">415</span><br><span class="line">416</span><br><span class="line">417</span><br><span class="line">418</span><br><span class="line">419</span><br><span class="line">420</span><br><span class="line">421</span><br><span class="line">422</span><br><span class="line">423</span><br><span class="line">424</span><br><span class="line">425</span><br><span class="line">426</span><br><span class="line">427</span><br><span class="line">428</span><br><span class="line">429</span><br><span class="line">430</span><br><span class="line">431</span><br><span class="line">432</span><br><span class="line">433</span><br><span class="line">434</span><br><span class="line">435</span><br><span class="line">436</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">from</span> __future__ <span class="keyword">import</span> absolute_import</span><br><span class="line"><span class="keyword">from</span> __future__ <span class="keyword">import</span> division</span><br><span class="line"><span class="keyword">from</span> __future__ <span class="keyword">import</span> print_function</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> collections</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"><span class="keyword">import</span> unicodedata</span><br><span class="line"><span class="keyword">import</span> six</span><br><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> traceback</span><br><span class="line"><span class="keyword">import</span> logging</span><br><span class="line"></span><br><span class="line">reload(sys)</span><br><span class="line">sys.setdefaultencoding(<span class="string">'utf8'</span>)</span><br><span class="line"></span><br><span class="line">logger=logging</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">validate_case_matches_checkpoint</span><span class="params">(do_lower_case, init_checkpoint)</span>:</span></span><br><span class="line">    <span class="string">"""Checks whether the casing config is consistent with the checkpoint name."""</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># The casing has to be passed in by the user and there is no explicit check</span></span><br><span class="line">    <span class="comment"># as to whether it matches the checkpoint. The casing information probably</span></span><br><span class="line">    <span class="comment"># should have been stored in the bert_config.json file, but it's not, so</span></span><br><span class="line">    <span class="comment"># we have to heuristically detect it to validate.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> init_checkpoint:</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">    m = re.match(<span class="string">"^.*?([A-Za-z0-9_-]+)/bert_model.ckpt"</span>, init_checkpoint)</span><br><span class="line">    <span class="keyword">if</span> m <span class="keyword">is</span> <span class="keyword">None</span>:</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">    model_name = m.group(<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">    lower_models = [</span><br><span class="line">        <span class="string">"uncased_L-24_H-1024_A-16"</span>, <span class="string">"uncased_L-12_H-768_A-12"</span>,</span><br><span class="line">        <span class="string">"multilingual_L-12_H-768_A-12"</span>, <span class="string">"chinese_L-12_H-768_A-12"</span></span><br><span class="line">    ]</span><br><span class="line"></span><br><span class="line">    cased_models = [</span><br><span class="line">        <span class="string">"cased_L-12_H-768_A-12"</span>, <span class="string">"cased_L-24_H-1024_A-16"</span>,</span><br><span class="line">        <span class="string">"multi_cased_L-12_H-768_A-12"</span></span><br><span class="line">    ]</span><br><span class="line"></span><br><span class="line">    is_bad_config = <span class="keyword">False</span></span><br><span class="line">    <span class="keyword">if</span> model_name <span class="keyword">in</span> lower_models <span class="keyword">and</span> <span class="keyword">not</span> do_lower_case:</span><br><span class="line">        is_bad_config = <span class="keyword">True</span></span><br><span class="line">        actual_flag = <span class="string">"False"</span></span><br><span class="line">        case_name = <span class="string">"lowercased"</span></span><br><span class="line">        opposite_flag = <span class="string">"True"</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> model_name <span class="keyword">in</span> cased_models <span class="keyword">and</span> do_lower_case:</span><br><span class="line">        is_bad_config = <span class="keyword">True</span></span><br><span class="line">        actual_flag = <span class="string">"True"</span></span><br><span class="line">        case_name = <span class="string">"cased"</span></span><br><span class="line">        opposite_flag = <span class="string">"False"</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> is_bad_config:</span><br><span class="line">        <span class="keyword">raise</span> ValueError(</span><br><span class="line">            <span class="string">"You passed in `--do_lower_case=%s` with `--init_checkpoint=%s`. "</span></span><br><span class="line">            <span class="string">"However, `%s` seems to be a %s model, so you "</span></span><br><span class="line">            <span class="string">"should pass in `--do_lower_case=%s` so that the fine-tuning matches "</span></span><br><span class="line">            <span class="string">"how the model was pre-training. If this error is wrong, please "</span></span><br><span class="line">            <span class="string">"just comment out this check."</span> %</span><br><span class="line">            (actual_flag, init_checkpoint, model_name, case_name,</span><br><span class="line">             opposite_flag))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">convert_to_unicode</span><span class="params">(text)</span>:</span></span><br><span class="line">    <span class="string">"""Converts `text` to Unicode (if it's not already), assuming utf-8 input."""</span></span><br><span class="line">    <span class="keyword">if</span> six.PY3:</span><br><span class="line">        <span class="keyword">if</span> isinstance(text, str):</span><br><span class="line">            <span class="keyword">return</span> text</span><br><span class="line">        <span class="keyword">elif</span> isinstance(text, bytes):</span><br><span class="line">            <span class="keyword">return</span> text.decode(<span class="string">"utf-8"</span>, <span class="string">"ignore"</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">"Unsupported string type: %s"</span> % (type(text)))</span><br><span class="line">    <span class="keyword">elif</span> six.PY2:</span><br><span class="line">        <span class="keyword">if</span> isinstance(text, str):</span><br><span class="line">            <span class="keyword">return</span> text.decode(<span class="string">"utf-8"</span>, <span class="string">"ignore"</span>)</span><br><span class="line">        <span class="keyword">elif</span> isinstance(text, unicode):</span><br><span class="line">            <span class="keyword">return</span> text</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">"Unsupported string type: %s"</span> % (type(text)))</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">raise</span> ValueError(<span class="string">"Not running on Python2 or Python 3?"</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">printable_text</span><span class="params">(text)</span>:</span></span><br><span class="line">    <span class="string">"""Returns text encoded in a way suitable for print or `tf.logging`."""</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># These functions want `str` for both Python2 and Python3, but in one case</span></span><br><span class="line">    <span class="comment"># it's a Unicode string and in the other it's a byte string.</span></span><br><span class="line">    <span class="keyword">if</span> six.PY3:</span><br><span class="line">        <span class="keyword">if</span> isinstance(text, str):</span><br><span class="line">            <span class="keyword">return</span> text</span><br><span class="line">        <span class="keyword">elif</span> isinstance(text, bytes):</span><br><span class="line">            <span class="keyword">return</span> text.decode(<span class="string">"utf-8"</span>, <span class="string">"ignore"</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">"Unsupported string type: %s"</span> % (type(text)))</span><br><span class="line">    <span class="keyword">elif</span> six.PY2:</span><br><span class="line">        <span class="keyword">if</span> isinstance(text, str):</span><br><span class="line">            <span class="keyword">return</span> text</span><br><span class="line">        <span class="keyword">elif</span> isinstance(text, unicode):</span><br><span class="line">            <span class="keyword">return</span> text.encode(<span class="string">"utf-8"</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">"Unsupported string type: %s"</span> % (type(text)))</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">raise</span> ValueError(<span class="string">"Not running on Python2 or Python 3?"</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">load_vocab</span><span class="params">(vocab_file)</span>:</span></span><br><span class="line">    <span class="string">"""Loads a vocabulary file into a dictionary."""</span></span><br><span class="line">    vocab = collections.OrderedDict()</span><br><span class="line">    index = <span class="number">0</span></span><br><span class="line">    <span class="keyword">with</span> tf.gfile.GFile(vocab_file, <span class="string">"r"</span>) <span class="keyword">as</span> reader:</span><br><span class="line">        <span class="keyword">while</span> <span class="keyword">True</span>:</span><br><span class="line">            token = convert_to_unicode(reader.readline())</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> token:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            token = token.strip()</span><br><span class="line">            vocab[token] = index</span><br><span class="line">            index += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> vocab</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">convert_by_vocab</span><span class="params">(vocab, items)</span>:</span></span><br><span class="line">    <span class="string">"""Converts a sequence of [tokens|ids] using the vocab."""</span></span><br><span class="line">    output = []</span><br><span class="line">    <span class="keyword">for</span> item <span class="keyword">in</span> items:</span><br><span class="line">        output.append(vocab[item])</span><br><span class="line">    <span class="keyword">return</span> output</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">convert_tokens_to_ids</span><span class="params">(vocab, tokens)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> convert_by_vocab(vocab, tokens)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">convert_ids_to_tokens</span><span class="params">(inv_vocab, ids)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> convert_by_vocab(inv_vocab, ids)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">whitespace_tokenize</span><span class="params">(text)</span>:</span></span><br><span class="line">    <span class="string">"""Runs basic whitespace cleaning and splitting on a piece of text."""</span></span><br><span class="line">    text = text.strip()</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> text:</span><br><span class="line">        <span class="keyword">return</span> []</span><br><span class="line">    tokens = text.split()</span><br><span class="line">    <span class="keyword">return</span> tokens</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FullTokenizer</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""Runs end-to-end tokenziation."""</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, vocab_file, do_lower_case=True)</span>:</span></span><br><span class="line">        self.vocab = load_vocab(vocab_file)</span><br><span class="line">        self.inv_vocab = &#123;v: k <span class="keyword">for</span> k, v <span class="keyword">in</span> self.vocab.items()&#125;</span><br><span class="line">        self.basic_tokenizer = BasicTokenizer(do_lower_case=do_lower_case)</span><br><span class="line">        self.wordpiece_tokenizer = WordpieceTokenizer(vocab=self.vocab)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tokenize</span><span class="params">(self, text)</span>:</span></span><br><span class="line">        split_tokens = []</span><br><span class="line">        logger.debug(<span class="string">"text is: &#123;:s&#125;"</span>.format(text))</span><br><span class="line">        basic_tokenizers = self.basic_tokenizer.tokenize(text)</span><br><span class="line">        logger.debug(<span class="string">"basic_tokenizers is &#123;&#125;"</span>.format(basic_tokenizers))</span><br><span class="line">        <span class="keyword">for</span> token <span class="keyword">in</span> basic_tokenizers:</span><br><span class="line">            logger.debug(<span class="string">"BasicTokenizer output: &#123;:s&#125;"</span>.format(token))</span><br><span class="line">            <span class="keyword">for</span> sub_token <span class="keyword">in</span> self.wordpiece_tokenizer.tokenize(token):</span><br><span class="line">                split_tokens.append(sub_token)</span><br><span class="line">        logger.debug(<span class="string">"Splits token is &#123;&#125;"</span>.format(split_tokens))</span><br><span class="line">        <span class="keyword">return</span> split_tokens</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tokenize_unicoder</span><span class="params">(self, text)</span>:</span></span><br><span class="line">        <span class="string">"""</span></span><br><span class="line"><span class="string">        [summary]</span></span><br><span class="line"><span class="string">            just token the textual part, do not change the rest</span></span><br><span class="line"><span class="string">        Arguments:</span></span><br><span class="line"><span class="string">            text &#123;[string]&#125; -- ["id\tdet1\tdet2\ttext"]</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        Returns:</span></span><br><span class="line"><span class="string">            details [list] -- [id,det1,det2,token1,token2,token3,token4...]</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="keyword">if</span> isinstance(text,str):</span><br><span class="line">                split_text = text.strip().split(<span class="string">"\t"</span>)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                split_text = text</span><br><span class="line">            logger.info(<span class="string">"split text &#123;&#125;"</span>.format(split_text))</span><br><span class="line">            image_id, image_url, title_text = split_text</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> AbaseClient.exists(Params.abase_image_text_relevance_data.format(image_id)):</span><br><span class="line">                logger.error(<span class="string">"&#123;&#125; do not exist in abase"</span>.format(image_id))</span><br><span class="line">                <span class="keyword">return</span> []</span><br><span class="line">            split_tokens = [image_id,image_url]</span><br><span class="line">            <span class="keyword">for</span> token <span class="keyword">in</span> self.basic_tokenizer.tokenize(title_text):</span><br><span class="line">                logger.debug(<span class="string">"tokenize_unicoder: BasicTokenizer output is --&gt; &#123;&#125;"</span>.format(token))</span><br><span class="line">                <span class="keyword">for</span> sub_token <span class="keyword">in</span> self.wordpiece_tokenizer.tokenize(token):</span><br><span class="line">                    split_tokens.append(sub_token)</span><br><span class="line">            <span class="keyword">return</span> split_tokens</span><br><span class="line">        <span class="keyword">except</span>:</span><br><span class="line">            logger.error(traceback.format_exc())</span><br><span class="line">            <span class="keyword">return</span> []</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">convert_tokens_to_ids</span><span class="params">(self, tokens)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> convert_by_vocab(self.vocab, tokens)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">convert_ids_to_tokens</span><span class="params">(self, ids)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> convert_by_vocab(self.inv_vocab, ids)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BasicTokenizer</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""Runs basic tokenization (punctuation splitting, lower casing, etc.)."""</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, do_lower_case=True)</span>:</span></span><br><span class="line">        <span class="string">"""Constructs a BasicTokenizer.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        Args:</span></span><br><span class="line"><span class="string">          do_lower_case: Whether to lower case the input.</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        self.do_lower_case = do_lower_case</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tokenize</span><span class="params">(self, text)</span>:</span></span><br><span class="line">        <span class="string">"""Tokenizes a piece of text."""</span></span><br><span class="line">        text = convert_to_unicode(text)</span><br><span class="line">        text = self._clean_text(text)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># This was added on November 1st, 2018 for the multilingual and Chinese</span></span><br><span class="line">        <span class="comment"># models. This is also applied to the English models now, but it doesn't</span></span><br><span class="line">        <span class="comment"># matter since the English models were not trained on any Chinese data</span></span><br><span class="line">        <span class="comment"># and generally don't have any Chinese data in them (there are Chinese</span></span><br><span class="line">        <span class="comment"># characters in the vocabulary because Wikipedia does have some Chinese</span></span><br><span class="line">        <span class="comment"># words in the English Wikipedia.).</span></span><br><span class="line">        text = self._tokenize_chinese_chars(text)</span><br><span class="line"></span><br><span class="line">        orig_tokens = whitespace_tokenize(text)</span><br><span class="line">        split_tokens = []</span><br><span class="line">        <span class="keyword">for</span> token <span class="keyword">in</span> orig_tokens:</span><br><span class="line">            <span class="keyword">if</span> self.do_lower_case:</span><br><span class="line">                token = token.lower()</span><br><span class="line">                token = self._run_strip_accents(token)</span><br><span class="line">            split_tokens.extend(self._run_split_on_punc(token))</span><br><span class="line"></span><br><span class="line">        output_tokens = whitespace_tokenize(<span class="string">" "</span>.join(split_tokens))</span><br><span class="line">        <span class="keyword">return</span> output_tokens</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_run_strip_accents</span><span class="params">(self, text)</span>:</span></span><br><span class="line">        <span class="string">"""Strips accents from a piece of text."""</span></span><br><span class="line">        text = unicodedata.normalize(<span class="string">"NFD"</span>, text)</span><br><span class="line">        output = []</span><br><span class="line">        <span class="keyword">for</span> char <span class="keyword">in</span> text:</span><br><span class="line">            cat = unicodedata.category(char)</span><br><span class="line">            <span class="keyword">if</span> cat == <span class="string">"Mn"</span>:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            output.append(char)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">""</span>.join(output)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_run_split_on_punc</span><span class="params">(self, text)</span>:</span></span><br><span class="line">        <span class="string">"""Splits punctuation on a piece of text."""</span></span><br><span class="line">        chars = list(text)</span><br><span class="line">        i = <span class="number">0</span></span><br><span class="line">        start_new_word = <span class="keyword">True</span></span><br><span class="line">        output = []</span><br><span class="line">        <span class="keyword">while</span> i &lt; len(chars):</span><br><span class="line">            char = chars[i]</span><br><span class="line">            <span class="keyword">if</span> _is_punctuation(char):</span><br><span class="line">                output.append([char])</span><br><span class="line">                start_new_word = <span class="keyword">True</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">if</span> start_new_word:</span><br><span class="line">                    output.append([])</span><br><span class="line">                start_new_word = <span class="keyword">False</span></span><br><span class="line">                output[<span class="number">-1</span>].append(char)</span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> [<span class="string">""</span>.join(x) <span class="keyword">for</span> x <span class="keyword">in</span> output]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_tokenize_chinese_chars</span><span class="params">(self, text)</span>:</span></span><br><span class="line">        <span class="string">"""Adds whitespace around any CJK character."""</span></span><br><span class="line">        output = []</span><br><span class="line">        <span class="keyword">for</span> char <span class="keyword">in</span> text:</span><br><span class="line">            cp = ord(char)</span><br><span class="line">            <span class="keyword">if</span> self._is_chinese_char(cp):</span><br><span class="line">                output.append(<span class="string">" "</span>)</span><br><span class="line">                output.append(char)</span><br><span class="line">                output.append(<span class="string">" "</span>)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                output.append(char)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">""</span>.join(output)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_is_chinese_char</span><span class="params">(self, cp)</span>:</span></span><br><span class="line">        <span class="string">"""Checks whether CP is the codepoint of a CJK character."""</span></span><br><span class="line">        <span class="comment"># This defines a "chinese character" as anything in the CJK Unicode block:</span></span><br><span class="line">        <span class="comment">#   https://en.wikipedia.org/wiki/CJK_Unified_Ideographs_(Unicode_block)</span></span><br><span class="line">        <span class="comment">#</span></span><br><span class="line">        <span class="comment"># Note that the CJK Unicode block is NOT all Japanese and Korean characters,</span></span><br><span class="line">        <span class="comment"># despite its name. The modern Korean Hangul alphabet is a different block,</span></span><br><span class="line">        <span class="comment"># as is Japanese Hiragana and Katakana. Those alphabets are used to write</span></span><br><span class="line">        <span class="comment"># space-separated words, so they are not treated specially and handled</span></span><br><span class="line">        <span class="comment"># like the all of the other languages.</span></span><br><span class="line">        <span class="keyword">if</span> ((cp &gt;= <span class="number">0x4E00</span> <span class="keyword">and</span> cp &lt;= <span class="number">0x9FFF</span>) <span class="keyword">or</span>  <span class="comment">#</span></span><br><span class="line">            (cp &gt;= <span class="number">0x3400</span> <span class="keyword">and</span> cp &lt;= <span class="number">0x4DBF</span>) <span class="keyword">or</span>  <span class="comment">#</span></span><br><span class="line">            (cp &gt;= <span class="number">0x20000</span> <span class="keyword">and</span> cp &lt;= <span class="number">0x2A6DF</span>) <span class="keyword">or</span>  <span class="comment">#</span></span><br><span class="line">            (cp &gt;= <span class="number">0x2A700</span> <span class="keyword">and</span> cp &lt;= <span class="number">0x2B73F</span>) <span class="keyword">or</span>  <span class="comment">#</span></span><br><span class="line">            (cp &gt;= <span class="number">0x2B740</span> <span class="keyword">and</span> cp &lt;= <span class="number">0x2B81F</span>) <span class="keyword">or</span>  <span class="comment">#</span></span><br><span class="line">            (cp &gt;= <span class="number">0x2B820</span> <span class="keyword">and</span> cp &lt;= <span class="number">0x2CEAF</span>) <span class="keyword">or</span></span><br><span class="line">            (cp &gt;= <span class="number">0xF900</span> <span class="keyword">and</span> cp &lt;= <span class="number">0xFAFF</span>) <span class="keyword">or</span>  <span class="comment">#</span></span><br><span class="line">                (cp &gt;= <span class="number">0x2F800</span> <span class="keyword">and</span> cp &lt;= <span class="number">0x2FA1F</span>)):  <span class="comment">#</span></span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_clean_text</span><span class="params">(self, text)</span>:</span></span><br><span class="line">        <span class="string">"""Performs invalid character removal and whitespace cleanup on text."""</span></span><br><span class="line">        output = []</span><br><span class="line">        <span class="keyword">for</span> char <span class="keyword">in</span> text:</span><br><span class="line">            cp = ord(char)</span><br><span class="line">            <span class="keyword">if</span> cp == <span class="number">0</span> <span class="keyword">or</span> cp == <span class="number">0xfffd</span> <span class="keyword">or</span> _is_control(char):</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            <span class="keyword">if</span> _is_whitespace(char):</span><br><span class="line">                output.append(<span class="string">" "</span>)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                output.append(char)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">""</span>.join(output)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">WordpieceTokenizer</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""Runs WordPiece tokenziation."""</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, vocab, unk_token=<span class="string">"[UNK]"</span>, max_input_chars_per_word=<span class="number">200</span>)</span>:</span></span><br><span class="line">        self.vocab = vocab</span><br><span class="line">        self.unk_token = unk_token</span><br><span class="line">        self.max_input_chars_per_word = max_input_chars_per_word</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tokenize</span><span class="params">(self, text)</span>:</span></span><br><span class="line">        <span class="string">"""Tokenizes a piece of text into its word pieces.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        This uses a greedy longest-match-first algorithm to perform tokenization</span></span><br><span class="line"><span class="string">        using the given vocabulary.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        For example:</span></span><br><span class="line"><span class="string">          input = "unaffable"</span></span><br><span class="line"><span class="string">          output = ["un", "##aff", "##able"]</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        Args:</span></span><br><span class="line"><span class="string">          text: A single token or whitespace separated tokens. This should have</span></span><br><span class="line"><span class="string">            already been passed through `BasicTokenizer.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        Returns:</span></span><br><span class="line"><span class="string">          A list of wordpiece tokens.</span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line"></span><br><span class="line">        text = convert_to_unicode(text)</span><br><span class="line"></span><br><span class="line">        output_tokens = []</span><br><span class="line">        <span class="keyword">for</span> token <span class="keyword">in</span> whitespace_tokenize(text):</span><br><span class="line">            chars = list(token)</span><br><span class="line">            <span class="keyword">if</span> len(chars) &gt; self.max_input_chars_per_word:</span><br><span class="line">                output_tokens.append(self.unk_token)</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line"></span><br><span class="line">            is_bad = <span class="keyword">False</span></span><br><span class="line">            start = <span class="number">0</span></span><br><span class="line">            sub_tokens = []</span><br><span class="line">            <span class="keyword">while</span> start &lt; len(chars):</span><br><span class="line">                end = len(chars)</span><br><span class="line">                cur_substr = <span class="keyword">None</span></span><br><span class="line">                <span class="keyword">while</span> start &lt; end:</span><br><span class="line">                    substr = <span class="string">""</span>.join(chars[start:end])</span><br><span class="line">                    <span class="keyword">if</span> start &gt; <span class="number">0</span>:</span><br><span class="line">                        substr = <span class="string">"##"</span> + substr</span><br><span class="line">                    <span class="keyword">if</span> substr <span class="keyword">in</span> self.vocab:</span><br><span class="line">                        cur_substr = substr</span><br><span class="line">                        <span class="keyword">break</span></span><br><span class="line">                    end -= <span class="number">1</span></span><br><span class="line">                <span class="keyword">if</span> cur_substr <span class="keyword">is</span> <span class="keyword">None</span>:</span><br><span class="line">                    is_bad = <span class="keyword">True</span></span><br><span class="line">                    <span class="keyword">break</span></span><br><span class="line">                sub_tokens.append(cur_substr)</span><br><span class="line">                start = end</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> is_bad:</span><br><span class="line">                output_tokens.append(self.unk_token)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                output_tokens.extend(sub_tokens)</span><br><span class="line">        <span class="keyword">return</span> output_tokens</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_is_whitespace</span><span class="params">(char)</span>:</span></span><br><span class="line">    <span class="string">"""Checks whether `chars` is a whitespace character."""</span></span><br><span class="line">    <span class="comment"># \t, \n, and \r are technically contorl characters but we treat them</span></span><br><span class="line">    <span class="comment"># as whitespace since they are generally considered as such.</span></span><br><span class="line">    <span class="keyword">if</span> char == <span class="string">" "</span> <span class="keyword">or</span> char == <span class="string">"\t"</span> <span class="keyword">or</span> char == <span class="string">"\n"</span> <span class="keyword">or</span> char == <span class="string">"\r"</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">    cat = unicodedata.category(char)</span><br><span class="line">    <span class="keyword">if</span> cat == <span class="string">"Zs"</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_is_control</span><span class="params">(char)</span>:</span></span><br><span class="line">    <span class="string">"""Checks whether `chars` is a control character."""</span></span><br><span class="line">    <span class="comment"># These are technically control characters but we count them as whitespace</span></span><br><span class="line">    <span class="comment"># characters.</span></span><br><span class="line">    <span class="keyword">if</span> char == <span class="string">"\t"</span> <span class="keyword">or</span> char == <span class="string">"\n"</span> <span class="keyword">or</span> char == <span class="string">"\r"</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line">    cat = unicodedata.category(char)</span><br><span class="line">    <span class="keyword">if</span> cat <span class="keyword">in</span> (<span class="string">"Cc"</span>, <span class="string">"Cf"</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_is_punctuation</span><span class="params">(char)</span>:</span></span><br><span class="line">    <span class="string">"""Checks whether `chars` is a punctuation character."""</span></span><br><span class="line">    cp = ord(char)</span><br><span class="line">    <span class="comment"># We treat all non-letter/number ASCII as punctuation.</span></span><br><span class="line">    <span class="comment"># Characters such as "^", "$", and "`" are not in the Unicode</span></span><br><span class="line">    <span class="comment"># Punctuation class but we treat them as punctuation anyways, for</span></span><br><span class="line">    <span class="comment"># consistency.</span></span><br><span class="line">    <span class="keyword">if</span> ((cp &gt;= <span class="number">33</span> <span class="keyword">and</span> cp &lt;= <span class="number">47</span>) <span class="keyword">or</span> (cp &gt;= <span class="number">58</span> <span class="keyword">and</span> cp &lt;= <span class="number">64</span>) <span class="keyword">or</span></span><br><span class="line">            (cp &gt;= <span class="number">91</span> <span class="keyword">and</span> cp &lt;= <span class="number">96</span>) <span class="keyword">or</span> (cp &gt;= <span class="number">123</span> <span class="keyword">and</span> cp &lt;= <span class="number">126</span>)):</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">    cat = unicodedata.category(char)</span><br><span class="line">    <span class="keyword">if</span> cat.startswith(<span class="string">"P"</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">  <span class="comment"># give the params: vocab file</span></span><br><span class="line">    tk = FullTokenizer(</span><br><span class="line">        <span class="string">"config/vocab.txt"</span>)</span><br><span class="line">    text = <span class="string">"10009090090 熊猫烧香"</span></span><br><span class="line">    tokens = tk.tokenize(text)</span><br><span class="line">    logger.info(<span class="string">"tokens: &#123;&#125;"</span>.format(tokens))</span><br><span class="line">    ids = tk.convert_tokens_to_ids(tokens)</span><br><span class="line">    logger.info(<span class="string">"ids: &#123;&#125;"</span>.format(ids))</span><br></pre></td></tr></table></figure><p>执行上述命令，可以得到</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">tokens: [u<span class="string">'1000'</span>, u<span class="string">'##90'</span>, u<span class="string">'##900'</span>, u<span class="string">'##90'</span>, u<span class="string">'\u718a'</span>, u<span class="string">'\u732b'</span>, u<span class="string">'\u70e7'</span>, u<span class="string">'\u9999'</span>]</span><br><span class="line">ids: [8212, 8599, 10589, 8599, 4220, 4344, 4173, 7676]</span><br></pre></td></tr></table></figure><p>可以看到对于中文进行wordpiece分词之后得到的是单个字，对于字符串分词之后得到的是多个字符串，在结果中<code>u&#39;##90&#39;, u&#39;##900&#39;, u&#39;##90&#39;</code> 可以看成这几个分割之后的属于一个此种的几个字，例如原词是“中国”，分词之后是‘##中’，‘##国’</p><h4 id="PositionEmbedding"><a href="#PositionEmbedding" class="headerlink" title="PositionEmbedding"></a>PositionEmbedding</h4><p>位置嵌入是指将单词的位置信息编码成特征向量，位置嵌入是向模型中引入单词位置关系的至关重要的一环。这里和<code>transformer</code>不同，在<code>transformer</code>中使用的是三角函数，在<code>Bert</code>中<code>position</code>是待学习参数，在Bert的参数定义文件中一开始设置最长的输入序列长度，这样就确定了输入的序列的最的位置的最大值和最小值，如此将token所在的位置按照词向量的方式生成<code>PositionEmbedding</code>。</p><h4 id="TokenType"><a href="#TokenType" class="headerlink" title="TokenType"></a>TokenType</h4><p>词的类别，这个很好理解，在<code>Bert</code>中输入的是大于等于两个句子，那么我们就规定分次之后的<code>token</code>归属于第一个句子那么其<code>tokentype</code>是$0$，如果归属于第二个句子那么<code>tokentype</code>是$1$。 下面是对输入<code>sequence</code>转化为<code>WordEmbedxidgs</code>之后进行后处理的代码，链接为 <a href="https://github.com/google-research/bert/blob/master/modeling.py#L428" target="_blank" rel="noopener">https://github.com/google-research/bert/blob/master/modeling.py#L428</a></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">embedding_postprocessor</span><span class="params">(input_tensor,</span></span></span><br><span class="line"><span class="function"><span class="params">                            use_token_type=False,</span></span></span><br><span class="line"><span class="function"><span class="params">                            token_type_ids=None,</span></span></span><br><span class="line"><span class="function"><span class="params">                            token_type_vocab_size=<span class="number">16</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                            token_type_embedding_name=<span class="string">"token_type_embeddings"</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                            use_position_embeddings=True,</span></span></span><br><span class="line"><span class="function"><span class="params">                            position_embedding_name=<span class="string">"position_embeddings"</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                            initializer_range=<span class="number">0.02</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                            max_position_embeddings=<span class="number">512</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                            dropout_prob=<span class="number">0.1</span>)</span>:</span></span><br><span class="line">  <span class="string">"""Performs various post-processing on a word embedding tensor.</span></span><br><span class="line"><span class="string">  Args:</span></span><br><span class="line"><span class="string">    input_tensor: float Tensor of shape [batch_size, seq_length,</span></span><br><span class="line"><span class="string">      embedding_size].</span></span><br><span class="line"><span class="string">    use_token_type: bool. Whether to add embeddings for `token_type_ids`.</span></span><br><span class="line"><span class="string">    token_type_ids: (optional) int32 Tensor of shape [batch_size, seq_length].</span></span><br><span class="line"><span class="string">      Must be specified if `use_token_type` is True.</span></span><br><span class="line"><span class="string">    token_type_vocab_size: int. The vocabulary size of `token_type_ids`.</span></span><br><span class="line"><span class="string">    token_type_embedding_name: string. The name of the embedding table variable</span></span><br><span class="line"><span class="string">      for token type ids.</span></span><br><span class="line"><span class="string">    use_position_embeddings: bool. Whether to add position embeddings for the</span></span><br><span class="line"><span class="string">      position of each token in the sequence.</span></span><br><span class="line"><span class="string">    position_embedding_name: string. The name of the embedding table variable</span></span><br><span class="line"><span class="string">      for positional embeddings.</span></span><br><span class="line"><span class="string">    initializer_range: float. Range of the weight initialization.</span></span><br><span class="line"><span class="string">    max_position_embeddings: int. Maximum sequence length that might ever be</span></span><br><span class="line"><span class="string">      used with this model. This can be longer than the sequence length of</span></span><br><span class="line"><span class="string">      input_tensor, but cannot be shorter.</span></span><br><span class="line"><span class="string">    dropout_prob: float. Dropout probability applied to the final output tensor.</span></span><br><span class="line"><span class="string">  Returns:</span></span><br><span class="line"><span class="string">    float tensor with same shape as `input_tensor`.</span></span><br><span class="line"><span class="string">  Raises:</span></span><br><span class="line"><span class="string">    ValueError: One of the tensor shapes or input values is invalid.</span></span><br><span class="line"><span class="string">  """</span></span><br><span class="line">  <span class="comment"># shape is [batch,sequence_length,embeding_dims]</span></span><br><span class="line">  input_shape = get_shape_list(input_tensor, expected_rank=<span class="number">3</span>)</span><br><span class="line">  batch_size = input_shape[<span class="number">0</span>]</span><br><span class="line">  seq_length = input_shape[<span class="number">1</span>]</span><br><span class="line">  width = input_shape[<span class="number">2</span>]</span><br><span class="line"></span><br><span class="line">  output = input_tensor</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> use_token_type:</span><br><span class="line">    <span class="keyword">if</span> token_type_ids <span class="keyword">is</span> <span class="keyword">None</span>:</span><br><span class="line">      <span class="keyword">raise</span> ValueError(<span class="string">"`token_type_ids` must be specified if"</span></span><br><span class="line">                       <span class="string">"`use_token_type` is True."</span>)</span><br><span class="line">    token_type_table = tf.get_variable(</span><br><span class="line">        name=token_type_embedding_name,</span><br><span class="line">        shape=[token_type_vocab_size, width],</span><br><span class="line">        initializer=create_initializer(initializer_range))</span><br><span class="line">    <span class="comment"># This vocab will be small so we always do one-hot here, since it is always</span></span><br><span class="line">    <span class="comment"># faster for a small vocabulary.</span></span><br><span class="line">    flat_token_type_ids = tf.reshape(token_type_ids, [<span class="number">-1</span>])</span><br><span class="line">    one_hot_ids = tf.one_hot(flat_token_type_ids, depth=token_type_vocab_size)</span><br><span class="line">    token_type_embeddings = tf.matmul(one_hot_ids, token_type_table)</span><br><span class="line">    token_type_embeddings = tf.reshape(token_type_embeddings,</span><br><span class="line">                                       [batch_size, seq_length, width])</span><br><span class="line">    output += token_type_embeddings</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> use_position_embeddings:</span><br><span class="line">    assert_op = tf.assert_less_equal(seq_length, max_position_embeddings)</span><br><span class="line">    <span class="keyword">with</span> tf.control_dependencies([assert_op]):</span><br><span class="line">      full_position_embeddings = tf.get_variable(</span><br><span class="line">          name=position_embedding_name,</span><br><span class="line">          shape=[max_position_embeddings, width],</span><br><span class="line">          initializer=create_initializer(initializer_range))</span><br><span class="line">      <span class="comment"># Since the position embedding table is a learned variable, we create it</span></span><br><span class="line">      <span class="comment"># using a (long) sequence length `max_position_embeddings`. The actual</span></span><br><span class="line">      <span class="comment"># sequence length might be shorter than this, for faster training of</span></span><br><span class="line">      <span class="comment"># tasks that do not have long sequences.</span></span><br><span class="line">      <span class="comment">#</span></span><br><span class="line">      <span class="comment"># So `full_position_embeddings` is effectively an embedding table</span></span><br><span class="line">      <span class="comment"># for position [0, 1, 2, ..., max_position_embeddings-1], and the current</span></span><br><span class="line">      <span class="comment"># sequence has positions [0, 1, 2, ... seq_length-1], so we can just</span></span><br><span class="line">      <span class="comment"># perform a slice.</span></span><br><span class="line">      position_embeddings = tf.slice(full_position_embeddings, [<span class="number">0</span>, <span class="number">0</span>],</span><br><span class="line">                                     [seq_length, <span class="number">-1</span>])</span><br><span class="line">      num_dims = len(output.shape.as_list())</span><br><span class="line"></span><br><span class="line">      <span class="comment"># Only the last two dimensions are relevant (`seq_length` and `width`), so</span></span><br><span class="line">      <span class="comment"># we broadcast among the first dimensions, which is typically just</span></span><br><span class="line">      <span class="comment"># the batch size.</span></span><br><span class="line">      position_broadcast_shape = []</span><br><span class="line">      <span class="keyword">for</span> _ <span class="keyword">in</span> range(num_dims - <span class="number">2</span>):</span><br><span class="line">        position_broadcast_shape.append(<span class="number">1</span>)</span><br><span class="line">      position_broadcast_shape.extend([seq_length, width])</span><br><span class="line">      position_embeddings = tf.reshape(position_embeddings,</span><br><span class="line">                                       position_broadcast_shape)</span><br><span class="line">      output += position_embeddings</span><br><span class="line"></span><br><span class="line">  output = layer_norm_and_dropout(output, dropout_prob)</span><br><span class="line">  <span class="keyword">return</span> output</span><br></pre></td></tr></table></figure><p>通过代码很容易看出来，在<code>BERT</code>中对于<code>tokentype</code>和<code>position</code>都是学习参数化。</p><p>就像上面代码所述</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">"""</span></span><br><span class="line"><span class="string">Input</span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line">output = input_tensor</span><br><span class="line"><span class="string">"""</span></span><br><span class="line"><span class="string">process token type</span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line">output += token_type_embeddings</span><br><span class="line"><span class="string">"""</span></span><br><span class="line"><span class="string">process position</span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line">output += position_embeddings</span><br><span class="line"><span class="string">"""</span></span><br><span class="line"><span class="string">LN and dropout</span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line">output = layer_norm_and_dropout(output, dropout_prob)</span><br></pre></td></tr></table></figure><p>将各个部分的<code>embedding</code>叠加在一起然后经过<code>LN</code>和<code>dropout</code>，就得到了进入<code>transformer</code>之前输入特征。</p><h3 id="数据特点"><a href="#数据特点" class="headerlink" title="数据特点"></a>数据特点</h3><p><code>BERT</code>是一个基础特征提取模型，主要学习的是词向量，具体到我们的下游任务，需要在<code>BERT</code>的基础上继续训练才可以。</p><h4 id="自然语料训练"><a href="#自然语料训练" class="headerlink" title="自然语料训练"></a>自然语料训练</h4><p>目的：学习模型的通用表达能力</p><p>语料：最好是<strong>document-level</strong>的，比如百科答，wiki，新闻等通用预料，如此能够保证预训练阶段得到的网络权重足够鲁棒以适应后续的特定领域的下游任务。</p><h4 id="下游任务训练"><a href="#下游任务训练" class="headerlink" title="下游任务训练"></a>下游任务训练</h4><p>目的：学习特定的模型表达能力，比如相关性，问答等子任务</p><p>语料：特定任务的标定数据，比如要学习<code>query</code>和<code>recall</code>的相关性，那么就要准备相应的数据进行训练。</p><h2 id="基于BERT的应用"><a href="#基于BERT的应用" class="headerlink" title="基于BERT的应用"></a>基于BERT的应用</h2><p>既然BERT这么好用，那么是否能在其他领域也借鉴使用呢，不止我这么想，很多大牛已经尝试过了，由此催生出了videoBert，ELECTRA，uniter，uncoder等这些基于BERT的相关研究。</p><h3 id="图文BERT"><a href="#图文BERT" class="headerlink" title="图文BERT"></a>图文BERT</h3><p>本人目前在做图片搜索相关工作，复现了uniter这篇论文的代码，由于工作原因，目前代码无法开源，实际上这部分还是很简单的，基于google-bert的代码，加入图片visual部分的处理前处理和后续loss的计算。</p><hr><img src="/2019/12/10/深度学习-NLP-BERT/uniter.png" title="UniterModel"><hr><p>本篇论文是将检测和图片描述作为提个一组输入，数据的构成方式和BERT类似，一些区别点是</p><ol><li>第一个句子用图片检测<code>RPN</code>之后得到的<code>ROIPooling</code>特征，第二个句子对应着当前这张图片的描述信息</li><li>使用和<code>BERT</code>相似的训练策略，不过略有不同，在原来<code>MLM</code>，<code>NSP</code>基础上加对视觉部分的<code>MRM</code></li><li><code>MLM</code>和<code>MRM</code>是基于条件性的，二者不能同时进行，这个在制作数据的时候要注意</li><li>在数据预处理过程中，视觉部分和文本部分分别处理之后进行<code>concat</code>，再输入给<code>transformer</code>部分</li><li><code>Loss</code>部分在原来的基础上计入了<code>MRM</code>部分的损失，并且论文中针对<code>MRM</code>部分的损失设计了三种方式<ol><li>视觉特征回归<code>MRFR</code></li><li>类别分类<code>MRC</code></li><li>KL散度类别回归<code>MRC-kl</code></li></ol></li></ol><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ol><li>Official Github: <a href="https://github.com/google-research/bert" target="_blank" rel="noopener">https://github.com/google-research/bert</a></li><li>知乎: <a href="https://zhuanlan.zhihu.com/p/48612853" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/48612853</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;img src=&quot;/2019/12/10/深度学习-NLP-BERT/bert.png&quot; title=&quot;BERT&quot;&gt;
&lt;p&gt;&lt;strong&gt;BERT&lt;/strong&gt;全名为(&lt;strong&gt;Bidirectional Encoder Representations from Transformers&lt;/strong&gt;), 在&lt;strong&gt;NLP&lt;/strong&gt;领域作为&lt;strong&gt;Word2vec&lt;/strong&gt;的替代者，将NLP中的$11$项任务精度向前大大推进，作为一名算法工程师，非常有必要好好研究一下。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="NLP" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/NLP/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="NLP" scheme="http://xiaofengshi.com/tags/NLP/"/>
    
  </entry>
  
  <entry>
    <title>Awesomes-Mac</title>
    <link href="http://xiaofengshi.com/2019/08/06/Awesomes-Mac/"/>
    <id>http://xiaofengshi.com/2019/08/06/Awesomes-Mac/</id>
    <published>2019-08-06T11:06:05.000Z</published>
    <updated>2020-02-03T13:28:54.366Z</updated>
    
    <content type="html"><![CDATA[<p>本篇设计mac的常用软件和bug处理</p><a id="more"></a><h2 id="BugFix"><a href="#BugFix" class="headerlink" title="BugFix"></a>BugFix</h2><p>Mac无法播放音乐或视频</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo killall coreaudiod</span><br></pre></td></tr></table></figure><h2 id="Softmare"><a href="#Softmare" class="headerlink" title="Softmare"></a>Softmare</h2><h3 id="Editor"><a href="#Editor" class="headerlink" title="Editor"></a>Editor</h3><ol><li>MD: Typora</li><li>MindBridge: XMind</li><li>Scripts: sublime3, vscode</li></ol><h3 id="terminal"><a href="#terminal" class="headerlink" title="terminal"></a>terminal</h3><ol><li>Item </li></ol><h3 id="远程服务器连接"><a href="#远程服务器连接" class="headerlink" title="远程服务器连接"></a>远程服务器连接</h3><pre><code>1. filezilla</code></pre><h3 id="微软套件"><a href="#微软套件" class="headerlink" title="微软套件"></a>微软套件</h3><ol><li>onedirve</li><li>OneNote</li></ol><p>快捷键查询</p><pre><code>1. cheatsheet</code></pre><p>数据库</p><ol><li><p>MySQL</p></li><li><p>Robo 3T</p></li></ol><p>硬盘工具</p><ol><li>paragon for Mac 15</li></ol><p>公式截图转latex</p><ol><li>mathpix snipping tool</li><li>texmaker</li></ol><h3 id="video"><a href="#video" class="headerlink" title="video"></a>video</h3><ol><li>IINA</li><li>MPV</li></ol><p>PDF阅读文献管理</p><ol><li>MarginNote3</li></ol><h3 id="虚拟环境"><a href="#虚拟环境" class="headerlink" title="虚拟环境"></a>虚拟环境</h3><ol><li>virtualbox</li></ol><h3 id="日程管理"><a href="#日程管理" class="headerlink" title="日程管理"></a>日程管理</h3><ol><li>Monday    <a href="https://shixiaofeng.monday.com/" target="_blank" rel="noopener">https://shixiaofeng.monday.com/</a></li></ol><h3 id="网络请求"><a href="#网络请求" class="headerlink" title="网络请求"></a>网络请求</h3><ol><li>Chrome+postman</li></ol><h3 id="键盘驱动管理"><a href="#键盘驱动管理" class="headerlink" title="键盘驱动管理"></a>键盘驱动管理</h3><ol><li>Karabiner</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本篇设计mac的常用软件和bug处理&lt;/p&gt;
    
    </summary>
    
      <category term="Awesomes" scheme="http://xiaofengshi.com/categories/Awesomes/"/>
    
      <category term="MAC" scheme="http://xiaofengshi.com/categories/Awesomes/MAC/"/>
    
    
      <category term="Awesomes" scheme="http://xiaofengshi.com/tags/Awesomes/"/>
    
      <category term="MAC" scheme="http://xiaofengshi.com/tags/MAC/"/>
    
  </entry>
  
  <entry>
    <title>DeepLearning_Tensorflow</title>
    <link href="http://xiaofengshi.com/2019/03/19/DeepLearning_Tensorflow/"/>
    <id>http://xiaofengshi.com/2019/03/19/DeepLearning_Tensorflow/</id>
    <published>2019-03-19T03:47:25.000Z</published>
    <updated>2020-01-31T07:53:23.945Z</updated>
    
    <content type="html"><![CDATA[<p>本篇主要记录在日常工作中遇到的TensorFlow的相关信息，包括如何处理报错信息，环境设置，训练测试，数据等等。</p><a id="more"></a><h2 id="如何安装"><a href="#如何安装" class="headerlink" title="如何安装"></a>如何安装</h2><p>安装tensorflow或者ubunt时，优先使用清华镜像</p><p>地址：<a href="https://mirrors.tuna.tsinghua.edu.cn/help/tensorflow/" target="_blank" rel="noopener">https://mirrors.tuna.tsinghua.edu.cn/help/tensorflow/</a></p><p>安装教程可以参考 <a href="https://www.tensorflow.org/install/source" target="_blank" rel="noopener">https://www.tensorflow.org/install/source</a></p><p>在安装GPU版本的时候，要安装对应的cuda和cudnn文件，详细信息可以参考NVIDIA的官网，对于CUDA 和TensorFlow-GPU的版本对照，</p><img src="/2019/03/19/DeepLearning_Tensorflow/cuda_tf.png"><h3 id="1-安装pipeline"><a href="#1-安装pipeline" class="headerlink" title="1. 安装pipeline"></a>1. 安装pipeline</h3><ol><li><p>安装NVIDIA驱动</p><p>使用<code>nvidia-smi</code>进行测试，如果没有该命令，需要安装，安装链接为<a href="https://www.nvidia.com/Download/index.aspx?lang=en-us" target="_blank" rel="noopener">https://www.nvidia.com/Download/index.aspx?lang=en-us</a> ，如果已经安装，进入下一步 </p></li><li><p>安装cuda</p><ol><li><p>首先要查询想要安装的TensorFlow版本与cuda版本的对应关系，可以在此处查询<a href="https://www.tensorflow.org/install/source，另外，补充TensorFlow1.14.0" target="_blank" rel="noopener">https://www.tensorflow.org/install/source，另外，补充TensorFlow1.14.0</a> 对应cuda10.0</p></li><li><p>确认好版本之后在此处进行下载<a href="https://developer.nvidia.com/cuda-toolkit-archive" target="_blank" rel="noopener">https://developer.nvidia.com/cuda-toolkit-archive</a> 以cuda10+linux为例，当安装的是debain版本时，选择14.04的版本，因为cuda是向下兼容的，这样可以避免出错，</p></li><li><p>推荐下载runfile的方式，安装方式如下</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Installation Instructions:</span><br><span class="line">Run `sudo sh cuda_10.0.130_410.48_linux.run`</span><br><span class="line">Follow the <span class="built_in">command</span>-line prompts</span><br></pre></td></tr></table></figure></li><li><p>按照提示一步步进行安装即可，最后在<code>.zshrc</code> 或者<code>.bashrc</code>中加入cuda的路径</p></li></ol></li><li><p>安装cudnn</p><ol><li><p>下载地址<a href="https://developer.nvidia.com/rdp/cudnn-download" target="_blank" rel="noopener">https://developer.nvidia.com/rdp/cudnn-download</a></p></li><li><p>根据安装的cuda版本找到对应的cudnn版本，尽量选择runtime版本，下载完之后，进行安装</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo dpkg -i xxx.deb</span><br></pre></td></tr></table></figure></li></ol></li><li><p>安装tensorflow</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pip install tensorflow-gpu=1.14.0</span><br></pre></td></tr></table></figure></li></ol><h2 id="API查询"><a href="#API查询" class="headerlink" title="API查询"></a>API查询</h2><p><a href="https://www.tensorflow.org/overview?hl=zh_cn" target="_blank" rel="noopener">https://www.tensorflow.org/overview?hl=zh_cn</a>该网址保存着tensorflow的实例，操作手册和api查询</p><h2 id="高质量仓库和博客"><a href="#高质量仓库和博客" class="headerlink" title="高质量仓库和博客"></a>高质量仓库和博客</h2><p>BLOG:</p><ol><li>总览：<a href="https://www.tensorflow.org/overview" target="_blank" rel="noopener">https://www.tensorflow.org/overview</a></li><li>常用指令的用法：<a href="https://www.tensorflow.org/guide" target="_blank" rel="noopener">https://www.tensorflow.org/guide</a></li><li>常见模型：<a href="https://www.tensorflow.org/tutorials" target="_blank" rel="noopener">https://www.tensorflow.org/tutorials</a></li></ol><p>GITHUB：</p><ol><li>官方仓库：<a href="https://github.com/tensorflow/models/" target="_blank" rel="noopener">https://github.com/tensorflow/models/</a></li><li>高星仓库：<a href="https://github.com/aymericdamien/TensorFlow-Examples" target="_blank" rel="noopener">https://github.com/aymericdamien/TensorFlow-Examples</a></li></ol><h2 id="报错信息及处理方案"><a href="#报错信息及处理方案" class="headerlink" title="报错信息及处理方案"></a>报错信息及处理方案</h2><h3 id="CentOS安装TensorFlow-ImportError-usr-lib64-libstdc-so-6-version-CXXABI-1-3-7’-not-found"><a href="#CentOS安装TensorFlow-ImportError-usr-lib64-libstdc-so-6-version-CXXABI-1-3-7’-not-found" class="headerlink" title="CentOS安装TensorFlow:ImportError: /usr/lib64/libstdc++.so.6: version CXXABI_1.3.7’ not found"></a>CentOS安装TensorFlow:ImportError: /usr/lib64/libstdc++.so.6: version CXXABI_1.3.7’ not found</h3><p>也有可能是这种信息,终端启动python，执行import tensorflow的操作出现的报错信息<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">(tf) shixiaofeng@n8-035-087:~$ python</span><br><span class="line">Python 2.7.16 |Anaconda, Inc.| (default, Mar 14 2019, 21:00:58)</span><br><span class="line">[GCC 7.3.0] on linux2</span><br><span class="line">Type <span class="string">"help"</span>, <span class="string">"copyright"</span>, <span class="string">"credits"</span> or <span class="string">"license"</span> <span class="keyword">for</span> more information.</span><br><span class="line">&gt;&gt;&gt; import tensorflow as tf</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line 1, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">  File <span class="string">"/data00/home/shixiaofeng/anaconda2/envs/tf/lib/python2.7/site-packages/tensorflow/__init__.py"</span>, line 24, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">    from tensorflow.python import *</span><br><span class="line">  File <span class="string">"/data00/home/shixiaofeng/anaconda2/envs/tf/lib/python2.7/site-packages/tensorflow/python/__init__.py"</span>, line 52, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">    from tensorflow.core.framework.graph_pb2 import *</span><br><span class="line">  File <span class="string">"/data00/home/shixiaofeng/anaconda2/envs/tf/lib/python2.7/site-packages/tensorflow/core/framework/graph_pb2.py"</span>, line 6, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">    from google.protobuf import descriptor as _descriptor</span><br><span class="line">  File <span class="string">"/data00/home/shixiaofeng/anaconda2/envs/tf/lib/python2.7/site-packages/google/protobuf/descriptor.py"</span>, line 47, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">    from google.protobuf.pyext import _message</span><br><span class="line">ImportError: /usr/lib/x86_64-linux-gnu/libstdc++.so.6: version `CXXABI_1.3.9<span class="string">' not found (required by /data00/home/shixiaofeng/anaconda2/envs/tf/lib/python2.7/site-packages/google/protobuf/pyext/_message.so)</span></span><br></pre></td></tr></table></figure></p><p>遇到这种相关信息是因为动态库版本过低造成的。对于TensorFlow的model目前一般使用的是最低1.5版本，这就需要对TensorFlow进行编码的时候需要一定的动态库版本。</p><ul><li><p>处理方式：</p><ul><li><p>查看虚拟环境中的动态库版本，下面的代码是找到名称为tf的虚拟环境下的动态库版本</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">strings ~/anaconda2/envs/tf/lib/libstdc++.so.6 | grep <span class="string">'CXXABI'</span></span><br></pre></td></tr></table></figure></li><li><p>查看系统的动态库版本</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">strings /usr/lib/x86_64-linux-gnu/libstdc++.so.6 | grep <span class="string">'CXXABI'</span></span><br><span class="line">strings</span><br><span class="line">或者</span><br><span class="line">/usr/lib64/libstdc++.so.6 | grep <span class="string">'CXXABI'</span></span><br></pre></td></tr></table></figure></li><li><p>如果发现系统的动态库版本较低并且就如报错信息所言，不存在需要的动态库版本，并且虚拟环境中的动态库版本较高，这个时候将虚拟环境下的动态库文件复制到系统环境下</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"># cd到系统路径</span><br><span class="line">cd /usr/lib/x86_64-linux-gnu</span><br><span class="line"># 或者</span><br><span class="line">cd /usr/lib64</span><br><span class="line"># 查询libstd++版本文件</span><br><span class="line">find . -name &quot;libstdc++&quot;</span><br><span class="line"># 复制动态库文件到系统目录</span><br><span class="line">sudo cp ~/anaconda2/envs/tf/lib/libstdc++.so.6.0.25 /usr/lib/x86_64-linux-gnu/</span><br><span class="line"># /usr/lib/x86_64-linux-gnu/目录下在创建软连接</span><br><span class="line">ln -snf ./libstdc++.so.6.0.25 ./libstdc++.so.6</span><br></pre></td></tr></table></figure></li></ul></li></ul><h3 id="查看tf在cpu还是gpu"><a href="#查看tf在cpu还是gpu" class="headerlink" title="查看tf在cpu还是gpu"></a>查看tf在cpu还是gpu</h3><p>激活环境<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy</span><br><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line">a = tf.constant([<span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>, <span class="number">4.0</span>, <span class="number">5.0</span>, <span class="number">6.0</span>], shape=[<span class="number">2</span>, <span class="number">3</span>], name=<span class="string">'a'</span>)</span><br><span class="line">b = tf.constant([<span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>, <span class="number">4.0</span>, <span class="number">5.0</span>, <span class="number">6.0</span>], shape=[<span class="number">3</span>, <span class="number">2</span>], name=<span class="string">'b'</span>)</span><br><span class="line">c = tf.matmul(a, b)</span><br><span class="line">sess = tf.Session(config=tf.ConfigProto(log_device_placement=<span class="keyword">True</span>))</span><br><span class="line"><span class="keyword">print</span> sess.run(c)</span><br></pre></td></tr></table></figure></p><p>会得到运行信息<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line">2019-04-03 16:20:34.035168: I tensorflow/core/platform/cpu_feature_guard.cc:137] Your CPU supports instructions that this TensorFlow binary was not compiled to use: SSE4.1 SSE4.2 AVX AVX2 FMA</span><br><span class="line">2019-04-03 16:20:34.833230: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1030] Found device 0 with properties:</span><br><span class="line">name: GeForce GTX 1080 Ti major: 6 minor: 1 memoryClockRate(GHz): 1.582</span><br><span class="line">pciBusID: 0000:02:00.0</span><br><span class="line">totalMemory: 10.92GiB freeMemory: 10.77GiB</span><br><span class="line">2019-04-03 16:20:34.945989: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1030] Found device 1 with properties:</span><br><span class="line">name: GeForce GTX 1080 Ti major: 6 minor: 1 memoryClockRate(GHz): 1.582</span><br><span class="line">pciBusID: 0000:03:00.0</span><br><span class="line">totalMemory: 10.92GiB freeMemory: 10.77GiB</span><br><span class="line">2019-04-03 16:20:35.058179: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1030] Found device 2 with properties:</span><br><span class="line">name: GeForce GTX 1080 Ti major: 6 minor: 1 memoryClockRate(GHz): 1.582</span><br><span class="line">pciBusID: 0000:82:00.0</span><br><span class="line">totalMemory: 10.92GiB freeMemory: 10.77GiB</span><br><span class="line">2019-04-03 16:20:35.171617: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1030] Found device 3 with properties:</span><br><span class="line">name: GeForce GTX 1080 Ti major: 6 minor: 1 memoryClockRate(GHz): 1.582</span><br><span class="line">pciBusID: 0000:83:00.0</span><br><span class="line">totalMemory: 10.92GiB freeMemory: 10.77GiB</span><br><span class="line">2019-04-03 16:20:35.173885: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1045] Device peer to peer matrix</span><br><span class="line">2019-04-03 16:20:35.173989: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1051] DMA: 0 1 2 3</span><br><span class="line">2019-04-03 16:20:35.174006: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1061] 0:   Y Y N N</span><br><span class="line">2019-04-03 16:20:35.174017: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1061] 1:   Y Y N N</span><br><span class="line">2019-04-03 16:20:35.174026: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1061] 2:   N N Y Y</span><br><span class="line">2019-04-03 16:20:35.174036: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1061] 3:   N N Y Y</span><br><span class="line">2019-04-03 16:20:35.174052: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1120] Creating TensorFlow device (/device:GPU:0) -&gt; (device: 0, name: GeForce GTX 1080 Ti, pci bus id: 0000:02:00.0, compute capability: 6.1)</span><br><span class="line">2019-04-03 16:20:35.174065: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1120] Creating TensorFlow device (/device:GPU:1) -&gt; (device: 1, name: GeForce GTX 1080 Ti, pci bus id: 0000:03:00.0, compute capability: 6.1)</span><br><span class="line">2019-04-03 16:20:35.174077: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1120] Creating TensorFlow device (/device:GPU:2) -&gt; (device: 2, name: GeForce GTX 1080 Ti, pci bus id: 0000:82:00.0, compute capability: 6.1)</span><br><span class="line">2019-04-03 16:20:35.174088: I tensorflow/core/common_runtime/gpu/gpu_device.cc:1120] Creating TensorFlow device (/device:GPU:3) -&gt; (device: 3, name: GeForce GTX 1080 Ti, pci bus id: 0000:83:00.0, compute capability: 6.1)</span><br><span class="line">Device mapping:</span><br><span class="line">/job:localhost/replica:0/task:0/device:GPU:0 -&gt; device: 0, name: GeForce GTX 1080 Ti, pci bus id: 0000:02:00.0, compute capability: 6.1</span><br><span class="line">/job:localhost/replica:0/task:0/device:GPU:1 -&gt; device: 1, name: GeForce GTX 1080 Ti, pci bus id: 0000:03:00.0, compute capability: 6.1</span><br><span class="line">/job:localhost/replica:0/task:0/device:GPU:2 -&gt; device: 2, name: GeForce GTX 1080 Ti, pci bus id: 0000:82:00.0, compute capability: 6.1</span><br><span class="line">/job:localhost/replica:0/task:0/device:GPU:3 -&gt; device: 3, name: GeForce GTX 1080 Ti, pci bus id: 0000:83:00.0, compute capability: 6.1</span><br><span class="line">2019-04-03 16:20:35.924911: I tensorflow/core/common_runtime/direct_session.cc:299] Device mapping:</span><br><span class="line">/job:localhost/replica:0/task:0/device:GPU:0 -&gt; device: 0, name: GeForce GTX 1080 Ti, pci bus id: 0000:02:00.0, compute capability: 6.1</span><br><span class="line">/job:localhost/replica:0/task:0/device:GPU:1 -&gt; device: 1, name: GeForce GTX 1080 Ti, pci bus id: 0000:03:00.0, compute capability: 6.1</span><br><span class="line">/job:localhost/replica:0/task:0/device:GPU:2 -&gt; device: 2, name: GeForce GTX 1080 Ti, pci bus id: 0000:82:00.0, compute capability: 6.1</span><br><span class="line">/job:localhost/replica:0/task:0/device:GPU:3 -&gt; device: 3, name: GeForce GTX 1080 Ti, pci bus id: 0000:83:00.0, compute capability: 6.1</span><br><span class="line"></span><br><span class="line">&gt;&gt;&gt; <span class="built_in">print</span> sess.run(c)</span><br><span class="line">MatMul: (MatMul): /job:localhost/replica:0/task:0/device:GPU:0</span><br><span class="line">2019-04-03 16:20:38.192824: I tensorflow/core/common_runtime/placer.cc:874] MatMul: (MatMul)/job:localhost/replica:0/task:0/device:GPU:0</span><br><span class="line">b: (Const): /job:localhost/replica:0/task:0/device:GPU:0</span><br><span class="line">2019-04-03 16:20:38.192864: I tensorflow/core/common_runtime/placer.cc:874] b: (Const)/job:localhost/replica:0/task:0/device:GPU:0</span><br><span class="line">a: (Const): /job:localhost/replica:0/task:0/device:GPU:0</span><br><span class="line">2019-04-03 16:20:38.192885: I tensorflow/core/common_runtime/placer.cc:874] a: (Const)/job:localhost/replica:0/task:0/device:GPU:0</span><br><span class="line">[[22. 28.]</span><br><span class="line"> [49. 64.]]</span><br></pre></td></tr></table></figure></p><h3 id="anaconda虚拟环境添加路径"><a href="#anaconda虚拟环境添加路径" class="headerlink" title="anaconda虚拟环境添加路径"></a>anaconda虚拟环境添加路径</h3><p>如果是个人创建的环境，则目录为，针对的是anaconda2，当然要根据自己安装的anaconda版本确定路径。<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">~/anaconda2/envs/tf/lib/python2.7/site-packages</span><br></pre></td></tr></table></figure></p><p>如果是base环境<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">~/anaconda2/lib/python2.7/site-packages</span><br></pre></td></tr></table></figure></p><p>在目录下创建文件<code>*.pth</code>文件<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim add_path.pth</span><br></pre></td></tr></table></figure></p><p>在文件下添加内容，如下针对的是对于目前本人使用的开发机<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/data00/home/xxx/repos/toutiao/lib/</span><br><span class="line">/data00/home/xxx/repos/toutiao/tools/rpc-tool</span><br><span class="line">/data00/home/xxx/ow_package/Theano</span><br></pre></td></tr></table></figure></p><p>如果想要cuda路径信息<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 添加cuda8路径</span></span><br><span class="line">/usr/<span class="built_in">local</span>/cuda-8.0/bin/</span><br><span class="line">/usr/<span class="built_in">local</span>/cuda-8.0/lib64</span><br></pre></td></tr></table></figure></p><p>也可以直接添加到<code>~/.bashrc</code>中<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=/usr/<span class="built_in">local</span>/cuda-8.0/bin/:<span class="variable">$PATH</span></span><br><span class="line"><span class="built_in">export</span> LD_LIBRARY_PATH=/usr/<span class="built_in">local</span>/cuda-8.0/lib64:<span class="variable">$LD_LIBRARY_PATH</span></span><br></pre></td></tr></table></figure></p><h3 id="无法导入tensorflow"><a href="#无法导入tensorflow" class="headerlink" title="无法导入tensorflow"></a>无法导入tensorflow</h3><p>已经安装<code>tensorflow</code>，但是在import的时候会出现<code>no module name tensorflow</code>的错误信息</p><p><strong>卸载tensorflow并重新安装</strong></p><h2 id="多GPU使用"><a href="#多GPU使用" class="headerlink" title="多GPU使用"></a>多GPU使用</h2><h3 id="Train"><a href="#Train" class="headerlink" title="Train"></a>Train</h3><p>官方的参考链接</p><p><a href="https://www.tensorflow.org/guide/using_gpu#using_multiple_gpus" target="_blank" rel="noopener">https://www.tensorflow.org/guide/using_gpu#using_multiple_gpus</a></p><p><a href="https://www.tensorflow.org/alpha/guide/using_gpu?hl=zh_cn#using_multiple_gpus" target="_blank" rel="noopener">https://www.tensorflow.org/alpha/guide/using_gpu?hl=zh_cn#using_multiple_gpus</a></p><p>官方代码，存在于tensorflow/model/</p><p>这个说的就是并行化，一般是模型并行化和数据并行化</p><ul><li>模型并行化：不同的gpu保存的模型不同，输入的数据相同，共同训练，可以认为是一种bagging，一般Deeplearning用的不多</li><li>数据并行化：不同的gpu保存的模型是相同，输入的数据不同，共同训练，指定一个device来保存模型参数，分配给使用的gpu模型中，使用所有模型的平均梯度来进行参数更新，一般用的是这种方式。</li></ul><img src="/2019/03/19/DeepLearning_Tensorflow/multi_gpu.png" title="多GPU训练数据流"><p><strong>使用Minist进行多GPU试验</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> tensorflow.contrib <span class="keyword">import</span> slim</span><br><span class="line"><span class="keyword">from</span> tensorflow.examples.tutorials.mnist <span class="keyword">import</span> input_data</span><br><span class="line"><span class="comment"># 读取minist数据集</span></span><br><span class="line">mnist = input_data.read_data_sets(<span class="string">"/tmp/mnist/"</span>, one_hot=<span class="keyword">True</span>)</span><br><span class="line"> </span><br><span class="line">num_gpus = <span class="number">2</span></span><br><span class="line">num_steps = <span class="number">1000</span></span><br><span class="line">learning_rate = <span class="number">0.001</span></span><br><span class="line">batch_size = <span class="number">1000</span></span><br><span class="line">display_step = <span class="number">10</span></span><br><span class="line"> </span><br><span class="line">num_input = <span class="number">784</span></span><br><span class="line">num_classes = <span class="number">10</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义minist训练网络</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">conv_net_with_layers</span><span class="params">(x,is_training,dropout = <span class="number">0.75</span>)</span>:</span></span><br><span class="line">    <span class="keyword">with</span> tf.variable_scope(<span class="string">"ConvNet"</span>, reuse=tf.AUTO_REUSE):</span><br><span class="line">        x = tf.reshape(x, [<span class="number">-1</span>, <span class="number">28</span>, <span class="number">28</span>, <span class="number">1</span>])</span><br><span class="line">        x = tf.layers.conv2d(x, <span class="number">12</span>, <span class="number">5</span>, activation=tf.nn.relu)</span><br><span class="line">        x = tf.layers.max_pooling2d(x, <span class="number">2</span>, <span class="number">2</span>)</span><br><span class="line">        x = tf.layers.conv2d(x, <span class="number">24</span>, <span class="number">3</span>, activation=tf.nn.relu)</span><br><span class="line">        x = tf.layers.max_pooling2d(x, <span class="number">2</span>, <span class="number">2</span>)</span><br><span class="line">        x = tf.layers.flatten(x)</span><br><span class="line">        x = tf.layers.dense(x, <span class="number">100</span>)</span><br><span class="line">        x = tf.layers.dropout(x, rate=dropout, training=is_training)</span><br><span class="line">        out = tf.layers.dense(x, <span class="number">10</span>)</span><br><span class="line">        out = tf.nn.softmax(out) <span class="keyword">if</span> <span class="keyword">not</span> is_training <span class="keyword">else</span> out</span><br><span class="line">    <span class="keyword">return</span> out</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">conv_net</span><span class="params">(x,is_training)</span>:</span></span><br><span class="line">    <span class="comment"># "updates_collections": None is very import ,without will only get 0.10</span></span><br><span class="line">    batch_norm_params = &#123;<span class="string">"is_training"</span>: is_training, <span class="string">"decay"</span>: <span class="number">0.9</span>, <span class="string">"updates_collections"</span>: <span class="keyword">None</span>&#125;</span><br><span class="line">    <span class="comment">#,'variables_collections': [ tf.GraphKeys.TRAINABLE_VARIABLES ]</span></span><br><span class="line">    <span class="keyword">with</span> slim.arg_scope([slim.conv2d, slim.fully_connected],</span><br><span class="line">                        activation_fn=tf.nn.relu,</span><br><span class="line">                        weights_initializer=tf.truncated_normal_initializer(mean=<span class="number">0.0</span>, stddev=<span class="number">0.01</span>),</span><br><span class="line">                        weights_regularizer=slim.l2_regularizer(<span class="number">0.0005</span>),</span><br><span class="line">                        normalizer_fn=slim.batch_norm, normalizer_params=batch_norm_params):</span><br><span class="line">        <span class="keyword">with</span> tf.variable_scope(<span class="string">"ConvNet"</span>,reuse=tf.AUTO_REUSE):</span><br><span class="line">            x = tf.reshape(x, [<span class="number">-1</span>, <span class="number">28</span>, <span class="number">28</span>, <span class="number">1</span>])</span><br><span class="line">            net = slim.conv2d(x, <span class="number">6</span>, [<span class="number">5</span>,<span class="number">5</span>], scope=<span class="string">"conv_1"</span>)</span><br><span class="line">            net = slim.max_pool2d(net, [<span class="number">2</span>, <span class="number">2</span>],scope=<span class="string">"pool_1"</span>)</span><br><span class="line">            net = slim.conv2d(net, <span class="number">12</span>, [<span class="number">5</span>,<span class="number">5</span>], scope=<span class="string">"conv_2"</span>)</span><br><span class="line">            net = slim.max_pool2d(net, [<span class="number">2</span>, <span class="number">2</span>], scope=<span class="string">"pool_2"</span>)</span><br><span class="line">            net = slim.flatten(net, scope=<span class="string">"flatten"</span>)</span><br><span class="line">            net = slim.fully_connected(net, <span class="number">100</span>, scope=<span class="string">"fc"</span>)</span><br><span class="line">            net = slim.dropout(net,is_training=is_training)</span><br><span class="line">            net = slim.fully_connected(net, num_classes, scope=<span class="string">"prob"</span>, activation_fn=<span class="keyword">None</span>,normalizer_fn=<span class="keyword">None</span>)</span><br><span class="line">            <span class="keyword">return</span> net</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">average_gradients</span><span class="params">(tower_grads)</span>:</span></span><br><span class="line">    average_grads = []</span><br><span class="line">    <span class="keyword">for</span> grad_and_vars <span class="keyword">in</span> zip(*tower_grads):</span><br><span class="line">        grads = []</span><br><span class="line">        <span class="keyword">for</span> g, _ <span class="keyword">in</span> grad_and_vars:</span><br><span class="line">            expend_g = tf.expand_dims(g, <span class="number">0</span>)</span><br><span class="line">            grads.append(expend_g)</span><br><span class="line">        grad = tf.concat(grads, <span class="number">0</span>)</span><br><span class="line">        grad = tf.reduce_mean(grad, <span class="number">0</span>)</span><br><span class="line">        v = grad_and_vars[<span class="number">0</span>][<span class="number">1</span>]</span><br><span class="line">        grad_and_var = (grad, v)</span><br><span class="line">        average_grads.append(grad_and_var)</span><br><span class="line">    <span class="keyword">return</span> average_grads</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">train</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">with</span> tf.device(<span class="string">"/cpu:0"</span>):</span><br><span class="line">      <span class="comment"># tower_grads变量保存在cpu中</span></span><br><span class="line">        global_step=tf.train.get_or_create_global_step()</span><br><span class="line">        tower_grads = []</span><br><span class="line">        </span><br><span class="line">        X = tf.placeholder(tf.float32, [<span class="keyword">None</span>, num_input])</span><br><span class="line">        Y = tf.placeholder(tf.float32, [<span class="keyword">None</span>, num_classes])</span><br><span class="line">        </span><br><span class="line">        opt = tf.train.AdamOptimizer(learning_rate)</span><br><span class="line">        <span class="keyword">with</span> tf.variable_scope(tf.get_variable_scope()):</span><br><span class="line">            <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">2</span>):</span><br><span class="line">                <span class="keyword">with</span> tf.device(<span class="string">"/gpu:%d"</span> % i):</span><br><span class="line">                    <span class="keyword">with</span> tf.name_scope(<span class="string">"tower_%d"</span> % i):</span><br><span class="line">                      <span class="comment"># 数据并行，使用该数据在当前设备下计算预测值</span></span><br><span class="line">                            _x = X[i * batch_size:(i + <span class="number">1</span>) * batch_size]</span><br><span class="line">                            _y = Y[i * batch_size:(i + <span class="number">1</span>) * batch_size]</span><br><span class="line">                            logits = conv_net(_x, <span class="keyword">True</span>)</span><br><span class="line">                            tf.get_variable_scope().reuse_variables()</span><br><span class="line">                            loss = tf.reduce_mean(tf.nn.softmax_cross_entropy_with_logits(labels=_y, logits=logits))</span><br><span class="line">                            <span class="comment"># 根据当前模型的损失计算梯度</span></span><br><span class="line">                            grads = opt.compute_gradients(loss)</span><br><span class="line">                            <span class="comment"># 将梯度保存在临时变量tower_grads中</span></span><br><span class="line">                            tower_grads.append(grads)</span><br><span class="line">                            <span class="comment"># 使用第一个gpu进行验证，计算正确率</span></span><br><span class="line">                            <span class="keyword">if</span> i == <span class="number">0</span>:</span><br><span class="line">                                logits_test = conv_net(_x, <span class="keyword">False</span>)</span><br><span class="line">                                correct_prediction = tf.equal(tf.argmax(logits_test, <span class="number">1</span>), tf.argmax(_y, <span class="number">1</span>))</span><br><span class="line">                                accuracy = tf.reduce_mean(tf.cast(correct_prediction, tf.float32))</span><br><span class="line">        <span class="comment"># 计算所有模型的平均梯度</span></span><br><span class="line">        grads = average_gradients(tower_grads)</span><br><span class="line">        <span class="comment"># 对优化器赋予当前的平均梯度进行参数更新</span></span><br><span class="line">        train_op = opt.apply_gradients(grads)</span><br><span class="line">        <span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">            sess.run(tf.global_variables_initializer())</span><br><span class="line">            <span class="keyword">for</span> step <span class="keyword">in</span> range(<span class="number">1</span>, num_steps + <span class="number">1</span>):</span><br><span class="line">              <span class="comment"># 假设模型中的batch为N，使用gpu数量为M，那么每次拿到的数据为M*N</span></span><br><span class="line"><span class="comment"># 每个模型中feed的数据量都是N</span></span><br><span class="line">                batch_x, batch_y = mnist.train.next_batch(batch_size * num_gpus)</span><br><span class="line">                sess.run(train_op, feed_dict=&#123;X: batch_x, Y: batch_y&#125;)</span><br><span class="line">                <span class="comment"># 每10次计算一次正确率</span></span><br><span class="line">                <span class="keyword">if</span> step % <span class="number">10</span> == <span class="number">0</span> <span class="keyword">or</span> step == <span class="number">1</span>:</span><br><span class="line">                    loss_value, acc = sess.run([loss, accuracy], feed_dict=&#123;X: batch_x, Y: batch_y&#125;)</span><br><span class="line">                    print(<span class="string">"Step:"</span> + str(step) + <span class="string">":"</span> + str(loss_value) + <span class="string">" "</span> + str(acc))</span><br><span class="line">            print(<span class="string">"Done"</span>)</span><br><span class="line">            print(<span class="string">"Testing Accuracy:"</span>,</span><br><span class="line">                  np.mean([sess.run(accuracy, feed_dict=&#123;X: mnist.test.images[i:i + batch_size],</span><br><span class="line">                                                         Y: mnist.test.labels[i:i + batch_size]&#125;) <span class="keyword">for</span> i <span class="keyword">in</span></span><br><span class="line">                           range(<span class="number">0</span>, len(mnist.test.images), batch_size)]))</span><br><span class="line">            </span><br><span class="line"><span class="comment"># 使用单个gpu设备进行训练</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">train_single</span><span class="params">()</span>:</span></span><br><span class="line">    X = tf.placeholder(tf.float32, [<span class="keyword">None</span>, num_input])</span><br><span class="line">    Y = tf.placeholder(tf.float32, [<span class="keyword">None</span>, num_classes])</span><br><span class="line">    logits=conv_net(X,<span class="keyword">True</span>)</span><br><span class="line">    loss=tf.reduce_mean(tf.nn.softmax_cross_entropy_with_logits(labels=Y,logits=logits))</span><br><span class="line">    opt=tf.train.AdamOptimizer(learning_rate)</span><br><span class="line">    train_op=opt.minimize(loss)</span><br><span class="line">    logits_test=conv_net(X,<span class="keyword">False</span>)</span><br><span class="line">    correct_prediction = tf.equal(tf.argmax(logits_test, <span class="number">1</span>), tf.argmax(Y, <span class="number">1</span>))</span><br><span class="line">    accuracy = tf.reduce_mean(tf.cast(correct_prediction, tf.float32))</span><br><span class="line">    <span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">        sess.run(tf.global_variables_initializer())</span><br><span class="line">        <span class="keyword">for</span> step <span class="keyword">in</span> range(<span class="number">1</span>,num_steps+<span class="number">1</span>):</span><br><span class="line">            batch_x, batch_y = mnist.train.next_batch(batch_size)</span><br><span class="line">            sess.run(train_op,feed_dict=&#123;X:batch_x,Y:batch_y&#125;)</span><br><span class="line">            <span class="keyword">if</span> step%display_step==<span class="number">0</span> <span class="keyword">or</span> step==<span class="number">1</span>:</span><br><span class="line">                loss_value,acc=sess.run([loss,accuracy],feed_dict=&#123;X:batch_x,Y:batch_y&#125;)</span><br><span class="line">                print(<span class="string">"Step:"</span> + str(step) + <span class="string">":"</span> + str(loss_value) + <span class="string">" "</span> + str(acc))</span><br><span class="line">        print(<span class="string">"Done"</span>)</span><br><span class="line">        print(<span class="string">"Testing Accuracy:"</span>,np.mean([sess.run(accuracy, feed_dict=&#123;X: mnist.test.images[i:i + batch_size],</span><br><span class="line">              Y: mnist.test.labels[i:i + batch_size]&#125;) <span class="keyword">for</span> i <span class="keyword">in</span></span><br><span class="line">              range(<span class="number">0</span>, len(mnist.test.images), batch_size)]))</span><br><span class="line"> </span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    <span class="comment">#train_single()</span></span><br><span class="line">    train()</span><br></pre></td></tr></table></figure><h3 id="Eval-amp-amp-Inference"><a href="#Eval-amp-amp-Inference" class="headerlink" title="Eval &amp;&amp; Inference"></a>Eval &amp;&amp; Inference</h3><p>在验证和测试的时候，每次输入的数据是单个数据，一般情况下无法进行拆分，因此，单gpu运算。如果增加服务器来处理大量的访问请求，要调用tensorflow serving，多个gpu不熟相同的graph，由tensorflow serving来控制请求的队列。</p><h4 id="GPU选择"><a href="#GPU选择" class="headerlink" title="GPU选择"></a>GPU选择</h4><p>在运行tensorflow gpu时候，如果机器上存在多块显卡，并且没有在代码中进行多gpu设置，最好只用一块gpu，在运行程序的时候，可以使用如下命令</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CUDA_VISIBLE_DEVICES=0 python **.py</span><br></pre></td></tr></table></figure><p>如此，之后使用第一块gpu进行计算。</p><p>当然也有其他的设置方法，可以在程序中设置TensorFlow的device环境。</p><h2 id="TfRecord"><a href="#TfRecord" class="headerlink" title="TfRecord"></a>TfRecord</h2><p>TensorFlow推荐使用tfrecord的数据格式。</p><h3 id="read"><a href="#read" class="headerlink" title="read"></a>read</h3><h3 id="write"><a href="#write" class="headerlink" title="write"></a>write</h3><p>将tfrecord中的数据读出，只读取一个epoch， 也就是不进行重复读取，该代码不是为了进行模型训练，只是单纯的读取tfrecord中的数据并保存本地</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">tf2image</span><span class="params">(swd,data_path)</span>:</span></span><br><span class="line"> <span class="string">"""</span></span><br><span class="line"><span class="string"> swd: 读取的样本要保存的路径</span></span><br><span class="line"><span class="string"> data_path: tfrecord的路径 'xxx/xxx/*.tfrecord'格式</span></span><br><span class="line"><span class="string"> """</span></span><br><span class="line">    <span class="comment"># get tfrecords</span></span><br><span class="line">    data_files = tf.gfile.Glob(data_path)</span><br><span class="line">    print(<span class="string">'data_files'</span>,data_files)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">      <span class="comment"># generator</span></span><br><span class="line">        filename_queue = tf.train.string_input_producer(data_files,shuffle=<span class="keyword">False</span>,num_epochs=<span class="number">1</span>) </span><br><span class="line">        <span class="comment"># get sample from tfrecod</span></span><br><span class="line">        reader = tf.TFRecordReader()</span><br><span class="line">        _, serialized_example = reader.read(filename_queue)   </span><br><span class="line">        features = tf.parse_single_example(serialized_example,</span><br><span class="line">                                        features=&#123;</span><br><span class="line">                                            <span class="string">'image_data'</span>: tf.FixedLenFeature([], dtype=tf.string)&#125;)  </span><br><span class="line">        image = tf.image.decode_jpeg(features[<span class="string">'image_data'</span>], channels=<span class="number">3</span>)</span><br><span class="line">        image = tf.image.convert_image_dtype(image, tf.uint8)</span><br><span class="line"></span><br><span class="line">        count=<span class="number">0</span></span><br><span class="line">        sess.run(tf.initialize_local_variables())</span><br><span class="line">        sess.run(tf.initialize_all_variables())</span><br><span class="line">        <span class="comment"># create queue</span></span><br><span class="line">        coord=tf.train.Coordinator()</span><br><span class="line">        <span class="comment"># start queue</span></span><br><span class="line">        threads = tf.train.start_queue_runners(coord=coord)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">          </span><br><span class="line">            <span class="keyword">while</span> <span class="keyword">not</span> coord.should_stop():</span><br><span class="line">                single= sess.run(image)</span><br><span class="line">                img=Image.fromarray(single, <span class="string">'RGB'</span>)</span><br><span class="line">                img.save(swd +<span class="string">'/'</span>+ str(count) + <span class="string">'.jpg'</span>)</span><br><span class="line">                count += <span class="number">1</span></span><br><span class="line">                <span class="keyword">if</span> count % <span class="number">100</span> == <span class="number">0</span>:</span><br><span class="line">                    <span class="keyword">print</span> (<span class="string">'Alreay run %d images'</span> % count)</span><br><span class="line">        <span class="keyword">except</span> tf.errors.OutOfRangeError:</span><br><span class="line">          <span class="comment"># if the queue is empty, break </span></span><br><span class="line">            print(<span class="string">'Done running'</span>)</span><br><span class="line">        <span class="keyword">finally</span>:</span><br><span class="line">            coord.request_stop()</span><br><span class="line">            coord.join(threads)</span><br></pre></td></tr></table></figure><h2 id="Fintune"><a href="#Fintune" class="headerlink" title="Fintune"></a>Fintune</h2><p>微调模型，一般指我们使用一些成型框架如VGG,GOOGLENET等，并在这个网络的基础上添加不同的网络训练层，以适应我们自己的任务。对于vgg等网络，一般是基于Imagenet预训练好的，因此我们没必要再重新从头训练，但是对于IMagenet是1000类，但是我们自己的任务很可能不是在这个数据集上进行的，为了快速训练模型，我们选用模型微调的方法。</p><p>加载一个预训练的模型，固定加载的这个模型的部分权重，只更新部分网络权重。</p><p>参考博客：<a href="https://blog.csdn.net/ying86615791/article/details/76215363" target="_blank" rel="noopener">https://blog.csdn.net/ying86615791/article/details/76215363</a></p><p>代码来源：tensorflow yolo3</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 训练数据集</span></span><br><span class="line">trainset = dataset(parser, train_tfrecord, BATCH_SIZE, shuffle=SHUFFLE_SIZE)</span><br><span class="line">testset = dataset(parser, test_tfrecord, BATCH_SIZE, shuffle=<span class="keyword">None</span>)</span><br><span class="line"></span><br><span class="line">is_training = tf.placeholder(tf.bool)</span><br><span class="line">example = tf.cond(is_training, <span class="keyword">lambda</span>: trainset.get_next(), <span class="keyword">lambda</span>: testset.get_next())</span><br><span class="line"></span><br><span class="line">images = example[<span class="number">0</span>]</span><br><span class="line">y_true = example[<span class="number">1</span>:]</span><br><span class="line"><span class="comment"># 整个模型结构</span></span><br><span class="line">model = yolov3.yolov3(NUM_CLASSES, ANCHORS)</span><br><span class="line"><span class="keyword">with</span> tf.variable_scope(<span class="string">'yolov3'</span>):</span><br><span class="line">    <span class="comment"># pred_feature_map contains three detection feature maps</span></span><br><span class="line">    pred_feature_map = model.forward(images, is_training=is_training)</span><br><span class="line">    loss = model.compute_loss(pred_feature_map, y_true)</span><br><span class="line">    y_pred = model.predict(pred_feature_map)</span><br><span class="line"><span class="comment"># 此时graph中保存着整个网络的全部节点  </span></span><br><span class="line">graph = tf.get_default_graph()</span><br><span class="line"></span><br><span class="line">tf.summary.scalar(<span class="string">"loss/coord_loss"</span>,   loss[<span class="number">1</span>])</span><br><span class="line">tf.summary.scalar(<span class="string">"loss/sizes_loss"</span>,   loss[<span class="number">2</span>])</span><br><span class="line">tf.summary.scalar(<span class="string">"loss/confs_loss"</span>,   loss[<span class="number">3</span>])</span><br><span class="line">tf.summary.scalar(<span class="string">"loss/class_loss"</span>,   loss[<span class="number">4</span>])</span><br><span class="line"></span><br><span class="line">global_step = tf.Variable(<span class="number">0</span>, trainable=<span class="keyword">False</span>, collections=[tf.GraphKeys.LOCAL_VARIABLES])</span><br><span class="line">write_op = tf.summary.merge_all()</span><br><span class="line">writer_train = tf.summary.FileWriter(<span class="string">"./checkpoint/summary/train"</span>)</span><br><span class="line">writer_test = tf.summary.FileWriter(<span class="string">"./checkpoint/summary/test"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 要恢复的权重参数</span></span><br><span class="line">saver_to_restore = tf.train.Saver(var_list=tf.contrib.framework.get_variables_to_restore(</span><br><span class="line">    include=[<span class="string">"yolov3/darknet-53"</span>]))</span><br><span class="line"><span class="comment"># 要更新的网络参数</span></span><br><span class="line">update_vars = tf.contrib.framework.get_variables_to_restore(include=[<span class="string">"yolov3/yolo-v3"</span>])</span><br><span class="line">learning_rate = tf.train.exponential_decay(</span><br><span class="line">    LR, global_step, decay_steps=DECAY_STEPS, decay_rate=DECAY_RATE, staircase=<span class="keyword">True</span>)</span><br><span class="line">optimizer = tf.train.AdamOptimizer(learning_rate)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只更新update_vars网络参数</span></span><br><span class="line">update_ops = tf.get_collection(tf.GraphKeys.UPDATE_OPS)</span><br><span class="line"><span class="keyword">with</span> tf.control_dependencies(update_ops):</span><br><span class="line">    train_op = optimizer.minimize(loss[<span class="number">0</span>], var_list=update_vars, global_step=global_step)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 整个模型的saver</span></span><br><span class="line">saver = tf.train.Saver(max_to_keep=<span class="number">2</span>)</span><br><span class="line"><span class="comment"># sess中的graph为整个模型的graph，很重要，这里如果不指定graph，那么sess的默认graph不会保留saver_to_restore中的节点</span></span><br><span class="line">sess = tf.Session(config=config,graph=graph)</span><br><span class="line"></span><br><span class="line">sess.run([tf.global_variables_initializer(), tf.local_variables_initializer()])</span><br><span class="line"></span><br><span class="line"><span class="comment"># 加载的权重文件位置</span></span><br><span class="line">ckpt = tf.train.get_checkpoint_state(<span class="string">"./checkpoint/ckpt"</span>)</span><br><span class="line">saver_to_restore.restore(sess, ckpt.model_checkpoint_path)</span><br><span class="line"></span><br><span class="line">stem = os.path.splitext(os.path.basename(ckpt.model_checkpoint_path))[<span class="number">-1</span>]</span><br><span class="line">restore_iter = int(stem.split(<span class="string">'-'</span>)[<span class="number">-1</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在tensorboard中显示graph</span></span><br><span class="line">writer_train.add_graph(sess.graph)</span><br><span class="line"></span><br><span class="line"><span class="keyword">print</span> <span class="string">'resotre iter:'</span>, restore_iter</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> step <span class="keyword">in</span> range(restore_iter, STEPS):</span><br><span class="line">    run_items = sess.run([train_op, write_op, y_pred, y_true] + loss, feed_dict=&#123;is_training: <span class="keyword">True</span>&#125;)</span><br><span class="line"><span class="comment"># 验证步数</span></span><br><span class="line">    <span class="keyword">if</span> (step+<span class="number">1</span>) % EVAL_INTERNAL == <span class="number">0</span>:</span><br><span class="line">        <span class="comment"># calculate recall and precision</span></span><br><span class="line">        train_rec_value, train_prec_value = utils.evaluate(run_items[<span class="number">2</span>], run_items[<span class="number">3</span>])</span><br><span class="line"></span><br><span class="line">    writer_train.add_summary(run_items[<span class="number">1</span>], global_step=step)</span><br><span class="line">    writer_train.flush()  <span class="comment"># Flushes the event file to disk</span></span><br><span class="line">    <span class="comment"># 保存网络权重</span></span><br><span class="line">    <span class="keyword">if</span> (step+<span class="number">1</span>) % SAVE_INTERNAL == <span class="number">0</span>:</span><br><span class="line">        saver.save(sess, save_path=<span class="string">"./checkpoint/ckpt/yolov3.ckpt"</span>, global_step=step + <span class="number">1</span>)</span><br></pre></td></tr></table></figure><h2 id="Export-Graph"><a href="#Export-Graph" class="headerlink" title="Export Graph"></a>Export Graph</h2><p>参考文章：</p><p><a href="https://blog.metaflow.fr/tensorflow-how-to-freeze-a-model-and-serve-it-with-a-python-api-d4f3596b3adc" target="_blank" rel="noopener">https://blog.metaflow.fr/tensorflow-how-to-freeze-a-model-and-serve-it-with-a-python-api-d4f3596b3adc</a></p><p><a href="https://blog.csdn.net/guyuealian/article/details/82218092" target="_blank" rel="noopener">https://blog.csdn.net/guyuealian/article/details/82218092</a></p><p>当训练好模型之后，默认会得到一些训练的权重文件</p><img src="/2019/03/19/DeepLearning_Tensorflow/ckpt.png" title="模型训练得到的权重参数"><ul><li>checkpoint文件保存着模型文件的路径</li></ul><img src="/2019/03/19/DeepLearning_Tensorflow/checkpoint.png" title="checkpoint下保存的权重文件路径"><ul><li>model.ckpt.meta保存了TensorFlow计算图的结构信息</li><li>model.ckpt保存每个变量的取值，此处文件名的写入方式会因不同参数的设置而不同，加载restore时的文件路径名是以checkpoint文件中的“model_checkpoint_path”值决定的</li></ul><p>在ckpt文件夹下面，存储着的信息包含着整个模型的全部信息，这些信息很显然是可以进行模型的重新加载的，但是有一些信息是没必要的，尤其是在进行测试阶段的时候，在inference的时候，只需要加载已经训练好的权重参数即可，该阶段只有正向传播，没有反向传播过程，只需要告诉模型如何输入如何输出即可，不再需要想训练阶段那样要进行模型初始化，模型保存，优化参数等设置。在tensorflow中推荐将模型进行固化的方式，只保留模型的参数。</p><p>具体实现代码为</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> os, argparse</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"></span><br><span class="line"><span class="comment"># The original freeze_graph function</span></span><br><span class="line"><span class="comment"># from tensorflow.python.tools.freeze_graph import freeze_graph </span></span><br><span class="line"></span><br><span class="line">dir = os.path.dirname(os.path.realpath(__file__))</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">freeze_graph</span><span class="params">(model_dir, output_node_names)</span>:</span></span><br><span class="line">    <span class="string">"""Extract the sub graph defined by the output nodes and convert </span></span><br><span class="line"><span class="string">    all its variables into constant </span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">        model_dir: the root folder containing the checkpoint state file</span></span><br><span class="line"><span class="string">        output_node_names: a string, containing all the output node's names, </span></span><br><span class="line"><span class="string">                            comma separated</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> tf.gfile.Exists(model_dir):</span><br><span class="line">        <span class="keyword">raise</span> AssertionError(</span><br><span class="line">            <span class="string">"Export directory doesn't exists. Please specify an export "</span></span><br><span class="line">            <span class="string">"directory: %s"</span> % model_dir)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> output_node_names:</span><br><span class="line">        print(<span class="string">"You need to supply the name of a node to --output_node_names."</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># We retrieve our checkpoint fullpath</span></span><br><span class="line">    checkpoint = tf.train.get_checkpoint_state(model_dir)</span><br><span class="line">    input_checkpoint = checkpoint.model_checkpoint_path</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># We precise the file fullname of our freezed graph</span></span><br><span class="line">    absolute_model_dir = <span class="string">"/"</span>.join(input_checkpoint.split(<span class="string">'/'</span>)[:<span class="number">-1</span>])</span><br><span class="line">    output_graph = absolute_model_dir + <span class="string">"/frozen_model.pb"</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># We clear devices to allow TensorFlow to control on which device it will load operations</span></span><br><span class="line">    <span class="comment"># if have cpu and gpu, we can load this graph on each device</span></span><br><span class="line">    clear_devices = <span class="keyword">True</span></span><br><span class="line">    <span class="comment"># We import the meta graph in the current default Graph</span></span><br><span class="line">saver = tf.train.import_meta_graph(input_checkpoint + <span class="string">'.meta'</span>, clear_devices=clear_devices)</span><br><span class="line">    <span class="comment"># We start a session using a temporary fresh Graph</span></span><br><span class="line">    graph=tf.get_default_graph()</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">with</span> tf.Session(graph=graph) <span class="keyword">as</span> sess:</span><br><span class="line">      <span class="comment"># 模型初始化</span></span><br><span class="line">        sess.run(tf.global_variables_initializer())</span><br><span class="line">        <span class="comment"># We restore the weights</span></span><br><span class="line">        saver.restore(sess, input_checkpoint)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># We use a built-in TF helper to export variables to constants</span></span><br><span class="line">        output_graph_def = tf.graph_util.convert_variables_to_constants(</span><br><span class="line">            sess, <span class="comment"># The session is used to retrieve the weights</span></span><br><span class="line">            graph.as_graph_def(), <span class="comment"># The graph_def is used to retrieve the nodes </span></span><br><span class="line">            output_node_names.split(<span class="string">","</span>) <span class="comment"># The output node names are used to select the usefull nodes</span></span><br><span class="line">        ) </span><br><span class="line"></span><br><span class="line">        <span class="comment"># Finally we serialize and dump the output graph to the filesystem</span></span><br><span class="line">        <span class="keyword">with</span> tf.gfile.GFile(output_graph, <span class="string">"wb"</span>) <span class="keyword">as</span> f:</span><br><span class="line">            f.write(output_graph_def.SerializeToString())</span><br><span class="line">        print(<span class="string">"%d ops in the final graph."</span> % len(output_graph_def.node))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> output_graph_def</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    parser = argparse.ArgumentParser()</span><br><span class="line">    parser.add_argument(<span class="string">"--model_dir"</span>, type=str, default=<span class="string">""</span>, help=<span class="string">"Model folder to export"</span>)</span><br><span class="line">    parser.add_argument(<span class="string">"--output_node_names"</span>, type=str, default=<span class="string">""</span>, help=<span class="string">"The name of the output nodes, comma separated."</span>)</span><br><span class="line">    args = parser.parse_args()</span><br><span class="line"></span><br><span class="line">    freeze_graph(args.model_dir, args.output_node_names)</span><br></pre></td></tr></table></figure><p>上述代码可以完成将ckpt中文件的固化，并输出<code>frozen_model.pb</code>文件，该文件中保存着模型的参数。</p><p>那么如何加载已经固化的文件呢？代码如下</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">load_graph</span><span class="params">(frozen_graph_filename)</span>:</span></span><br><span class="line">    <span class="comment"># We load the protobuf file from the disk and parse it to retrieve the </span></span><br><span class="line">    <span class="comment"># unserialized graph_def</span></span><br><span class="line">    <span class="keyword">with</span> tf.gfile.GFile(frozen_graph_filename, <span class="string">"rb"</span>) <span class="keyword">as</span> f:</span><br><span class="line">        graph_def = tf.GraphDef()</span><br><span class="line">        graph_def.ParseFromString(f.read())</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Then, we import the graph_def into a new Graph and returns it </span></span><br><span class="line">    <span class="keyword">with</span> tf.Graph().as_default() <span class="keyword">as</span> graph:</span><br><span class="line">        <span class="comment"># The name var will prefix every op/nodes in your graph</span></span><br><span class="line">        <span class="comment"># Since we load everything in a new graph, this is not needed</span></span><br><span class="line">        tf.import_graph_def(graph_def, name=<span class="string">"prefix"</span>)</span><br><span class="line">    <span class="keyword">return</span> graph</span><br></pre></td></tr></table></figure><p>加载<code>*.pb</code>文件并返回模型的graph</p><h2 id="下载数据集COCO-amp-VOC"><a href="#下载数据集COCO-amp-VOC" class="headerlink" title="下载数据集COCO&amp;VOC"></a>下载数据集COCO&amp;VOC</h2><ul><li><p>VOC数据集地址</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wget http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCtrainval_06-Nov-2007.tar</span><br><span class="line">wget http://host.robots.ox.ac.uk/pascal/VOC/voc2012/VOCtrainval_11-May-2012.tar</span><br><span class="line">wget http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCtest_06-Nov-2007.tar</span><br></pre></td></tr></table></figure></li><li><p>COCO数据集</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">wget http://images.cocodataset.org/zips/train2017.zip</span><br><span class="line">wget http://images.cocodataset.org/annotations/annotations_trainval2017.zip</span><br><span class="line">wget http://images.cocodataset.org/zips/test2017.zip</span><br><span class="line">wget http://images.cocodataset.org/annotations/image_info_test2017.zip</span><br></pre></td></tr></table></figure></li></ul><ul><li><p>代码实现</p><p>Reference  from tensorflow model</p></li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#! /usr/bin/env python</span></span><br><span class="line"><span class="comment"># coding=utf-8</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> six.moves.urllib <span class="keyword">as</span> urllib</span><br><span class="line"><span class="keyword">import</span> tarfile</span><br><span class="line"><span class="keyword">import</span> zipfile</span><br><span class="line"></span><br><span class="line">MODEL_NAME=<span class="string">'ssd_mobilenet_v2_oid_v4_2018_12_12'</span></span><br><span class="line">MODEL_FILE = MODEL_NAME + <span class="string">'.tar.gz'</span></span><br><span class="line">DOWNLOAD_BASE = <span class="string">'http://download.tensorflow.org/models/object_detection/'</span></span><br><span class="line"><span class="keyword">print</span> <span class="string">'model name is:'</span>, MODEL_FILE</span><br><span class="line"><span class="comment"># Path to frozen detection graph. This is the actual model that is used for the object detection.</span></span><br><span class="line">opener = urllib.request.URLopener()</span><br><span class="line">opener.retrieve(DOWNLOAD_BASE + MODEL_FILE, MODEL_FILE)</span><br><span class="line">tar_file = tarfile.open(MODEL_FILE)</span><br><span class="line"><span class="keyword">for</span> file <span class="keyword">in</span> tar_file.getmembers():</span><br><span class="line">  file_name = os.path.basename(file.name)</span><br><span class="line">  <span class="keyword">if</span> <span class="string">'frozen_inference_graph.pb'</span> <span class="keyword">in</span> file_name:</span><br><span class="line">    tar_file.extract(file, os.getcwd())</span><br></pre></td></tr></table></figure><p>Use the wget</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#! /usr/bin/env python</span></span><br><span class="line"><span class="comment"># coding=utf-8</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> zipfile</span><br><span class="line"><span class="keyword">import</span> tarfile</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">import</span> wget</span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> argparse</span><br><span class="line"></span><br><span class="line"><span class="comment"># VOC urls</span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line"><span class="string">wget http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCtrainval_06-Nov-2007.tar</span></span><br><span class="line"><span class="string">wget http://host.robots.ox.ac.uk/pascal/VOC/voc2012/VOCtrainval_11-May-2012.tar</span></span><br><span class="line"><span class="string">wget http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCtest_06-Nov-2007.tar</span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># COCO urls</span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line"><span class="string">wget http://images.cocodataset.org/zips/train2017.zip</span></span><br><span class="line"><span class="string">wget http://images.cocodataset.org/annotations/annotations_trainval2017.zip</span></span><br><span class="line"><span class="string">wget http://images.cocodataset.org/zips/test2017.zip</span></span><br><span class="line"><span class="string">wget http://images.cocodataset.org/annotations/image_info_test2017.zip </span></span><br><span class="line"><span class="string">"""</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">parser</span><span class="params">(argparse.ArgumentParser)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, description)</span>:</span></span><br><span class="line">        super(parser, self).__init__(description)</span><br><span class="line"></span><br><span class="line">        self.add_argument(</span><br><span class="line">            <span class="string">"--dataset"</span>, <span class="string">"-data"</span>, default=<span class="string">'voc'</span>, type=str, choices=&#123;<span class="string">'voc'</span>, <span class="string">'coco'</span>&#125;,</span><br><span class="line">            help=<span class="string">"[default: %(default)s] The type  of dataset ..."</span></span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">voc_url = [<span class="string">'http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCtrainval_06-Nov-2007.tar'</span>,</span><br><span class="line">           <span class="string">'http://host.robots.ox.ac.uk/pascal/VOC/voc2012/VOCtrainval_11-May-2012.tar'</span>,</span><br><span class="line">           <span class="string">'http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCtest_06-Nov-2007.tar'</span>]</span><br><span class="line"></span><br><span class="line">coco_url = [<span class="string">'http://images.cocodataset.org/zips/train2017.zip'</span>,</span><br><span class="line">            <span class="string">'http://images.cocodataset.org/annotations/annotations_trainval2017.zip'</span>,</span><br><span class="line">            <span class="string">'http://images.cocodataset.org/zips/test2017.zip'</span>,</span><br><span class="line">            <span class="string">'http://images.cocodataset.org/annotations/image_info_test2017.zip'</span>]</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">(args)</span>:</span></span><br><span class="line">    flags = parser(description=<span class="string">"Download dataset"</span>).parse_args()</span><br><span class="line">    <span class="keyword">if</span> flags.dataset == <span class="string">'voc'</span>:</span><br><span class="line">        saved_path = [os.path.join(<span class="string">'/data00/home/shixiaofeng/data'</span>, <span class="string">'voc'</span>)]</span><br><span class="line">        urls = voc_url</span><br><span class="line">    <span class="keyword">elif</span> flags.dataset == <span class="string">'coco'</span>:</span><br><span class="line">        saved_path = [os.path.join(<span class="string">'/data00/home/shixiaofeng/data'</span>, <span class="string">'coco'</span>)]</span><br><span class="line">        urls = coco_url</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        saved_path = [os.path.join(<span class="string">'/data00/home/shixiaofeng/data'</span>, <span class="string">'voc'</span>),</span><br><span class="line">                      os.path.join(<span class="string">'/data00/home/shixiaofeng/data'</span>, <span class="string">'coco'</span>)]</span><br><span class="line">        urls = [voc_url, coco_url]</span><br><span class="line">    <span class="keyword">for</span> _path <span class="keyword">in</span> saved_path:</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(_path):</span><br><span class="line">            os.makedirs(_path)</span><br><span class="line">    <span class="keyword">for</span> _path <span class="keyword">in</span> saved_path:</span><br><span class="line">        <span class="keyword">for</span> url <span class="keyword">in</span> urls:</span><br><span class="line">            DATA_NAME = url.split(<span class="string">'/'</span>)[<span class="number">-1</span>]</span><br><span class="line">            <span class="keyword">print</span> <span class="string">'Downloading %s'</span> % DATA_NAME</span><br><span class="line">            DATA_FILE = os.path.join(_path, DATA_NAME)</span><br><span class="line">            <span class="keyword">print</span> <span class="string">'Download the file to : %s'</span> %DATA_FILE</span><br><span class="line">            wget.download(url, DATA_FILE)</span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                <span class="keyword">if</span> url.split(<span class="string">'.'</span>)[<span class="number">-1</span>] == <span class="string">'tar'</span>:</span><br><span class="line">                    tar_file = tarfile.open(DATA_FILE)</span><br><span class="line">                    <span class="keyword">for</span> file_name <span class="keyword">in</span> tar_file.getnames():</span><br><span class="line">                        tar_file.extract(file_name, _path)</span><br><span class="line">                    tar_file.close()</span><br><span class="line"></span><br><span class="line">                <span class="keyword">elif</span> url.split(<span class="string">'.'</span>)[<span class="number">-1</span>] == <span class="string">'zip'</span>:</span><br><span class="line">                    zip_file = zipfile.ZipFile(DATA_FILE)</span><br><span class="line">                    <span class="keyword">for</span> file_name <span class="keyword">in</span> zip_file.namelist():</span><br><span class="line">                        zip_file.extract(file_name, _path)</span><br><span class="line">                    zip_file.close()</span><br><span class="line"></span><br><span class="line">            <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">                <span class="keyword">print</span> e</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main(sys.argv[<span class="number">1</span>:])</span><br></pre></td></tr></table></figure><p>直接执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">python xxx.py --dataset coco</span><br></pre></td></tr></table></figure><p>可以直接下载coco数据集到设置的位置，并将数据集解压缩</p><h2 id="OpenImage-下载"><a href="#OpenImage-下载" class="headerlink" title="OpenImage 下载"></a>OpenImage 下载</h2><p>由于OpenImage文件较多，单次下载非常慢，特此保存一个下载脚本，输入想要的模型名称，下载openimage中对应的数据图片和标注信息</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">import</span> boto3</span><br><span class="line"><span class="keyword">from</span> botocore <span class="keyword">import</span> UNSIGNED</span><br><span class="line"><span class="keyword">from</span> botocore.config <span class="keyword">import</span> Config</span><br><span class="line"><span class="keyword">import</span> botocore</span><br><span class="line"><span class="keyword">import</span> logging</span><br><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Pool, Manager</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> argparse</span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> functools</span><br><span class="line"><span class="keyword">from</span> urllib <span class="keyword">import</span> request</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">s3 = boto3.client(<span class="string">'s3'</span>, config=Config(signature_version=UNSIGNED))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">download</span><span class="params">(bucket, root, retry, counter, lock, path)</span>:</span></span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    src = path</span><br><span class="line">    dest = <span class="string">"&#123;root&#125;/&#123;path&#125;"</span>.format(root=root, path=path)</span><br><span class="line">    <span class="keyword">while</span> i &lt; retry:</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(dest):</span><br><span class="line">                s3.download_file(bucket, src, dest)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                logging.info(<span class="string">"&#123;dest&#125; already exists."</span>.format(dest=dest))</span><br><span class="line">            <span class="keyword">with</span> lock:</span><br><span class="line">                counter.value += <span class="number">1</span></span><br><span class="line">                <span class="keyword">if</span> counter.value % <span class="number">100</span> == <span class="number">0</span>:</span><br><span class="line">                    logging.warning(<span class="string">"Downloaded &#123;&#125; images: &#123;&#125;"</span>.format(counter.value,dest))</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        <span class="keyword">except</span> botocore.exceptions.ClientError <span class="keyword">as</span> e:</span><br><span class="line">            <span class="keyword">if</span> e.response[<span class="string">'Error'</span>][<span class="string">'Code'</span>] == <span class="string">"404"</span>:</span><br><span class="line">                logging.warning(<span class="string">"The file s3://&#123;bucket&#125;/&#123;src&#125; does not exist."</span>.format(bucket=bucket,src=src))</span><br><span class="line">                <span class="keyword">return</span></span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line">            logging.warning(<span class="string">"Sleep &#123;i&#125; and try again."</span>.format(i))</span><br><span class="line">            time.sleep(i)</span><br><span class="line">    logging.warning(<span class="string">"Failed to download the file s3://&#123;bucket&#125;/&#123;src&#125;. Exception: &#123;e&#125;"</span>.format(bucket=bucket,src=src,e=e))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">batch_download</span><span class="params">(bucket, file_paths, root, num_workers=<span class="number">10</span>, retry=<span class="number">10</span>)</span>:</span></span><br><span class="line">    <span class="keyword">with</span> Pool(num_workers) <span class="keyword">as</span> p:</span><br><span class="line">        m = Manager()</span><br><span class="line">        counter = m.Value(<span class="string">'i'</span>, <span class="number">0</span>)</span><br><span class="line">        lock = m.Lock()</span><br><span class="line">        download_ = functools.partial(download, bucket, root, retry, counter, lock)</span><br><span class="line">        p.map(download_, file_paths)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">http_download</span><span class="params">(url, path)</span>:</span></span><br><span class="line">    <span class="keyword">with</span> request.urlopen(url) <span class="keyword">as</span> f:</span><br><span class="line">        <span class="keyword">with</span> open(path, <span class="string">"wb"</span>) <span class="keyword">as</span> fout:</span><br><span class="line">            buf = f.read(<span class="number">1024</span>)</span><br><span class="line">            <span class="keyword">while</span> buf:</span><br><span class="line">                fout.write(buf)</span><br><span class="line">                buf = f.read(<span class="number">1024</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">log_counts</span><span class="params">(values)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> k, count <span class="keyword">in</span> values.value_counts().iteritems():</span><br><span class="line">        logging.warning(<span class="string">"&#123;&#125;: &#123;&#125;/&#123;&#125; = &#123;:.8f&#125;."</span>.format(k,count,len(values),count/len(values)))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">parse_args</span><span class="params">()</span>:</span></span><br><span class="line">    parser = argparse.ArgumentParser(</span><br><span class="line">        description=<span class="string">'Dowload open image dataset by class.'</span>)</span><br><span class="line"></span><br><span class="line">    parser.add_argument(<span class="string">"--root"</span>, type=str,</span><br><span class="line">                        help=<span class="string">'The root directory that you want to store the open image data.'</span>)</span><br><span class="line">    parser.add_argument(<span class="string">"include_depiction"</span>, action=<span class="string">"store_true"</span>,</span><br><span class="line">                        help=<span class="string">"Do you want to include drawings or depictions?"</span>)</span><br><span class="line">    parser.add_argument(<span class="string">"--class_names"</span>, type=str,</span><br><span class="line">                        help=<span class="string">"the classes you want to download."</span>)</span><br><span class="line">    parser.add_argument(<span class="string">"--class_names_file"</span>, type=str,</span><br><span class="line">                        help=<span class="string">"the classes you want to download."</span>)</span><br><span class="line">    parser.add_argument(<span class="string">"--num_workers"</span>, type=int, default=<span class="number">10</span>,</span><br><span class="line">                        help=<span class="string">"the classes you want to download."</span>)</span><br><span class="line">    parser.add_argument(<span class="string">"--retry"</span>, type=int, default=<span class="number">10</span>,</span><br><span class="line">                        help=<span class="string">"retry times when downloading."</span>)</span><br><span class="line">    parser.add_argument(<span class="string">"--filter_file"</span>, type=str, default=<span class="string">""</span>,</span><br><span class="line">                        help=<span class="string">"This file specifies the image ids you want to exclude."</span>)</span><br><span class="line">    parser.add_argument(<span class="string">'--remove_overlapped'</span>, action=<span class="string">'store_true'</span>,</span><br><span class="line">                        help=<span class="string">"Remove single boxes covered by group boxes."</span>)</span><br><span class="line">    <span class="keyword">return</span> parser.parse_args()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    logging.basicConfig(stream=sys.stdout, level=logging.WARNING,</span><br><span class="line">                        format=<span class="string">'%(asctime)s - %(name)s - %(message)s'</span>)</span><br><span class="line"></span><br><span class="line">    args = parse_args()</span><br><span class="line">    bucket = <span class="string">"open-images-dataset"</span></span><br><span class="line">    names = [e.strip() <span class="keyword">for</span> e <span class="keyword">in</span> args.class_names.split(<span class="string">","</span>)]</span><br><span class="line">    class_names = []</span><br><span class="line">    group_filters = []</span><br><span class="line">    percentages = []</span><br><span class="line">    <span class="keyword">for</span> name <span class="keyword">in</span> names:</span><br><span class="line">        t = name.split(<span class="string">":"</span>)</span><br><span class="line">        class_names.append(t[<span class="number">0</span>].strip())</span><br><span class="line">        <span class="keyword">if</span> len(t) &gt;= <span class="number">2</span> <span class="keyword">and</span> t[<span class="number">1</span>].strip():</span><br><span class="line">            group_filters.append(t[<span class="number">1</span>].strip())</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            group_filters.append(<span class="string">""</span>)</span><br><span class="line">        <span class="keyword">if</span> len(t) &gt;= <span class="number">3</span> <span class="keyword">and</span> t[<span class="number">2</span>].strip():</span><br><span class="line">            percentages.append(float(t[<span class="number">2</span>].strip()))</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            percentages.append(<span class="number">1.0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(args.root):</span><br><span class="line">        os.makedirs(args.root)</span><br><span class="line"></span><br><span class="line">    excluded_images = set()</span><br><span class="line">    <span class="keyword">if</span> args.filter_file:</span><br><span class="line">        <span class="keyword">for</span> line <span class="keyword">in</span> open(args.filter_file):</span><br><span class="line">            img_id = line.strip()</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> img_id:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            excluded_images.add(img_id)</span><br><span class="line"></span><br><span class="line">    class_description_file = os.path.join(args.root, <span class="string">"class-descriptions-boxable.csv"</span>)</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(class_description_file):</span><br><span class="line">        url = <span class="string">"https://storage.googleapis.com/openimages/2018_04/class-descriptions-boxable.csv"</span></span><br><span class="line">        logging.warning(<span class="string">"Download &#123;url&#125;."</span>.format(url=url))</span><br><span class="line">        http_download(url, class_description_file)</span><br><span class="line"></span><br><span class="line">    class_descriptions = pd.read_csv(class_description_file,</span><br><span class="line">                                    names=[<span class="string">"id"</span>, <span class="string">"ClassName"</span>])</span><br><span class="line">    </span><br><span class="line">    class_names=[]</span><br><span class="line">    <span class="keyword">for</span> line <span class="keyword">in</span> open(args.class_names_file, <span class="string">'r'</span>).readlines():</span><br><span class="line">        x=line.strip().split(<span class="string">'/'</span>) </span><br><span class="line">        x = [i.capitalize() <span class="keyword">for</span> i <span class="keyword">in</span> x]</span><br><span class="line">        class_names.extend(x)</span><br><span class="line">        </span><br><span class="line">    class_names = list(set(class_names))</span><br><span class="line">    </span><br><span class="line">    class_descriptions = class_descriptions[class_descriptions[<span class="string">'ClassName'</span>].isin(class_names)]</span><br><span class="line">    print(<span class="string">'class_descriptions'</span>,class_descriptions)</span><br><span class="line">    image_files = []</span><br><span class="line">    <span class="keyword">for</span> dataset_type <span class="keyword">in</span> [<span class="string">"train"</span>, <span class="string">"validation"</span>, <span class="string">"test"</span>]:</span><br><span class="line">        image_dir = os.path.join(args.root, dataset_type)</span><br><span class="line">        os.makedirs(image_dir, exist_ok=<span class="keyword">True</span>)</span><br><span class="line"></span><br><span class="line">        annotation_file = <span class="string">"&#123;&#125;/&#123;&#125;-annotations-bbox.csv"</span>.format(args.root,dataset_type)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(annotation_file):</span><br><span class="line">            url = <span class="string">"https://storage.googleapis.com/openimages/2018_04/&#123;&#125;/&#123;&#125;-annotations-bbox.csv"</span>.format(dataset_type,dataset_type)</span><br><span class="line">            logging.warning(<span class="string">"Download &#123;url&#125;."</span>.format(url=url))</span><br><span class="line">            http_download(url, annotation_file)</span><br><span class="line">        logging.warning(<span class="string">"Read annotation file &#123;&#125;"</span>.format(annotation_file))</span><br><span class="line">        annotations = pd.read_csv(annotation_file)</span><br><span class="line">        annotations = pd.merge(annotations, class_descriptions,</span><br><span class="line">                               left_on=<span class="string">"LabelName"</span>, right_on=<span class="string">"id"</span>,</span><br><span class="line">                               how=<span class="string">"inner"</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> args.include_depiction:</span><br><span class="line">            annotations = annotations.loc[annotations[<span class="string">'IsDepiction'</span>] != <span class="number">1</span>, :]</span><br><span class="line"></span><br><span class="line">        filtered = []</span><br><span class="line">        print(<span class="string">"class_names"</span>, class_names)</span><br><span class="line">        group_filters = [<span class="string">'~group'</span>] * len(class_names)</span><br><span class="line">        percentages=[<span class="number">1.0</span>]*len(class_names)</span><br><span class="line">        <span class="keyword">for</span> class_name, group_filter, percentage <span class="keyword">in</span> zip(class_names, group_filters, percentages):</span><br><span class="line">            sub = annotations.loc[annotations[<span class="string">'ClassName'</span>] == class_name, :]</span><br><span class="line">            excluded_images |= set(sub[<span class="string">'ImageID'</span>].sample(frac=<span class="number">1</span> - percentage))</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> group_filter == <span class="string">'~group'</span>:</span><br><span class="line">                excluded_images |= set(sub.loc[sub[<span class="string">'IsGroupOf'</span>] == <span class="number">1</span>, <span class="string">'ImageID'</span>])</span><br><span class="line">            <span class="keyword">elif</span> group_filter == <span class="string">'group'</span>:</span><br><span class="line">                excluded_images |= set(sub.loc[sub[<span class="string">'IsGroupOf'</span>] == <span class="number">0</span>, <span class="string">'ImageID'</span>])</span><br><span class="line">            filtered.append(sub)</span><br><span class="line"></span><br><span class="line">        print(<span class="string">"annotations"</span>, annotations.shape)</span><br><span class="line">        print(<span class="string">"filtered"</span>,len(filtered))</span><br><span class="line">        annotations = pd.concat(filtered)</span><br><span class="line">        annotations = annotations.loc[~annotations[<span class="string">'ImageID'</span>].isin(excluded_images), :]</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> args.remove_overlapped:</span><br><span class="line">            images_with_group = annotations.loc[annotations[<span class="string">'IsGroupOf'</span>] == <span class="number">1</span>, <span class="string">'ImageID'</span>]</span><br><span class="line">            annotations = annotations.loc[~(annotations[<span class="string">'ImageID'</span>].isin(set(images_with_group)) &amp; (annotations[<span class="string">'IsGroupOf'</span>] == <span class="number">0</span>)), :]</span><br><span class="line">        annotations = annotations.sample(frac=<span class="number">1.0</span>)</span><br><span class="line"></span><br><span class="line">        logging.warning(<span class="string">"&#123;&#125; bounding boxes size: &#123;&#125;"</span>.format(dataset_type,annotations.shape[<span class="number">0</span>]))</span><br><span class="line">        logging.warning(<span class="string">"Approximate Image Stats: "</span>)</span><br><span class="line">        log_counts(annotations.drop_duplicates([<span class="string">"ImageID"</span>, <span class="string">"ClassName"</span>])[<span class="string">"ClassName"</span>])</span><br><span class="line">        logging.warning(<span class="string">"Label distribution: "</span>)</span><br><span class="line">        log_counts(annotations[<span class="string">'ClassName'</span>])</span><br><span class="line"></span><br><span class="line">        logging.warning(<span class="string">"Shuffle dataset."</span>)</span><br><span class="line"></span><br><span class="line">        sub_annotation_file = <span class="string">"&#123;&#125;/sub-&#123;&#125;-annotations-bbox.csv"</span>.format(args.root,dataset_type)</span><br><span class="line">        logging.warning(<span class="string">"Save &#123;&#125; data to &#123;&#125;."</span>.format(dataset_type,sub_annotation_file))</span><br><span class="line">        annotations.to_csv(sub_annotation_file, index=<span class="keyword">False</span>)</span><br><span class="line">        image_files.extend(<span class="string">"&#123;&#125;/&#123;&#125;.jpg"</span>.format(dataset_type, ids) <span class="keyword">for</span> ids <span class="keyword">in</span> set(annotations[<span class="string">'ImageID'</span>]))</span><br><span class="line">    print(<span class="string">'image_files'</span>,len(image_files))</span><br><span class="line">    logging.warning(<span class="string">"Start downloading &#123;&#125; images."</span>.format(len(image_files)))</span><br><span class="line">    batch_download(bucket, image_files, args.root, args.num_workers, args.retry)</span><br><span class="line">    logging.warning(<span class="string">"Task Done."</span>)</span><br></pre></td></tr></table></figure><p>可以使用如下方法进行下载</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">python open_images_downloader.py --root ~/datas/openimage  --class_names_file dataset/awesome_open.names  --num_workers 500</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本篇主要记录在日常工作中遇到的TensorFlow的相关信息，包括如何处理报错信息，环境设置，训练测试，数据等等。&lt;/p&gt;
    
    </summary>
    
      <category term="Tensorflow" scheme="http://xiaofengshi.com/categories/Tensorflow/"/>
    
    
      <category term="Tensorflow" scheme="http://xiaofengshi.com/tags/Tensorflow/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-OCR-从零开始</title>
    <link href="http://xiaofengshi.com/2019/03/13/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-OCR-%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B/"/>
    <id>http://xiaofengshi.com/2019/03/13/深度学习-OCR-从零开始/</id>
    <published>2019-03-13T08:03:05.000Z</published>
    <updated>2020-02-03T13:33:32.236Z</updated>
    
    <content type="html"><![CDATA[<p>本篇详细说明了OCR的应用，技术实现，当前相关进展等，不定时补充更新。</p><p>本篇博客最初想写成一本掘金小册，可是掘金的那边看了我的提纲说太难了，应该没什么受众，额……，那我就自己写一下记录下来，不会太讲究斟词酌句了，希望见谅。</p><p>本篇博客算是一篇针对OCR相关学习或者初步接触者的引导和入门，讲的不深，实际上针对每个模块，比如<code>CTPN</code>，<code>CTC</code>，<code>Attention</code>，<code>Densenet</code>这些我都有专门写过博客来讲述过，感兴趣的可以翻翻我的博客文章。</p><p>更新记录：</p><p>2020.01.31: 自从19年4月份入职字节跳动之后一直忙于工作，很久没有更新个人博客了，今天看到各位的留言很是感动，为自己长久的拖延感到羞愧。由于武汉肺炎延迟开工，今天努力把以前写的提纲补充齐全，这一年来，相比于之前增加了更多的工业界算法思维，一个好的算法是在能够实现预定目的的同时，尽可能节省算力的，接下来也会把自己的一些感触和经验体现在博客里面。</p><a id="more"></a><h2 id="OCR任务详解及应用场景"><a href="#OCR任务详解及应用场景" class="headerlink" title="OCR任务详解及应用场景"></a>OCR任务详解及应用场景</h2><h3 id="什么叫OCR"><a href="#什么叫OCR" class="headerlink" title="什么叫OCR"></a>什么叫OCR</h3><p>按照维基百科的定义，(<strong>Optical Character Recognition,OCR</strong>)光学字符识别是指对文本资料的图像文件进行分析识别处理，获取文字及版面信息的过程。通俗来讲，<strong>OCR任务就是我们输入一张包含文本资料的图片，得到该图像中包含的文字和版面信息输出，图形中包含的文字很好理解，版面信息对应的是在输入的图片中文字所在的位置信息。</strong></p><p>下面通过一张简单的图像进行OCR任务的说明</p><img src="/2019/03/13/深度学习-OCR-从零开始/text.png" title="OCR待识别图片"><p>通过OCR得到的返回结果</p><img src="/2019/03/13/深度学习-OCR-从零开始/ali_text.png" title="OCR返回结果"><p>上图为使用阿里云提供的OCR服务得到的返回结果，可以看到，通过OCR识别技术，不仅得到了输入图像中的文字信息，并且包含输入图像的版面信息。</p><h3 id="OCR用于何处"><a href="#OCR用于何处" class="headerlink" title="OCR用于何处"></a>OCR用于何处</h3><p>目前OCR识别在身份证，驾驶证，营业执照，银行卡，车牌，名片，合同文本等具有规范版面格式的图像场景中已经达到了商用指标，并取得了广泛的应用；在学术界则在探索更广泛的应用，研究更先进高效的算法以适用于街景文字识别，手写体识别等场景中。</p><p>人的懒惰推动着技术的进步，之所以开发并使用OCR相关技术，就是为了帮助我们更快的进行信息提取。但是在提取图像信息的同时人们对信息提取的准确率是有着的要求的，任何一个算法都要抱着宁缺毋滥的原则，并且在OCR识别中对于小说等文学文档的信息提取准确率，显然和对身份证、驾驶证等识别结果的要求是不同的，这就代表着要开发不同的算法以适应不同的场景需求。</p><p>接下来使用阿里云提供的OCR服务：<a href="https://ai.aliyun.com/ocr/general?spm=5176.12188956.1280361.58.4ddb1a9eyHHIYB" target="_blank" rel="noopener">https://ai.aliyun.com/ocr/general?spm=5176.12188956.1280361.58.4ddb1a9eyHHIYB</a></p><p>进行不同类型照片的测试</p><p>对于身份证照片</p><img src="/2019/03/13/深度学习-OCR-从零开始/id_card.jpg" title="身份证照片"><img src="/2019/03/13/深度学习-OCR-从零开始/id_card_out.png" title="身份证识别结果"><p>对于汽车车牌照片识别</p><img src="/2019/03/13/深度学习-OCR-从零开始/car_id.jpg" title="汽车牌照照片"><img src="/2019/03/13/深度学习-OCR-从零开始/car_id_out.png" title="汽车牌照识别结果"><p>如上的识别结果由阿里云OCR服务提供，并且目前已经得到了广泛的应用，可以看到对于具有标准格式的照片OCR识别结果非常准确。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>本章节结合实例对OCR的概念和应用场景进行了讲解，目前OCR技术已经在众多领域得到了广泛应用，相信大家一定好奇OCR识别具体是如何实现的，会在接下来的章节进行详细讲解</p><h3 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h3><p>请大家思考一个问题，对于OCR识别结果，如何确保其正确性的呢？</p><h2 id="中文OCR任务解决方案"><a href="#中文OCR任务解决方案" class="headerlink" title="中文OCR任务解决方案"></a>中文OCR任务解决方案</h2><p>本章节主要针对OCR识别具体的实现方案进行详细的阐述。</p><p>环境需求：</p><p>python==3.6</p><p>opencv-python==4.0</p><p>启动jupyter之后，首先导入依赖</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> cv2</span><br><span class="line"><span class="keyword">from</span> PIL <span class="keyword">import</span> Image</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> matplotlib <span class="keyword">import</span> pyplot <span class="keyword">as</span> plt</span><br></pre></td></tr></table></figure><h3 id="OCR识别过程"><a href="#OCR识别过程" class="headerlink" title="OCR识别过程"></a>OCR识别过程</h3><ul><li><p>图片预处理</p><p>对于输入的图片，一般来源是手机拍摄，摄像头拍摄，打印机扫描等，这就意味着输入的图片具有多种格式和色彩特征和多种数据格式，在OCR识别时，要对输入的图片进行预处理，一般的预处理方式包括如下几种。</p><ul><li><p>图像读取</p><p>该步骤要读取图像为统一格式，输入的图片包含’jpg’、’png’、’jpeg’等格式，不同的图片格式意味着不同的数据格式，因此在读取图片的时候要转换成一个固定的格式，一般情况下，将所有转换的图像都转换成灰度图数据格式</p><p>以上一章节的身份证照片为例</p><img src="/2019/03/13/深度学习-OCR-从零开始/id_card.jpg" title="原始身份证图片"><p>将读取的图像转化成灰度图</p><img src="/2019/03/13/深度学习-OCR-从零开始/id_card_gray.png" title="转换成灰度图"><p>可以看到，通过转换成灰度图，输入的彩色图片转换成了灰度图</p><p><strong>代码实现如下</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 图片地址</span></span><br><span class="line">img_path=<span class="string">'id_card.png'</span></span><br><span class="line"><span class="comment"># 读取图片</span></span><br><span class="line">img=Image.open(img_path)</span><br><span class="line"><span class="comment"># 显示图片</span></span><br><span class="line">plt.imshow(img)</span><br><span class="line"><span class="comment"># 数据转换成numpy格式</span></span><br><span class="line">img_data=np.asarray(img)</span><br><span class="line"><span class="comment"># 图片转换成灰度图</span></span><br><span class="line">gray_img=cv2.cvtColor(img_data,cv2.COLOR_RGB2GRAY)</span><br><span class="line"><span class="comment"># 显示灰度图</span></span><br><span class="line">plt.imshow(gray_img)</span><br><span class="line"><span class="comment"># 保存图片</span></span><br><span class="line">cv2.imwrite(<span class="string">'gray.png'</span>,gray_img)</span><br></pre></td></tr></table></figure></li><li><p>图像色彩处理</p><p>一般情况下，OCR输入的图像输入的是彩色图像，图像中包含着大量的色彩信息，为了提高OCR的识别效率，更快的提取图像中的文字信息，通常对输入的图像进行二值化处理，所谓二值化就是将图像进行处理，使其只包含黑白二色。</p><img src="/2019/03/13/深度学习-OCR-从零开始/id_card_binary.png" title="转化为黑白二值图"><p><strong>代码实现如下</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 动态阈值进行图像二值化处理</span></span><br><span class="line">blur_img = cv2.adaptiveThreshold(</span><br><span class="line">            gray_img, <span class="number">255</span>, cv2.ADAPTIVE_THRESH_MEAN_C, cv2.THRESH_BINARY, <span class="number">25</span>, <span class="number">20</span>)</span><br><span class="line"><span class="comment"># 显示二值化图像</span></span><br><span class="line">plt.imshow(blur_img)</span><br><span class="line"><span class="comment"># 保存图片</span></span><br><span class="line">cv2.imwrite(<span class="string">'blur.png'</span>,blur_img)</span><br></pre></td></tr></table></figure></li><li><p>图片去噪</p><p>将图像转换成二值图之后，会发现图片中存在很多噪声，对于噪声的分类一般为椒盐噪声，高斯噪声。在图像处理中使用中值滤波或者均值滤波进行图片去噪处理</p><img src="/2019/03/13/深度学习-OCR-从零开始/id_card_denoised.png" title="图片去噪"><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 对二值图片进行噪声去除</span></span><br><span class="line"><span class="comment"># fastNlMeansDenoising的参数为</span></span><br><span class="line"><span class="comment"># 待处理图片，滤波器强度，对彩色图片滤波摩人与滤波器强度相等，模板尺寸，搜索窗口尺寸</span></span><br><span class="line">img_denoised=cv2.fastNlMeansDenoising(blur_img,<span class="number">10</span>,<span class="number">10</span>,<span class="number">7</span>,<span class="number">21</span>)</span><br><span class="line"><span class="comment"># 显示图片</span></span><br><span class="line">plt.imshow(img_denoised)</span><br><span class="line"><span class="comment"># 保存图片</span></span><br><span class="line">cv2.imwrite(<span class="string">'denoised.png'</span>,img_denoised)</span><br></pre></td></tr></table></figure></li><li><p>图片倾斜矫正</p><p>如果输入的图片是手机拍摄，一般情况下照片会不可避免的存在倾斜，这时候就要对输入的图片进行倾斜矫正。倾斜矫正包括两种，一种是图片旋转，一种是图片的仿射变换。</p><p>对于图片旋转，首先考虑如何实现图片的旋转呢，实际上对图片进行旋转就是进行矩阵旋转，首先分析旋转在二维平面中围绕一个点进行旋转</p><img src="/2019/03/13/深度学习-OCR-从零开始/rotate_2d.png" title="旋转示意图"><p>图中所示为点V绕远点旋转角度$\theta$，得到旋转后的点$\mathbf{v}^{\prime}$，假设$v$点原来的坐标为$(x,y)$，进行推导</p><script type="math/tex; mode=display">\begin{array}{l}{x=r \cos \phi \\ y=r \sin \phi} \\ {x^{\prime}=r \cos (\theta+\phi)\\y^{\prime}=r \sin (\theta+\phi)}\end{array}</script><p>使用三角函数进行展开</p><script type="math/tex; mode=display">\begin{aligned} x^{\prime} &=r \cos \theta \cos \phi-r \sin \theta \sin \phi \\ y^{\prime} &=r \sin \theta \cos \phi+r \cos \theta \sin \phi \end{aligned}</script><p>再将x，y带入到上面式子中可以得到表达式</p><script type="math/tex; mode=display">\begin{array}{l}{x^{\prime}=x \cos \theta-y \sin \theta} \\ {y^{\prime}=x \sin \theta+y \cos \theta}\end{array}</script><p>这是对单个点以原点为中心进行旋转，当扩展到一个图像中的时候，写成矩阵形式</p><script type="math/tex; mode=display">\left[ \begin{array}{l}{x^{\prime}} \\ {y^{\prime}}\end{array}\right]=\left[ \begin{array}{cc}{\cos \theta} & {-\sin \theta} \\ {\sin \theta} & {\cos \theta}\end{array}\right] * \left[ \begin{array}{l}{x} \\ {y}\end{array}\right]</script><p>如果旋转中心点不是原点，而是任意一个点，那么这时候该怎么做呢？</p><p>在计算机图形学中，为了统一将图形平移、旋转、缩放等都用矩阵形式表示，需要引入<a href="https://zh.wikipedia.org/wiki/%E9%BD%90%E6%AC%A1%E5%9D%90%E6%A0%87" target="_blank" rel="noopener">齐次坐标</a>。将上述问题进行拆分，首先将旋转点移动到原点处，之后按照原点进行旋转，最后将旋转点从原点移动到原来的位置。</p><img src="/2019/03/13/深度学习-OCR-从零开始/move2d.jpeg" title="二维平移"><p>首先进行二维平移操作，如上图所示，一个点$P(x,y)$，移动到点$P^{\prime}(x^{\prime},y^{\prime})$，从图中可以很明显的看出在二维空间的坐标位置关系</p><script type="math/tex; mode=display">x^{\prime}=x+t x \\y^{\prime}=y+t y</script><p>使用其次坐标进行表示二维平移</p><script type="math/tex; mode=display">\left[ \begin{array}{c}{x^{\prime}} \\ {y^{\prime}} \\ {1}\end{array}\right]=\left[ \begin{array}{lll}{1} & {0} & {t x} \\ {0} & {1} & {t y} \\ {0} & {0} & {1}\end{array}\right] * \left[ \begin{array}{l}{x} \\ {y} \\ {1}\end{array}\right]</script><p>同理，结合二维空间的旋转操作，在其次坐标下进行旋转操作，得到变换关系式为</p><script type="math/tex; mode=display">\left[ \begin{array}{c}{x^{\prime}} \\ {y^{\prime}} \\ {1}\end{array}\right]=\left[ \begin{array}{ccc}{\cos \theta} & {-\sin \theta} & {0} \\ {\sin \theta} & {\cos \theta} & {0} \\ {0} & {0} & {1}\end{array}\right] * \left[ \begin{array}{l}{x} \\ {y} \\ {1}\end{array}\right]</script><p>按照前面对绕任一点旋转的拆分，先进行平移，之后旋转，最后再反向平移</p><p>可以得到旋转矩阵为</p><script type="math/tex; mode=display">M=\left[ \begin{array}{ccc}{1} & {0} & {\operatorname{-tx}} \\ {0} & {1} & {-t y} \\ {0} & {0} & {1}\end{array}\right] * \left[ \begin{array}{ccc}{\cos \theta} & {-\sin \theta} & {0} \\ {\sin \theta} & {\cos \theta} & {0} \\ {0} & {0} & {1}\end{array}\right] * \left[ \begin{array}{ccc}{1} & {0} & {tx} \\ {0} & {1} & {t y} \\ {0} & {0} & {1}\end{array}\right]\\=\left[ \begin{array}{ccc}{\cos \theta} & {-\sin \theta} & {(1-\cos \theta) t x-t y * \sin \theta} \\ {\sin \theta} & {\cos \theta} & {(1-\cos \theta) t y+t x * \sin \theta} \\ {0} & {0} & {1}\end{array}\right]</script><p>如上便是在计算机图形学中对图像进行旋转的数学原理，在代码实现中一般使用opencv实现。</p><img src="/2019/03/13/深度学习-OCR-从零开始/id_card_rotated.png" title="旋转图片45度"><p>实现代码</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">h,w=img_denoised.shape</span><br><span class="line"><span class="comment"># 计算图片中心点</span></span><br><span class="line">center=(w//<span class="number">2</span>,h//<span class="number">2</span>)</span><br><span class="line"><span class="comment"># 创建旋转矩阵，旋转角度为45度</span></span><br><span class="line">M = cv2.getRotationMatrix2D(center, <span class="number">45</span>, <span class="number">1.0</span>) </span><br><span class="line"><span class="comment"># 得到旋转之后的图像</span></span><br><span class="line">img_rotated = cv2.warpAffine(img_denoised, M, (w, h))</span><br><span class="line"><span class="comment"># 显示图片</span></span><br><span class="line">plt.imshow(img_rotated)</span><br><span class="line"><span class="comment"># 保存图片</span></span><br><span class="line">cv2.imwrite(<span class="string">'rotated.png'</span>, img_rotated)</span><br></pre></td></tr></table></figure><p>上述是进行图像旋转的代码实现。可以打印矩阵M查看和使用公式计算的结果是一致的。</p><p>可以看到，旋转前后的图像的尺寸保持一致，这就会造成旋转之后的图片会丢失部分图片信息，实际上可以对上面的代码进行修正</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 计算图片的几何尺寸</span></span><br><span class="line">h,w=img_denoised.shape</span><br><span class="line"><span class="comment"># 计算图片中心点</span></span><br><span class="line">center=(w//<span class="number">2</span>,h//<span class="number">2</span>)</span><br><span class="line"><span class="comment"># 创建旋转矩阵，旋转角度为45度</span></span><br><span class="line">M = cv2.getRotationMatrix2D(center, <span class="number">45</span>, <span class="number">1.0</span>)</span><br><span class="line"><span class="comment"># 计算旋转角度的正余弦值</span></span><br><span class="line">cos = np.abs(M[<span class="number">0</span>, <span class="number">0</span>])</span><br><span class="line">sin = np.abs(M[<span class="number">0</span>, <span class="number">1</span>])</span><br><span class="line"><span class="comment"># 计算旋转之后的图片的宽度和高度</span></span><br><span class="line">nW = int((h * sin) + (w * cos))</span><br><span class="line">nH = int((h * cos) + (w * sin))</span><br><span class="line"><span class="comment"># 在旋转矩阵的基础上加入角度旋转造成的图片尺寸的影响</span></span><br><span class="line">M[<span class="number">0</span>, <span class="number">2</span>] += (nW / <span class="number">2</span>) - center[<span class="number">0</span>]</span><br><span class="line">M[<span class="number">1</span>, <span class="number">2</span>] += (nH / <span class="number">2</span>) - center[<span class="number">1</span>]</span><br><span class="line"><span class="comment"># 旋转图像</span></span><br><span class="line">rotated2=cv2.warpAffine(img_denoised, M, (nW, nH))</span><br><span class="line"><span class="comment"># 显示图像</span></span><br><span class="line">plt.imshow(rotated2)</span><br></pre></td></tr></table></figure><img src="/2019/03/13/深度学习-OCR-从零开始/id_card_rotated2.png" title="修正旋转矩阵"><p>对图片进行修正之后，可以看到旋转之后的图片保留了原图片的全部信息，</p></li><li><p>版面分析</p><p>将输入的文档图片按照段落或行为单位进行版面分析，在实际操作中，由于文档是丰富多样的，因此目前没有固定的版面分析算法，要根据实际应用场景进行设计，这也是目前OCR识别任务重比较重要的一部，会在之后部分详细说明。</p></li><li><p>字符识别</p><p>在OCR识别任务中，存在多种字符识别的方法，早期使用的是模板匹配的方法，后期有特征提取的方法，直至目前的深度学习的方法，该步骤对OCR识别准确率至关重要，同样会在之后的部分进行详细说明。</p></li><li><p>版面还原</p><p>对于OCR任务中，我们希望识别后的文字仍然按照输入文档图片那样排列，保持段落不变，文字位置不变，顺序不变的输出，该步骤就是版面还原的过程。在该步骤中，版面还原的信息依赖于前期的版面分析的到的版面信息。</p></li><li><p>输出校对</p><p>最后是对输出的识别结果使用自然语言的方法进行识别结果的校正。</p></li></ul><p>上述过程就是OCR识别的一般过程，简单来讲就是，先对图像进行预处理，将输入的图像变成统一的格式，之后进行版面分析和文字识别，最终进行版面还原输出。在整个过程中，图像预处理一般使用的是通用的技术解决方案，就如上文所说，进行图像转成灰度图，二值化处理，提高对比度亮度，旋转仿射变换等常规处理手段，经过如上的图像预处理，输入的图像变成相对统一的图像数据。之后便要对输入的图像进行版面分析，主要是找到图像中的待识别文字的位置区域，这里的文字区域可以是中文中的单个文字，可以使英文中的单个单词，也可以是一行文字；当找到待识别文字的位置区域之后，就下来就是对区域内图像文字进行识别。接下来便对版面分析和文字识别进行着重说明。</p></li></ul><h3 id="版面分析"><a href="#版面分析" class="headerlink" title="版面分析"></a>版面分析</h3><p>正如上文所述，版面分析就是要找到待识别文字的位置区域，所谓文字区域，一般是指单个文字或者一整行的文字</p><h4 id="传统手工特征方法"><a href="#传统手工特征方法" class="headerlink" title="传统手工特征方法"></a>传统手工特征方法</h4><p>如果要提取单文字的位置区域，存在多种技术实现方法，下面进行简略说明。</p><ul><li>图像投影方法</li></ul><p>对预处理之后的图像，将图像对垂直方向的坐标轴进行投影，图像中不同行的文字经过投影会被间隔开，如此就可以提取到不同的文本行，如下所示为对进行噪声去噪之后的二值图进行水平方向的投影。</p><img src="/2019/03/13/深度学习-OCR-从零开始/id_card_horizonal.png" title="图像进行水平方向投影"><p>代码实现</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 图片预处理</span></span><br><span class="line">img_path=<span class="string">'id_card.png'</span></span><br><span class="line">img_color=cv2.imread(img_path)</span><br><span class="line">img_gray=cv2.cvtColor(image_color, cv2.COLOR_BGR2GRAY)</span><br><span class="line">blur_img = cv2.adaptiveThreshold(</span><br><span class="line">            img_gray, <span class="number">255</span>, cv2.ADAPTIVE_THRESH_MEAN_C, cv2.THRESH_BINARY_INV, <span class="number">25</span>, <span class="number">20</span>)</span><br><span class="line"><span class="comment"># 对图像中每一行进行求和</span></span><br><span class="line">horizontal_sum=np.sum(blur_img,axis=<span class="number">1</span>)</span><br><span class="line"><span class="comment"># 绘制图形</span></span><br><span class="line">plt.figure(<span class="string">'horizonal'</span>,dpi=<span class="number">100</span>)</span><br><span class="line">grid = plt.GridSpec(<span class="number">4</span>, <span class="number">4</span>, wspace=<span class="number">0.5</span>, hspace=<span class="number">0.5</span>)</span><br><span class="line"></span><br><span class="line">plt.subplot(grid[<span class="number">0</span>:<span class="number">3</span>,<span class="number">1</span>:<span class="number">4</span>])</span><br><span class="line"><span class="comment"># 显示灰度图</span></span><br><span class="line">plt.imshow(blur_img,<span class="string">'gray'</span>)</span><br><span class="line"><span class="comment"># 显示垂直方向投影图</span></span><br><span class="line">plt.subplot(grid[<span class="number">0</span>:<span class="number">3</span>,<span class="number">0</span>])</span><br><span class="line">plt.plot(horizontal_sum,range(horizontal_sum.shape[<span class="number">0</span>]))</span><br><span class="line">plt.xlabel(<span class="string">'datasam'</span>)</span><br><span class="line">plt.ylabel(<span class="string">'ImageHeight'</span>)</span><br><span class="line">plt.gca().invert_yaxis()</span><br><span class="line">plt.savefig(<span class="string">'horizonal.png'</span>)</span><br></pre></td></tr></table></figure><p>从图中可以看出，对输入图像进行处理之后，图像的背景色为黑色，文字部分为白色，在图像数据中黑色对应的值为$0$，白色对应的值为$255$。对图像进行垂直方向投影，输入图像中的文字区域部分垂直投影之后得到的值较大，背景区域投影之后得到的值较小，通过对垂直投影之后的数据进行提取，就可以确定图像中存在多少行和每行在图片高度方向上的位置。</p><p>确定输入图像中的每行文字区域之后，接下来对每行的图像进行水平方向的投影，由此确定当前行中的每个文字所在的位置，处理方法是和竖直方向投影方法相同。</p><img src="/2019/03/13/深度学习-OCR-从零开始/text_split.png" title="对每个字符进行提取"><p>使用如上方法可以实现对输入图像中每个字符进行提取，也就是找到每个文字在输入图像中的位置，提取到单字符之后便可以对每个文字进行识别。</p><p>由于对文字的提取基于竖直方向投影和水平方向投影得到，因此提取到的字符依赖于算法中设定的阈值，该阈值用于区分每一行文字或每行中的每个文字。从图中可以看到，提取到的单字文字存在一定错误，这正是由于算法中设定的阈值造成的，这也是该方法的局限性，尤其是对于中文字体结构，在中文中存在很多左右结构的文字，比如“小”，“林”，“从”等文字，如果算法中设定的阈值不合适，那很有可能将左右结构的一个字提取城两个字。</p><p>除了上述的方法之外，还有基于连通区域的方法，其假设字符本身是具有连通性的，之后通过连通区域的检测方法好到文字的候选区域，感兴趣的读者可以参考论文<a href="http://pages.ucsd.edu/~ztu/publication/cvpr12_textdetection.pdf" target="_blank" rel="noopener">Detecting Texts of Arbitrary Orientations in Natural Images</a>；或者使用最大稳定极值区域的方法，使用该方法得到候选字符区域，并将这些区域看做是连通图的顶点，因为，同一行的文本通常具有相同的文字方向，颜色，字体等，所以可以将文本行的确定转化为连通图聚类的过程，通过该方法确定文本行，感兴趣的读者可以参考文献<a href="https://pdfs.semanticscholar.org/ba05/72bb8d90fef080dc5790545383507c0722ea.pdf" target="_blank" rel="noopener">Text Detection for Multi-Orientation Scene Images using Adaptive Clustering</a>。</p><p>对于上述的基于人工特征方法的OCR技术实现方案，都伴随着复杂的特征工程，不论是从文本图形的文字的连通性考虑或者从文字的字体等信息考虑，这些方法的适用性都相对狭小，只能处理相对单一类别的文本图像，此外提取的效果也不够鲁棒，容易受到输入图像中的背景信息影响，并且提取结果很容易受到人为设置的一些参数的影响。随着计算机硬件计算能力的大幅度提升，在2014年左右，出现了深度学习方法的OCR技术方案，OCR技术不论是从识别效果还是从OCR识别场景适用性都取得了巨大进步。</p><h4 id="深度学习OCR技术"><a href="#深度学习OCR技术" class="headerlink" title="深度学习OCR技术"></a>深度学习OCR技术</h4><p>正如上文所述的使用人工特征的方法在OCR识别过程中存在诸多问题，深度学习OCR技术以其适用性更广，人工特征更少，识别准确率更高的优点，目前已经成为OCR识别的主流应用技术。对于OCR识别过程，上文已经有所描述，过程中的难点，一个是版面分析，也就是找到输入图像的文本区域所在的位置，另一个是文本识别，也就是对找到的文本图像进行识别。这里所说的深度学习OCR技术，也是着重解决这两个问题。目前存在的深度学习OCR方案主要是分两种，一种是端到端实现OCR识别，也就是设计一个网络模型，实现文字区域检测和文本识别，另一种是进行对文本检测和文本识别都设计开发单独的网络模型以实现任务目标，下面进行分别说明。</p><h5 id="文本检测"><a href="#文本检测" class="headerlink" title="文本检测"></a>文本检测</h5><p>说到文本检测，也就是检测到输入图像的文本区域，得到文字区域的位置信息。目前主要有两种深度学习实现思路，一种是基于目标检测方法，一种是基于图像分割的方法，其中基于目标检测的方法是目前应用最广泛的方法。</p><p>目前来说，在文本检测中，以目标检测为baseline仍然是占据主流，因为这两个任务具有天然的相关性，文本检测可以看做是目标检测中的一个分支，因此通用的目标检测技术经过一定定制优化是可以直接移植到文本检测上的，本人不太推荐基于分割的技术方法，首先在数据标注上，分割显然比检测的标注难度提升好几个等级，再者就是分割算法相比于检测算法需要更多的参数量，需要更多的计算资源和更久的运算时间，在工业界不太推荐。</p><ul><li><p>基于目标检测的方法</p><ol><li><p>早期比较有代表性的深度学习OCR识别算法是由大名鼎鼎的VGG实验室开发的<a href="https://arxiv.org/abs/1412.1842" target="_blank" rel="noopener">Reading Text in the Wild with Convolutional Neural Networks</a>，该模型可以实现文本检测和文字识别的端到端OCR识别过程，模型中最具代表性的是文本检测的实现方法。</p><img src="/2019/03/13/深度学习-OCR-从零开始/ReadingText.png" title="检测方案"><ul><li>使用<a href="https://web.bii.a-star.edu.sg/~zhangxw/files/EdgeBoxes_ECCV2014.pdf" target="_blank" rel="noopener">EdgeBoxes:边缘框算法</a>生成大量的文本提议区域，该步骤要尽可能生成足够多的候选文本区域，要尽量能够更好的覆盖真实文本区域；</li><li>使用随机森林分类器对提议文本区域进行分类，过滤第一步生成的非文本提议区域；</li><li>使用目标检测模型<a href="https://arxiv.org/abs/1311.2524" target="_blank" rel="noopener">RCNN</a>中对文本提议框回归的计算方法，对过滤之后的文本提议框进行修正以接近真实文本区域范围；</li><li>最后使用一个文字识别算法进一步过滤非文本提议区域。</li></ul><p>该方法可以看做是在RCNN基础上的应用，在RCNN中使用随机搜索的方法生成了候选文本提议，在该算法中使用的则是边缘框算法来实现，该算法是早期比较有代表性的深度学习在OCR识别任务中的应用，在2014-2015年一直保持领先地位。</p></li><li><p>2016年中科院深圳先进技术研究院乔宇老师研究组，基于目标检测模型<a href="https://arxiv.org/abs/1506.01497" target="_blank" rel="noopener">FasterRcnn</a>开发了适用于文本行检测的深度学习模型<a href="https://arxiv.org/abs/1609.03605" target="_blank" rel="noopener">CTPN</a>。</p><img src="/2019/03/13/深度学习-OCR-从零开始/CTPN.png" title="CTPN"><ul><li><p>根据文本检测任务中文本区域的特点，设计了区别于常规目标检测任务的文本区域划分方案；</p></li><li><p>在VGG模型最后一层的特征图上进行AnchorBox计算，计算方法与FasterRcnn相同；</p></li><li>在计算过程中考虑上下信息，将每行的文本特征输入到双向LSTM中以便更好的判断文字区域的概率</li><li>判断文字的高度，起始位置和终止位置</li></ul><p>CTPN首次在场景文字检测中使用RNN方法，但是受限于CTPN的文本区域划分方案，该算法模型默认只能用于输入图像的水平文本检测。</p></li><li><p>同样是在FasterRcnn的基础上改进而来，<a href="https://arxiv.org/abs/1703.01086" target="_blank" rel="noopener">RRPN</a>对AnhocrBox进行了设计，在原FaterRcnn中RPN网络的AnhorBox的基础上加入了角度旋转信息，设计了RRPN网络用以生成带角度信息的文本区域提议，此外，为了适应RRPN，将原FasterRcnn中ROI进行改进设计了RROI，以实现可以计算处理任意方向的文本提议区域。</p><img src="/2019/03/13/深度学习-OCR-从零开始/RRPN.png" title="RRPN"></li><li><p>基于文本在图像中是具有不规则多边形的轮廓的角度思考，<a href="https://arxiv.org/abs/1703.01425" target="_blank" rel="noopener">DMPNet</a>将目标检测任务中目标区域为矩形框修改为多边形目标区域，在目标检测模型<a href="https://arxiv.org/abs/1512.02325" target="_blank" rel="noopener">SSD</a>的基础上进行整个模型的设计。</p><img src="/2019/03/13/深度学习-OCR-从零开始/DMPNet.png" title="DMPNet中Anchor生成"><p>如图是<code>DMPNet</code>中关于<code>Anchor</code>的的设计方案，在图$a$中，如果使用用于的目标检测的<code>Anchor</code>生成方法，得到的检测区域是蓝色虚线部分，因为在传统的<code>Anchor</code>生成过程中，先验的<code>Anchor</code>都是矩形形状，因此得到的检测框肯定也是矩形的，但是在街景<code>OCR</code>中，使用矩形的区域进行文本区域检测显然会引入更多的背景信息，对后续的文本识别带来干扰。在<code>DMPNet</code>中的<code>Anchor</code>设计使用的是图<code>C</code>这种方式，加入了旋转角度，这样的<code>Anchor</code>设计会检测到图$a$中红色虚线的区域，显然相比于传统目标检测得到的结果更优。</p></li></ol></li></ul><h5 id="文本识别"><a href="#文本识别" class="headerlink" title="文本识别"></a>文本识别</h5><p>检测之后肯定就是识别了，由于2019年中旬之后更换工作，没有太多关注OCR相关方面的发展，根据我曾经的浅薄的经验，使用何种文本识别技术基于我们使用了何种文本检测技术，尤其在工业界，我们的目的是输出图片中的信息，而不是检测的检测到文本区域。</p><p>前面已经说过了，目前的主流做法是基于目标检测的文本检测实现方案，检测到的一般情况是文本区域而不是单个的字，这个时候我们在进行文本识别的时候显然也要对文本区域进行识别，现在的<code>CNN</code>技术发展迅速，几乎都快到瓶颈了，这些我们先不管，我们只需要知道对于文本识别目前来讲基于<code>CNN</code>是一种非常可行的方案，不管是<code>CRNN</code>(将<code>CNN,RNN</code>联系起，模拟语音识别的方法建立前后文联系)，还是<code>CNN+CTC</code>的方案，都是将文本区域使用<code>CNN</code>进行特征提取，然后使用<code>NLP</code>中解决方案。</p><p>至于更传统的单字符识别目前应该没有人使用了，这里也就不过多介绍了。关于该部分的内容，如果想了解更多，我也写过针对<code>OCR</code>的一篇博客<a href="[http://xiaofengshi.com/2019/01/05/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-OCR_Overview/](http://xiaofengshi.com/2019/01/05/深度学习-OCR_Overview/">文章: 深度学习-OCR_Overview</a>)</p><h2 id="深度学习技术实现方案综述"><a href="#深度学习技术实现方案综述" class="headerlink" title="深度学习技术实现方案综述"></a>深度学习技术实现方案综述</h2><p>前文已经对深度学习在<code>OCR</code>上的应用有所涉及，目前的不论学术界还是工业界应该都已经抛弃了传统的直方图或单字符识别的研究和投入，但是，还是那句话，最后的算法是在满足需求的条件下尽可能节省算力，如果我们的场景是具有规定排版样式的场景，比如扫描合同，身份证，银行卡，发票单据等，这些使用传统方法实际上完全可以<code>cover</code>住，没有<code>low</code>的算法，只有<code>low</code>的开发者，这是近一年来在字节我最大的收获。</p><p>接下来说一下目前的主流解决方案，<code>OCR</code>目前来讲分为两个过程文本区域检测和文本识别，目前应该还没有<code>End2End</code>的解决方案。这两个过程主要就是上面所说的那样，文本检测一般是基于目标检测来实现，文本识别一般是不定长文本识别，因此更多的采用的是<code>NLP</code>中关于语音识别的相关方法。</p><h2 id="连续文本提议网络-CTPN-模型原理"><a href="#连续文本提议网络-CTPN-模型原理" class="headerlink" title="连续文本提议网络(CTPN)模型原理"></a>连续文本提议网络(CTPN)模型原理</h2><p>这一部分，我已经写作一篇博客了，可以看一下这里<a href="[http://xiaofengshi.com/2019/01/23/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-TextDetection/](http://xiaofengshi.com/2019/01/23/深度学习-TextDetection/">深度学习-TextDetection</a>)</p><h2 id="卷积循环神经网络-CRNN-模型原理"><a href="#卷积循环神经网络-CRNN-模型原理" class="headerlink" title="卷积循环神经网络(CRNN)模型原理"></a>卷积循环神经网络(CRNN)模型原理</h2><h2 id="常用卷积网络模型结构"><a href="#常用卷积网络模型结构" class="headerlink" title="常用卷积网络模型结构"></a>常用卷积网络模型结构</h2><h2 id="DenseNet和CTC解码原理及代码实现"><a href="#DenseNet和CTC解码原理及代码实现" class="headerlink" title="DenseNet和CTC解码原理及代码实现"></a>DenseNet和CTC解码原理及代码实现</h2><h2 id="深度学习的数据标准化原理及选择"><a href="#深度学习的数据标准化原理及选择" class="headerlink" title="深度学习的数据标准化原理及选择"></a>深度学习的数据标准化原理及选择</h2><h2 id="序列到序列算法模型原理"><a href="#序列到序列算法模型原理" class="headerlink" title="序列到序列算法模型原理"></a>序列到序列算法模型原理</h2><h2 id="不定长文字识别的编码器设计及代码实现"><a href="#不定长文字识别的编码器设计及代码实现" class="headerlink" title="不定长文字识别的编码器设计及代码实现"></a>不定长文字识别的编码器设计及代码实现</h2><h2 id="注意力机制原理详解"><a href="#注意力机制原理详解" class="headerlink" title="注意力机制原理详解"></a>注意力机制原理详解</h2><h2 id="解码器的贪婪搜索和集束搜索选择和实现"><a href="#解码器的贪婪搜索和集束搜索选择和实现" class="headerlink" title="解码器的贪婪搜索和集束搜索选择和实现"></a>解码器的贪婪搜索和集束搜索选择和实现</h2><h2 id="循环神经网的模型设计策略及代码实现"><a href="#循环神经网的模型设计策略及代码实现" class="headerlink" title="循环神经网的模型设计策略及代码实现"></a>循环神经网的模型设计策略及代码实现</h2><h2 id="搭建中文OCR任务pipeline"><a href="#搭建中文OCR任务pipeline" class="headerlink" title="搭建中文OCR任务pipeline"></a>搭建中文OCR任务pipeline</h2><h2 id="技术总结及触类旁通"><a href="#技术总结及触类旁通" class="headerlink" title="技术总结及触类旁通"></a>技术总结及触类旁通</h2>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本篇详细说明了OCR的应用，技术实现，当前相关进展等，不定时补充更新。&lt;/p&gt;
&lt;p&gt;本篇博客最初想写成一本掘金小册，可是掘金的那边看了我的提纲说太难了，应该没什么受众，额……，那我就自己写一下记录下来，不会太讲究斟词酌句了，希望见谅。&lt;/p&gt;
&lt;p&gt;本篇博客算是一篇针对OCR相关学习或者初步接触者的引导和入门，讲的不深，实际上针对每个模块，比如&lt;code&gt;CTPN&lt;/code&gt;，&lt;code&gt;CTC&lt;/code&gt;，&lt;code&gt;Attention&lt;/code&gt;，&lt;code&gt;Densenet&lt;/code&gt;这些我都有专门写过博客来讲述过，感兴趣的可以翻翻我的博客文章。&lt;/p&gt;
&lt;p&gt;更新记录：&lt;/p&gt;
&lt;p&gt;2020.01.31: 自从19年4月份入职字节跳动之后一直忙于工作，很久没有更新个人博客了，今天看到各位的留言很是感动，为自己长久的拖延感到羞愧。由于武汉肺炎延迟开工，今天努力把以前写的提纲补充齐全，这一年来，相比于之前增加了更多的工业界算法思维，一个好的算法是在能够实现预定目的的同时，尽可能节省算力的，接下来也会把自己的一些感触和经验体现在博客里面。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="OCR" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/OCR/"/>
    
    
  </entry>
  
  <entry>
    <title>深度学习-Normalization</title>
    <link href="http://xiaofengshi.com/2019/03/06/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-Normalization/"/>
    <id>http://xiaofengshi.com/2019/03/06/深度学习-Normalization/</id>
    <published>2019-03-06T04:02:30.000Z</published>
    <updated>2020-01-19T03:18:36.000Z</updated>
    
    <content type="html"><![CDATA[<p>在深度学习领域，往往需要处理复杂的任务场景，一般使用较深层数的模型进行网络设计，这就涉及到复杂困难的模型调参：学习率的设置，权重初始化的设置以及激活函数的设置等。深度学习之所以难以训练，一个重要的原因在神经网络中层与层之间存在着极强的关联性，并且目前绝大部分使用的都是基于梯度下降的方法进行反向传播训练，当网络的底层发生微弱变化时，这些变化会随着层数的增加被放大，意味着对于高层网络要不断进行参数更新以适应底层参数的变化，如此导致了训练的困难，很容易会出现梯度爆炸或者梯度消失的情况，导致模型训练失败。</p><p>本文主要分析深度学习中的各种标准化方法(<strong>BN,LN,IN,GN,WN</strong>)的原理，来帮助实现对深度学习网络的训练。</p><img src="/2019/03/06/深度学习-Normalization/normalization.png" title="BN_LN_WN"><a id="more"></a><p>深度学习中的 <code>Normalization</code>，因为神经网络里主要有两类实体：神经元或者连接神经元的边，所以按照规范化操作涉及对象的不同可以分为两大类。</p><p>一类是对第 <code>L</code> 层每个神经元的激活值或者说对于第<code>L+1</code> 层网络神经元的输入值进行 Normalization操作，比如BatchNorm/LayerNorm/InstanceNorm/GroupNorm 等方法都属于这一类；</p><p>另外一类是对神经网络中连接相邻隐层神经元之间的边上的权重进行规范化操作，比如 Weight Norm 就属于这一类。</p><p>一般机器学习里看到的损失函数里面加入的对参数的的 L1/L2 等正则项，本质上也属于这第二类规范化操作。L1 正则的规范化目标是造成参数的稀疏化，就是争取达到<strong>让大量参数值取得 0 值的效果</strong>，而 L2 正则的规范化目标是有效减小原始参数值的大小。有了这些规范目标，通过具体的规范化手段来改变参数值，以达到避免模型过拟合的目的。</p><h2 id="BatchNormalization"><a href="#BatchNormalization" class="headerlink" title="BatchNormalization"></a>BatchNormalization</h2><p>按照BN论文所述，BN主要针对深度学习模型中的存在的ICS问题进行的设计。</p><h3 id="内部协变量转移ICS"><a href="#内部协变量转移ICS" class="headerlink" title="内部协变量转移ICS"></a>内部协变量转移ICS</h3><p><strong>在深度学习网络的训练过程中，由于网络中参数的变化而引起内部节点数据分布发生变化的过程称为内部协变量转移(Internal Covariate Shift-ICS)。</strong></p><p>正如开篇所述，在神经网络中，对于各个层之间的输出，由于在层内经过权重点乘和非线性激活的操作，每个层之间的输入信号的分布显然会发生变化，并且这种变化随着模型深度的增加会不断增大。</p><h3 id="ICS会造成的问题"><a href="#ICS会造成的问题" class="headerlink" title="ICS会造成的问题"></a>ICS会造成的问题</h3><ul><li>随着网络深度的增加，高层的网络参数要不断适应新的数据分布，会使训练周期变长；</li><li>前向传播时，如果使用sigmoid或者tanh激活函数，随着深度的增加，高层的网络参数受到底层参数的影响，会使得在进行梯度反向求导时，高层的网络输出落入激活函数的饱和区，也就是梯度消失。</li></ul><h3 id="如何缓解ICS问题"><a href="#如何缓解ICS问题" class="headerlink" title="如何缓解ICS问题"></a>如何缓解ICS问题</h3><p>针对ICS带来的问题和形成的原因（网络深度增加，层与层之间的数据分布发生改变，并且改变不断累积），一般有两种解决方法</p><ul><li>使用非饱和的激活函数，例如使用Relu或者Elu等激活函数，可以一定程度解决梯度消失的问题</li><li>让神经网络层与层之间输入保持在一个稳定的分布中，也就是数据Normalization处理</li></ul><h3 id="白化whitening"><a href="#白化whitening" class="headerlink" title="白化whitening"></a>白化whitening</h3><p>在机器学习中一般使用的PCA或者ZCA白化方法，对输入的数据分布进行变换。主要实现以下的数据转换</p><ul><li><p>输入数据具有相同的均值和方差</p><p>PCA控制所有的特征分布均值为0，方差为1</p><p>ZCA控制所有的特征分布均值为0，方差相同</p></li><li><p>去除特征的相关性</p></li></ul><p>但是直接对数据进行白化处理会存在两个问题</p><ul><li>计算成本过高，在每次训练的时候对神经网络每一层都进行白化操作</li><li>白化改变了数据分布，会改变网络层中输入数据本身的表达能力，底层网络学习到的参数信息会被白化操作丢失了</li></ul><p>针对白化存在的问题，提出了<code>BN</code>，一方面是简化计算过程，降低计算开销；另一方面是<strong>经过批规范化使输出的数据尽量保留原始的数据特征</strong>，可以认为BN也是一种白化操作方法，相比白化更简单，并且<strong>设置了学习参数，使得BN可以一定程度保留原始的数据特征</strong>。</p><p><code>Batch</code>的含义是，<code>BN</code>操作针对一个<code>MiniBatch</code>的数据样本，<code>Normalization</code>是指数据标准化。</p><p><code>BN</code>中对每个<code>MiniBatch</code>中的样本的每个特征进行单独的标准化处理，<strong>处理之后每个特征变成均值为0，标准化为$1$的分布</strong>；并且在白化的基础上在增加一个线性变换，使输出的数据能够尽量恢复原本输入数据的表达能力。</p><h3 id="MLP训练过程"><a href="#MLP训练过程" class="headerlink" title="MLP训练过程"></a>MLP训练过程</h3><h4 id="前向传播"><a href="#前向传播" class="headerlink" title="前向传播"></a>前向传播</h4><p>在训练过程中，使用<code>MiniBatch</code>进行网络训练</p><img src="/2019/03/06/深度学习-Normalization/bn_1.png" title="Fig1. BN"><p>如图1所示为MLP神经网络中的某一层，当前层的节点数量为4，也就是当前层的输入的数据特征数量为4，也就是4行；在当前MiniBatch中包含m个样本，也就是m列。对于BN来说，针对的是一个MiniBatch上的每个特征上单独进行的标准化处理，处理方式就是针对每个特征，计算当前MiniBatch中的该特征的均值和方差，然后对原始数据进行缩放和平移标准化处理。</p><p>对于MLP中的计算过程如下</p><img src="/2019/03/06/深度学习-Normalization/BN_alg1.jpg" title="Fig2. BN Algorithm 1"><p>输入：每个MiniBatch的数据$\mathcal{B}=\left\{x_{1 \dots m}\right\}$，样本数量为$m$</p><p>学习参数：线性变换参数$\gamma, \beta$</p><p>算法步骤为：</p><ul><li><p>在当前MiniBatch上计算每个特征的平均值$\mu_{\mathcal{B}}$和方差$\sigma_{\mathcal{B}}^{2}$</p><script type="math/tex; mode=display">\begin{array}{l}{\mu_{\mathcal{B}} \leftarrow \frac{1}{m} \sum_{i=1}^{m} x_{i}} \\ {\sigma_{\mathcal{B}}^{2} \leftarrow \frac{1}{m} \sum_{i=1}^{m}\left(x_{i}-\mu_{\mathcal{B}}\right)^{2}}\end{array}</script><p>计算当前MiniBatch中所有样本的每个特征的均值和方差，也就是在特征的维度上计算均值和方差，统计的范围是当前MiniBatch中的样本</p></li><li><p>根据每个特征的均值和方差，对输入数据进行规范化处理得到$\widehat{x}_{i}$</p><script type="math/tex; mode=display">\widehat{x}_{i} \leftarrow \frac{x_{i}-\mu_{\mathcal{B}}}{\sqrt{\sigma_{\mathcal{B}}^{2}+\epsilon}}</script><p>其中参数$\epsilon$是一个很小的常数，是为了防止原数据的方差为$0$。</p><p>经过该处理，意味着对<strong>原始数据进行规范化处理，使得当前层的输入的每个特征的分布的均值为$0$，方差为$1$</strong>。即满足关系式$\sum_{i=1}^{m} \widehat{x}_{i}=0$和$\frac{1}{m} \sum_{i=1}^{m} \widehat{x}_{i}^{2}=1$。这样会使得数据分布变得统一并且稳定，但是这同样导致了数据表达能力的缺失，<strong>为了使得规范化的数据尽量保留原始数据的信息，在<code>BN</code>中引入两个可学习参数，对标准化的数据进行线性变换</strong></p></li><li><p>对规范化的数据进行缩放和平移处理得到批规范化处理后的数据</p><script type="math/tex; mode=display">y_{i} \leftarrow \gamma \widehat{x}_{i}+\beta \equiv \mathrm{B} \mathrm{N}_{\gamma, \beta}\left(x_{i}\right)</script><p>经过上述操作，如果可学习参数满足关系$\gamma=\sigma_{\mathcal{B}}^{2}, \beta=\mu_{\mathcal{B}}$，这相当于对数据进行了反向变换$y_{i}=x_{i}$，保留了原始输入的数据信息。</p></li></ul><p>如上就是训练阶段<code>BN</code>层前向传播的计算过程。</p><h4 id="反向传播"><a href="#反向传播" class="headerlink" title="反向传播"></a>反向传播</h4><p>在<code>BN</code>层，学习参数为$\beta,\gamma$</p><p>根据上述的前向过程，使用目标函数对<code>BN</code>层参数进行更新。</p><script type="math/tex; mode=display">\begin{aligned} \frac{\partial \ell}{\partial \hat{x}_{i}} &=\frac{\partial \ell}{\partial y_{i}} \cdot \gamma \\ \frac{\partial \ell}{\partial \sigma_{\mathcal{B}}^{2}} &=\sum_{i=1}^{m} \frac{\partial \ell}{\partial \hat{x}_{i}} \cdot\left(x_{i}-\mu_{\mathcal{B}}\right) \cdot \frac{-1}{2}\left(\sigma_{\mathcal{B}}^{2}+\epsilon\right)^{-3 / 2} \\ \frac{\partial \ell}{\partial \mu_{\mathcal{B}}} &=\left(\sum_{i=1}^{m} \frac{\partial \ell}{\partial \hat{x}_{i}} \cdot \frac{-1}{\sqrt{\sigma_{\mathcal{B}}^{2}+\epsilon}}\right)+\frac{\partial \ell}{\partial \sigma_{\mathcal{B}}^{2}} \cdot \frac{\sum_{i=1}^{m}-2\left(x_{i}-\mu_{\mathcal{B}}\right)}{m} \\\frac{\partial \ell}{\partial x_{i}} &=\frac{\partial \ell}{\partial \widehat{x}_{i}} \cdot \frac{1}{\sqrt{\sigma_{\mathcal{B}}^{2}+\epsilon}}+\frac{\partial \ell}{\partial \sigma_{\mathcal{B}}^{2}} \cdot \frac{2\left(x_{i}-\mu_{\mathcal{B}}\right)}{m}+\frac{\partial \ell}{\partial \mu_{\mathcal{B}}} \cdot \frac{1}{m} \\ \frac{\partial \ell}{\partial \gamma} &=\sum_{i=1}^{m} \frac{\partial \ell}{\partial y_{i}} \cdot \widehat{x}_{i} \\ \frac{\partial \ell}{\partial \beta} &=\sum_{i=1}^{m} \frac{\partial \ell}{\partial y_{i}} \end{aligned}</script><p>通过链式求导进行参数更新。</p><h3 id="测试过程"><a href="#测试过程" class="headerlink" title="测试过程"></a>测试过程</h3><p>正如上面训练过程部分的计算，训练的时候按照每个<code>Batch</code>进行数据训练和参数更新，但是在测试的时候，每次只输入一个数据而不像训练时候使用的是一个Batch的数据，并且测试阶段使用的是测试阶段固定的参数，不涉及参数更新，因此<code>BN</code>的计算和训练阶段有所不同。</p><p>在训练阶段针对每个<code>BN</code>层固定并保留了每个<code>MiniBatch</code>数据特征的均值和方差$\mu_{batch},\sigma_{batch}^{2}$，并计算均值和方差的无偏估计</p><script type="math/tex; mode=display">\begin{aligned} \mu_{t e s t} &=\mathbb{E}\left(\mu_{b a t c h}\right) \\ \sigma_{t e s t}^{2} &=\frac{m}{m-1} \mathbb{E}\left(\sigma_{b a t c h}^{2}\right) \end{aligned}</script><p>根据这个无偏估计对测试数据在<code>BN</code>阶段进行标准化</p><script type="math/tex; mode=display">B N\left(X_{t e s t}\right)=\gamma \cdot \frac{X_{t e s t}-\mu_{t e s t}}{\sqrt{\sigma_{\text {test}}^{2}+\epsilon}}+\beta</script><p>其中参数$\beta,\gamma$是网络模型训练阶段学习到的参数。</p><p>上述为<code>BN</code>在MLP的计算过程</p><img src="/2019/03/06/深度学习-Normalization/bn_mlp.png" title="Fig3. BN for MLP 统计范围为batch中所有样本的同一个特征"><h3 id="CNN中BN的应用"><a href="#CNN中BN的应用" class="headerlink" title="CNN中BN的应用"></a>CNN中BN的应用</h3><img src="/2019/03/06/深度学习-Normalization/bn_cnn.gif" title="Fig4. CNN"><p>如图所示为卷积计算过程，只考虑单个样本，输入数据为$7 \times7 \times 3$，使用两个$3\times3$的卷积核，步长为2进行计算，得到的输出维度为$3\times 3 \times 2$。</p><p><strong>和MLP方式不同，对于卷积中<code>BN</code>的应用特别说明。</strong></p><p>在<code>CNN</code>中，以二维卷积为例，输入的数据维度为$NHWC$，表示数据样本数量为$N$，特征图的高度为$H$，宽度为$W$，上一层卷积核的数量为$C$。那么对于<code>BN</code>在$C$的维度上进行特征的标准化处理，$C$可以看做卷积对上一层进行了$C$次卷积计算，得到了$C$个特征，在这个$C$个特征中，每个特征对应的样本数量为$N$，并且每个样本的位置为$H \times W$，这意味着对于每个特征，该特征包含的数据的数量为$N\times H\times W$。</p><img src="/2019/03/06/深度学习-Normalization/batchnormal.png" title="Fig5. Batch Normalization for CNN"><h4 id="训练过程"><a href="#训练过程" class="headerlink" title="训练过程"></a>训练过程</h4><h5 id="前向传播-1"><a href="#前向传播-1" class="headerlink" title="前向传播"></a>前向传播</h5><p>在训练时，以<code>MiniBatch</code>为最小单位进行迭代</p><ul><li><p>输入：待进入激活函数的变量<code>NHWC</code></p></li><li><p>学习参数：$\gamma, \beta$</p></li><li><p>算法过程</p><p>整体算法按照论文所述如下</p></li></ul><img src="/2019/03/06/深度学习-Normalization/BN_alg2.png" title="Fig5. BN Algorithm2"><p>在训练阶段，在图中所示的行$2$中$K$对应输入变量的特征，也就是$K=C$，对每一个特征进行<code>BN</code>计算，由此需要循环计算BN，在每次循环中，按照图中所示方法进行<code>MiniBatch</code>计算，计算方法和MLP的方式相同，也就是说在MLP的基础上，CNN会在卷积核数量上进行BN的循环计算。</p><h4 id="测试过程-1"><a href="#测试过程-1" class="headerlink" title="测试过程"></a>测试过程</h4><p>在上图中同样描述了在Inference阶段BN的计算，在C维度上，进行BN的Inference计算。</p><h3 id="BN的作用"><a href="#BN的作用" class="headerlink" title="BN的作用"></a>BN的作用</h3><ul><li><p><strong>BN使网络中每层的数据分布相对稳定，加速模型的学习速度</strong></p><p>BN会使得每层的数据的均值和方差控制在一定的范围内，上层网络不必不断去适应底层网络中的输入的变化，实现了层与层之间的解耦，允许每一层进行独立学习，会提高神经网络的学习速度。</p></li><li><p><strong>BN使模型对网络中参数不太敏感，简化调参过程，学习更稳定</strong></p><p>在深度学习网络中，调参艰难且复杂，学习率过大的话，参数更新的步伐过大，容易出现震荡和不收敛，使用BN可以使得网络不收到参数数值大小的影响。</p><p>如果参数为$W$,对其进行缩放为$aW$，输入数据为$x$，那么原来的值为$\mu_1$，方差为$\sigma_{1}^{2}$，对于缩放之后的值，假设均值为$\mu_2$，方差为$\sigma_2^2$，则存在关系式$\mu_{2}=a \mu_{1}  ，\sigma_{2}^{2}=a^{2} \sigma_{1}^{2}$</p><p>对数据进行BN处理，忽略公式中的参数$\epsilon$</p><script type="math/tex; mode=display">\begin{align}&B N(a W x)=\gamma \cdot \frac{a W x-\mu_{2}}{\sqrt{\sigma_{2}^{2}}}+\beta=\gamma \cdot \frac{a W x-a \mu_{1}}{\sqrt{a^{2} \sigma_{1}^{2}}}+\beta\\&B N(W x)=\gamma \cdot \frac{W x-\mu_{1}}{\sqrt{\sigma_{1}^{2}}}+\beta\end{align}</script><p>也就是经过BN处理后，数据$aWx$和$Wx$是相同的。</p><p>对参数进行更新</p><script type="math/tex; mode=display">\frac{\partial B N((a W) x)}{\partial x}=\gamma \cdot \frac{a W}{\sqrt{\sigma_{2}^{2}}}=\gamma \cdot \frac{a W}{\sqrt{a^{2} \sigma_{1}^{2}}}=\frac{\partial B N(W x)}{\partial x}</script><p>可以看到权重的缩放不会影响到对$x$的梯度计算</p><script type="math/tex; mode=display">\frac{\partial B N((a W) u)}{\partial(a W)}=\gamma \cdot \frac{u}{\sqrt{\sigma_{2}^{2}}}=\gamma \cdot \frac{u}{a \sqrt{\sigma_{1}^{2}}}=\frac{1}{a} \cdot \frac{\partial B N(W u)}{\partial W}</script><p>BN之后对参数的更新，可以得到如果权重越大，也就是$a$越大，$\frac{1}{a}$越小，<strong>这意味着权重$W$的梯度更小，如此BN就保证了梯度不会依赖于参数的缩放，使得参数的更新处在更加稳定的状态。</strong></p><p><strong>如此，BN抑制了参数变化会随着网络深度增加被放大的问题，使得网络对参数大小的适应能力更好。</strong></p></li><li><p><strong>BN允许网络使用饱和性激活函数，缓解梯度消失</strong></p><p>正如ICS带来的问题，随着网络的深度和复杂性增加，底层网络的变化会累积到高层网络中，会导致模型的训练了很容易进入到激活函数的梯度饱和区；通过BN处理，数据都变成期望为0方差为1的高斯分布，使得激活函数的数据数据更容易落在激活函数的梯度非饱和区，并且通过学习参数$\gamma, \beta$可以使得BN之后的数据对原始数据的信息得以保留。</p></li><li><p><strong>BN具有一定正则化效果</strong></p><p>每一个batch的数据的均值和方差会有所不同，这为网络学习过程中增加了随机噪音，这个Droupout通过随机舍弃神经元为网络带来噪音的方式相似，在一定程度上对模型起到了正则化的效果。</p></li></ul><h3 id="BN的思考"><a href="#BN的思考" class="headerlink" title="BN的思考"></a>BN的思考</h3><ul><li>BN以每个MiniBatch对每个特征进行规范化处理，这要求每个MiniBatch的统计量是整体统计量的近似估计，这就意味着每个MiniBatch和整体数据应该是近似同分布的，如此，如果Minibatch比较小的话，那么不同Batch之间的数据分布差异性会更大；</li><li>分布差距较小的MiniBatch的数据可以认为规范化处理给模型带来了噪声，可以增加模型的泛化能力；如果每个MiniBatch的数据分布差距较大，那么不同的MiniBatch的数据将会进行不同的数据变换，着会增加模型的训练难度；</li><li>BN适用于每个MiniBatch的数据分布差距不大的情况，并且训练数据要进行充分的shuffle，不然效果反而变差，增大训练难度；</li><li>BN不适用于动态的网络结构和RNN网络。</li><li>BN由于基于MiniBatch的归一化统计量来代替全局统计量，相当于在梯度计算中引入了噪声，因此一般不适用与生成模型，强化学习等对噪声敏感的网络中。</li></ul><h2 id="LayerNormalization"><a href="#LayerNormalization" class="headerlink" title="LayerNormalization"></a>LayerNormalization</h2><p>正如上述BN由于使用的是MiniBatch的统计量，因此无法处理RNN网络结构。对于RNN网络结构来说，当前时间步输出的改变和下一个时间的输入高度相关，为了减小ICS的影响，对每层的神经元的输入数据进行归一化处理。</p><p>在某一个时间步，假设当前的数据维度为$(B,Hidden_{num})$，在$Hidden_{num}$维度上进行标准化，和BN不同的是，BN计算Batch中所有样本的每个特征的标准化，LN分别对单个样本进行计算，对每个样本的所有特征进行标准化。计算方式相比BN更加简单。</p><p>LN在MLP，CNN和RNN的应用如下所示</p><img src="/2019/03/06/深度学习-Normalization/layernormal.png" title="Fig6. Layer Normalization for MLP, 统计范围为当个样本当前隐藏层所有节点"><img src="/2019/03/06/深度学习-Normalization/ln_cnn.png" title="Fig7. LN for CNN 统计范围为每个样本所有通道的所有神经元"><img src="/2019/03/06/深度学习-Normalization/ln_rnn.png" title="Fig8. LN for RNN 统计范围为每个样本当前时间步隐藏层所有节点"><p><strong>LN一般只用于RNN的场景下，在CNN中LN规范化效果不如BN,WN,GN,IN的</strong></p><h3 id="计算方式"><a href="#计算方式" class="headerlink" title="计算方式"></a>计算方式</h3><p>计算当前层的参数的平均值和方差</p><script type="math/tex; mode=display">\mu^{l}=\frac{1}{H} \sum_{i=1}^{H} a_{i}^{l} \quad (\sigma^{l})^2=\frac{1}{H} \sum_{i=1}^{H}\left(a_{i}^{l}-\mu^{l}\right)^{2}</script><p>其中$H$对应着当前层神经元的数量，也就是<code>RNN</code>中隐藏层节点数，计算所有隐藏层节点的均值和方差，然后使用整个隐藏层的均值和方差来对当前隐藏层进行标准化。</p><script type="math/tex; mode=display">\widehat{x}_{i}=\frac{x_i-\mu ^l}{\sqrt {\sigma^l +\epsilon }}</script><p>标准化之后经过缩放和平移得到LN之后的输出输出</p><script type="math/tex; mode=display">LN(x)=\gamma \widehat{x}_{i} +\beta</script><p>参数$\beta,\gamma$是学习参数<br>和BN类似，规范化后的数据进行缩放和平移，以保留原始数据的信息。</p><h3 id="LN思考以及和BN的区别"><a href="#LN思考以及和BN的区别" class="headerlink" title="LN思考以及和BN的区别"></a>LN思考以及和BN的区别</h3><ul><li>LN针对当个训练样本进行规范化，不像BN那样依赖于一个MiniBatch的数据，因此避免了不同样本之间的相互影响，可以适用于动态网络场景，RNN和自然语言处理领域</li><li>LN不需要保存MiniBatch的均值和方差，相比BN计算量更小</li><li>BN针对单个神经元进行规范化，不同神经元的输入经过平移和缩放使不同神经元的分布在不同的区间中，LN对于整个一层的神经元进行转换，所有的输入全部控制在同一个区间范围内。在CNN中，假设输入的是一张图片，同一层的输入特征表征着不同的特征，例如有的位置神经元表征的是颜色信息，有的位置神经元表征的是形状信息，那么这个时候BN是更合适的，使不同的特征信息进行分别处理，但是如果这种输入使用LN处理，将一个样本的所有特征的分布全部标准化到一个统一的区间，显然会降低模型的表达能力。</li></ul><h3 id="LN-有效的原因研究"><a href="#LN-有效的原因研究" class="headerlink" title="LN 有效的原因研究"></a>LN 有效的原因研究</h3><p>​    在NLP中，尤其是在BERT等各种变体中，目前基本都是使用LN归一化，那么为什么LN是有效的呢？2019年关注了论文“Understanding and Improving Layer Normalization”，按照论文所属，均值跟方差对梯度的正则化才是层归一化有效的原因，而不是之前广泛认识的前向向量分布稳定性，另外根据论文所述，层归一化的参数部分增加了模型过拟合的风险，引入的参数在很多任务中都带来了效果的下降。</p><p>根据链式求导法则</p><script type="math/tex; mode=display">\frac{\partial \ell}{\partial \mathbf{x}} \leftarrow \frac{d \mathbf{y}}{d \mathbf{x}} \frac{\partial \ell}{\partial \mathbf{y}}</script><p>其中$\ell$是模型的损失，$\mathbf{y}$对应着经过LN标准化之后的向量，$\mathbf{x}$对应着原始数据输入，根据上面部分LN的推导，存在关系式</p><script type="math/tex; mode=display">\mathbf{y}=\frac{\mathbf{x}-\hat{\mu}}{\hat{\sigma}}, \quad \hat{\mu}=\theta(\mu), \quad \hat{\sigma}=\theta(\sigma)</script><p>对于原始输入，未经过LN正则化，损失函数对输入变量$\mathbf{x}$的梯度</p><script type="math/tex; mode=display">\frac{\partial \ell}{\partial x}=\left(a_{1}, a_{2}, \dots, a_{H}\right)^{T}</script><p>$\bar{a}$和$D_{a}$是梯度的均值和方差</p><p>如果经过LN正则化，也就是</p><script type="math/tex; mode=display">\mathbf{y}=LN(\mathbf{x})=\frac{\mathbf{x}-\hat{\mu}}{\hat{\sigma}}</script><p>那么损失函数$\ell$对$\mathbf {y}$的梯度为</p><script type="math/tex; mode=display">\frac{\partial \ell}{\partial y}=\left(g_{1}, g_{2}, \dots, g_{H}\right)^{T}</script><p>对应的均值和方差为$\bar{g}$和$D_{g}$</p><p>那么满足关系</p><script type="math/tex; mode=display">\bar{a}=\bar{g} / \sigma, \quad  D_{a}=D_{g} /\left(\sigma^{2}\right)</script><p>如此，LN实现了对梯度的缩放和平移变换</p><h2 id="Inastance-Normalization（IN）"><a href="#Inastance-Normalization（IN）" class="headerlink" title="Inastance Normalization（IN）"></a>Inastance Normalization（IN）</h2><p><strong>InstanceNormalization主要是针对CNN设计，并且是为了弥补BN的对batch的依赖。</strong></p><p>LN去放弃了BN对batch的依赖，为了能够统计均值和方差，把同层内所有神经元的响应值作为统计范围。为了进一步缩小统计范围，减少噪声的输入，对于CNN，同一个卷积层内，每一个卷积核都会产生一个输出通道，每个输出通道对应一个二维平面，尺寸都为$H\times W$，也包含多个激活神经元。在卷积层中来说，将单个样本的每个输出通道内的神经元会作为一个集合来统计均值和方差。使用该二维平面统计得到的均值和方差，对该二维平面内的元素进行标准化。</p><img src="/2019/03/06/深度学习-Normalization/InstanceNormal.png" title="Instance Normalization for CNN"><p>MLP和RNN无法使用InstanceNormalization，可以将InstanceNormalization看做Btahc=1的BN。</p><p><strong>InstanceNormalization对于图像生成任务效果明显优于BN，在图像分类上不如BN。</strong></p><h2 id="GroupNormalization-GN"><a href="#GroupNormalization-GN" class="headerlink" title="GroupNormalization(GN)"></a>GroupNormalization(GN)</h2><p>对于LN使用同层所有神经元作为统计范围，用于RNN中。IN将CNN中同一卷积层中每个卷积核对应的输出通道单独作为统计范围。虽然LN用于RNN中，IN用于CNN中，但是可看出这是两种计算情况，LN使用当前层的所有神经元，IN使用当前的单个featuremap，设计的GN的统计范围是介于两者之间，对CNN的在输出通道上进行分组，在分组范围内进行统计。</p><img src="/2019/03/06/深度学习-Normalization/groupnormalization.png" title="Group Normalization for CNN"><p><strong>GN在要求batch比较小的场景下或者目标检测，视频分类等任务由于BN</strong></p><h2 id="WeightNormalization"><a href="#WeightNormalization" class="headerlink" title="WeightNormalization"></a>WeightNormalization</h2><p>目前没有API的方式可以实现权重归一化，对于前面的BN和LN，都是将输入的数据进行规范化，WN是从权重的角度进行规范化，WN将权值向量$\mathbf{w}$在欧式范数和其方向上进行解耦成参数向量$\mathbf{v}$和参数标量$g$，在训练时使用SGD分别优化这两个参数。</p><p>WN和BN不同，与样本数量无关，可以用于batchsize较小和RNN等动态场景中；并且WN不会在梯度计算中像BN那样引入噪声。</p><h3 id="WN训练方法"><a href="#WN训练方法" class="headerlink" title="WN训练方法"></a>WN训练方法</h3><p>在神经网络中，一个节点的计算可以表示为</p><script type="math/tex; mode=display">y=\phi(\mathbf{w} \cdot \mathbf{x}+b)</script><p>其中$\mathbf{w}$是一个k维的特征向量，$y$是该神经元节点的输出，输出是一个标量。在计算得到损失之后会根据损失使用SGD的优化策略对参数$\mathbf{w}$和$b$进行更新。</p><p>WN的归一化策略将$\mathbf{w}$分解</p><script type="math/tex; mode=display">\mathbf{w}=\frac{g}{\|\mathbf{v}\|} \mathbf{v}</script><p>其中$|\mathbf{v}|$表示$\mathbf{v}$的欧氏范数，当$\mathbf{v}=\mathbf{w}$并且$g=|\mathbf{w}||$时，WN会被还原为普通的计算方式。</p><p>如果将参数$g$固定为$|\mathbf{w}|$时，只对参数$\mathbf{v}$进行优化，相当于只优化$\mathbf{w}$的方向，并保留其范数的大小；如果固定参数$\mathbf{v}=\mathbf{w}$，只对参数$g$进行优化 ，相当于只优化$\mathbf{w}$的范数，并且保留其方向。</p><p>在优化$g$时，一般优化$g$的$\log$参数$s$来实现，即$g=e^{s}$</p><p>参数更新方式</p><script type="math/tex; mode=display">\nabla_{g} L=\frac{\nabla_{\mathbf{w}} L \cdot \mathbf{v}}{\|\mathbf{v}\|} \quad \nabla_{\mathbf{v}} L=\frac{g}{\|\mathbf{v}\|} \nabla_{\mathbf{w}} L-\frac{g \nabla_{g} L}{\|\mathbf{v}\|^{2}} \mathbf{v}</script><p>其中$L$是损失函数值，$\nabla_{\mathbf{w}} L=\frac{\partial L}{\partial \mathbf{w} }$表示损失函数$L$对参数$\mathbf{w}$求偏导。</p><p>对$\nabla_{\mathbf{v}} L$进行整理可以得到</p><script type="math/tex; mode=display">\begin{array}{l}{\nabla_{\mathbf{v}} L=\frac{g}{\|\mathbf{v}\|} \nabla_{\mathbf{w}} L-\frac{g \nabla_{g} L}{\|\mathbf{v}\|^{2}} \mathbf{v}} \\ {=\frac{g}{\|\mathbf{v}\|} \nabla_{\mathbf{w}} L-\frac{g}{\|\mathbf{v}\|^{2}} \frac{\nabla_{\mathbf{w}} L \cdot \mathbf{v}}{\|\mathbf{v}\|} \mathbf{v}} \\ {=\frac{g}{\|\mathbf{v}\|}\left(I-\frac{\mathbf{v} \mathbf{v}^{\prime}}{\|\mathbf{v}\|^{2}}\right) \nabla_{\mathbf{w}} L} \\ {=\frac{g}{\|\mathbf{v}\|}\left(I-\frac{\mathbf{w} \mathbf{w}^{\prime}}{\|\mathbf{w}\|^{2}}\right) \nabla_{\mathbf{w}} L} \\ {=\frac{g}{\|\mathbf{v}\|} M_{\mathbf{w}} \nabla_{\mathbf{w}} L}\end{array}</script><p>上述转换可以看出WN的两个重要特征</p><ul><li><p>$\frac{g}{|\mathbf{v}|}$表明WN对权重梯度进行了$\frac{g}{|\mathbf{v}|}$的缩放</p></li><li><p>$M_{\mathrm{w}} \nabla_{\mathrm{w}} L$表明WN将梯度投影到远离$ \nabla_{\mathbf{w}} L$的方向</p><p>观察式子$M_{\mathrm{w}}=I-\frac{\mathbf{w} \mathbf{w}^{\prime}}{|\mathbf{w}|^{2}}$，可以得到$\mathbf{w}$与$M_{\mathrm{w}}$垂直，所以$\nabla_{\mathbf{v}} L$会非常接近参数$ \nabla_{\mathbf{w}} L$的垂直方向，如此对矫正梯度更新方向非常有效。</p><p>$\mathbf{v}$和梯度更新值中的噪声量成正比，而且$\mathbf{v}$是和更新量成反比，所以当更新值中噪音较多时，更新值就会变小，也就是WN具有自稳定的作用。由此在训练的时候可以使用较大的学习率。</p></li></ul><h3 id="WN-amp-BN的关系"><a href="#WN-amp-BN的关系" class="headerlink" title="WN&amp;BN的关系"></a>WN&amp;BN的关系</h3><p>假设$t=\mathbf{v} \mathbf{x}$，$\mu[t]$和$\sigma[t]$分别为$t$的期望和方差，如果使用BN的方式进行标准化</p><script type="math/tex; mode=display">t^{\prime}=\frac{t-\mu[t]}{\sigma[t]}=\frac{\mathbf{v}}{\sigma[t]} \mathbf{x}-\frac{\mu[t]}{\sigma[t]}</script><p>当网络只有一层并且输入样本服从期望为0，方差为1的独立分布时，有$\mu[t]=0$并且$\sigma[t]=|\mathbf{v}|$，此时BN和WN是等价的。</p><h3 id="Mean-Only-Batch-Normalization"><a href="#Mean-Only-Batch-Normalization" class="headerlink" title="Mean-Only Batch Normalization"></a>Mean-Only Batch Normalization</h3><p>在每一层的layer的激活函数之前</p><script type="math/tex; mode=display">t=\boldsymbol{w} \cdot \boldsymbol{x}=\frac{g}{\|\boldsymbol{v}\|} \boldsymbol{v} \cdot x</script><p>虽然将权重$w$进行了分离，但是每一层激活函数之前的输出的均值仍然与$v$有关。将BN和WN进行结合，采用移动平均计算每个minibatch的均值$\mu[t]$，由此</p><script type="math/tex; mode=display">\begin{array}{c}{t=\boldsymbol{w} \boldsymbol{x}} \\ {\hat{t}=\boldsymbol{\mu}[t]+b} \\ {y=\phi(\hat{t})}\end{array}</script><p>在激活函数之前的$t$的反向传播的损失函数为</p><script type="math/tex; mode=display">\nabla_{t} L=\nabla_{\hat{t}} L-\mu\left[\nabla_{\hat{t}} L\right]</script><p>这相当于只进行减去均值的BN，舍弃了BN中的除去方差的操作，这样处理就避免了BN中除去方差带来的额外噪声。</p><h3 id="WN-相比BN的优势"><a href="#WN-相比BN的优势" class="headerlink" title="WN 相比BN的优势"></a>WN 相比BN的优势</h3><ul><li>WN对权重W进行规范化，没有引入batch的依赖，适用于RNN网络，BN不能用于RNN，这是因为RNN是变长的，RNN是基于timestep进行计算，如果直接使用BN，需要保存每个time step下batch的均值和方差，效率很低</li><li>BN基于batch进行均值和放计算，而不是基于整个Training Set来计算，这相当于在梯度计算的时候引入了噪声。BN不适合对噪声敏感的强化学习，生成模型。WN通过对标量g和向量V对权重W进行规范化，重写向量v是固定的。</li><li>不需要额外空间保存batch的均值和方差，在实现WN时，对训练过程中的正向传递和反向计算的计算开销更小。</li></ul><h2 id="Normalization为什么有效"><a href="#Normalization为什么有效" class="headerlink" title="Normalization为什么有效"></a>Normalization为什么有效</h2><ul><li><p>权重伸缩不变性</p><p>回忆上面BN,LN的计算方式，当权重进行缩放使，得到规范化之后的值是不变的</p><script type="math/tex; mode=display">\begin{array}{c}{\operatorname{Norm}\left(\mathbf{W}^{\prime} \mathbf{x}\right)=\operatorname{Norm}\left(\mathbf{g} \cdot \frac{\mathbf{W}^{\prime} \mathbf{x}-\mu^{\prime}}{\sigma^{\prime}}+\mathbf{b}\right)} \\ {=\operatorname{Norm}\left(\mathbf{g} \cdot \frac{\lambda \mathbf{W} \mathbf{x}-\lambda \mu}{\lambda \sigma}+\mathbf{b}\right)} \\ {=\operatorname{Norm}\left(\mathbf{g} \cdot \frac{\mathbf{W} \mathbf{x}-\mu}{\sigma}+\mathbf{b}\right)=N o r m\left(\mathbf{W}{\mathbf{x}}\right)}\end{array}</script><p>这是因为当权重缩放时，对应的均值和方差都会等比例缩放，最终分子分母相抵消。</p><p>这个性质带来的好处</p><ul><li><p>提高反向传播的效率</p><script type="math/tex; mode=display">\frac{\partial N o r m\left(\mathbf{W}^{\prime} \mathbf{x}\right)}{\partial \mathbf{x}}=\frac{\partial N o r m(\mathbf{W} \mathbf{x})}{\partial \mathbf{x}}</script><p>避免了反向传播时会因为梯度过大或者过小导致出现梯度爆炸或消失的问题</p></li><li><p>具有参数正则化的效果，可以使用更高的学习率</p><script type="math/tex; mode=display">\frac{\partial B N((a W) u)}{\partial(a W)}=\gamma \cdot \frac{u}{\sqrt{\sigma_{2}^{2}}}=\gamma \cdot \frac{u}{a \sqrt{\sigma_{1}^{2}}}=\frac{1}{a} \cdot \frac{\partial B N(W u)}{\partial W}</script><p>下层的权重值越大，其梯度就会越小，如此，参数的变化就会越稳定，这相当于实现了参数正则化的效果，避免参数的大幅震荡，提高了网络的泛化能力。</p></li></ul></li><li><p>数据伸缩不变性</p><p>当数据x按照常量进行伸缩时，得到的规范化后的值保持不变</p><script type="math/tex; mode=display">\operatorname{Norm}\left(\mathbf{W} \mathbf{x}^{\prime}\right)=\operatorname{Norm}(\mathbf{W} \mathbf{x})</script><p>其中$\mathbf{x}^{\prime}=\lambda \mathbf{x}$</p><p><strong>数据伸缩不变性仅对BN,LN,CN有效，这三种规范化方式，都是对输入的数据进行规范化，当数据进行缩放时，对应的均值和方差都会变化。</strong></p><p><strong>数据伸缩不变性可以有效的减少梯度弥散，减少对学习率的选择</strong></p></li></ul><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul><li><a href="https://arxiv.org/abs/1502.03167" target="_blank" rel="noopener">Batch Normalization: Accelerating Deep Network Training by Reducing Internal Covariate Shift</a></li><li><a href="https://arxiv.org/abs/1805.11604" target="_blank" rel="noopener">How Does Batch Normalization Help Optimization?</a></li><li><a href="http://www.cvmart.net/community/article/detail/368" target="_blank" rel="noopener">深度学习中的Normalization模型</a><ul><li><a href="https://www.jiqizhixin.com/articles/2018-08-29-7" target="_blank" rel="noopener">机器之心：深度学习的Normalization</a></li></ul></li><li><a href="https://zhuanlan.zhihu.com/p/33173246" target="_blank" rel="noopener">详解深度学习中的Normalization，BN/LN/WN</a></li><li><a href="https://zhuanlan.zhihu.com/p/34879333" target="_blank" rel="noopener">Batch Normalization原理与实战</a></li><li><a href="https://zhuanlan.zhihu.com/p/55102378" target="_blank" rel="noopener">知乎：WN</a></li><li><a href="http://mlexplained.com/2018/01/13/weight-normalization-and-layer-normalization-explained-normalization-in-deep-learning-part-2/" target="_blank" rel="noopener">Blog: BN,WN,LN</a></li><li><a href="http://skyhigh233.com/blog/2017/07/21/norm/" target="_blank" rel="noopener">加速网络收敛——BN、LN、WN与selu</a></li><li><a href="https://cloud.tencent.com/developer/article/1052868" target="_blank" rel="noopener">Batchnorm原理详解</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在深度学习领域，往往需要处理复杂的任务场景，一般使用较深层数的模型进行网络设计，这就涉及到复杂困难的模型调参：学习率的设置，权重初始化的设置以及激活函数的设置等。深度学习之所以难以训练，一个重要的原因在神经网络中层与层之间存在着极强的关联性，并且目前绝大部分使用的都是基于梯度下降的方法进行反向传播训练，当网络的底层发生微弱变化时，这些变化会随着层数的增加被放大，意味着对于高层网络要不断进行参数更新以适应底层参数的变化，如此导致了训练的困难，很容易会出现梯度爆炸或者梯度消失的情况，导致模型训练失败。&lt;/p&gt;
&lt;p&gt;本文主要分析深度学习中的各种标准化方法(&lt;strong&gt;BN,LN,IN,GN,WN&lt;/strong&gt;)的原理，来帮助实现对深度学习网络的训练。&lt;/p&gt;
&lt;img src=&quot;/2019/03/06/深度学习-Normalization/normalization.png&quot; title=&quot;BN_LN_WN&quot;&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="Normalization" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/Normalization/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="Normalization" scheme="http://xiaofengshi.com/tags/Normalization/"/>
    
  </entry>
  
  <entry>
    <title>机器学习-朴素贝叶斯</title>
    <link href="http://xiaofengshi.com/2019/02/25/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0-%E6%9C%B4%E7%B4%A0%E8%B4%9D%E5%8F%B6%E6%96%AF/"/>
    <id>http://xiaofengshi.com/2019/02/25/机器学习-朴素贝叶斯/</id>
    <published>2019-02-25T07:22:57.000Z</published>
    <updated>2019-02-26T03:29:14.000Z</updated>
    
    <content type="html"><![CDATA[<p>朴素贝叶斯从是基于贝叶斯定理和条件独立性假设的一种分类器，是一种基于概率统计的分类方法。对于给定的数据，基于条件独立性假设学习输入和输出的联合概率分布，然后根据给定的输入数据利用贝叶斯定理求解最大的后验概率的的输出类别，是一种非常常见的分类算法。</p><p>对于贝叶斯定理，实际上就是求解条件概率的方法</p><script type="math/tex; mode=display">P(A | B)=\frac{P(B | A)P(A)}{P(B)}</script><p>$P(A)​$就是先验概率，是事件A发生之前事件B发生的概率</p><p>$P(A|B)$是后验概率，是事件B发生以后发生事件A的概率，也是条件概率</p><a id="more"></a><h2 id="概率论相关知识"><a href="#概率论相关知识" class="headerlink" title="概率论相关知识"></a>概率论相关知识</h2><p>$P(A)​$事件A发生的概率</p><p>$P(B)​$事件B发生的概率</p><p>$P(A,B)​$事件AB发生的联合的概率</p><p>$P(A|B)​$事件B发生的情况下事件A发生的概率</p><p>引入一个例子，我们要进行这样一个任务，输入一个句子，输出该句子属于赌博类句子的概率，只考虑两种情况，输入的句子为赌博或者正常。</p><blockquote><p>P(A) -&gt; A 事件发生的概率，例如明天天晴的概率</p><p>P(A|B) -&gt; 条件概率，B 事件发生的前提下 A 事件发生的概率，例如明天天晴而我又没带伞的概率</p><p>P(输入句子) -&gt; 这个句子在训练数据中出现的概率</p><p>P(赌博) -&gt; 赌博类别的句子在训练数据中出现的概率</p><p>P(赌博 | 输入句子) -&gt; 输入句子是赌博类别的概率（也是我们最终要求的值）</p><p>P(赌博 | 输入句子) + P(正常 | 输入句子) = 100%</p></blockquote><img src="/2019/02/25/机器学习-朴素贝叶斯/Probability_distributions.png" title="Probability_distributions"><h2 id="朴素贝叶斯分类器的推导"><a href="#朴素贝叶斯分类器的推导" class="headerlink" title="朴素贝叶斯分类器的推导"></a>朴素贝叶斯分类器的推导</h2><h3 id="贝叶斯定理及分类器优化目标"><a href="#贝叶斯定理及分类器优化目标" class="headerlink" title="贝叶斯定理及分类器优化目标"></a>贝叶斯定理及分类器优化目标</h3><p>根据条件概率的计算方法$P(A|B)=P(AB)/P(B)$，可以得到$P(AB)=P(A|B)P(B)=P(B|A)P(A)$，由此得到贝叶斯定理公式</p><script type="math/tex; mode=display">P(A | B)=\frac{P(B | A)P(A)}{P(B)}</script><p>带入到例子中，由此可以得到</p><p><strong>P(赌博|输入句子)=P(输入句子|赌博)*P(赌博)/P(输入句子)</strong></p><p>这就是我们要求的结果，输入一个句子，输出该句子属于赌博的概率。</p><ul><li><p>P(赌博)</p><p>赌博类别在整个样本集中的所占的百分比，<strong>这是先验概率</strong>，直接可以从训练集中得到</p></li><li><p>P(输入句子)</p><p>当前输入的句子出现在整个样本集中的概率。我们最终会想要得到的是输入的句子属于某一类别的概率，在当前的例子中，类别分为两类，赌博和正常。</p><p>P(赌博 | 输入句子) = P(输入句子 | 赌博) * P(赌博) / P(输入句子) </p><p>P(正常 | 输入句子) = P(输入句子 | 正常) * P(正常) / P(输入句子) </p><p>两个例子的分母都是p(输入句子)，那么可以认为这一项不会对整个类别预测产生影响，因此在计算的时候是可以省略的。</p><p>由此，可以得到<strong>P(赌博|输入句子)=P(输入句子|赌博)*P(赌博)</strong></p></li><li><p>P(输入句子|赌博)</p><p><strong>表示的是当前输入的句子属于赌博这一类别的概率</strong>，这一项是很难直接从数据集中求解的，因为输入的句子是多变的，不一定会正好出现在数据集中，在预测的时候如果遇到不存在训练集中的数据，那么就会使得P(输入句子|赌博)=0，这显然不是我们想要的结果。</p><p>将输入的句子分解成不同的词语来进行联合分析，把当前的输入句子当成不同词语的集合</p><p><strong>P(输入句子 | 赌博) = (P(词语 1) <em> P(词语 2 | 词语 1) </em> P(词语 3 | 词语 2))| 赌博) ≈ P(词语 1)|P(赌博) <em> P(词语 2)|P(赌博) </em> P(词语 3)|P(赌博)</strong></p><p>将P(输入句子 | 赌博) 分解成所有 P(词语 | 赌博) 概率的乘积，<strong>这里利用了条件独立性假设，用于分类的特征在类别确定的条件下都是条件独立的，公式如下所示：</strong></p><script type="math/tex; mode=display">\begin{aligned} P(X=x | Y=c_{k}) &=P\left(X^{(1)}=x^{(1)}, \cdots, X^{(n)}=x^{(n)} | Y=c_{k}\right) \\ &=\prod_{j=1}^{n} P\left(X^{(j)}=x^{(j)} | Y=c_{k}\right) \end{aligned}</script><p>然后通过训练数据，计算每个词语在不同类别出现的概率。最终获取的是输入句子有效词语在不同类别中的概率。</p></li></ul><p>根据上面结合例子分析可以得到我们要求解的目标为</p><script type="math/tex; mode=display">P\left(Y=c_{k} | X=x\right)=\arg \max _{c_{k}} P\left(Y=c_{k}\right) \prod_{j} P\left(X^{(j)}=x^{(j)} | Y=c_{k}\right)</script><p>也就是在输入输入数据为$x$的条件下输出最大概率的类别，$P\left(Y=c_{k} | X=x\right)​$称作<strong>后验概率</strong>，对应着朴素贝叶斯分类器的会将输入数据分类到后验概率最大的类别。</p><h3 id="最大化后验概率"><a href="#最大化后验概率" class="headerlink" title="最大化后验概率"></a>最大化后验概率</h3><p>根据上面所述，对于朴素贝叶斯算法，优化目标就是最大化后验概率。上面是从贝叶斯定理的角度进行推导得到，实际上也可以从最小期望风险的角度推导得到。</p><p>对于分类任务，假设是一个二分类，使用0-1损失函数</p><script type="math/tex; mode=display">L(Y, f(X))=\left\{\begin{array}{ll}{1,} & {Y \neq f(X)} \\ {0,} & {Y=f(X)}\end{array}\right.</script><p>$f(X)$对应着决策函数，此时的期望风险就是</p><script type="math/tex; mode=display">\begin{align}R_{\mathrm{exp}}(f)&=E[L(Y, f(X))] \\&=E_{X} \sum_{k=1}^{K}\left[L\left(c_{k}, f(X)\right)\right] P\left(c_{k} | X\right)\end{align}</script><p>为了最小化期望风险，对$X=x$进行逐个最小化，就可以得到最优的决策函数</p><script type="math/tex; mode=display">\begin{aligned} f(x) &=\arg \min _{y \in \mathcal{Y}} \sum_{k=1}^{K} L\left(c_{k}, y\right) P\left(c_{k} | X=x\right) \\&=\arg \min _{y \in \mathcal{Y}} [\sum_{k=1}^{K} P\left(y \neq c_{k} | X=x\right)+\sum_{k=1}^{K} P\left(y = c_{k} | X=x\right)] \\ &=\arg \min _{y \in \mathcal{Y}} [\sum_{k=1}^{K} P\left(y \neq c_{k} | X=x\right)+0] \\&=\arg \min _{y \in \mathcal{Y}}\left(1-P\left(y=c_{k} | X=x\right)\right) \\ &=\arg \max _{y \in \mathcal{Y}} P\left(y=c_{k} | X=x\right) \end{aligned}</script><p>如此，最优的决策函数和最大化后验概率是等价的。</p><h3 id="贝叶斯估计的参数求解"><a href="#贝叶斯估计的参数求解" class="headerlink" title="贝叶斯估计的参数求解"></a>贝叶斯估计的参数求解</h3><p>前文已经对整个过程进行了推导，贝叶斯估计的过程就是最大化后验概率</p><script type="math/tex; mode=display">P\left(Y=c_{k} | X=x\right)=\arg \max _{c_{k}} P\left(Y=c_{k}\right) \prod_{j} P\left(X^{(j)}=x^{(j)} | Y=c_{k}\right)</script><p>存在两个需要求解的参数，一个是先验概率</p><script type="math/tex; mode=display">P\left(Y=c_{k}\right)=\frac{\sum_{i=1}^{N} I\left(y_{i}=c_{k}\right)}{N}, k=1,2, \cdots, K</script><p>其中$I​$对应着决策函数，例如0-1决策</p><script type="math/tex; mode=display">I(x,y)=\left\{\begin{array}{ll}{0,} & {y \neq x} \\ {1,} & {y=x}\end{array}\right.</script><p>另一个是条件概率，设第j个特征$x^{(j)}$可能取值的集合为$\left\{a_{j 1}, a_{j 2}, \cdots, a_{j s_{j}}\right\}$，条件概率$P\left(X^{(j)}=a_{j l} | Y=c_{k}\right)$可以求解</p><script type="math/tex; mode=display">\begin{array}{c}{P\left(X^{(j)}=a_{j l} | Y=c_{k}\right)=\frac{\sum_{i=1}^{N} I\left(x_{i}^{(j)}=a_{j i} y_{i}=c_{k}\right)}{\sum_{i=1}^{N} I\left(y_{i}=c_{k}\right)}} \\ {j=1,2, \cdots, n ; \quad l=1,2, \cdots, S_{j} ; \quad k=1,2, \cdots, K}\end{array}</script><p>对于贝叶斯分类器，要跟深度学习进行区别，在传统机器学习中，贝叶斯估计，决策树，KNN算法是按照设定的分类策略进行类别的预测，这算是一种基于统计的方法。</p><h2 id="贝叶斯估计示例"><a href="#贝叶斯估计示例" class="headerlink" title="贝叶斯估计示例"></a>贝叶斯估计示例</h2><p>给定训练集，学习一个朴素贝叶斯分类器输出$x=(2, S)^{T}​$的类别，数据集如下所示(示例来源《统计学习方法》)</p><img src="/2019/02/25/机器学习-朴素贝叶斯/bayes_example.png" title="bayes_example"><p>数据集中每一列对应着样本的特征$X^{(1)}, X^{(2)}$，取值集合分别为$A_{1}=\{1,2,3\}, A_{2}=\{S, M, L\}$，$Y$为类别的标记，$Y \in C=\{1,-1\}​$</p><p>根据上述的贝叶斯估计的参数求解过程</p><script type="math/tex; mode=display">P\left(Y=c_{k}\right)=\frac{\sum_{i=1}^{N} I\left(y_{i}=c_{k}\right)}{N}, k=1,2, \cdots, K</script><p>根据给定的样例</p><script type="math/tex; mode=display">P(Y=1)=\frac{9}{15}, \quad P(Y=-1)=\frac{6}{15}</script><p>再结合条件概率的计算公式</p><script type="math/tex; mode=display">\begin{array}{c}{P\left(X^{(j)}=a_{j l} | Y=c_{k}\right)=\frac{\sum_{i=1}^{N} I\left(x_{i}^{(j)}=a_{j i} y_{i}=c_{k}\right)}{\sum_{i=1}^{N} I\left(y_{i}=c_{k}\right)}} \\ {j=1,2, \cdots, n ; \quad l=1,2, \cdots, S_{j} ; \quad k=1,2, \cdots, K}\end{array}</script><p>结合给定的样例</p><script type="math/tex; mode=display">\begin{array}{l}{P\left(X^{(1)}=1 | Y=1\right)=\frac{2}{9}, \quad P\left(X^{(1)}=2 | Y=1\right)=\frac{3}{9}, \quad P\left(X^{(1)}=3 | Y=1\right)=\frac{4}{9}} \\{P\left(X^{(2)}=S | Y=1\right)=\frac{1}{9}, \quad P\left(X^{(2)}=M | Y=1\right)=\frac{4}{9}, \quad P\left(X^{(2)}=L | Y=1\right)=\frac{4}{9}} \\ {P\left(X^{(1)}=1 | Y=-1\right)=\frac{3}{6}, \quad P\left(X^{(1)}=2\left|Y=-1)=\frac{2}{6}, \quad P\left(X^{(1)}=3 | Y=-1\right)=\frac{1}{6}\right.\right.} \\ {P\left(X^{(2)}=S | Y=-1\right)=\frac{3}{6}, \quad P\left(X^{(2)}=M | Y=-1\right)=\frac{2}{6}, \quad P\left(X^{(2)}=L | Y=-1\right)=\frac{1}{6}}\end{array}</script><p>可以看到对于每一行，概率之和都为1。</p><p>按照最大化后验概率</p><script type="math/tex; mode=display">P\left(Y=c_{k} | X=x\right)=\arg \max _{c_{k}} P\left(Y=c_{k}\right) \prod_{j} P\left(X^{(j)}=x^{(j)} | Y=c_{k}\right)</script><p>结合给定的样例，给定样本属于$-1$的概率</p><script type="math/tex; mode=display">P(Y=-1) P\left(X^{(1)}=2 | Y=-1\right) P\left(X^{(2)}=S | Y=-1\right)=\frac{6}{15} \cdot \frac{2}{6} \cdot \frac{3}{6}=\frac{1}{15}</script><p>给定样本属于$1$的概率</p><script type="math/tex; mode=display">P(Y=1) P\left(X^{(1)}=2 | Y=1\right) P\left(X^{(2)}=S | Y=1\right)=\frac{9}{15} \cdot \frac{3}{9} \cdot \frac{1}{9}=\frac{1}{45}</script><p>取最大的后验概率作为当前样本类别的输出，也就是$y=-1$。</p><h2 id="朴素贝叶斯处理离散数据"><a href="#朴素贝叶斯处理离散数据" class="headerlink" title="朴素贝叶斯处理离散数据"></a>朴素贝叶斯处理离散数据</h2><p>对于朴素贝叶斯，一般处理的是离散数据，就想上述例子所示，或者文本分类中将句子转化成词语，也是离散值。但是如果数据中给定的数据是一种连续值，有两种方法将数据转化为离散值，以便使用朴素贝叶斯分类器。</p><ul><li>设置阈值范围，将不同范围内的连续值划分为不同的类别</li><li>使用连续随机变量的概率密度函数，将数值转换为概率</li></ul><h3 id="概率密度函数，概率质量函数"><a href="#概率密度函数，概率质量函数" class="headerlink" title="概率密度函数，概率质量函数"></a>概率密度函数，概率质量函数</h3><p>概率分布（probability distribution）用来描述随机变量或一簇随机变量在每一个可能取到的状态的可能性大小。描述概率分布的方式取决于随机变量是离散的还是连续的。</p><ul><li><p>概率质量函数</p><p>对于离散型随机变量，使用概率质量函数(probability mass function, PMF)来描述，必须满足以下条件</p><ul><li>函数的定义域必须是随机变量所有可能状态的集合</li><li>满足条件$\forall x \in \mathrm{x}, 0 \leq P(x) \leq 1​$</li><li>归一化要求$\sum_{x \in \mathrm{x}} P(x)=1$</li></ul></li><li><p>概率密度函数</p><p>对于连续随机变量，使用概率密度函数(probability density function, PDF)来描述，必须满足一下条件</p><ul><li>函数的定义域必须是随机变量所有可能状态的集合</li><li>满足条件$\forall x \in \mathrm{x}, p(x) \geq 0$，并不要求$p(x) \leq 1$</li><li>归一化要求$\int p(x) d x=1$</li></ul></li></ul><h3 id="常用分布函数"><a href="#常用分布函数" class="headerlink" title="常用分布函数"></a>常用分布函数</h3><ul><li><p>Bernoulli分布</p><script type="math/tex; mode=display">\begin{array}{c}{P(\mathrm{x}=1)=\phi} \\ {P(\mathrm{x}=0)=1-\phi} \\ {P(\mathrm{x}=x)=\phi^{x}(1-\phi)^{1-x}} \\ {\mathbb{E}_{\mathrm{x}}[\mathrm{x}]=\phi} \\ {\operatorname{Var}_{\mathrm{x}}(\mathrm{x})=\phi(1-\phi)}\end{array}</script></li><li><p>高斯分布/正态分布</p><script type="math/tex; mode=display">\mathcal{N}\left(x ; \mu, \sigma^{2}\right)=\sqrt{\frac{1}{2 \pi \sigma^{2}}} \exp \left(-\frac{1}{2 \sigma^{2}}(x-\mu)^{2}\right)</script></li><li><p>指数分布</p><script type="math/tex; mode=display">p(x ; \lambda)=\lambda \mathbf{1}_{x \geq 0} \exp (-\lambda x)</script><p>$\mathbf{1}_{x \geq 0}$用来指示当$x&lt;0$时概率为零</p></li><li><p>Laplace分布</p><script type="math/tex; mode=display">\operatorname{Laplace}(x ; \mu, \gamma)=\frac{1}{2 \gamma} \exp \left(-\frac{|x-\mu|}{\gamma}\right)</script></li></ul><img src="/2019/02/25/机器学习-朴素贝叶斯/curves.png" title="概率分布曲线指数分布和laplace分布"><h3 id="高斯分布"><a href="#高斯分布" class="headerlink" title="高斯分布"></a>高斯分布</h3><p>承接上文，如果数据是连续的，那么使用概率密度函数，将数值转换为概率，高斯分布是在自然场景中最常用的分布，比如人的身高，学习成绩排名等等</p><script type="math/tex; mode=display">\mathcal{N}\left(x ; \mu, \sigma^{2}\right)=\sqrt{\frac{1}{2 \pi \sigma^{2}}} \exp \left(-\frac{1}{2 \sigma^{2}}(x-\mu)^{2}\right)</script><p>其中$\mu$为数据的期望，$\sigma$为数据的标准差。</p><p>可以先统计数据数据得到高斯分布的函数的参数，之后将要求解的概率带入概率密度函数中，计算其相对概率。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;朴素贝叶斯从是基于贝叶斯定理和条件独立性假设的一种分类器，是一种基于概率统计的分类方法。对于给定的数据，基于条件独立性假设学习输入和输出的联合概率分布，然后根据给定的输入数据利用贝叶斯定理求解最大的后验概率的的输出类别，是一种非常常见的分类算法。&lt;/p&gt;
&lt;p&gt;对于贝叶斯定理，实际上就是求解条件概率的方法&lt;/p&gt;
&lt;script type=&quot;math/tex; mode=display&quot;&gt;
P(A | B)=\frac{P(B | A)P(A)}{P(B)}&lt;/script&gt;&lt;p&gt;$P(A)​$就是先验概率，是事件A发生之前事件B发生的概率&lt;/p&gt;
&lt;p&gt;$P(A|B)$是后验概率，是事件B发生以后发生事件A的概率，也是条件概率&lt;/p&gt;
    
    </summary>
    
      <category term="机器学习" scheme="http://xiaofengshi.com/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="朴素贝叶斯" scheme="http://xiaofengshi.com/categories/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/%E6%9C%B4%E7%B4%A0%E8%B4%9D%E5%8F%B6%E6%96%AF/"/>
    
    
      <category term="机器学习" scheme="http://xiaofengshi.com/tags/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="朴素贝叶斯" scheme="http://xiaofengshi.com/tags/%E6%9C%B4%E7%B4%A0%E8%B4%9D%E5%8F%B6%E6%96%AF/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-对抗生成网络</title>
    <link href="http://xiaofengshi.com/2019/02/23/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-%E5%AF%B9%E6%8A%97%E7%94%9F%E6%88%90%E7%BD%91%E7%BB%9C/"/>
    <id>http://xiaofengshi.com/2019/02/23/深度学习-对抗生成网络/</id>
    <published>2019-02-23T12:23:04.000Z</published>
    <updated>2019-03-05T09:02:38.000Z</updated>
    
    <content type="html"><![CDATA[<p>GAN(Generative Adversary Networks)的思想是是一种二人零和博弈思想（two-player game），博弈双方的利益之和是一个常数，比如两个人掰手腕，假设总的空间是一定的，你的力气大一点，那你就得到的空间多一点，相应的我的空间就少一点，相反我力气大我就得到的多一点，但有一点是确定的就是，我两的总空间是一定的，这就是二人博弈，但是总利益是一定的。</p><p>引申到GAN里面就是可以看成，GAN中有两个这样的博弈者，一个人名字是生成模型(G)，另一个人名字是判别模型(D)。他们各自有各自的功能。</p><img src="/2019/02/23/深度学习-对抗生成网络/gans.jpg" title="Gan"><a id="more"></a><p>首先说明什么是生成模型：</p><ul><li>密度估计：在事先不了解事件概率分布的情况下，先假设随机分布，然后用过数据观测来确定真实的概率密度，就想极大似然估计。</li><li>样本生成：手上存在一个训练样本集，通过训练之后生成类似的样本</li></ul><p>对抗生成网络主要是解决生成类型的任务，比如风格迁移任务，将黑白图像生成彩色图像；修复破损的图片，生成超分辨率图像，性别转变，表情转变，发型转变等等。在对抗生成网络中包含生成器和判别器，生成器用于生成任务需要的数据，而判别器用于判断生成的样本是否为真，两个网络使用的是博弈论的思想，<strong>一般情况下，我们的目的是得到一个最优的生成器，判别器是用来辅助我们得到该生成器的手段。</strong></p><h2 id="生成网络基本原理"><a href="#生成网络基本原理" class="headerlink" title="生成网络基本原理"></a>生成网络基本原理</h2><p>生成网络主要针对生成类型的任务，对于最初始的Gan，输入一个噪声，模拟得到一个人的图像，并且希望这个图像足以以假乱真。针对这样的任务，设计一个生成器和一个判别器，使用生成器，输入噪声数据输出一个生成的图像，而判别器用来判断生成的图像是否是一张人像，判别器是一个二分类网络。</p><ul><li><p>判别网络的目的：就是能判别出来属于的一张图它是来自真实样本集还是假样本集。假如输入的是真样本，网络输出就接近1，输入的是假样本，网络输出接近0，那么很完美，达到了很好判别的目的。</p></li><li><p>生成网络的目的：生成网络是造样本的，它的目的就是使得自己造样本的能力尽可能强，强到什么程度呢，判别网络没法判断我是真样本还是假样本。</p></li></ul><p>通俗理解对抗生成网络就是，判别器会说，我很强，来一个样本我就会知道这个样本是真是假(来源于数据集的数据认为是真的，生成器生成的任务是假的)，但是生成器也很不服气，他也认为自己很强，我能生成一个假样本，虽然我知道是真的，但是你判别器判断不出来。Gan的目的就是通过这种博弈的思想得到最优的生成器。</p><p>总地来说，Goodfellow 等人提出来的 GAN 是通过对抗过程估计生成模型的新框架。在这种框架下，我们需要同时训练两个模型，即一个能捕获数据分布的生成模型G 和一个能估计数据来源于真实样本概率的判别模型D。生成器G的训练过程是最大化判别器犯错误的概率，即判别器误以为数据是真实样本而不是生成器生成的假样本。因此，这一框架就对应于两个参与者的极小极大博弈（minimax game）。在所有可能的函数G和D中，我们可以求出唯一均衡解，即G可以生成与训练样本相同的分布，而D判断的概率处处为 1/2，无法分辨数据是来源于真实样本还是生成器生成的数据，这一过程的推导与证明将在后文详细解释。</p><p>目前对于生成器G和判别器D使用的是神经网络的方式。为了学习到生成器在输入数据x上的分布$p_G$，对于生成器来说，最初的框架中输入的是噪声信号，假设分布为$p_z(t)$，然后根据$G(z;\theta_g)$将其映射到数据空间中，对于$G(z;\theta_g)$表示生成器所表征的函数变换。同理，定义判别器的函数为$D(s,\theta_d)$它的输出是单个标量，$D(x)$表示数据来源于真实数据而不是生成器生成的假数据的概率。我们训练D以最大化正确分配真实样本和生成样本的概率，因此我们就可以通过最小化$ log(1-D(G(z)))$而同时训练G，也就是说判别器D和生成器对价值函数V(G,D)进行了极小极大化博弈。</p><p>判别器D会判断数据是否为真实数据的概率，那么我们可以认为数据分为两类，一类是真实样本的数据$x$，另一类是生成器生成的数据$G(x)​$，对于一个最优的判别器来说，肯定是能够争取分辨出数据的真假，对于分类任务我们一般使用交叉熵作为优化目标，对于真实数据，判别器会给出1的评价，对于生成器的数据，判别器会给出0的评价，那么参考交叉熵的损失函数</p><blockquote><p>假设$y$表示真实类别为$(0,1)$，$a$为模型的预测值，那么交叉熵可以表述为</p><script type="math/tex; mode=display">Crossentropy(a)=-\frac{1}{n}\sum [yln(a)+(1-y)ln(1-a)]</script></blockquote><p>交叉熵表述从极大似然估计而来，表述的是预测值和真实值之间的差距，训练的过程是最小化损失函数的过程。</p><p>在生成对抗的网络框架中，对于判别器的训练，可以认为是真实数据的判别结果逼近于1，而生成数据的判别结果逼近于0，由此可以得到</p><script type="math/tex; mode=display">V ( G , D ) : = E _ { x \sim p _ { d a t a } ( x ) } \log ( D ( x ) ) + E _ { z \sim p _ { z } ( z ) } \log ( 1 - D ( G ( z ) ) )</script><p>式子中的$E_{p_{data}}$和$E_{P_{generate}}$表示的是$x​$期望，</p><p>期望的定义为，对于离散变量</p><script type="math/tex; mode=display">\mathbb{E}_{\mathrm{x} \sim P}[f(x)]=\sum_{x} P(x) f(x)</script><p>对于连续变量</p><script type="math/tex; mode=display">\mathbb{E}_{\mathbf{x} \sim p}[f(x)]=\int p(x) f(x) d x</script><p>期望的定义告诉我们上面的式子和交叉熵是等价的，对于对抗生成网络的判别器的优化目标就是最大化目标函数</p><script type="math/tex; mode=display">D _ { G } ^ { * } = \operatorname { argmax } _ { D } V ( G , D )</script><p>最优的判别器通过最大化目标函数得到。</p><p>当得到最优的判别器之后，来对生成器进行优化，生成器的目的是生成和真实数据相似的样本，相似到让判别器难分真假，对于上面的目标函数最小化，就可以得到最优的生成器。在目标函数$V(G,D)$的第二项，当$D(G(x))$接近于1时，表示判别器会判断$D(G(x))$为真，也就是得到了最优的生成器，此时的整个优化目标函数为0，即最小化优化目标得到最优的生成器</p><script type="math/tex; mode=display">G^{*}=argmin_G \:\: E_{p_{data}}\log D(x)+E_{P_{generate}}\log(1-D(G(x)))</script><p>进行联立，可以得到论文中的公式</p><script type="math/tex; mode=display">\min _ { G } \max _ { D } V ( D , G ) = \mathbb { E } _ { \boldsymbol { x } \sim p _ { \mathrm { data } } ( \boldsymbol { x } ) } [ \log D ( \boldsymbol { x } ) ] + \mathbb { E } _ { \boldsymbol { z } \sim p _ { \boldsymbol { z } } ( \boldsymbol { z } ) } [ \log ( 1 - D ( G ( \boldsymbol { z } ) ) ) ]</script><h2 id="原始GAN详细理论推导"><a href="#原始GAN详细理论推导" class="headerlink" title="原始GAN详细理论推导"></a>原始GAN详细理论推导</h2><h3 id="KL散度"><a href="#KL散度" class="headerlink" title="KL散度"></a>KL散度</h3><p>引用《深度学习》对KL散度的说明</p><blockquote><p>KL散度是信息论中的概念，定义一个数据的自信息$\mathrm{x}=x$为$I(x)=-\log P(x)$，单位为奈特。使用信息熵对整个概率分布中的不确定性进行量化，信息熵的定义为</p><script type="math/tex; mode=display">H(x)=- \mathbb { E } _ { \mathbf { x } \sim P } [ \log P ( x ) ]</script><p>KL散度衡量不同分布之间的差异</p><script type="math/tex; mode=display">D _ { \mathrm { KL } } ( P \| Q ) = \mathbb { E } _ { \mathrm { x } \sim P } \left[ \log \frac { P ( x ) } { Q ( x ) } \right] = \mathbb { E } _ { \mathrm { x } \sim P } [ \log P ( x ) - \log Q ( x ) ]</script><p>式子中的$P(x)$和$Q(x)$表示不同的分布。</p><p><strong>KL散度最重要的性质是它是非负的，当且仅当两个分布相等或者非常接近的时候对应的KL散度为0。这个性质可以用来在当做不同分布之间的距离。但是，KL散度不是严格对称的，对于某些P和Q分布存在$D_{-} K L(P | Q)  \neq D_{-} K L(Q| | P)$，因此KL不是真正的距离。</strong></p><p>和KL散度关系紧密的量是交叉熵，$H(P, Q)=-\mathbb{E}_{\mathrm{x} \sim P} \log Q(x)$和KL散度相比，交叉熵为最小化Q进行最小化，等价于最小化$D _ { \mathrm { KL } } ( P | Q )$，因为在计算交叉熵时Q并不会参与被省略的一项。</p></blockquote><p>KL散度可以从极大似然估计中推导而出。若给定一个样本数据的分布 $P_{data}(x) $和生成的数据分布$P_{G}(x;\theta)$，那么 GAN 希望能找到一组参数$\theta$使分布 $p_G(x;\theta)$ 和$ P_{data}(x)$ 之间的距离最短，也就是找到一组生成器参数而使得生成器能生成十分逼真的图片。</p><h3 id="极大似然估计"><a href="#极大似然估计" class="headerlink" title="极大似然估计"></a>极大似然估计</h3><p>从极大似然估计的角度来解释对抗生成网络的优化目标。</p><p>数据样本的分布为$P_{data}(x) ​$那么从该分布中抽取m个真实的样本$\{𝑥^1,𝑥^2,…,𝑥^𝑚\}​$，那么这m个真实的样本在生成数据分布$ P_G(x;\theta)​$中全部出现的概率为</p><script type="math/tex; mode=display">L = \prod _ { i = 1 } ^ { m } P _ { G } \left( x ^ { i } ; \theta \right)</script><p>上述的概率被称为似然函数。</p><p>如果生成数据分布$ P_G(x;\theta)$和数据样本的分布$P_{data}(x) $相同，那么真实的数据就有极大的可能性出现在生成数据分布中，也就是$m$个数据出现在生成数据分布中的概率非常大。这样，就可以通过极大化似然函数得到离真实数据分布最近的分布，也就是最优的参数$\theta​$，由此得到生成数据分布的参数</p><script type="math/tex; mode=display">\begin{aligned} \theta ^ { * } & = \arg \max _ { \theta } \prod _ { i = 1 } ^ { m } P _ { G } \left( x ^ { i } ; \theta \right) = \arg \max _ { \theta } \log \prod _ { i = 1 } ^ { m } P _ { G } \left( x ^ { i } ; \theta \right) \\ & = \arg \max _ { \theta } \sum _ { i = 1 } ^ { m } \log P _ { G } \left( x ^ { i } ; \theta \right) \quad \left\{ x ^ { 1 } , x ^ { 2 } , \ldots , x ^ { m } \right\} \text { from } P _ { d a t a } ( x ) \\ & \approx \arg \max _ { \theta } E _ { x \sim P _ { d a t a } } \left[ \log P _ { G } ( x ; \theta ) \right] \\\end{aligned}</script><p>上式就是极大似然的过程，将似然函数变为对数似然函数，这样原来似然函数中的连乘就变成了求和。对于期望的计算方式上文已经进行了说明 ，对于离散型随机变量，期望为$E(X)=\sum_{i}x_{i}p_{i}$，其中$p_i$为对应的概率，那么对于上述的极大似然的过程，可以将近似为求解期望，这就是求解交叉熵的过程。</p><p>因为上述的过程的目的是为了查找最优的参数$\theta$，因此可以对上述公式进一步推导</p><script type="math/tex; mode=display">\begin{aligned} \theta ^ { * } &=\arg \max _ { \theta } E _ { x \sim P _ { d a t a } } \left[ \log P _ { G } ( x ; \theta ) \right] \\& =\arg \max _{\theta} \int P_{d a t a}(x) \log P_{G}(x ; \theta) d x-\int P_{d a t a}(x) \log P_{d a t a}(x) d x \\&=\arg \min \int_{x} P_{d a t a}(x) \log \frac{P_{G}(x, \theta)}{P_{d a t a}(x)} dx\\&=\arg \min _{\theta} K L\left(P_{d a t a}(x)| | P_{G}(x ; \theta)\right) \end{aligned}</script><p>对上述的期望公式添加不包含$\theta$的一项，这一项的添加并不会改变最优$\theta$的求解，合并这两个积分就可以得到$KL​$的形式。</p><p>求取最优的参数$\theta$就可以使得生成数据分布$P _ { G } ( x ; \theta )$和数据样本$P_{d a t a}(x) $相似或接近，也就是令KL散度取得最小的参数$\theta$，在得到最优参数$\theta$时，就意味着生成器生成的数据和原始数据非常接近。</p><p>上述部分就从极大似然估计的角度对优化目标进行了分析，在深度学习领域，很多的优化目标都可以从极大似然估计的角度进行分析得到最终的优化目标。</p><h3 id="最优的判别器"><a href="#最优的判别器" class="headerlink" title="最优的判别器"></a>最优的判别器</h3><p>正如上文所述，判别器用来判断输入样本的真假，对于最优的判别器应该能够有效的判别出输入的样本为真或为假，我们认为来源于数据样本的数据为真，来源于生成器的生成数据为假。不论是从极大似然估计的角度还是从KL散度的角度，可以得到判别器的优化目标，最大化优化目标对应着判别器对样本分布$p_G​$和分布$p_{data}​$之间的差异或距离的评判能力。</p><p>按照原论文所述，价值函数可写成在$x$上的积分方式，对数学期望进行展开，可以得到</p><script type="math/tex; mode=display">V(G, D)=\int_{x} p_{\text { data }}(x) \log (D(x))+p_{G}(x) \log (1-D(x)) d x</script><p>求解积分的最大值可以转换为被积分函数的最大值，而求解被积分函数的最大值是为了得到最优判别器D，由此，不涉及判别器的项就可以看成是常数项，在被积分函数中$p_{ data }(x) \log (D(x))+p_{G}(x) \log (1-D(x))$，$p_{data}$和$p_G(x)$都可以看作是常数项，由此被积分函数可以看做</p><script type="math/tex; mode=display">p_{\text { data }}(x) \log (D(x))+p_{g}(x) \log (1-D(x))=a^{*} \log (D(x))+b^{*} \log (1-D(x))</script><p>对于式子中的常数项为<script type="math/tex">a^*=p_{data}(x)​</script>和$b^{*}=p_{G}(x)​$。</p><p>令式中$y=D(x)​$，可以得到</p><script type="math/tex; mode=display">f(y)=a^{*} \log y+b^{*} \log (1-y)</script><p>可以查看$f(y)​$的曲线</p><img src="/2019/02/23/深度学习-对抗生成网络/dis_curve.png" title="Dis_curve"><p>为了找到使得$f(y)​$得到最大值的y，对$f(y)​$求偏导，得到其极值点</p><script type="math/tex; mode=display">f^{\prime}(y)=0 \Rightarrow \frac{a}{y}-\frac{b}{1-y}=0 \Rightarrow y=\frac{a}{a+b}</script><p>由此可以得到最优的判别器为</p><script type="math/tex; mode=display">D(x)=\frac{P_{data}(x)}{P_{data}(x)+P_G(x)}</script><p>这是从理论上对最优解进行的推导，实际上该最优解是无法计算得到的，因为一开始我们并不知道数据样本分布$p_{data}(x)​$，但是该理论证明了最优的生成器$G​$是存在的，因此在实际的训练过程中我们只需要逼近最优的判别器D。</p><h3 id="最优的生成器"><a href="#最优的生成器" class="headerlink" title="最优的生成器"></a>最优的生成器</h3><p>对于对抗生成网络，目的就是得到最优的生成器，用来生成和数据样本数据相似的数据。这就意味着最优的生成器会满足</p><script type="math/tex; mode=display">p_{G}(x)=p_{data}(x)</script><p>如此，最优的判别器为</p><script type="math/tex; mode=display">D(x)=\frac{P_{data}(x)}{P_{data}(x)+P_G(x)} =\frac{1}{2}</script><p>这种情况意味着判别器已经无法分别何为真实的数据何为生成器生成的数据，对于来源于生成器的数据何数据样本的数据，判别器都会给出概率为$\frac{1}{2}​$，实际上这就体现了对抗生成网络中的极大极小博弈，<strong>当且仅当$p_{G}(x)=p_{data}(x)​$时，优化目标$V ( G , D )​$达到全局最优点。</strong></p><p>当$p_{G}(x)=p_{data}(x)​$时，就是使优化目标$V ( G , D ^{*})​$取得最小值的生成器，在这个时候$p_{G}(x)​$和$p_{data}(x)​$的JS散度为零。</p><p>将上一步得到的最优的判别器带入到优化目标中，进行整理可以得到如下公式条推导</p><script type="math/tex; mode=display">\begin{align}V(G, D^{*})&=\int_{x} p_{\text { data }}(x) \log (D(x))+p_{G}(x) \log (1-D(x)) d x \\&=\int_{x} p_{d a t a}(x) \log \left(\frac{p_{\text {data}}(x)}{p_{G}(x)+p_{\text {data}}(x)}\right)+p_{G}(x) \log \left(\frac{p_{G}(x)}{p_{G}(x)+p_{\text {data}}(x)}\right) \mathrm{d} x \\&=\int_{x}(\log 2-\log 2) p_{d a t a}(x)+p_{d a t a}(x) \log \left(\frac{p_{d a t a}(x)}{p_{G}(x)+p_{d a t a}(x)}\right) \\&\:\:\:\:\:+(\log 2-\log 2) p_{G}(x)+p_{G}(x) \log \left(\frac{p_{G}(x)}{p_{G}(x)+p_{d a t a}(x)}\right) \mathrm{d} x \\& =-\log 2[\int_{x}P_G(x)dx+\int_{x}p_{data}(x)dx]+\int_{x}p_{data}(x)[\log2+\log \left(\frac{p_{d a t a}(x)}{p_{G}(x)+p_{d a t a}(x)}\right) ]dx\\&\:\:\:\:\:+\int_{x}p_G(x)[\log2+\log \left(\frac{p_{G}(x)}{p_{G}(x)+p_{d a t a}(x)}\right)]dx \\&=-\log2(1+1)+\int_{x}p_{data}(x)\log \left(\frac{p_{d a t a}(x)}{[p_{G}(x)+p_{d a t a}(x)]/2}\right)dx\\&\:\:\:\:\:+\int_{x}p_{G}(x)\log \left(\frac{p_{G}(x)}{[p_{G}(x)+p_{d a t a}(x)]/2}\right)dx \\&=-\log 4+K L\left(p_{\text {data}} | \frac{p_{\text {data}}+p_{G}}{2}\right)+K L\left(p_{G} | \frac{p_{\text {data}}+p_{G}}{2}\right)\end{align}</script><p>上述的推导清晰的显示了在得到最优的判别器之后求解最优的生成器的过程。</p><p>因为KL散度是非负的，可以得到最优的生成器可以使优化目标取得的全局最优值为$-\log 4​$。接下来推导证明$p_{G}(x)=p_{data}(x)​$是优化目标取得最优解的唯一解，就可以证明，$p_{G}(x)=p_{data}(x)​$是优化目标取得最小值的充分必要条件。</p><blockquote><p>根据JS散度的定义，可以得到</p><script type="math/tex; mode=display">\begin{array}{r}{\mathrm{JSD}(P \| Q)=\frac{1}{2} KL(P \| M)+\frac{1}{2} KL(Q \| M)} \\  where \:\:\:{M=\frac{1}{2}(P+Q)}\end{array}</script><p>假设存在两个分布 P 和 Q，且这两个分布的平均分布 M=(P+Q)/2，那么这两个分布之间的 JS 散度为 P 与 M 之间的 KL 散度加上 Q 与 M 之间的 KL 散度再除以 2。</p><p>JS散度的值的范围为$0-\log 2$，如果两个分布完全没有关系，那么JS散度取得最大值$\log2$，如果两个分布完全相同，那么JS散度取得最大值0。</p></blockquote><p>根据JS散度的性质，可以对上述的最优目标进一步优化</p><script type="math/tex; mode=display">\begin{align}V(G, D^{*})&=-\log 4+K L\left(p_{\text {data}} | \frac{p_{\text {data}}+p_{G}}{2}\right)+K L\left(p_{G} | \frac{p_{\text {data}}+p_{G}}{2}\right)\\&=-\log4+2JS(p_{data}|p_G)\end{align}</script><p>当分布满足$p_{G}(x)=p_{data}(x)$的条件下$JS(p_{data}|p_G)=0$。</p><p>综上可以得到，当且仅当生成数据分布和数据样本分布相等时，可以得到最优的生成器。</p><h3 id="网络训练过程"><a href="#网络训练过程" class="headerlink" title="网络训练过程"></a>网络训练过程</h3><p>上述是从理论的角度进行最优生成器和最优判别器的推导，先求出最优判别器，再使用最优判别器计算最优的生成器。优化目标如下</p><script type="math/tex; mode=display">V(G, D)=E _ { x \sim p _ { d a t a } ( x ) } \log ( D ( x ) ) + E _ { z \sim p _ { z } ( z ) } \log ( 1 - D ( G ( z ) ) ) \\</script><p>这是在最开始的时候提出的优化目标，其中涉及两个求解期望的过程，在实际训练中使用SGD的方法，针对每个batch的数据，假设存在数据数量为m。</p><ul><li><p>从真实数据$p_{data}​$中采样$m​$个样本，$\left\{x^{1}, x^{2}, \ldots, x^{m}\right\}​$为正样本</p></li><li><p>从先验分布$p_{prior}$噪声样本中抽取$m$个噪声样本$\left\{z^{1}, z^{2}, \ldots, z^{m}\right\}$</p></li><li><p>将噪声样本输入生成器G中，生成数据$\left\{\tilde{x}^{1}, \widetilde{x}^{2}, \ldots, \tilde{x}^{m}\right\}, \widetilde{x}^{i}=G\left(z^{i}\right)$，通过最大化目标函数</p><script type="math/tex; mode=display">\tilde{V}=\frac{1}{m} \sum_{i=1}^{m} \log D\left(x^{i}\right)+\frac{1}{m} \sum_{i=1}^{m} \log \left(1-D\left(\tilde{x}^{i}\right)\right)</script><p>更新判别器D的参数<script type="math/tex">\theta_d</script>，参数的更新方式为$\theta_{d} \leftarrow \theta_{d}+\eta \nabla \widetilde{V}\left(\theta_{d}\right)$</p></li></ul><p>上述为判别器的更新方式，我们希望最大化目标函数</p><ul><li><p>从先验分布$p_{prior}$中采样个$m$噪声样本$\left\{\tilde{z}^{1}, \widetilde{z}^{2}, \ldots, \tilde{z}^{m}\right\}$</p></li><li><p>通过极小化目标函数</p><script type="math/tex; mode=display">\widetilde{V}=\frac{1}{m} \sum_{i=1}^{m} \log \left(1-D\left(G\left(z^{i}\right)\right)\right)</script><p>更新生成器G的参数$\theta_g$，生成器参数的更新方式为$\theta_{g} \leftarrow \theta_{g}-\eta \nabla \tilde{V}\left(\theta_{g}\right)$</p></li></ul><p>以上是生成器的训练过程，该过程 在一次迭代中只进行一次更新，可以避免更新太多使JS散度上升。</p><p><strong>NOTE</strong>:</p><ul><li><u><strong>在进行判别器参数更新时，因为是最大化优化目标，更新方式为</strong></u>$\theta_{d} \leftarrow \theta_{d}+\eta \nabla \widetilde{V}\left(\theta_{d}\right)​$</li><li><strong><u>在进行生成器参数更新时，因为是最小化优化目标，更新方式为</u></strong>$\theta_{g} \leftarrow \theta_{g}-\eta \nabla \tilde{V}\left(\theta_{g}\right)$</li><li><strong><u>判别器和生成器的更新方式：没对判别器更新一次接着对生成器更新一次，或者多判别器更新k次后对生成器更新一次。</u></strong></li></ul><h3 id="代码说明"><a href="#代码说明" class="headerlink" title="代码说明"></a>代码说明</h3><p>代码地址<a href="https://github.com/jiqizhixin/ML-Tutorial-Experiment" target="_blank" rel="noopener">https://github.com/jiqizhixin/ML-Tutorial-Experiment</a></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#下面在满足epoch条件下进行训练</span></span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> range(<span class="number">30</span>):</span><br><span class="line"></span><br><span class="line">    <span class="comment">#计算一个epoch所需要的迭代数量，即训练样本数除批量大小数的值取整；其中shape[0]就是读取矩阵第一维度的长度</span></span><br><span class="line">    print(<span class="string">"Number of batches"</span>, int(X_train.shape[<span class="number">0</span>]/BATCH_SIZE))</span><br><span class="line"></span><br><span class="line">    <span class="comment">#在一个epoch内进行迭代训练</span></span><br><span class="line">    <span class="keyword">for</span> index <span class="keyword">in</span> range(int(X_train.shape[<span class="number">0</span>]/BATCH_SIZE)):</span><br><span class="line"></span><br><span class="line">        <span class="comment">#随机生成的噪声服从均匀分布，且采样下界为-1、采样上界为1，输出BATCH_SIZE×100个样本；即抽取一个批量的随机样本</span></span><br><span class="line">        noise = np.random.uniform(<span class="number">-1</span>, <span class="number">1</span>, size=(BATCH_SIZE, <span class="number">100</span>))</span><br><span class="line"></span><br><span class="line">        <span class="comment">#抽取一个批量的真实图片</span></span><br><span class="line">        image_batch = X_train[index*BATCH_SIZE:(index+<span class="number">1</span>)*BATCH_SIZE]</span><br><span class="line"></span><br><span class="line">        <span class="comment">#生成的图片使用生成器对随机噪声进行推断；verbose为日志显示，0为不在标准输出流输出日志信息，1为输出进度条记录</span></span><br><span class="line">        <span class="comment"># 使用噪声数据生成图片,g为生成器</span></span><br><span class="line">        generated_images = g.predict(noise, verbose=<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment">#每经过100次迭代输出一张生成的图片</span></span><br><span class="line">        <span class="keyword">if</span> index % <span class="number">100</span> == <span class="number">0</span>:</span><br><span class="line">            image = combine_images(generated_images)</span><br><span class="line">            image = image*<span class="number">127.5</span>+<span class="number">127.5</span></span><br><span class="line">            Image.fromarray(image.astype(np.uint8)).save(</span><br><span class="line">                <span class="string">"./GAN/"</span>+str(epoch)+<span class="string">"_"</span>+str(index)+<span class="string">".png"</span>)</span><br><span class="line"></span><br><span class="line">            <span class="comment">#将真实的图片和生成的图片以多维数组的形式拼接在一起，真实图片在上，生成图片在下</span></span><br><span class="line">            X = np.concatenate((image_batch, generated_images))</span><br><span class="line"></span><br><span class="line">            <span class="comment">#生成图片真假标签，即一个包含两倍批量大小的列表；前一个批量大小都是1，代表真实图片，后一个批量大小都是0，代表伪造图片</span></span><br><span class="line">            y = [<span class="number">1</span>] * BATCH_SIZE + [<span class="number">0</span>] * BATCH_SIZE</span><br><span class="line"></span><br><span class="line">            <span class="comment">#判别器的损失；在一个batch的数据上进行一次参数更新</span></span><br><span class="line">            d_loss = d.train_on_batch(X, y)</span><br><span class="line">            print(<span class="string">"batch %d d_loss : %f"</span> % (index, d_loss))</span><br><span class="line"></span><br><span class="line">            <span class="comment">#随机生成的噪声服从均匀分布</span></span><br><span class="line">            noise = np.random.uniform(<span class="number">-1</span>, <span class="number">1</span>, (BATCH_SIZE, <span class="number">100</span>))</span><br><span class="line"></span><br><span class="line">            <span class="comment">#固定判别器，此时判别器参数被固定不再更新</span></span><br><span class="line">            d.trainable = <span class="keyword">False</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">#计算生成器损失；在一个batch的数据上进行一次参数更新</span></span><br><span class="line">            g_loss = d_on_g.train_on_batch(noise, [<span class="number">1</span>] * BATCH_SIZE)</span><br><span class="line"></span><br><span class="line">            <span class="comment">#令判别器可训练</span></span><br><span class="line">            d.trainable = <span class="keyword">True</span></span><br><span class="line">            print(<span class="string">"batch %d g_loss : %f"</span> % (index, g_loss))</span><br><span class="line"></span><br><span class="line">            <span class="comment">#每100次迭代保存一次生成器和判别器的权重</span></span><br><span class="line">            <span class="keyword">if</span> index % <span class="number">100</span> == <span class="number">9</span>:</span><br><span class="line">                g.save_weights(<span class="string">'generator'</span>, <span class="keyword">True</span>)</span><br><span class="line">                d.save_weights(<span class="string">'discriminator'</span>, <span class="keyword">True</span>)</span><br></pre></td></tr></table></figure><p>查看上述的代码可以看出，在每个Batch中，选择一个批次的数据和同样数量的噪声数据，认为真实数据标签为1，生成器生成的噪声数据标签为0，使用交叉熵损失来训练判别器；之后固定判别器参数，再次选择一个批次数量的噪声，此时认为生成器生成的噪声数据标签为1，同样通过交叉熵损失来更新生成器参数。由此反复，不断更新判别器和生成器的参数。</p><p>再借鉴DCGAN中的代码<a href="https://github.com/carpedm20/DCGAN-tensorflow" target="_blank" rel="noopener">https://github.com/carpedm20/DCGAN-tensorflow</a></p><p>生成网络结构为</p><img src="/2019/02/23/深度学习-对抗生成网络/DCGAN.png" title="DCGAN_Generator"><p>G的输入是一个100位的向量z，也就是噪声向量，首先经过全连接层，将100维的向量变成$4\times4\times1024$的向量，从第二层开始，使用反卷积进行上采样，增大尺寸并减小通道数量，最后得到的输出为$64\times64\times3$。</p><p>实现细节</p><ul><li>不使用池化层，在判别器中使用带有步长的卷积代替池化</li><li>在G,D中均使用BN防止过拟合</li><li>G中除了最后一层都是用relu激活函数，最后一层使用tanh</li><li>D中激活函数都是用Leaky Relu激活函数</li></ul><p>如下是判别器的代码</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">discriminator</span><span class="params">(self, image, y=None, reuse=False)</span>:</span></span><br><span class="line">    <span class="keyword">with</span> tf.variable_scope(<span class="string">"discriminator"</span>) <span class="keyword">as</span> scope:</span><br><span class="line">      <span class="keyword">if</span> reuse:</span><br><span class="line">        scope.reuse_variables()</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> <span class="keyword">not</span> self.y_dim:</span><br><span class="line">        h0 = lrelu(conv2d(image, self.df_dim, name=<span class="string">'d_h0_conv'</span>))</span><br><span class="line">        h1 = lrelu(self.d_bn1(conv2d(h0, self.df_dim*<span class="number">2</span>, name=<span class="string">'d_h1_conv'</span>)))</span><br><span class="line">        h2 = lrelu(self.d_bn2(conv2d(h1, self.df_dim*<span class="number">4</span>, name=<span class="string">'d_h2_conv'</span>)))</span><br><span class="line">        h3 = lrelu(self.d_bn3(conv2d(h2, self.df_dim*<span class="number">8</span>, name=<span class="string">'d_h3_conv'</span>)))</span><br><span class="line">        h4 = linear(tf.reshape(h3, [self.batch_size, <span class="number">-1</span>]), <span class="number">1</span>, <span class="string">'d_h4_lin'</span>)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> tf.nn.sigmoid(h4), h4</span><br><span class="line">      <span class="keyword">else</span>:</span><br><span class="line">        yb = tf.reshape(y, [self.batch_size, <span class="number">1</span>, <span class="number">1</span>, self.y_dim])</span><br><span class="line">        x = conv_cond_concat(image, yb)</span><br><span class="line"></span><br><span class="line">        h0 = lrelu(conv2d(x, self.c_dim + self.y_dim, name=<span class="string">'d_h0_conv'</span>))</span><br><span class="line">        h0 = conv_cond_concat(h0, yb)</span><br><span class="line"></span><br><span class="line">        h1 = lrelu(self.d_bn1(conv2d(h0, self.df_dim + self.y_dim, name=<span class="string">'d_h1_conv'</span>)))</span><br><span class="line">        h1 = tf.reshape(h1, [self.batch_size, <span class="number">-1</span>])      </span><br><span class="line">        h1 = concat([h1, y], <span class="number">1</span>)</span><br><span class="line">        </span><br><span class="line">        h2 = lrelu(self.d_bn2(linear(h1, self.dfc_dim, <span class="string">'d_h2_lin'</span>)))</span><br><span class="line">        h2 = concat([h2, y], <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">        h3 = linear(h2, <span class="number">1</span>, <span class="string">'d_h3_lin'</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> tf.nn.sigmoid(h3), h3</span><br></pre></td></tr></table></figure><p>最后一层实现线性连接，将$NHWC$转变为$N,1$并经过sigmoid激活转化为概率</p><h2 id="pix2pix"><a href="#pix2pix" class="headerlink" title="pix2pix"></a>pix2pix</h2><p>上面部分是对原始的Gan进行了推导和说明，输入的数据是噪声数据，这显然会带来一个问题，<strong>给定一个噪声虽然能生成新样本，但是我们无法控制生成的图像的类型</strong>，以Minist举例，使用噪声数据训练，只能得到类似于Minist相似的数据，但是无法控制具体是实现哪个数字，整个数据的生成过程的是随机的。</p><p><strong>NOTE: 对于pix2pix来说，训练时的输入图片和输出图片必须是成对出现的</strong>。</p><p>原来的Gan的输入和输出</p><ul><li>生成器：输入一个噪声z，输出一个图像G(z)</li><li>判别器：输入一个图像x，输出该图像的真实的概率$D(x)​$</li></ul><p>针对上面所述的问题，引入了条件对抗生成网络。</p><p>对于cGan的输入输出为：</p><ul><li>生成器：输入一个噪声z，一个条件y，输出符合该条件的图像$G(z|y)$</li><li>判别器：输入一张图像x，一个条件y，输出该图像在该条件下的真实的概率$D(x|y)​$</li></ul><p>Pix2Pix就是这种网络架构。pix2pix可以用来为线稿图上色，表情转换，性别转换等任务。</p><img src="/2019/02/23/深度学习-对抗生成网络/pix2pix.png" title="pix2pix"><p>正如图中所示，整个数据集中存在着成对的图像，分别为线稿的形式和上色的形式，线稿图输入生成网络生成假的彩色图，判别器判别该生成数据为假，判别真实的上色图为真来优判别器，使用真实的上色图和生成网络生成的彩色图之间的的差距来优化生成器。</p><p>上图为pix2pix在tensorflow训练之后的graphic，可以从整个图中看出整个模型包含一个生成器一个判别器，包含3个损失函数，对上面的图形进行简化，如下图所示。</p><img src="/2019/02/23/深度学习-对抗生成网络/pix2pix_model.jpg" title="pix2pix model"><p>如上图所示为pix2pix的模型结构，数据集中存储中成对的数据RealA和RealB，也就是上文所述的X,Y类型的图片。生成网络输入生成网络G中生成Fake_B。</p><p>对于判别网络</p><ul><li>输入RealA和RealB，使判别器认为该类型为真</li><li>输入RealA和FakeB，使判别器认为该类型为假</li></ul><p>对于生成器，固定判别器参数</p><ul><li>输入Real和FakeB，使判别器认为该类型为真</li><li>FakeB和RealB之间的L1损失</li></ul><h3 id="优化目标"><a href="#优化目标" class="headerlink" title="优化目标"></a>优化目标</h3><p>和传统的使用对抗生成网络不同，对于pix2pix使用了两个优化目标，一个为Gan优化目标</p><script type="math/tex; mode=display">\begin{aligned} \mathcal{L}_{c G A N}(G, D)=& \mathbb{E}_{x, y}[\log D(x, y)]+\\ & \mathbb{E}_{x, z}[\log (1-D(x, G(x, z))]\end{aligned}</script><p>该优化目标和传统的损失函数略有不同，其中$x$为观察数据，$y$为对应的上色图，也就是label，$z$为噪声数据。</p><script type="math/tex; mode=display">\mathcal{L}_{L 1}(G)=\mathbb{E}_{x, y, z}\left[\|y-G(x, z)\|_{1}\right]</script><p>判别器的优化目标还是尽可能的对数据进行划分，而生成器的优化目标不仅仅是欺骗判别器，而且还加入了和数据集中样本的差距，并且经过衡量之后L1损失相比L1损失更能生成边缘清晰的图像。</p><p>整个模型的优化目标就是</p><script type="math/tex; mode=display">G^{*}=\arg \min _{G} \max _{D} \mathcal{L}_{c G A N}(G, D)+\lambda \mathcal{L}_{L 1}(G)</script><h3 id="判别器设计"><a href="#判别器设计" class="headerlink" title="判别器设计"></a>判别器设计</h3><p>判别器的结构如下所示<a href="https://medium.com/@ManishChablani/cyclegans-and-pix2pix-5e6a5f0159c4%" target="_blank" rel="noopener">reference</a></p><img src="/2019/02/23/深度学习-对抗生成网络/pix2pix_dis.png" title="pix2pix discriminator"><p>在判别器设计中，由于是条件对抗生成网络，在判别器阶段输入时是两类图像合并之后的数据，在pix2pix中，进行的将Y类图像转化为X类图像，在判别器中输入的是$[X,Y]$的合并之后的数据，并且经过Unet卷积之后得到的图像为$NHW1$的数据维度，并且最后一层使用的Sigmoid激活函数，这是在pix2pix中引入的PathcGan机制，对每张图像使用$H\times W$的小块来计算输入图像的概率，这样的设计可以加快计算速度和收敛，代码如下所示</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_discriminator</span><span class="params">(discrim_inputs, discrim_targets)</span>:</span></span><br><span class="line">    n_layers = <span class="number">3</span></span><br><span class="line">    layers = []</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 2x [batch, height, width, in_channels] =&gt; [batch, height, width, in_channels * 2]</span></span><br><span class="line">    input = tf.concat([discrim_inputs, discrim_targets], axis=<span class="number">3</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># layer_1: [batch, 256, 256, in_channels * 2] =&gt; [batch, 128, 128, ndf]</span></span><br><span class="line">    <span class="keyword">with</span> tf.variable_scope(<span class="string">"layer_1"</span>):</span><br><span class="line">        convolved = conv(input, a.ndf, stride=<span class="number">2</span>)</span><br><span class="line">        rectified = lrelu(convolved, <span class="number">0.2</span>)</span><br><span class="line">        layers.append(rectified)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># layer_2: [batch, 128, 128, ndf] =&gt; [batch, 64, 64, ndf * 2]</span></span><br><span class="line">        <span class="comment"># layer_3: [batch, 64, 64, ndf * 2] =&gt; [batch, 32, 32, ndf * 4]</span></span><br><span class="line">        <span class="comment"># layer_4: [batch, 32, 32, ndf * 4] =&gt; [batch, 31, 31, ndf * 8]</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(n_layers):</span><br><span class="line">            <span class="keyword">with</span> tf.variable_scope(<span class="string">"layer_%d"</span> % (len(layers) + <span class="number">1</span>)):</span><br><span class="line">                out_channels = a.ndf * min(<span class="number">2</span>**(i+<span class="number">1</span>), <span class="number">8</span>)</span><br><span class="line">                stride = <span class="number">1</span> <span class="keyword">if</span> i == n_layers - <span class="number">1</span> <span class="keyword">else</span> <span class="number">2</span>  <span class="comment"># last layer here has stride 1</span></span><br><span class="line">                convolved = conv(layers[<span class="number">-1</span>], out_channels, stride=stride)</span><br><span class="line">                normalized = batchnorm(convolved)</span><br><span class="line">                rectified = lrelu(normalized, <span class="number">0.2</span>)</span><br><span class="line">                layers.append(rectified)</span><br><span class="line"></span><br><span class="line">                <span class="comment"># layer_5: [batch, 31, 31, ndf * 8] =&gt; [batch, 30, 30, 1]</span></span><br><span class="line">                <span class="keyword">with</span> tf.variable_scope(<span class="string">"layer_%d"</span> % (len(layers) + <span class="number">1</span>)):</span><br><span class="line">                    convolved = conv(rectified, out_channels=<span class="number">1</span>, stride=<span class="number">1</span>)</span><br><span class="line">                    output = tf.sigmoid(convolved)</span><br><span class="line">                    layers.append(output)</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">return</span> layers[<span class="number">-1</span>]</span><br><span class="line"><span class="comment">## pix2pix网络搭建</span></span><br><span class="line"><span class="keyword">with</span> tf.variable_scope(<span class="string">"generator"</span>) <span class="keyword">as</span> scope:</span><br><span class="line">    out_channels = int(targets.get_shape()[<span class="number">-1</span>])</span><br><span class="line">    <span class="comment"># output为生成器生成的图像</span></span><br><span class="line">    outputs = create_generator(inputs, out_channels)</span><br><span class="line"></span><br><span class="line"><span class="string">""" 使用目标图像创建真实判别器，使用generator生成的创建假判别器 """</span></span><br><span class="line"><span class="comment"># create two copies of discriminator, one for real pairs and one for fake pairs</span></span><br><span class="line"><span class="comment"># they share the same underlying variables</span></span><br><span class="line"><span class="keyword">with</span> tf.name_scope(<span class="string">"real_discriminator"</span>):</span><br><span class="line">    <span class="keyword">with</span> tf.variable_scope(<span class="string">"discriminator"</span>):</span><br><span class="line">        <span class="comment"># 2x [batch, height, width, channels] =&gt; [batch, 30, 30, 1]</span></span><br><span class="line">        <span class="comment"># target为真实的图像</span></span><br><span class="line">        predict_real = create_discriminator(inputs, targets)</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.name_scope(<span class="string">"fake_discriminator"</span>):</span><br><span class="line">    <span class="keyword">with</span> tf.variable_scope(<span class="string">"discriminator"</span>, reuse=<span class="keyword">True</span>):</span><br><span class="line">        <span class="comment"># 2x [batch, height, width, channels] =&gt; [batch, 30, 30, 1]</span></span><br><span class="line">        predict_fake = create_discriminator(inputs, outputs)</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.name_scope(<span class="string">"discriminator_loss"</span>):</span><br><span class="line">    <span class="comment"># minimizing -tf.log will try to get inputs to 1</span></span><br><span class="line">    <span class="comment"># predict_real =&gt; 1</span></span><br><span class="line">    <span class="comment"># predict_fake =&gt; 0</span></span><br><span class="line">    <span class="string">""" 判别器损失，使用类似的交叉熵损失 """</span></span><br><span class="line">    discrim_loss = tf.reduce_mean(-(tf.log(predict_real + EPS) + tf.log(<span class="number">1</span> - predict_fake + EPS)))</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.name_scope(<span class="string">"generator_loss"</span>):</span><br><span class="line">    <span class="comment"># predict_fake =&gt; 1</span></span><br><span class="line">    <span class="comment"># abs(targets - outputs) =&gt; 0</span></span><br><span class="line">    gen_loss_GAN = tf.reduce_mean(-tf.log(predict_fake + EPS))</span><br><span class="line">    gen_loss_L1 = tf.reduce_mean(tf.abs(targets - outputs))</span><br><span class="line">    gen_loss = gen_loss_GAN * a.gan_weight + gen_loss_L1 * a.l1_weight</span><br></pre></td></tr></table></figure><p>在涉及的模型<code>create_discriminator</code>中，如果输入的target是真实样本集中X类图像，那么判别器判定其为1，如果输入的是生成器生成的图像，那么判别器判定为假。</p><p>从代码中可以看到，一共包含3个损失</p><ul><li>判别器：使用交叉熵损失</li><li>生成器：L1损失和类别损失</li></ul><h3 id="生成器设计"><a href="#生成器设计" class="headerlink" title="生成器设计"></a>生成器设计</h3><p>对于生成器的设计，pix2pix使用的是Unet，结构如下图所示</p><img src="/2019/02/23/深度学习-对抗生成网络/pix2pix_g.png" title="Unet"><p>在Unet中，可以从结构图中看到，对于输入的图像，先进行Conv下采样，再TransConv上采样，并且连接上下采样中相同的尺寸。这样能够有效的保留图像的语义信息，并且输入数据的尺寸和输出数据的尺寸相等</p><img src="/2019/02/23/深度学习-对抗生成网络/pix2pix_unet.png" title="pix2pix_generator"><p>Unet最初被提出是用来解决图像分割任务，相关论文可以点击参考文献查看。在对抗生成网络中Unet一般被用来作为生成器。</p><h4 id="Tensorflow-implement"><a href="#Tensorflow-implement" class="headerlink" title="Tensorflow implement"></a>Tensorflow implement</h4><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">create_generator</span><span class="params">(generator_inputs, generator_outputs_channels)</span>:</span></span><br><span class="line">    <span class="string">""" 输入为原始图像，首先进行卷积encode，之后进行反卷积decode最终生成与输入图像大小相等的图像 """</span></span><br><span class="line">    layers = []</span><br><span class="line"></span><br><span class="line">    <span class="comment"># encoder_1: [batch, 256, 256, in_channels] =&gt; [batch, 128, 128, ngf]</span></span><br><span class="line">    <span class="keyword">with</span> tf.variable_scope(<span class="string">"encoder_1"</span>):</span><br><span class="line">        output = conv(generator_inputs, a.ngf, stride=<span class="number">2</span>)</span><br><span class="line">        layers.append(output)</span><br><span class="line"></span><br><span class="line">    layer_specs = [</span><br><span class="line">        a.ngf * <span class="number">2</span>,  <span class="comment"># encoder_2: [batch, 128, 128, ngf] =&gt; [batch, 64, 64, ngf * 2]</span></span><br><span class="line">        a.ngf * <span class="number">4</span>,  <span class="comment"># encoder_3: [batch, 64, 64, ngf * 2] =&gt; [batch, 32, 32, ngf * 4]</span></span><br><span class="line">        a.ngf * <span class="number">8</span>,  <span class="comment"># encoder_4: [batch, 32, 32, ngf * 4] =&gt; [batch, 16, 16, ngf * 8]</span></span><br><span class="line">        a.ngf * <span class="number">8</span>,  <span class="comment"># encoder_5: [batch, 16, 16, ngf * 8] =&gt; [batch, 8, 8, ngf * 8]</span></span><br><span class="line">        a.ngf * <span class="number">8</span>,  <span class="comment"># encoder_6: [batch, 8, 8, ngf * 8] =&gt; [batch, 4, 4, ngf * 8]</span></span><br><span class="line">        a.ngf * <span class="number">8</span>,  <span class="comment"># encoder_7: [batch, 4, 4, ngf * 8] =&gt; [batch, 2, 2, ngf * 8]</span></span><br><span class="line">        a.ngf * <span class="number">8</span>,  <span class="comment"># encoder_8: [batch, 2, 2, ngf * 8] =&gt; [batch, 1, 1, ngf * 8]</span></span><br><span class="line">    ]</span><br><span class="line">    <span class="string">""" encode首先使用一层卷积，之后添加7层卷积，一共8层进行encode卷积处理 """</span></span><br><span class="line">    <span class="keyword">for</span> out_channels <span class="keyword">in</span> layer_specs:</span><br><span class="line">        <span class="keyword">with</span> tf.variable_scope(<span class="string">"encoder_%d"</span> % (len(layers) + <span class="number">1</span>)):</span><br><span class="line">            rectified = lrelu(layers[<span class="number">-1</span>], <span class="number">0.2</span>)</span><br><span class="line">            <span class="comment"># [batch, in_height, in_width, in_channels] =&gt; [batch, in_height/2, in_width/2, out_channels]</span></span><br><span class="line">            convolved = conv(rectified, out_channels, stride=<span class="number">2</span>)</span><br><span class="line">            output = batchnorm(convolved)</span><br><span class="line">            layers.append(output)</span><br><span class="line"></span><br><span class="line">    layer_specs = [</span><br><span class="line">        (a.ngf * <span class="number">8</span>, <span class="number">0.5</span>),   <span class="comment"># decoder_8: [batch, 1, 1, ngf * 8] =&gt; [batch, 2, 2, ngf * 8 * 2]</span></span><br><span class="line">        (a.ngf * <span class="number">8</span>, <span class="number">0.5</span>),   <span class="comment"># decoder_7: [batch, 2, 2, ngf * 8 * 2] =&gt; [batch, 4, 4, ngf * 8 * 2]</span></span><br><span class="line">        (a.ngf * <span class="number">8</span>, <span class="number">0.5</span>),   <span class="comment"># decoder_6: [batch, 4, 4, ngf * 8 * 2] =&gt; [batch, 8, 8, ngf * 8 * 2]</span></span><br><span class="line">        (a.ngf * <span class="number">8</span>, <span class="number">0.0</span>),   <span class="comment"># decoder_5: [batch, 8, 8, ngf * 8 * 2] =&gt; [batch, 16, 16, ngf * 8 * 2]</span></span><br><span class="line">        <span class="comment"># decoder_4: [batch, 16, 16, ngf * 8 * 2] =&gt; [batch, 32, 32, ngf * 4 * 2]</span></span><br><span class="line">        (a.ngf * <span class="number">4</span>, <span class="number">0.0</span>),</span><br><span class="line">        <span class="comment"># decoder_3: [batch, 32, 32, ngf * 4 * 2] =&gt; [batch, 64, 64, ngf * 2 * 2]</span></span><br><span class="line">        (a.ngf * <span class="number">2</span>, <span class="number">0.0</span>),</span><br><span class="line">        (a.ngf, <span class="number">0.0</span>),       <span class="comment"># decoder_2: [batch, 64, 64, ngf * 2 * 2] =&gt; [batch, 128, 128, ngf * 2]</span></span><br><span class="line">    ]</span><br><span class="line"></span><br><span class="line">    num_encoder_layers = len(layers)  <span class="comment"># 8</span></span><br><span class="line">    <span class="keyword">for</span> decoder_layer, (out_channels, dropout) <span class="keyword">in</span> enumerate(layer_specs):</span><br><span class="line">        <span class="comment"># skip_layer in [7,6,5,...,1]</span></span><br><span class="line">        skip_layer = num_encoder_layers - decoder_layer - <span class="number">1</span>  <span class="comment"># 8-0-1,8-1-1,...8-6-1</span></span><br><span class="line">        <span class="keyword">with</span> tf.variable_scope(<span class="string">"decoder_%d"</span> % (skip_layer + <span class="number">1</span>)):</span><br><span class="line">            <span class="keyword">if</span> decoder_layer == <span class="number">0</span>:</span><br><span class="line">                <span class="comment"># first decoder layer doesn't have skip connections</span></span><br><span class="line">                <span class="comment"># since it is directly connected to the skip_layer</span></span><br><span class="line">                input = layers[<span class="number">-1</span>]</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                input = tf.concat([layers[<span class="number">-1</span>], layers[skip_layer]], axis=<span class="number">3</span>)</span><br><span class="line"></span><br><span class="line">            rectified = tf.nn.relu(input)</span><br><span class="line">            <span class="comment"># [batch, in_height, in_width, in_channels] =&gt; [batch, in_height*2, in_width*2, out_channels]</span></span><br><span class="line">            output = deconv(rectified, out_channels)</span><br><span class="line">            output = batchnorm(output)</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> dropout &gt; <span class="number">0.0</span>:</span><br><span class="line">                output = tf.nn.dropout(output, keep_prob=<span class="number">1</span> - dropout)</span><br><span class="line"></span><br><span class="line">            layers.append(output)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># decoder_1: [batch, 128, 128, ngf * 2] =&gt; [batch, 256, 256, generator_outputs_channels]</span></span><br><span class="line">    <span class="keyword">with</span> tf.variable_scope(<span class="string">"decoder_1"</span>):</span><br><span class="line">        input = tf.concat([layers[<span class="number">-1</span>], layers[<span class="number">0</span>]], axis=<span class="number">3</span>)</span><br><span class="line">        rectified = tf.nn.relu(input)</span><br><span class="line">        output = deconv(rectified, generator_outputs_channels)</span><br><span class="line">        <span class="comment"># in the last layer use tanh activation function to generate the color img</span></span><br><span class="line">        output = tf.tanh(output)</span><br><span class="line">        layers.append(output)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> layers[<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><h4 id="Keras-implement"><a href="#Keras-implement" class="headerlink" title="Keras implement"></a>Keras implement</h4><p>链接：<a href="https://github.com/zhixuhao/unet" target="_blank" rel="noopener">https://github.com/zhixuhao/unet</a></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> skimage.io <span class="keyword">as</span> io</span><br><span class="line"><span class="keyword">import</span> skimage.transform <span class="keyword">as</span> trans</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> keras.models <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> keras.layers <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> keras.optimizers <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> keras.callbacks <span class="keyword">import</span> ModelCheckpoint, LearningRateScheduler</span><br><span class="line"><span class="keyword">from</span> keras <span class="keyword">import</span> backend <span class="keyword">as</span> keras</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">unet</span><span class="params">(pretrained_weights = None,input_size = <span class="params">(<span class="number">256</span>,<span class="number">256</span>,<span class="number">1</span>)</span>)</span>:</span></span><br><span class="line">    inputs = Input(input_size)</span><br><span class="line">    conv1 = Conv2D(<span class="number">64</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(inputs)</span><br><span class="line">    conv1 = Conv2D(<span class="number">64</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv1)</span><br><span class="line">    pool1 = MaxPooling2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>))(conv1)</span><br><span class="line">    conv2 = Conv2D(<span class="number">128</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(pool1)</span><br><span class="line">    conv2 = Conv2D(<span class="number">128</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv2)</span><br><span class="line">    pool2 = MaxPooling2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>))(conv2)</span><br><span class="line">    conv3 = Conv2D(<span class="number">256</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(pool2)</span><br><span class="line">    conv3 = Conv2D(<span class="number">256</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv3)</span><br><span class="line">    pool3 = MaxPooling2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>))(conv3)</span><br><span class="line">    conv4 = Conv2D(<span class="number">512</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(pool3)</span><br><span class="line">    conv4 = Conv2D(<span class="number">512</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv4)</span><br><span class="line">    drop4 = Dropout(<span class="number">0.5</span>)(conv4)</span><br><span class="line">    pool4 = MaxPooling2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>))(drop4)</span><br><span class="line"></span><br><span class="line">    conv5 = Conv2D(<span class="number">1024</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(pool4)</span><br><span class="line">    conv5 = Conv2D(<span class="number">1024</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv5)</span><br><span class="line">    drop5 = Dropout(<span class="number">0.5</span>)(conv5)</span><br><span class="line"></span><br><span class="line">    up6 = Conv2D(<span class="number">512</span>, <span class="number">2</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(UpSampling2D(size = (<span class="number">2</span>,<span class="number">2</span>))(drop5))</span><br><span class="line">    merge6 = concatenate([drop4,up6], axis = <span class="number">3</span>)</span><br><span class="line">    conv6 = Conv2D(<span class="number">512</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(merge6)</span><br><span class="line">    conv6 = Conv2D(<span class="number">512</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv6)</span><br><span class="line"></span><br><span class="line">    up7 = Conv2D(<span class="number">256</span>, <span class="number">2</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(UpSampling2D(size = (<span class="number">2</span>,<span class="number">2</span>))(conv6))</span><br><span class="line">    merge7 = concatenate([conv3,up7], axis = <span class="number">3</span>)</span><br><span class="line">    conv7 = Conv2D(<span class="number">256</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(merge7)</span><br><span class="line">    conv7 = Conv2D(<span class="number">256</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv7)</span><br><span class="line"></span><br><span class="line">    up8 = Conv2D(<span class="number">128</span>, <span class="number">2</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(UpSampling2D(size = (<span class="number">2</span>,<span class="number">2</span>))(conv7))</span><br><span class="line">    merge8 = concatenate([conv2,up8], axis = <span class="number">3</span>)</span><br><span class="line">    conv8 = Conv2D(<span class="number">128</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(merge8)</span><br><span class="line">    conv8 = Conv2D(<span class="number">128</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv8)</span><br><span class="line"></span><br><span class="line">    up9 = Conv2D(<span class="number">64</span>, <span class="number">2</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(UpSampling2D(size = (<span class="number">2</span>,<span class="number">2</span>))(conv8))</span><br><span class="line">    merge9 = concatenate([conv1,up9], axis = <span class="number">3</span>)</span><br><span class="line">    conv9 = Conv2D(<span class="number">64</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(merge9)</span><br><span class="line">    conv9 = Conv2D(<span class="number">64</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv9)</span><br><span class="line">    conv9 = Conv2D(<span class="number">2</span>, <span class="number">3</span>, activation = <span class="string">'relu'</span>, padding = <span class="string">'same'</span>, kernel_initializer = <span class="string">'he_normal'</span>)(conv9)</span><br><span class="line">    conv10 = Conv2D(<span class="number">1</span>, <span class="number">1</span>, activation = <span class="string">'sigmoid'</span>)(conv9)</span><br><span class="line"></span><br><span class="line">    model = Model(input = inputs, output = conv10)</span><br><span class="line"></span><br><span class="line">    model.compile(optimizer = Adam(lr = <span class="number">1e-4</span>), loss = <span class="string">'binary_crossentropy'</span>, metrics = [<span class="string">'accuracy'</span>])</span><br><span class="line">    </span><br><span class="line">    <span class="comment">#model.summary()</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(pretrained_weights):</span><br><span class="line">    model.load_weights(pretrained_weights)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> model</span><br><span class="line">model=unet()</span><br></pre></td></tr></table></figure><h3 id="Training"><a href="#Training" class="headerlink" title="Training"></a>Training</h3><p>整个pix2pix中包含一个生成器一个判别器，并且数据集图像成对出现，整个训练过程如下</p><img src="/2019/02/23/深度学习-对抗生成网络/pix2pix_train_dis.png" title="pix2pix Training dircirminator"><p>训练判别器，判别器用来分辨输入样本的真假，在pix2pix中判别器的输入是两张图像拼接之后的新的图像，如果使用的都是数据集中的数据，那么判别器会判别为真；如果使用的是数据集的数据和生成器生成的数据，那么判别器会判别为假。</p><img src="/2019/02/23/深度学习-对抗生成网络/pix2pix_train_gen.png" title="pix2pix training generator"><p>训练生成器，包含了两个损失，一个是对判别器的迷惑，使用判别器生成的数据和数据集中的数据合并，输入判别器，并判断为真；判别器生成的数据和真实的目标数据进行比较，缩小生成器的生成数据和真实目标数据的差异。</p><h2 id="CycleGan"><a href="#CycleGan" class="headerlink" title="CycleGan"></a>CycleGan</h2><p>顾名思义，CycleGan也是一种条件对抗生成网络，是对pix2pix的改进，<strong>在pix2pix模型中，是基于成对的数据类型的，但是在现实中一般没有这么多成对出现的数据，为了改进这个问题，CycleGan应运而生，只需要准备两种类型的数据，而不需要这些数据是成对出现的。</strong></p><p>cyclegan可以使用不成对的数据训练处x与y之间的相互映射，比如可以搜集大量的照片和油画，可以实现二者的相会转化。</p><p>直接看一下cyclegan的模型结构</p><img src="/2019/02/23/深度学习-对抗生成网络/cyclegan.png" title="cyclegan model"><p>在CycleGan模型中，训练数据集中包含两类图片(realA和realB)，我们最终想要的是能够实现对图像类别进行转换。在整个模型中存在两个生成器和两个判别器</p><ul><li>生成器<ul><li>GAB，将类别A图像生成类别B图像</li><li>GBA，将类别B图像生成类别A图像</li></ul></li><li>判别器<ul><li>DB，判别B类图像的真假</li><li>DA，判别A类图像的真假</li></ul></li></ul><p>算法的目标是为了学习类别转换的映射函数。GAB将类别A的图像转换为类别B的图像GAB(X)，对于生成的图像会使用判别器DB判别其真假，由此构成对抗生成网络，和传统的DCGAN相似，得到优化目标</p><script type="math/tex; mode=display">\begin{aligned} \mathcal{L}_{\mathrm{GAN}}\left(G_{AB}, D_{B}, X, Y\right) &=\mathbb{E}_{y \sim p_{\text { data }}(y)}\left[\log D_{B}(y)\right] \\ &+\mathbb{E}_{x \sim p_{\text { data }}(x)}\left[\log \left(1-D_{B}(G_{AB}(x))\right]\right.\end{aligned}</script><p>此处的优化目标和DCGAN是相同的，优化目标最大化来训练判别器$D_B$，优化目标最小化来训练生成器$G_{AB}$。</p><p>因为提供的数据集中不同类别的数据不是成对出现的，因此只是用这个损失是不可行的，<strong>生成器可以将所有输入数据x都映射到目标类的同一张图像，也会使得生成器的优化目标最小，这样显然不可行。</strong>在Cyclegan中提出了循环损失，再设计一个生成器，可以将类别B的图片映射到类别A的图片$G_{BA}$，在整个模型中学习两个生成器，和两个判别器。定义一个循环损失</p><script type="math/tex; mode=display">\begin{aligned} \mathcal{L}_{\mathrm{cyc}}(G_{AB}, G_{BA}) &=\mathbb{E}_{x \sim p_{\text { data }}(x)}\left[\|G_{BA}(G_{AB}(x))-x\|_{1}\right] \\ &+\mathbb{E}_{y \sim p_{\text { data }}(y)}\left[\|G_{AB}(G_{BA}(y))-y\|_{1}\right] \end{aligned}</script><p><strong>这样是为了将A类图片通过一个映射函数转换为B类图片之后，应该还可以通过另一个映射函数转换回来。</strong></p><p>整个损失包含两个对抗生成损失和一个循环损失。</p><script type="math/tex; mode=display">\begin{aligned} \mathcal{L}\left(G_{AB}, G_{BA}, D_{A}, D_{B}\right) &=\mathcal{L}_{\mathrm{GAN}}\left(G_{AB}, D_{B}, X, Y\right) \\ &+\mathcal{L}_{\mathrm{GAN}}\left(G_{BA}, D_{A}, Y, X\right) \\ &+\lambda \mathcal{L}_{\mathrm{cyc}}(G_{AB}, G_{BA}) \end{aligned}</script><p>在作者公布的代码中，还有一项损失，对于生成器$G_{AB}​$，将A类图像映射成B类图像，对于$G_{BA}​$将B类图像映射成$A​$类图像。那么如果输入是B类图像，使用生成器$G_{AB}​$应该映射成什么样子呢？正常讲，输入生成器为了实现更好的生成效果，生成器应该只负责映射一类的图像，也就是说，对于B类图像，使用生成器$G_{AB}​$应该生成的还是B类图像，生成器$G_{AB}​$对B类图像不起作用，这就引入了一个新的约束</p><script type="math/tex; mode=display">\begin{aligned} \mathcal{L}_{\mathrm{Ide}}(G_{AB}, G_{BA}) &=\mathbb{E}_{x \sim p_{\text { data }}(x)}\left[\|G_{BA}(G_{AB}(x))-x\|_{1}\right] \\ &+\mathbb{E}_{y \sim p_{\text { data }}(y)}\left[\|G_{AB}(G_{BA}(y))-y\|_{1}\right] \end{aligned}</script><h2 id="Gan训练技巧"><a href="#Gan训练技巧" class="headerlink" title="Gan训练技巧"></a>Gan训练技巧</h2><h3 id="输入归一化"><a href="#输入归一化" class="headerlink" title="输入归一化"></a>输入归一化</h3><ul><li><p>对输入的数据进行归一化处理，一般归一化值$-1,1$之间</p></li><li><p>对于生成器，最后一层使用Tanh激活函数，使输出数据压缩至$-1,1$之间</p></li></ul><h3 id="损失函数"><a href="#损失函数" class="headerlink" title="损失函数"></a>损失函数</h3><ul><li>对于生成器的损失，论文中一般使用$\min \log(1-D)​$，使生成器的生成数据被判别器判定为真，但是这种方式会使得造成梯度消失</li><li>实际中一般使用$\max \log(d(x))​$来优化生成器。</li></ul><h3 id="噪声数据的采样"><a href="#噪声数据的采样" class="headerlink" title="噪声数据的采样"></a>噪声数据的采样</h3><ul><li>不要从标准分布中进行噪声的采样</li></ul><img src="/2019/02/23/深度学习-对抗生成网络/noise_cube.png" title="noise_cube"><ul><li>使用高斯分布进行采样</li></ul><img src="/2019/02/23/深度学习-对抗生成网络/gans_sphere.png" title="sphere"><p>在进行插值操作时，使用大圆上的数据分布进行插值操作，而不是使用小圆的连接直线上进行插值。</p><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul><li><a href="https://zh.wikipedia.org/wiki/%E7%94%9F%E6%88%90%E5%AF%B9%E6%8A%97%E7%BD%91%E7%BB%9C" target="_blank" rel="noopener">wiki: 对抗生成网络</a></li><li><a href="https://www.jiqizhixin.com/articles/2017-10-1-1" target="_blank" rel="noopener">机器之心GitHub项目：GAN完整理论推导与实现，Perfect！</a></li><li><a href="https://arxiv.org/abs/1505.04597" target="_blank" rel="noopener">U-Net: Convolutional Networks for Biomedical Image Segmentation</a></li><li><a href="https://junyanz.github.io/CycleGAN/" target="_blank" rel="noopener">cycleGan_project</a></li><li><a href="https://www.microsoft.com/developerblog/2017/06/12/learning-image-image-translation-cyclegans/" target="_blank" rel="noopener">bolg: cyclegan tensorflow</a></li><li><a href="https://affinelayer.com/pixsrv/" target="_blank" rel="noopener">pix2pix_project</a></li><li><a href="https://affinelayer.com/pix2pix/" target="_blank" rel="noopener">blog: pix2pix</a></li><li><a href="https://medium.com/@ManishChablani/cyclegans-and-pix2pix-5e6a5f0159c4" target="_blank" rel="noopener">blog: CycleGANS and Pix2Pix </a></li><li><a href="https://github.com/soumith/ganhacks" target="_blank" rel="noopener">Gans trick</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;GAN(Generative Adversary Networks)的思想是是一种二人零和博弈思想（two-player game），博弈双方的利益之和是一个常数，比如两个人掰手腕，假设总的空间是一定的，你的力气大一点，那你就得到的空间多一点，相应的我的空间就少一点，相反我力气大我就得到的多一点，但有一点是确定的就是，我两的总空间是一定的，这就是二人博弈，但是总利益是一定的。&lt;/p&gt;
&lt;p&gt;引申到GAN里面就是可以看成，GAN中有两个这样的博弈者，一个人名字是生成模型(G)，另一个人名字是判别模型(D)。他们各自有各自的功能。&lt;/p&gt;
&lt;img src=&quot;/2019/02/23/深度学习-对抗生成网络/gans.jpg&quot; title=&quot;Gan&quot;&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="Gan" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/Gan/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="Gan" scheme="http://xiaofengshi.com/tags/Gan/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-NLP-Attention_is_all_you_need</title>
    <link href="http://xiaofengshi.com/2019/02/15/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-NLP-Attention_is_all_you_need/"/>
    <id>http://xiaofengshi.com/2019/02/15/深度学习-NLP-Attention_is_all_you_need/</id>
    <published>2019-02-15T09:42:31.000Z</published>
    <updated>2020-02-03T13:32:43.078Z</updated>
    
    <content type="html"><![CDATA[<p>Transformer是在2017年的Google发布的论文《Attention is all you need》中提出，主要用于解决RNN相关网络无法捕获序列的长期依赖，以及网络无法并行化的问题，其网络结构示意图如下</p><img src="/2019/02/15/深度学习-NLP-Attention_is_all_you_need/transformer_model.jpeg" title="transformer_model"><p>从示意图中可以看出在Transformer中包含多个组件，分别为Multi-Head Attention, Masked Multi-Head Attention, Add, Norm, Feed Forward, Position Encoding，会在接下来的内容中结合Transformer的前向传播过程依次解释。</p><a id="more"></a><h2 id="Seq2Seq"><a href="#Seq2Seq" class="headerlink" title="Seq2Seq"></a>Seq2Seq</h2><p>NLP领域内的相关任务为机器翻译，语音识别，问答系统，文本分类等等，多数情况下处理的是序列结构的数据，长久以来，RNN是序列模型的首选工具，因为RNN基于时间步传递，天然的可以捕获序列的前后语义关系和位置关系，这就是目前广泛使用的Seq2seq模型结构，包含Encoder部分和Decoder部分，Encoder部分负责对输入的数据进行编码处理，Decoder部分根据Encoder的输出进行解码输出，最开始的Decoder接受Encoder部分的最后一个时间步的隐藏层输出作为其初始状态输入，但是这很显然的会存在两个问题，将输入序列编码之后信息只是用最后一个时间步的隐藏层输出是不合理的，因为时间步储存的信息是优先的，无法捕获输入数据的长期依赖，距离该时间步近的序列保存的多，远离的保留的就少；另一个问题，在解码的时候使用最后一层时间步，无法捕获输出信息和输入信息之间的依赖关系。为了解决Encoder和Decoder之间的依赖问题，就在Decoder阶段引入了Attention机制，可以看做是当前步的Decoder输出和整个Encoder的输出序列的依赖关系。但是seq2seq模型还存在一个问题，那就是基于RNN建模会导致模型无法并行运算，因为在RNN运算时是基于时间传播的，当前时间步接收的输入为上一个时间步的输出和当前时间步的输入，因此无法进行并行运算。针对这些问题，提出了多种改进方案，Transformer正是从提升seq2seq模型运算速度的角度出发进行设计的。更多关于seq2seq的相关内容，可以参看另一篇文章<a href="http://xiaofengshi.com/2018/11/27/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-Python-tutorial-LSTM-GRU-Attention/">深度学习-PythonTutorial_LSTM_GRU_Attention_LNLSTM_LNGRU</a></p><h2 id="Why-Transformer"><a href="#Why-Transformer" class="headerlink" title="Why Transformer?"></a>Why Transformer?</h2><p>首先需要说明的是为什么设计开发了Transformer这种模型结构，也就是目前的模型存在着哪些问题。</p><ol><li><p>RNN运行速度的问题。对于序列相关数据结构，使用RNN基于时间步进行传递，很天然的能够有效处理序列数据，但是同样是由于RNN的信息是基于时间步传递的，会存在无法并行化的问题，在RNN中，当前时间步的计算结果是由上一个时间步的输出结果和当前时间步的输入序列数据相关的，也就是说当前的计算是依赖于上一个时间步的，在没计算出上一个时间步的情况下是无法计算当前步的，这就导致了模型无法进行并行化训练，模型的运行时间过长。</p><p>针对RNN结构无法并行化的问题，提出了一些改进措施，例如使用跨步卷积，wavenet等，跨步卷积由于感受野的范围有限只能通过增加网络深度的方法进行感受野的扩增。而Transformer是通过的设计了新的Attention计算方式实现并行运算</p></li><li><p>捕获序列的长时依赖。在RNN的变体LSTM,GRU中使用门控的方式来捕获序列之间的长时依赖，但是实验证明，当序列过长时，LSTM，GRU仍然无法有效捕获这种依赖，此外在机器翻译任务或者语音识别,TTS等任务中存在着输入序列或者输入序列中同字异音，同字异意的问题，这就需要设计更好的模型来捕获序列的内部依和序列之间的依赖。</p></li></ol><p>Transformer正是针对上述这两点进行设计的，在论文中整体的框架仍然是seq2seq，但是摒弃了RNN结构，全部使用经过合理设计的Attention结构代替，实现了当时的SOTA结果。</p><h2 id="Structure"><a href="#Structure" class="headerlink" title="Structure"></a>Structure</h2><p>整个模型的结构如文首所述，摒弃了RNN结构，全部使用的Attention结构。在论文中和一般的博客中是按照的各个组件进行的介绍和讲解，本文将针对整个模型结构进行剖析和解释。</p><p>按照数据前向传播的方式进行简要说明</p><ul><li><p>输入序列的序列内部依赖关系提取</p><p>对输入的序列数据转化为词向量和位置编码，使用多头Attention进行输入序列间依赖关系的提取，并使用残差网络的连接方式连接多头Attention的输出结果和输入的分析序列，经过层归一化处理，之后使用前向网络进行维度转换并再次进行层归一化处理</p></li><li><p>输出序列的序列间内部关系提取</p><p>和输入的序列处理方式类似，只是在使用多头Attention的时候要加入mask的机制，这是为了保证在预测输出的时候只能使用当前位置之间的序列信息，因为在预测的时候，输出序列是未知的，当前待计算的位置之后的部分的信息无法给当前位置的预测提供帮助</p></li><li><p>输入序列和输出序列之间的依赖关系</p><p>在该阶段和传统的Attention相同，捕获的是预测的节点和输入序列经过编码之后的输出序列之间的依赖关系。</p></li><li><p>预测输出</p><p>在预测输出时，每个节点使用softmax的方式进行输出，输出每个节点属于不同字符的概率，这里可以使用贪心搜索的方式或者集束搜索的方式进行预测输出。</p></li></ul><p>整个结构中包含多个组件，下面进行分别说明。</p><h3 id="Positional-Encoding"><a href="#Positional-Encoding" class="headerlink" title="Positional Encoding"></a>Positional Encoding</h3><p>顾名思义，对序列进行位置编码，因为在设计的Attention和传统的RNN结构不同，对于RNN结构是对数据按照时间步进行计算的，但是Attention是针对整个序列进行的计算，因此需要在输入的序列中不但要进行词向量的转化，还要进行位置编码，由此得到的序列既包含序列的语义信息又包含序列的位置信息。</p><p>根据论文中所述，在位置编码的时候将序列按照时间步的不同进行三角函数变换，使其满足不同位置之间能通过线性变换的方式进行转换。在序列的位置$pos$的计算方式，假设序列的维度为$d_{model}$，计算公式为</p><script type="math/tex; mode=display">\begin{align}POS(pos,2*i)=\sin(pos/(10000^{(\frac{2*i}{d_{model}})})) \\POS(pos,2*i+1)=\cos(pos/(10000^{(\frac{2*i+1}{d_{model}})}))\end{align}</script><p>在维度方向上，在偶数位置上使用正弦函数，在奇数位置上使用余弦函数。对于正余弦函数存在关系式</p><script type="math/tex; mode=display">\begin{align}\sin(x+y)=\sin(x) \cos(y) +\sin(y)\cos(x) \\\cos(x+y)=\cos(x)\cos(y) -\sin(x)\sin(y)\end{align}</script><p>由此对于位置$POS(pos+k)$可以经过线性变换得到</p><h3 id="Multi-Head-Attention"><a href="#Multi-Head-Attention" class="headerlink" title="Multi-Head Attention"></a>Multi-Head Attention</h3><p>在Encoder阶段，序列经过词向量转化和位置编码，二者的维度均为$d_{model}$，将二者相加得到的序列认为包含着语义信息和位置信息，接下来将该序列作为待分析的序列，并使用Multi-Head Attention计算，对于Multi-Head Attention又叫做self-Attention，主要是为了提取序列内部的依赖关系。对于传统的Attention，是为了捕获输出序列和输入序列之间的关系，引入新的关系式query表示Decoder部分序列输出，key，value表示Encoder部分的输出，使用Decoder和key进行Attention score的计算，<strong>包括加性注意力(BahdanauAttention)，点乘注意力(LuongAttention)，自注意力(SelfAttention)和关键值注意力(KeyValueAttention)</strong>，对计算结果使用softmax进行归一化计算得到Attention weight，和value计算得到了contex，将contex和query合并在一起，作为Decoder当前位置的新输出，并将该输出传递至下一个时刻。具体模型如下所示</p><img src="/2019/02/15/深度学习-NLP-Attention_is_all_you_need/original_att.png" title="original Attention"><p>公式过程为</p><script type="math/tex; mode=display">\begin{align}Score=score(query,key)\\Attention_{weight}=softmax(Socre) \\Contex=Attention_{weight}*value \\new_{out}=\tanh([Contex,query]*w)\end{align}</script><p>上面式子中的score用来计算目标序列和原序列的对齐成对，一般使用加性，点积的方式</p><script type="math/tex; mode=display">\begin{align}Score &= value* \tanh(w*[query,key])  \:\:\:\:\:\:\:\:\:\: & add\\Score &= query* key^T & dot   \\Score &= query *w*key^T & gernel\end{align}</script><p><strong>对于加性attention和点乘attention的计算方式，二者在计算复杂度上式相似的，但是点乘注意力一般更快并且具有更高效的存储，因为点乘的方式可以使用矩阵操作更高效的实现，在低纬度情况下，加性注意力和点乘注意力相似，但是在高纬度上，加性注意力比点乘注意力效果更好。</strong></p><hr><p>对于<code>Multi-head Attention</code>的序列间依赖关系的计算和传统<code>Attention</code>计算方式相同，不同的有两点</p><ul><li>序列内部依赖关系，query，key，value均为当前序列</li><li>多头Attention，针对多个维度空间计算Attention，最后再合并多头Attention的计算结果</li></ul><img src="/2019/02/15/深度学习-NLP-Attention_is_all_you_need/sacle_dot_multi_head_attention.png" title="Fig.3 Scale dot Attention and Multi-Head Attention"><p>下面分别说明</p><h4 id="计算方法"><a href="#计算方法" class="headerlink" title="计算方法"></a>计算方法</h4><ul><li><p>score的计算</p><p>对于<code>Multi-Head Attention</code>，<strong>使用<code>query</code>，<code>key</code>，和<code>value</code>来表示待匹配的序列，原始序列和目标序列</strong>，如果要捕获序列内部的依赖关系，存在关系为$query=key=value=sequence$，对于序列依赖关系的计算在Multi-Head Attention中使用的点积的方式，也就是</p><script type="math/tex; mode=display">score(query,key)=query*key^T</script><p>因为<code>query</code>和<code>key</code>来自同一个序列，如果使用点积的方式会存在当<code>query</code>和<code>key</code>的维度变大的时候二者的点乘结果要么很大要么很小，在这里引入了Scale dot product Attention的方式，算是对DotAttention的一种修正。</p></li><li><p>Scale dot product Attention</p><p>假设query和key均为标准正太分布，并且存在关系式</p></li></ul><script type="math/tex; mode=display">Var(\sum_{i=1}^{m}X_i)=\sum_{i=1}^{m}Var(X_i)</script><p>​    也就是数据和的方差和方差的和是相等的，那么对于$socre$的计算过程中就会存在</p><script type="math/tex; mode=display">query*key^T=\sum_{i=1}^{dk}query_i*key_i</script><p>​    其中$d_k$对应着序列的维度</p><p>​    得到的期望为0，方差为$d_k$，如果$d_k$很大的话，那么內积的总和就会很大，使得<code>softmax</code>计算之后就会变成非零即1。</p><p>​    对<code>score</code>进行缩放，这样<code>score</code>的计算变成了</p><script type="math/tex; mode=display">score(query,key)=\frac{query*key^T}{\sqrt{d_k}}</script><p>​    也就是对传统Attention score计算的基础上，对计算结果进行了缩放，在Attention score计算时，使用的有相加的方式或者前文所述的点乘的方式，相比较而言两者效果差不多，但是使用点乘的方式只是用一步操作，而使用相加的方式要进行尺度缩放之后相加，因此<strong>点乘的方式运行的时候会更快</strong>，在序列维度$d_k$不大的情况下，二者效果可以认为一致，但是当$d_k$变大的时候，对于点乘的方式就会存在结果过大或者过小的问题，因此需要进行缩放，前文中对此已经进行了一定叙述。</p><ul><li><p>Attention weight的计算</p><p>在得到<code>Attention score</code>之后同样使用<code>softmax</code>的方式对<code>socre</code>进行归一化处理，得到<code>Attention weight</code>，可以认为这就是query和key之间的依赖的分布，将得到的结果和value点乘，就得到了被更新之后的序列</p><p>整个计算公式如下所示</p></li></ul><script type="math/tex; mode=display">Attention(query,key,value)=softmax(\frac{query*key^T}{\sqrt{d_k}})*value</script><p>​    和传统的Encoder-Decoder Attention不同的是，这里不再计算contex，而是对value进行更新，去掉了原来的求和操作。</p><h4 id="Multi的解释"><a href="#Multi的解释" class="headerlink" title="Multi的解释"></a>Multi的解释</h4><p>之所以叫做多头<code>Attention</code>，是相比较传统的<code>Attention</code>而言的，<strong>传统<code>Attention</code>对整个序列只进行一次计算，并且是直接计算整个序列的所有维度，多头表示针对序列的不同维度空间进行计算，对整个维度空间进行线性投影，投影到几个相同的更小的维度空间，并对该维度空间进行<code>Attention</code>计算</strong>，实际上在实现的过程中是很简单的，下面进行阐述。</p><p>假设序列的维度是$d_{model}$，如果想在$n$个维度空间下进行<code>Attention</code>的计算，那么可以得到每个维度空间下的维度为$d_{feature}$，并且满足关系式$d_{model}=n*d_{feature}$，根据$d_{model}$得到$d_{feature}$的方法使用线性变换的方式得到，其中线性变换的参数是学习参数</p><script type="math/tex; mode=display">d_{feature}=w*d_{model}</script><p>如此重复$n$次，可以得到$n$个不同的$d_{feature}$，将每个$d_{feature}$进行<code>Attention</code>计算，并将$n$个计算结果在维度方向上拼接，就得到了<code>Multi-Head Attention</code>的输出，也就是如图三中所显示的那样。</p><h3 id="ResidualConnect-amp-LayerNormal"><a href="#ResidualConnect-amp-LayerNormal" class="headerlink" title="ResidualConnect &amp; LayerNormal"></a>ResidualConnect &amp; LayerNormal</h3><p>在模型前向传播过程中，每个<code>Transformer block</code>中存在两个部分，一个部分就是前文所述的<code>Multi-head Attention</code>，另一个就是一个简单的前馈神经网络FFD。</p><p>在完成<code>Multi-head Attention</code>到传递至<code>FFD</code>之间使用看了残差连接和层归一化的方法，如图4所示</p><img src="/2019/02/15/深度学习-NLP-Attention_is_all_you_need/residual_layernormal.png" title="Fig.4 residual_layernormal"><p><strong>残差连接，使得模型在训练时，微小的变化可以被注意到，更多的可以查看残差网络相关论文及文章，本人在其他<a href="http://xiaofengshi.com/2018/11/23/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-%E5%9B%BE%E5%83%8F%E8%AF%86%E5%88%AB/">文章</a>中已有所述</strong></p><p><strong>层归一化，最常和<code>batch normalization</code>进行比较，<code>layer normalization</code>的优点在于它是独立计算的，也就是针对单一样本进行正规化，<code>batch normalization</code>则是针对各维度，因此和batch size有所关联。</strong></p><p>归纳出数学公式就是</p><script type="math/tex; mode=display">y=LayerNormal(x+Sublayer(x))</script><p>其中<code>Sublayer</code>​对应着<code>Multi-Head Attention</code>和一个前馈神经网络，如图四中所示</p><h3 id="Masked-Multi-head-Attention"><a href="#Masked-Multi-head-Attention" class="headerlink" title="Masked Multi-head Attention"></a>Masked Multi-head Attention</h3><p><code>Decoder</code>的运作模式和<code>Encoder</code>大同小异，也都是经过<code>residual connections</code>再到<code>layer normalization</code>。<code>Encoder</code>中的<code>SelfAttention</code>在计算时，<code>key</code>, <code>value</code>, <code>query</code>都是来自<code>Encoder</code>前一层的输出，<code>Decoder</code>亦然。</p><p>不同的是，在<code>Decoder</code>阶段使用的是添加<code>Mask</code>的多头<code>Attention</code>，来计算目标序列的内部依赖关系，<strong><code>Mask</code>是为了避免在解码的时候，还在翻译前半段时，就突然翻译到后半段的句子，会在计算<code>SelfAttention</code>时的<code>softmax</code>前先<code>mask</code>掉未来的位置(设定成-∞)。这个步骤确保在预测位置<code>i</code>的时候只能根据<code>i</code>之前位置的输出，其实这个是因应<code>Encoder-Decoder Attention</code> 的特性而做的配套措施，因为<code>Encoder-Decoder Attention</code>可以看到<code>Encoder</code>的整个句子</strong>。</p><h3 id="Encoder-Decoder-Attention"><a href="#Encoder-Decoder-Attention" class="headerlink" title="Encoder-Decoder Attention"></a>Encoder-Decoder Attention</h3><p>该步骤是为了将目标序列和输入序列进行对齐，也就是常用的<code>Attention</code>结构，在计算的时候<code>query</code>对应着<code>Decoder</code>的<code>SelfAttention</code>的结果，而<code>key</code>和<code>value</code>对应着<code>Encoder</code>的<code>SelfAttention</code>的结果，在此不再赘述，可以参考另一文章<a href="http://xiaofengshi.com/2018/11/27/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-Python-tutorial-LSTM-GRU-Attention/">深度学习-PythonTutorial_LSTM_GRU_Attention_LNLSTM_LNGRU</a></p><h3 id="Final-Layer-and-Softmax"><a href="#Final-Layer-and-Softmax" class="headerlink" title="Final Layer and Softmax"></a>Final Layer and Softmax</h3><p>在<code>Encoder-Decoder</code>之后得到了整个序列的输出，经过线性连接维度转换，实际上就是将最终得到的维度控制在和整个字表的大小一致的情况，采用<code>softmax</code>计算得分，选择得分最大的作为当前点的输出，在论文中使用的贪心的方法直接对每个点进行输出，当然也可以使用集束搜索的方法，设置输出的备选范围，拿到每个点上输出最大的<code>k</code>个，之后再使用这几个计算下一个单位，这样就可以得到累计概率，最后输出累计概率最大的一个序列语句，这显然就是一种动态规划的思想。</p><h2 id="overview"><a href="#overview" class="headerlink" title="overview"></a>overview</h2><p>整个过程说的比较简单，实际上可以记住<code>Transformer</code>的模型结构图，内部的组件包括position encoding，Multi-head Attention，residual connect，layernormal，masked-Multi-head Attention，Encoder-deocder Attention，以及最后的linear和softmax等，每个组件都不复杂，最核心的就是位置编码和Multi-Head Attention，masked Multi-head Attention了。</p><p>可以结合参考文献中的的<a href="https://github.com/keitakurita/Practical_NLP_in_PyTorch/blob/master/deep_dives/transformer_from_scratch.ipynb" target="_blank" rel="noopener">jupyter notebook</a>进行运行和测试。</p><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul><li><a href="https://arxiv.org/abs/1706.03762" target="_blank" rel="noopener">paper: Attention Is All You Need</a></li><li><a href="http://jalammar.github.io/illustrated-transformer/" target="_blank" rel="noopener">blog:The Illustrated Transformer</a></li><li><a href="https://wallstreetcn.com/articles/3417279" target="_blank" rel="noopener">从Seq2seq到Attention模型到Self Attention</a></li><li><a href="http://mlexplained.com/2017/12/29/Attention-is-all-you-need-explained/" target="_blank" rel="noopener">Paper Dissected: “Attention is All You Need” Explained</a><ul><li><a href="https://github.com/keitakurita/Practical_NLP_in_PyTorch/blob/master/deep_dives/transformer_from_scratch.ipynb" target="_blank" rel="noopener">jupyter: Building the Transformer from Scratch</a></li></ul></li><li><a href="http://nlp.seas.harvard.edu/2018/04/03/Attention.html" target="_blank" rel="noopener">The Annotated Transformer</a><ul><li><a href="https://github.com/harvardnlp/annotated-transformer" target="_blank" rel="noopener">code</a></li></ul></li><li><a href="https://mchromiak.github.io/articles/2017/Sep/12/Transformer-Attention-is-all-you-need/#.XGoc0JMzZZh" target="_blank" rel="noopener">The Transformer-Attention is all you need</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Transformer是在2017年的Google发布的论文《Attention is all you need》中提出，主要用于解决RNN相关网络无法捕获序列的长期依赖，以及网络无法并行化的问题，其网络结构示意图如下&lt;/p&gt;
&lt;img src=&quot;/2019/02/15/深度学习-NLP-Attention_is_all_you_need/transformer_model.jpeg&quot; title=&quot;transformer_model&quot;&gt;
&lt;p&gt;从示意图中可以看出在Transformer中包含多个组件，分别为Multi-Head Attention, Masked Multi-Head Attention, Add, Norm, Feed Forward, Position Encoding，会在接下来的内容中结合Transformer的前向传播过程依次解释。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="seq2seq" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/seq2seq/"/>
    
    
      <category term="深度学习，NLP" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%EF%BC%8CNLP/"/>
    
      <category term="Transformer" scheme="http://xiaofengshi.com/tags/Transformer/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-CTC算法原理</title>
    <link href="http://xiaofengshi.com/2019/02/14/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-CTC%E7%AE%97%E6%B3%95%E5%8E%9F%E7%90%86/"/>
    <id>http://xiaofengshi.com/2019/02/14/深度学习-CTC算法原理/</id>
    <published>2019-02-14T02:47:32.000Z</published>
    <updated>2020-02-03T13:34:13.732Z</updated>
    
    <content type="html"><![CDATA[<p>本来以前深入看过CTC的相关论文，前段时间有人问我相关算法原理，瞬间懵逼，以前看过的东西愣是一点没记起来，这就很尴尬了，本人在ocr任务的文字识别中还用过相关的解码方式，一定要搞明白为什么这种方法是可行的。在这里对CTC进行分析和总结，还是验证是否掌握的最好方法就是看能否做到有效的输出，主要参考论文和相关讲解博客。</p><p>先上图</p><img src="/2019/02/14/深度学习-CTC算法原理/CTC.png" title="CTC对语音进行对齐与文本对齐方式比较"><a id="more"></a><h2 id="NLP解码方式"><a href="#NLP解码方式" class="headerlink" title="NLP解码方式"></a>NLP解码方式</h2><p>在NLP相关任务中，如机器翻译，语音识别中，输入和输出均为序列，目前主要存在着两种模型架构方式，<strong>一种是基于seq2seq的模型框架（目前的主流方式），另一种是基于CTC的解码方式(方法更老一些，在一些任务重仍然在使用，语音识别，不定长文字识别等)。</strong></p><hr><ul><li>Seq2seq</li></ul><img src="/2019/02/14/深度学习-CTC算法原理/nmt-model-fast.gif" title="nmt-model"><p>如图中所示，对于seq2seq框架分为encoder和decoder，encoder部分对输入序列进行编码，decoder部分对编码之后的序列进行解码输出。使用seq2seq的方式可以解码输入序列和输出序列不一致的任务，并且不需要对训练数据的label进行对于，只需要保证长度一致即可，例如，输入的是一段语音，输出的是对应的文字，一般情况下输入的序列长度和输出的序列长度是不一样的，在seq2seq模型中，首先将输入的语音序列进行mfcc提取因素，之后进行编码，编码之后的序列长度和输入的序列长度相同；在传统的seq2seq中使用编码之后的最后一个时间步的隐藏层输出(认为该隐藏层中保存着输入序列的全部信息，显然对序列较长的情况下，会造成大量的信息丢失，因此也就有了之后的attention机制)作为decoder的初始输入，输出的序列长度由数据中的label的序列长度决定，这样就可以解决输入输出序列长度不同的情况，并且不需要对数据进行对齐，只需要知道输入的语音中对应的文字是什么，而不需要具体知道每个音节是什么。</p><hr><ul><li>CTC</li></ul><p>另一种解码方式是基于CTC的解码策略，在没有提出CTC策略之前，对于语音识别任务，要对输入的语音和目标文本进行对齐，这就需要繁琐的特征工程。而CTC策略就是针对该任务提出的，不需要对输入的序列进行label对齐，将原来的单音节的正确率的计算转义到输出序列的正确率计算上，比较的是输入的序列数据经过神经网络之后得到的预测输出和真实的输出之间的差距。</p><p><strong>此外有一点需要格外明确，CTC只能处理输出序列比输入序列短的任务</strong></p><h2 id="CTC详解"><a href="#CTC详解" class="headerlink" title="CTC详解"></a>CTC详解</h2><h3 id="参数说明"><a href="#参数说明" class="headerlink" title="参数说明"></a>参数说明</h3><p>如下这些参数会在下面介绍中出现，再次先进行一下声明，本文中所用参数符号和论文一致。</p><ul><li><p>输入</p><p>输入样本数据$x$</p><p>输入样本数据$T$</p></li><li><p>输出集合</p><p>$L$正常的label</p><p>$LL=L \cup \{blank \}$CTC输出的label全集</p></li><li><p>输出序列</p><p>$\pi$输出的序列路径</p><p>$I$输出的label序列</p><p>$B$路径多对一映射关系，将不同的路径去除空格去除重复字符</p></li><li><p>概率说明</p><p>$y_k^t$在时间步t输出k的概率</p><p>$u_k^t$在时间步t的在节点k上神经网络的输出，满足关系式$y_k^t=\exp u_k^t$</p><p>$p(\pi|x)= \prod_{t=1}^{T}y_{\pi_t}^t$表示在输入数据为x的情况下输出路径$\pi$的概率</p><p>$p(I|x)= \sum_{ \pi \in B^{-1}(I)} p(\pi |x)$表示输出label序列的概率是满足$B$变换条件的多条路径的概率之和</p></li><li><p>前向传播和后向传播</p><p>$\alpha _t(s)$表示时刻t在节点s的前向概率值</p><p>$\beta_t(s)$表示时刻t在节点s的后向概率值</p></li></ul><p>在语音识别任务中，输入的序列为$x$，对应的长度为$T$，定义一个神经网络$N_\omega(x)$来代表对输入数据的处理过程，实现对输入序列的特征提取和非线性变换，使用$y=N_\omega(x)$表示神经网络经过<code>softmax</code>标准化之后的输出，令$y_k^t$表示在时间步t输出为$k$的概率，$k$属于整个字表和观察字符的并集中。使用$L$表示整个字表，那么在<code>CTC</code>中在原字表基础上加入一个空格字符表示观察字符，由此，模型的字表为$LL=L \cup \{blank \}$，满足关系式$k \in LL$。</p><p>对于输出的$y_k^t$满足关系式$\sum _{t-1}^T y_k^t=1$，此处使用的是<code>softmax</code>进行处理。具体过程如下图所示。</p><img src="/2019/02/14/深度学习-CTC算法原理/mfcc_dnn_ctc.png" title="mfcc_dnn_ctc"><p>给定数据$x$，预测的序列为$\pi$，也就是在每个时间步预测的结果的连乘</p><script type="math/tex; mode=display">p(\pi |x)= \prod_{t=1}^{T}y_{\pi_t}^t, \:\:\forall \pi \in LL^{T}</script><h3 id="路径-pi-和-B-变换"><a href="#路径-pi-和-B-变换" class="headerlink" title="路径$\pi$和$B$变换"></a>路径$\pi$和$B$变换</h3><p>使用$\pi$表示经过预测输出的序列，表示一条由$LL$中的元素组成的长度为$T$的路径，而对于$B$变换为一个多对一的映射函数，满足</p><script type="math/tex; mode=display">B(a-ab-)=B(-aa--abb)=aab</script><p>将输入的字符串中的空格和重复字符删除来实现对输入数据的压缩，使用$I \in L^{\leq T}$表示给定序列的<code>label</code>，<strong>对于模型的预测输出有很多种，输出路径经过$B$变换之后与给定<code>label</code>的路径有很多，由此可以得到给定输入数据$x$之后模型的预测输出与<code>label</code>一致的概率就等于所有满足经过$B$变换之后与给定<code>label</code>相同的可能路径的总和，</strong>满足如下关系式</p><script type="math/tex; mode=display">p(I|x)=\sum_{\pi \in B^{-1}(I)}p(\pi |x)</script><p>其中$B^{-1}(I)$表示路径经过变化之后和给定的label一致，也就是满足$B(\pi)=I$</p><p>假如给定的<code>label</code>为“nihao”,那么可能的路径为</p><p><strong>“nniihhaaaaaoooo”</strong></p><p><strong>“nnnnniiiihhhaaao”</strong></p><p><strong>“nniiiiiihhhhhaaao”</strong>等等</p><p>路径的长度均为$T$，这些路径经过$B$变换之后都能变为给定的<code>label</code>，也就是都可以变为<strong>“nihao”</strong>。因此目标函数就应该是在给定输入序列的情况下输出满足$B$变换之后与给定的<code>label</code>相同所有序列的概率之和的最大值，也就是使得预测的序列跟给定的<code>label</code>最大概率</p><script type="math/tex; mode=display">\arg \max_{I \in L^{\leq T}}p(I|x)=\max \sum_{B( \pi)==label}p(\pi |x)</script><p>但是这里存在一个问题，那就是对于给定的<code>label</code>，那么满足B变换之后和给定<code>label</code>相同的可能的序列的数量实在是太多。</p><p>输入序列的长度为$T$，整个词表的大小为$L$，当前<code>label</code>中包含的的<code>label</code>的数量为$n$，那么可能的序列数量为$C_{T-1}^{n}$，这么多的数量显然是无法计算的，并且随着输入序列的长度的增加，计算量指数级增加。</p><h3 id="CTC模型的训练"><a href="#CTC模型的训练" class="headerlink" title="CTC模型的训练"></a>CTC模型的训练</h3><h4 id="forward前向预测过程"><a href="#forward前向预测过程" class="headerlink" title="forward前向预测过程"></a>forward前向预测过程</h4><p>为了保证模型能够进行训练，使用了DP动态规划的方法。引入一个变量$\alpha_{t}(s)$表示从初始时刻到当前时刻$t$所有可能的正向路径的概率之和(到当前时刻之前输出的序列经过B变换之后和给定的<code>label</code>之间是相同的)，公式如下</p><script type="math/tex; mode=display">\alpha_{t}(s)=\sum_{\pi \in N^T，B(\pi _{1:t})=I_{1:s}} \prod _{t^{*}=1}^t y_{\pi _{t^*}}^{t^*}</script><p>这显然就是一个动态规划的问题，如果在时刻t的预测的字符为$s$，那么在上一个时刻可能是字符$s$或者可能是$s-1$的，也就是满足递归关系式，其中$s$表示在label中的第$s$个字符</p><script type="math/tex; mode=display">\alpha_{t}(s)=\alpha_{t-1}(s)+\alpha_{t-1}(s-1)</script><p>在<code>CTC</code>处理中，为了区分实现对label字符之间的分割，对<code>label</code>进行了插入空格符的处理，在给定的<code>label</code>的前后及相邻字符之间插入空格符，新的<code>label</code>变为$\widetilde {I}$，那么新的<code>label</code>为$\widetilde {I}$的长度变为$2|I|+1$。</p><p>在前向传播整个生成过程为从左向右生成，时间步1生成的字符只允许为空格(<strong>使用b表示</strong>)$y_b^1$，或者为原来<code>label</code>中的第一个字符$y_{I_1}^1$，因为在时间步$1$的左侧已经不存在其他的字符预测，因此在时间步$1$上，进行了如下的限制：</p><script type="math/tex; mode=display">\begin{align}& \alpha_{1}(1)=y_b^1 \\& \alpha_{1}(2)=y_{I_{1}}^1 \\& \alpha_{1}(s)=0,\:\:\forall s>2\end{align}</script><p>在其他时刻满足如下关系式</p><script type="math/tex; mode=display">\begin{equation}\alpha_{t}(s)= \left\{\begin{array}{}& [{\alpha}_{t-1}(s)+\alpha_{t-1}(s-1)] y_{\widetilde {I}_s}^t  & if \:\:\:\widetilde {I}_s=blank \:\:\: or \:\:\: \widetilde {I}_{s-2}=\widetilde {I}_s  \\& [({\alpha}_{t-1}(s)+\alpha_{t-1}(s-1)+\alpha_{t-1}(s-2)]y_{\widetilde {I}_s}^t & otherwise\end{array}\right. \end{equation}</script><img src="/2019/02/14/深度学习-CTC算法原理/ctc_forward.png" title="CTC_FORWARD"><p>对上面的公式进行说明：</p><p>由于对原始的<code>label</code>进行了插入空格的处理，</p><p>例如原始为<strong>“abbd”</strong>，经过处理之后变成了<strong>“-a-b-b-d-”</strong>，<code>label</code>发生了变化。</p><p>在计算$\alpha_t(s)$时，对于时刻$t$取到新的<code>label</code>中第$s$个字符的计算要进行特殊处理，如果第s个字符是空格，或者当前字符$s$和$s-2$字符相等，那么当前$t$取到字符$s$，上一个时刻只能取到字符s或者s-1，也就对应着方程组中第一个式子，即$\alpha_{t}(s)=\alpha_{t-1}(s)+\alpha_{t-1}(s-1)$</p><p>使用<strong>“-a-b-b-d-”</strong>举例说明</p><ul><li>当在时刻$t$取到字符第二个$b$的时候，那么上个时刻可能取到$b$之前的一个空格，或者当前的第$2$个$b$，只有这样，经过$B$变换，才能得到“bb”这样的输出；</li><li>如果$t$时刻取到了空格字符，那么上一个时刻只能取到前一个字符或者当前同样位置的空格；</li></ul><p>当然也存在另外的情况，那就是当前时刻取到的不是空格字符并且当前字符$s$也不和$s-2$相同，那么在时刻$t$取到字符$s$,在上一个时刻可能取到$s,s-1,s-2$中的三种可能，即$ \alpha_t(s)= \alpha_{t-1}(s)+\alpha_{t-1}(s-1)+\alpha_{t-1}(s-2)$</p><p>同样使用例子<strong>“-a-b-b-d-”</strong>说明，</p><ul><li>如果当前时刻取到的既不是空格字符，也不满足$I_s==I_{S-2}$，在例子中，可以看成在时刻t取到字符“d”那么上一课时刻可能取到的是当前位置的“d”，也有可能是上一位的空格字符，也有可能是再上一位的“b”，也就是对应着公式中的第二种情况</li></ul><p>在前向过程中，传播过程如图所示</p><img src="/2019/02/14/深度学习-CTC算法原理/recurrent_ctc_forward.jpg" title="ctc_forward_recurrent"><p>红色线所示为第一种的情况，黑色线所示为第二种情况。经过上面所述的前向传播迭代，最终可以得到在给定输入数据x的情况下得到目标label序列的概率，也就是</p><script type="math/tex; mode=display">p(I|x)=\alpha_T(|\widetilde{I}|)+\alpha_T(|\widetilde{I}|-1)</script><p>也就是在最后的时间步$T$，预测的只能是$\widetilde {I}$中的最后一个字符（空格符）或倒数第二个字符（真实字符）。</p><h4 id="反向传播"><a href="#反向传播" class="headerlink" title="反向传播"></a>反向传播</h4><p>和前向传播过程相同，将箭头反转就可以得到<code>CTC</code>的反向传播过程，同样定义$\beta_t(s)$表示在时刻$t$，预测序列取到的字符为<code>label</code>中第<code>s</code>个字符的所有可能的路径的概率之和</p><script type="math/tex; mode=display">\beta_{t}(s)=\sum_{\pi \in N^T，B(\pi _{t:T})=I_{s:|I|}} \prod _{t^{*}=t}^T y_{\pi _{t^*}}^{t^*}</script><p>那么存在的递归关系式实际上和前向过程相同，就是将$t-1$改变成$t+1$即可。同样对于左右侧的预测输出，必须是最后一个空格或者最后<code>label</code>中的最后一个字符，公式如下所示</p><script type="math/tex; mode=display">\begin{align}& \beta_{T}(|\widetilde {I}|)=y_b^T \\& \beta_{T}(|\widetilde {I}|-1)=y_{I_{|I|}}^T \\& \beta_{T}(s)=0,\:\:\forall s<|\widetilde {I}|-1\end{align}</script><p>对于小于时间$T$的情况，公式如下所示</p><script type="math/tex; mode=display">\begin{equation}\beta_{t}(s)= \left\{\begin{array}{}& [{\beta}_{t+1}(s)+\beta_{t+1}(s+1)] y_{\widetilde {I}_s}^t  & if \:\:\:\widetilde {I}_s=blank \:\:\: or \:\:\: \widetilde {I}_{s+2}=\widetilde {I}_s  \\& [({\beta}_{t+1}(s)+\beta_{t+1}(s+1)+\beta_{t+1}(s+2)]y_{\widetilde {I}_s}^t & otherwise\end{array}\right. \end{equation}</script><p>整个后向的传播过程实际上就是前向传播过程的箭头反转</p><h4 id="存在的问题"><a href="#存在的问题" class="headerlink" title="存在的问题"></a>存在的问题</h4><p>由于序列预测过程中，按照上述的递归过程，每个时间步上对结果的预测时基于上一步预测的结果，<strong>为了求得序列的概率，因此这就是一个概率连乘的情形$\prod _{t^{<em>}=1}^t y_{\pi _{t^</em>}}^{t^*}$，非常多个小于1的数相乘，在计算时很容易造成数据下溢，导致无法计算。</strong>在论文中提出的解决方式是，在每个时刻t预测得到$\alpha_t(s)$之后进行尺度缩放，将当前时刻能够取到的所有$\alpha_t(s)$求和作为当前时刻取得的所有序列的概率之和，之后再当前时刻得到的每个$\alpha_t(s)$与和概率作比作为下一层的概率进行写一个时刻的迭代</p><script type="math/tex; mode=display">\begin{align}&C_t=\sum_s\alpha_t(s)\\& \hat {\alpha}_t(s)=\frac{\alpha_t(s)}{C_t}\end{align}</script><p>其中$\hat {\alpha}_t(s)$代表经过缩放之后进行下一时刻传递计算的概率</p><p>加入在时间步$t$预测的字符包含$s,s-1,s-2$三种</p><script type="math/tex; mode=display">\begin{align}& \alpha_t(s)=0.05 \\& \alpha_t(s-1)=0.03 \\& \alpha_t(s-2)=0.08 \\\end{align}</script><p>因为概率是从上一步基于上面所述的正向传播的传递方式传递过来的，因此概率值会非常小，在此处对得到的概率进行缩放，但是并不影响当前时间步预测的不同字符之间的相对概率的大小（因为在此处的概率的变化是线性的），因此对后续时间步的计算不会产生负面影响。</p><p>对反向传播进行同样的缩放</p><script type="math/tex; mode=display">\begin{align}& D_t=\sum_{s}\beta_t(s) \\& \hat{\beta}_{t}(s)=\frac{\beta_t(s)}{D_t}\end{align}</script><h3 id="优化目标"><a href="#优化目标" class="headerlink" title="优化目标"></a>优化目标</h3><p>在训练的过程的优化目标就是最大化似然估计，对于给定的输入数据为$x$，得到的预测序列经过$B$变换之后与标准<code>label</code>相同的概率，使用最大似然估计的方法得到优化目标</p><script type="math/tex; mode=display">L(S)=\sum_{(x,z) \in S}L(x,I)</script><p>其中$S$对应着整个训练集，$x$输入的样本，$I$为对应的label</p><p>针对每个样本，对于给定样本$x$，输出数据与<code>label</code>相等的概率为</p><script type="math/tex; mode=display">L(x,I)=- \ln p(I|x)</script><p>使用对数似然是为了将概率连乘变为连加。</p><p>在分析前向传播和后向传播是，定义了两个表示在时间步序列概率的式子$\alpha_{t}(s)$和$\beta_{t}(s)$，对两个式子进行相乘即可表示整个序列的概率</p><script type="math/tex; mode=display">\alpha_{t}(s)\beta_{t}(s)=\sum_{\pi \in N^T，B(\pi)=I} y_{\widetilde{I}_{s}}^t \prod _{t=1}^T y_{\pi _{t}}^{t}</script><p>表示预测的输出序列经过B变换之后和给定的label相同的概率之和</p><p>由于存在关系式</p><script type="math/tex; mode=display">p(\pi |x)= \prod_{t=1}^{T}y_{\pi_t}^t, \:\:\forall \pi \in LL^{T}</script><p>因此对上面的式子进行整理可以得到如下</p><script type="math/tex; mode=display">\begin{align}& \alpha_{t}(s)\beta_{t}(s) = & \sum_{\pi \in N^T，B(\pi)=I} y_{\widetilde{I}_{s}}^t \prod _{t=1}^T y_{\pi _{t}}^{t} \\& \frac{\alpha_{t}(s)\beta_{t}(s)}{y_{\widetilde{I}_{s}}^t}  = &\sum_{\pi \in N^T，B(\pi)=I}  \prod _{t=1}^T y_{\pi _{t}}^{t} \\\because & & p(\pi |x)= \prod_{t=1}^{T}y_{\pi_t}^t \\\therefore  &\frac{\alpha_{t}(s)\beta_{t}(s)}{y_{\widetilde{I}_{s}}^t} =  & \sum_{\pi \in N^T，B(\pi)=I} p(\pi |x) \end{align}</script><p>有因为存在着</p><script type="math/tex; mode=display">p(I|x)=\sum_{\pi \in B^{-1}(I)}p(\pi |x)</script><p>可以得到</p><script type="math/tex; mode=display">p(I|x)=\sum_{s=1}^{|\widetilde {I}|}\frac{\alpha_{t}(s)\beta_{t}(s)}{y_{\widetilde{I}_{s}}^t}</script><p>上式就对应着优化目标，只考虑在时刻t经过节点k的路径，对上式求偏导</p><script type="math/tex; mode=display">\frac{\partial p(I|x)}{\partial y_k^t}=\frac{1}{(y_k^t)^2} \sum _{s \in label(1,k)} \alpha_t(s)\beta_t(s)</script><h3 id="误差反向传播"><a href="#误差反向传播" class="headerlink" title="误差反向传播"></a>误差反向传播</h3><p>整理前面部分的优化目标</p><script type="math/tex; mode=display">\begin{align}& L(S)=\sum_{(x,z) \in S}L(x,z)= \sum_{(x,z) \in S} - \ln p(I|x) \\\Rightarrow & \frac{\partial L(S)}{\partial y_k^t} = -\frac{\partial \ln(p(I|x))}{\partial y_k^t}=-\frac{1}{p(I|x)} \frac{\partial p(I|x)}{\partial y_k^t} \\& =-\frac{1}{p(I|x)} \frac{1}{(y_k^t)^2} \sum _{s \in label(1,k)} \alpha_t(s)\beta_t(s) \\& = -\frac{1}{\sum_{s=1}^{|\widetilde {I}|}\frac{\alpha_{t}(s)\beta_{t}(s)}{y_{\widetilde{I}_{s}}^t}} \frac{1}{(y_k^t)^2} \sum _{s \in label(1,k)} \alpha_t(s)\beta_t(s) \end{align}</script><p>因为在此处$y_k^t$对应的是在时刻$t$输出为$k$的概率，是根据神经网络的输出经过<code>softmax</code>变换之后得到的概率值，这是一个固定变换，没有学习参数需要反向更新，神经网络的输出为$u_k^t$，则存在着变换关系式</p><script type="math/tex; mode=display">y_k^t= \frac{\exp {u_k^t}}{ \sum_{k \in I} \exp u_k^t}</script><p>由此可以得到</p><img src="/2019/02/14/深度学习-CTC算法原理/deroate.jpg" title="求导过程"><p>因此对上面的优化目标进行再次整理为</p><script type="math/tex; mode=display">\begin{align}\frac{\partial L(S)}{\partial u_k^t} & =\frac{\partial L(S)}{\partial y_k^t}  \frac{\partial y_k^t}{\partial u_k^t} =y_k^t \frac{\partial L(S)}{\partial y_k^t} \\&= - y_k^t \frac{1}{\sum_{s=1}^{|\widetilde {I}|}\frac{\alpha_{t}(s)\beta_{t}(s)}{y_{\widetilde{I}_{s}}^t}} \frac{1}{(y_k^t)^2} \sum _{s \in label(1,k)} \alpha_t(s)\beta_t(s)  \\& = -\frac{1}{\sum_{s=1}^{|\widetilde {I}|}\frac{\alpha_{t}(s)\beta_{t}(s)}{y_{\widetilde{I}_{s}}^t}} \frac{1}{y_k^t} \sum _{s \in label(1,k)} \alpha_t(s)\beta_t(s) \end{align}</script><p>由此得到在训练的时候每一步对参数的更新，此时的$u_k^t$对应着神经网络的输出，对于之后的网络使用BPTT或者BP进行更新</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><ul><li><a href="https://distill.pub/2017/ctc/" target="_blank" rel="noopener">Sequence ModelingWith CTC</a></li><li><a href="https://blog.csdn.net/luodongri/article/details/80100297" target="_blank" rel="noopener">语音识别中的CTC算法的基本原理解释</a></li><li><a href="https://blog.csdn.net/xmdxcsj/article/details/51763886" target="_blank" rel="noopener">CTC公式推导</a></li><li></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本来以前深入看过CTC的相关论文，前段时间有人问我相关算法原理，瞬间懵逼，以前看过的东西愣是一点没记起来，这就很尴尬了，本人在ocr任务的文字识别中还用过相关的解码方式，一定要搞明白为什么这种方法是可行的。在这里对CTC进行分析和总结，还是验证是否掌握的最好方法就是看能否做到有效的输出，主要参考论文和相关讲解博客。&lt;/p&gt;
&lt;p&gt;先上图&lt;/p&gt;
&lt;img src=&quot;/2019/02/14/深度学习-CTC算法原理/CTC.png&quot; title=&quot;CTC对语音进行对齐与文本对齐方式比较&quot;&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="seq2seq" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/seq2seq/"/>
    
    
      <category term="nlp" scheme="http://xiaofengshi.com/tags/nlp/"/>
    
      <category term="不定长字符识别" scheme="http://xiaofengshi.com/tags/%E4%B8%8D%E5%AE%9A%E9%95%BF%E5%AD%97%E7%AC%A6%E8%AF%86%E5%88%AB/"/>
    
  </entry>
  
  <entry>
    <title>Boosting_And_Bagging</title>
    <link href="http://xiaofengshi.com/2019/01/25/Boosting-And-Bagging/"/>
    <id>http://xiaofengshi.com/2019/01/25/Boosting-And-Bagging/</id>
    <published>2019-01-25T08:09:43.000Z</published>
    <updated>2019-01-25T12:53:28.000Z</updated>
    
    <content type="html"><![CDATA[<p>在文章<a href="http://xiaofengshi.com/2018/11/09/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0-%E5%86%B3%E7%AD%96%E6%A0%91/">机器学习-决策树</a>中详细阐述了目前常用的决策树TD3,C4.5,CART，但是在实际应用中，一般很少使用单个树进行模型设计，因为单个数很难实现一个更全面的模型，由此引入了<strong>集成学习</strong>，俗话说，三个臭皮匠赛过诸葛亮，集成学习可以看成将多个弱学习器的组合，以期望达到比单个弱学习器更好的监督模型，集成学习的思想是即使某一个弱分类器得到了错误的预测那么其他的弱分类器也可以将错误纠正。</p><img src="/2019/01/25/Boosting-And-Bagging/overbview.png" title="integrated Model"><a id="more"></a><p>根据个体学习器的生成方式，目前的主要集成学习方法可以分为两大类</p><ul><li>个体学习器之间存在强依赖关系，必须串行生成的序列化方法</li><li>个体学习器之间不存在强依赖关系，可以同时生成的并行化方法</li></ul><p>对于前者代表的是Boosting，后者的代表是Bagging</p><h2 id="Boosting"><a href="#Boosting" class="headerlink" title="Boosting"></a>Boosting</h2><p><strong>和Bagging不同的是，Boosting主要针对模型的偏差进行优化，也就是提高模型的预测准确率，主要针对欠拟合进行优化，并且使用的是串行的训练方式，也就是要先训练一个学习器，基于上一个学习器的结果来训练第二个学习器，以此类推，对于Boosting来说，代表性的算法为</strong>AdaBoost<strong>和</strong>GBDT<strong>。</strong></p><h3 id="AdaBoost分类"><a href="#AdaBoost分类" class="headerlink" title="AdaBoost分类"></a>AdaBoost分类</h3><p>根据弱学习的学习误差率表现来更新训练样本的权重，使得之前弱学习器1学习误差率高的训练样本点的权重变高，使得这些误差率高的点在后面的弱学习器2中得到更多的重视。然后基于调整权重后的训练集来训练弱学习器2.，如此重复进行，直到弱学习器数达到事先指定的数目T，最终将这T个弱学习器通过集合策略进行整合，得到最终的强学习器。</p><p>对于AdaBoost分类算法的过程如下所示</p><img src="/2019/01/25/Boosting-And-Bagging/AdaBoost_cls.png" title="AdaBoost 分类算法"><p>对于Adaboost多元分类算法，其实原理和二元分类类似，最主要区别在弱分类器的系数上。比如Adaboost SAMME算法，它的弱分类器的系数：</p><script type="math/tex; mode=display">\alpha_m=\frac{1}{2} \log \frac{1-e_m}{e_m}+\log (R-1)</script><p>其中$R$为类别的数量，如果是二分类，则R=2和上述的二分类算法的弱分类器的系数是一致的。</p><h3 id="AdaBoost回归"><a href="#AdaBoost回归" class="headerlink" title="AdaBoost回归"></a>AdaBoost回归</h3><img src="/2019/01/25/Boosting-And-Bagging/AdaBoost_Reg.png" title="AdaBoost回归算法"><h3 id="AdaBoost优缺点"><a href="#AdaBoost优缺点" class="headerlink" title="AdaBoost优缺点"></a>AdaBoost优缺点</h3><p><strong>优点</strong></p><ol><li>Adaboost作为分类器时，分类精度很高</li><li>在Adaboost的框架下，可以使用各种回归分类模型来构建弱学习器，非常灵活。</li><li>作为简单的二元分类器时，构造简单，结果可理解。</li><li>不容易发生过拟合</li></ol><p><strong>缺点</strong></p><p>对异常样本敏感，异常样本在迭代中可能会获得较高的权重，影响最终的强学习器的预测准确性。</p><h3 id="GBDT-梯度提升决策树"><a href="#GBDT-梯度提升决策树" class="headerlink" title="GBDT-梯度提升决策树"></a>GBDT-梯度提升决策树</h3><p><strong>与AdaBoost不同，GBDT不再是为了更新样本权重，而是使用缩小残差的方法。</strong></p><h4 id="GBM-梯度提升算法"><a href="#GBM-梯度提升算法" class="headerlink" title="GBM-梯度提升算法"></a>GBM-梯度提升算法</h4><p>在监督学习中模型的优化目标是最小化损失函数的期望$E[L(y,f(x))]$，因此优化过程可以看成$f^*=\ argmin _{f}E[L(y,f(x))]$，目前常用的是基于梯度下降的方法。在GBDT中采用的是分层学习的方法，经过m个步骤得到最终的模型$f$，其中第$m$步学习一个较弱的模型$f_m$，在第$m+1$步时，不直接优化$f_m$，而是学习一个基本模型$h(x)$，使得其拟合残差项$y-f_m$，这样就是的$m+1$步的预测值$f_{m+1}=f_m+h(x)$更接近于真实值$y$，有点残差网络类似，经过这样的设计优化目标就是找到最小的残差$h(x)=f_{m+1}-f_m$，最终得到了函数空间中的一组$h(x)$使得$f(x)=\sum_{i=1}^{M} \gamma _{i} h_{i}(x)+const$，优化目标就是找到最优的$f(x)$使得误差最小，梯度提升的算法如下</p><img src="/2019/01/25/Boosting-And-Bagging/gbdt_alg.png" title="Gradient Boosting Algorithm"><p>对于GBM，理论上可以选择不同的学习算法作为基学习器，实际应用中，多数使用的是决策树作为基学习器，也就是GBDT。</p><h4 id="GBDT"><a href="#GBDT" class="headerlink" title="GBDT"></a>GBDT</h4><p>之所以选择决策树作为基学习器(通常为CART)这决策树算法本身的特性有很大的关系，决策树可以看做是一种if-then的规则集合，易于理解，可解释性强，预测速度快，对数据敏感，特征工程少，但是对于单颗树来说，容易产生过拟合的风险，这就要进行剪枝，这也是为什么引入了集成学习。GBDT机制单颗决策树的复杂度，降低单颗树的拟合能力，在通过梯度提升的方法集成多个决策树，最终实现解决过拟合的问题。抑制单颗决策树的复杂度的方法为</p><blockquote><p>抑制单颗决策树的复杂度的方法有很多，比如限制树的最大深度、限制叶子节点的最少样本数量、限制节点分裂时的最少样本数量、吸收bagging的思想对训练样本采样（subsample），在学习单颗决策树时只使用一部分训练样本、借鉴随机森林的思路在学习单颗决策树时只采样一部分特征、在目标函数中添加正则项惩罚复杂的树结构等。现在主流的GBDT算法实现中这些方法基本上都有实现，因此GBDT算法的超参数还是比较多的，应用过程中需要精心调参，并用交叉验证的方法选择最佳参数。</p></blockquote><h2 id="Bagging"><a href="#Bagging" class="headerlink" title="Bagging"></a>Bagging</h2><p><strong>对于Bagging主要是针对模型的方差进行优化，也就是提高模型的泛化能力。</strong></p><p>Bagging 使用bootstrap的方法从整体数据集中有放回的抽样得到N个数据集，bootstrap也成为自助法，目的是为了得到统计量的分布及置信区间。</p><ul><li>采用有放回抽样从原始样本中抽取一定数量的样本</li><li>根据抽取的样本计算想要得到的统计量</li><li>重复N次得到N个统计量</li><li>根据这N个统计量，得到统计量的置信区间</li></ul><p>在Bagging中采用有放回的抽样方法得到N个数据集，在每个数据集中学习一个模型，最后的预测模型结果使用N个模型的输出得到，对于分类问题，采用N个模型预测投票的方法，对于回归问题采用N个模型预测的平均值。</p><p>对于Bagging，多个训练集训练的学习器之间是相互独立的，可以进行并行训练，最常用的是随机森林(RF)，在随机森林算法中，使用随机的方式建立一个森林，森林由很多的决策树组成，随机森林的每一颗决策树之间是没有关联的。</p><h3 id="随机森林的构建"><a href="#随机森林的构建" class="headerlink" title="随机森林的构建"></a>随机森林的构建</h3><p>对于随机森林的构建步骤</p><ol><li><p>假设有N个样本，采用有放回抽样的方式选择N个样本，选择好的N个样本用来训练一个决策树，作为决策树根节点出的样本</p></li><li><p>假设每个样本都有M个属性，在决策树的每个节点需要分裂的时候，对于传统的决策树来数一般采用的是信息论的评价指标或者最小平常差损失，但是在随机森林中，随机的从这M个属性中选择出m个属性，满足条件$m&lt;&lt;M$。然后从这m个属性中采用决策树的的分类策略或回归策略，在这个属性自己种选择一个最优的属性来进行当前节点的划分</p></li><li><p>决策树形成过程中每个节点的划分都是按照上述方法进行划分(如果下一个节点选出来的属性刚刚是父节点分裂时用过的属性，则认为该节点已经达到了叶节点，因此当前节点无需分裂)，一直到不能分裂为止，整个决策树的形成过程无需进行剪枝</p></li><li><p>按照上述的1-3步骤建立多个决策树，如此构成了整个随机森林，多个决策树之间是相互独立的，可以进行分别单独训练</p></li></ol><p><strong>NOTE</strong>：在每个决策树的创建过程中存在两个随机过程</p><ul><li><strong>样本的随机性</strong>，采用有放回的抽样方式，总样本数量为N，放回抽样N次总共得到样本N个，由此得到的样本中肯定存在着重复的样本，当然也会有样本不会被抽取到，根据bootstrap的抽样方法，假设总样本集的数量为N，每个样本被抽取到的概率为$\frac{1}{N}$，那么在样本集中样本在N次采样中始终不被采样到的概率为$(1-\frac{1}{N})^N$,由此求极限可以得到$\lim _{N \rightarrow \infty} (1-\frac{1}{N})^N \mapsto \frac{1}{e} \approx 0.368$，可以认为初始数据集中约有$36 \%$的样本从未出现在采样的数据集中</li><li><strong>属性的随机性</strong>，在每个节点的划分时，不是针对所有的属性空间上而是针对一个属性空间的子集，该子集也是通过随机选择的方式从整个特种空间中选取。</li></ul><h3 id="随机森林的优缺点"><a href="#随机森林的优缺点" class="headerlink" title="随机森林的优缺点"></a>随机森林的优缺点</h3><p>优点</p><ul><li>在随机森林中，由于这两个随机性的加入，对单个决策树不进行剪枝的情况下有效避免了过拟合的风险</li><li>能够处理高维数据(样本属性很多)，并且不同进行特征选择，对数据集的适应性较强</li><li>在创建随机森林的时候，对generlization error使用的是无偏估计，模型泛化能力强</li><li>在训练过程中，能够检测到feature间的互相影响，$p_{ij}=\frac{a_{ij}}{N}$其中$a_{ij}$表示样本i和j出现在随机森林中同一个叶子节点的次数，N为随机森林中树的数量</li><li>训练速度快，可以得到变量重要性排序</li><li>容易进行并行训练</li></ul><p>缺点</p><ul><li><p>随机森林已经被证明在某些<strong>噪音较大</strong>的分类或回归问题上会过拟</p></li><li><p>对于有不同取值的属性的数据，取值划分较多的属性会对随机森林产生更大的影响，所以随机森林在这种数据上产出的属性权值是不可信的。</p></li></ul><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ol><li><a href="https://blog.csdn.net/qq_28031525/article/details/70207918" target="_blank" rel="noopener">RF,GBDT,XGBOOST</a></li><li><a href="https://blog.csdn.net/v_JULY_v/article/details/81410574" target="_blank" rel="noopener">通俗理解kaggle比赛大杀器xgboost</a></li><li><a href="https://zhuanlan.zhihu.com/p/39972832" target="_blank" rel="noopener">Adaboost算法</a></li><li><a href="https://en.wikipedia.org/wiki/Gradient_boosting" target="_blank" rel="noopener">Wiki: Gradient boosting</a></li><li><a href="https://zhuanlan.zhihu.com/p/29765582" target="_blank" rel="noopener">知乎：GBDT</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在文章&lt;a href=&quot;http://xiaofengshi.com/2018/11/09/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0-%E5%86%B3%E7%AD%96%E6%A0%91/&quot;&gt;机器学习-决策树&lt;/a&gt;中详细阐述了目前常用的决策树TD3,C4.5,CART，但是在实际应用中，一般很少使用单个树进行模型设计，因为单个数很难实现一个更全面的模型，由此引入了&lt;strong&gt;集成学习&lt;/strong&gt;，俗话说，三个臭皮匠赛过诸葛亮，集成学习可以看成将多个弱学习器的组合，以期望达到比单个弱学习器更好的监督模型，集成学习的思想是即使某一个弱分类器得到了错误的预测那么其他的弱分类器也可以将错误纠正。&lt;/p&gt;
&lt;img src=&quot;/2019/01/25/Boosting-And-Bagging/overbview.png&quot; title=&quot;integrated Model&quot;&gt;
    
    </summary>
    
      <category term="集成学习" scheme="http://xiaofengshi.com/categories/%E9%9B%86%E6%88%90%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="集成学习" scheme="http://xiaofengshi.com/tags/%E9%9B%86%E6%88%90%E5%AD%A6%E4%B9%A0/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-TextDetection</title>
    <link href="http://xiaofengshi.com/2019/01/23/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-TextDetection/"/>
    <id>http://xiaofengshi.com/2019/01/23/深度学习-TextDetection/</id>
    <published>2019-01-23T07:35:30.000Z</published>
    <updated>2020-01-31T06:30:14.133Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要对常用的文本检测模型算法进行总结及分析，有的模型笔者切实run过，有的是通过论文及相关代码的分析，如有错误，请不吝指正。</p><a id="more"></a><p>一下进行各个模型的详细解析</p><h2 id="CTPN-详解"><a href="#CTPN-详解" class="headerlink" title="CTPN 详解"></a>CTPN 详解</h2><p>代码链接：<a href="https://github.com/xiaofengShi/CHINESE-OCR" target="_blank" rel="noopener">https://github.com/xiaofengShi/CHINESE-OCR</a></p><p>CTPN是目前应用非常广泛的印刷体文本检测模型算法。</p><p>CTPN由fasterrcnn改进而来，可以看下二者的异同</p><div class="table-container"><table><thead><tr><th style="text-align:center">网络结构</th><th style="text-align:center">FasterRcnn</th><th style="text-align:center">CTPN</th></tr></thead><tbody><tr><td style="text-align:center">basenet</td><td style="text-align:center">Vgg16 ,Vgg19,resnet</td><td style="text-align:center">Vgg16,也可以使用其他CNN结构</td></tr><tr><td style="text-align:center">RPN预测</td><td style="text-align:center">basenet的predict layer使用CNN生成</td><td style="text-align:center">basenet之后使用双向RNN使用FC生成</td></tr><tr><td style="text-align:center">ROI</td><td style="text-align:center">模型适用于目标检测，为多分类任务，包含ROI及类别损失和BOX回归</td><td style="text-align:center">文本提取为二分类任务，不包含ROI及类别损失，只在RPN层计算目标损失及BOX回归</td></tr><tr><td style="text-align:center">Anchor</td><td style="text-align:center">一共9种anchor尺寸,3比例，3尺寸</td><td style="text-align:center">固定anchor宽度，高度为10种</td></tr><tr><td style="text-align:center">batch</td><td style="text-align:center">每次只能训练一个样本</td><td style="text-align:center">每次只能训练一个样本</td></tr></tbody></table></div><p>根据ctpn的网络设计，可以看到看到ctpn一般使用预训练的vggnet，并且只用来检测水平文本，一般可以用来进行标准格式印刷体的检测，在目标框回归预测时，加上回归框的角度信息，就可以用来检测旋转文本，比如EAST模型。</p><h3 id="代码分析"><a href="#代码分析" class="headerlink" title="代码分析"></a>代码分析</h3><h4 id="网络模型"><a href="#网络模型" class="headerlink" title="网络模型"></a>网络模型</h4><p>直接看<code>CTPN</code>的网络代码</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">VGGnet_train</span><span class="params">(Network)</span>:</span></span><br><span class="line">    <span class="comment"># 继承自NetWork,关与NetWork可以看这里：https://github.com/xiaofengShi/CHINESE-OCR/blob/master/ctpn/lib/networks/network.py</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, trainable=True)</span>:</span></span><br><span class="line">        self.inputs = []</span><br><span class="line">        self.data = tf.placeholder(tf.float32, shape=[<span class="keyword">None</span>, <span class="keyword">None</span>, <span class="keyword">None</span>, <span class="number">3</span>], name=<span class="string">'data'</span>)</span><br><span class="line">        self.im_info = tf.placeholder(tf.float32, shape=[<span class="keyword">None</span>, <span class="number">3</span>], name=<span class="string">'im_info'</span>)</span><br><span class="line">        self.gt_boxes = tf.placeholder(tf.float32, shape=[<span class="keyword">None</span>, <span class="number">5</span>], name=<span class="string">'gt_boxes'</span>)</span><br><span class="line">        self.gt_ishard = tf.placeholder(tf.int32, shape=[<span class="keyword">None</span>], name=<span class="string">'gt_ishard'</span>)</span><br><span class="line">        self.dontcare_areas = tf.placeholder(tf.float32, shape=[<span class="keyword">None</span>, <span class="number">4</span>], name=<span class="string">'dontcare_areas'</span>)</span><br><span class="line">        self.keep_prob = tf.placeholder(tf.float32)</span><br><span class="line">        self.layers = dict(&#123;<span class="string">'data'</span>: self.data, <span class="string">'im_info'</span>: self.im_info, <span class="string">'gt_boxes'</span>: self.gt_boxes,<span class="string">'gt_ishard'</span>: self.gt_ishard, <span class="string">'dontcare_areas'</span>: self.dontcare_areas&#125;)</span><br><span class="line">        self.trainable = trainable</span><br><span class="line">        self.setup()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">setup</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="comment"># 对于文本提议来说，类别为2，一类为为文字部分，另一类为背景</span></span><br><span class="line">        n_classes = cfg.NCLASSES</span><br><span class="line">        <span class="comment"># anchor的初始尺寸，论文中使用的是16</span></span><br><span class="line">        anchor_scales = cfg.ANCHOR_SCALES</span><br><span class="line">        _feat_stride = [<span class="number">16</span>, ]</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># base net is vgg16</span></span><br><span class="line">        <span class="comment"># 内部使用的函数</span></span><br><span class="line">        (self.feed(<span class="string">'data'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">64</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv1_1'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">64</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv1_2'</span>)</span><br><span class="line">            .max_pool(<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, padding=<span class="string">'VALID'</span>, name=<span class="string">'pool1'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">128</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv2_1'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">128</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv2_2'</span>)</span><br><span class="line">            .max_pool(<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, padding=<span class="string">'VALID'</span>, name=<span class="string">'pool2'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">256</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv3_1'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">256</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv3_2'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">256</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv3_3'</span>)</span><br><span class="line">            .max_pool(<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, padding=<span class="string">'VALID'</span>, name=<span class="string">'pool3'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">512</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv4_1'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">512</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv4_2'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">512</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv4_3'</span>)</span><br><span class="line">            .max_pool(<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, padding=<span class="string">'VALID'</span>, name=<span class="string">'pool4'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">512</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv5_1'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">512</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv5_2'</span>)</span><br><span class="line">            .conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">512</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'conv5_3'</span>))</span><br><span class="line">        <span class="comment"># RPN </span></span><br><span class="line">        <span class="comment"># 该层对上层的feature map进行卷积，生成512通道的的feature map</span></span><br><span class="line">        (self.feed(<span class="string">'conv5_3'</span>).conv(<span class="number">3</span>, <span class="number">3</span>, <span class="number">512</span>, <span class="number">1</span>, <span class="number">1</span>, name=<span class="string">'rpn_conv/3x3'</span>))</span><br><span class="line">        <span class="comment"># 卷积最后一层的的feature_map尺寸为batch*h*w*512</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 原来的单层双向LSTM</span></span><br><span class="line">        (self.feed(<span class="string">'rpn_conv/3x3'</span>).Bilstm(<span class="number">512</span>, <span class="number">128</span>, <span class="number">512</span>, name=<span class="string">'lstm_o'</span>))</span><br><span class="line">        <span class="comment"># bilstm之后输出的尺寸为(N, H, W, 512)</span></span><br><span class="line"></span><br><span class="line">        <span class="string">""" </span></span><br><span class="line"><span class="string">        和faster—rcnn相似，在ctpn的rpn网络中，使用双向lstm和全连接得到预测的</span></span><br><span class="line"><span class="string">        目标概率和回归框，在faster-rcnn中使用的是卷积的方式从basenet的最后一层生成</span></span><br><span class="line"><span class="string">        使用LSTM的输出来计算位置偏移和类别概率（判断是否是物体，不判断类别的种类）</span></span><br><span class="line"><span class="string">        输入尺寸为(N, H, W, 512)  输出尺寸（N, H, W, int(d_o)）</span></span><br><span class="line"><span class="string">        可以将这一层当做目标检测中的最后一层feature_map</span></span><br><span class="line"><span class="string">        rpn_bbox_pred--对于h*w的尺寸上，每一anchor上生成4个位置偏移量</span></span><br><span class="line"><span class="string">        rpn_cls_score--对于h*w的尺寸上，每一anchor上生成2个置信度得分，判断是否为物体</span></span><br><span class="line"><span class="string">            </span></span><br><span class="line"><span class="string">        """</span></span><br><span class="line">        (self.feed(<span class="string">'lstm_o'</span>).lstm_fc(<span class="number">512</span>, len(anchor_scales) * <span class="number">10</span> * <span class="number">4</span>, name=<span class="string">'rpn_bbox_pred'</span>))</span><br><span class="line">        (self.feed(<span class="string">'lstm_o'</span>).lstm_fc(<span class="number">512</span>, len(anchor_scales) * <span class="number">10</span> * <span class="number">2</span>, name=<span class="string">'rpn_cls_score'</span>))</span><br><span class="line"></span><br><span class="line">        <span class="comment"># generating training labels on the fly</span></span><br><span class="line">        <span class="comment"># output: rpn_labels(HxWxA, 2) rpn_bbox_targets(HxWxA, 4) rpn_bbox_inside_weights rpn_bbox_outside_weights</span></span><br><span class="line">        <span class="comment"># 给每个anchor上标签，并计算真值（也是delta的形式），以及内部权重和外部权重</span></span><br><span class="line">        (self.feed(<span class="string">'rpn_cls_score'</span>, <span class="string">'gt_boxes'</span>, <span class="string">'gt_ishard'</span>, <span class="string">'dontcare_areas'</span>, <span class="string">'im_info'</span>)</span><br><span class="line">            .anchor_target_layer(_feat_stride, anchor_scales, name=<span class="string">'rpn-data'</span>))</span><br><span class="line"></span><br><span class="line">        <span class="comment"># shape is (1, H, W, Ax2) -&gt; (1, H, WxA, 2)</span></span><br><span class="line">        <span class="comment"># 给之前得到的score进行softmax，得到0-1之间的得分</span></span><br><span class="line">        (self.feed(<span class="string">'rpn_cls_score'</span>)</span><br><span class="line">            .spatial_reshape_layer(<span class="number">2</span>, name=<span class="string">'rpn_cls_score_reshape'</span>)</span><br><span class="line">            .spatial_softmax(name=<span class="string">'rpn_cls_prob'</span>))</span><br><span class="line">        <span class="string">'''</span></span><br><span class="line"><span class="string">        # the below is the rcnn net model from faster_rcnn</span></span><br><span class="line"><span class="string">        # 后面的部分是fasterrcnn之后的ROIPooling部分</span></span><br><span class="line"><span class="string">        (self.feed('rpn_cls_prob').spatial_reshape_layer(len(anchor_scales) * 10 * 2, name='rpn_cls_prob_reshape'))</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        self.feed('rpn_cls_prob_reshape', 'rpn_bbox_pred', 'im_info').proposal_layer(</span></span><br><span class="line"><span class="string">            _feat_stride, anchor_scales, 'TRAIN', name='rpn_rois')</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        (self.feed('rpn_rois', 'gt_boxes').proposal_target_layer(n_classes, name='roi-data'))</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        # ========= RCNN ============</span></span><br><span class="line"><span class="string">        (self.feed('conv5_3', 'roi-data').roi_pool(7, 7, 1.0/16, name='pool_5')</span></span><br><span class="line"><span class="string">             .fc(4096, name='fc6').dropout(0.5, name='drop6')</span></span><br><span class="line"><span class="string">             .fc(4096, name='fc7').dropout(0.5, name='drop7')</span></span><br><span class="line"><span class="string">             .fc(n_classes, relu=False, name='cls_score').softmax(name='cls_prob'))</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        (self.feed('drop7').fc(n_classes*4, relu=False, name='bbox_pred'))</span></span><br><span class="line"><span class="string">        '''</span></span><br></pre></td></tr></table></figure><p>可以看到<code>CTPN</code>的网络结构有<code>FasterRcnn</code>改变而来，使用<code>vggnet</code>进行图像的特征提取，对得到的最后一层<code>featuremap</code>的尺寸为$[N,H,W,C]$，进行维度变换为$[NH,W,C]$成为序列，使用<code>BLSTM</code>得到的维度为$[NH,W,2D]$其中$D$为单向<code>RNN</code>的隐藏层节点数，转换维度为$[NHW,2D]$，使用全连接进行维度转换为$[NHW,C]$，最后再reshape成$[N,H,W,C]$，在这一步中，使用<code>RNN</code>对<code>CNN</code>之后的特征图进行特征图长度方向上的连接；接下来使用<code>lstm_fc</code>函数对<code>anchor</code>进行目标类别预测和边界回归框预测，在这一层的特征图上，每个点生成A个<code>anchor</code>，每个<code>anchor</code>存在目标类别预测和边界回归预测：对于回归预测，每个格点生成<code>2A</code>个目标预测；对于边界回归预测，每个格点生成<code>4A</code>个边界预测。</p><p>网络模型结构如下所示</p><img src="/2019/01/23/深度学习-TextDetection/ctpn_model.png" title="CTPN MODEL STRUCTURE"><h4 id="anchor生成及筛选"><a href="#anchor生成及筛选" class="headerlink" title="anchor生成及筛选"></a>anchor生成及筛选</h4><p>在整个模型中，<code>AnchorGen</code>处需要详细说明，这就是大名鼎鼎的RPN，下面结合代码说明：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br><span class="line">212</span><br><span class="line">213</span><br><span class="line">214</span><br><span class="line">215</span><br><span class="line">216</span><br><span class="line">217</span><br><span class="line">218</span><br><span class="line">219</span><br><span class="line">220</span><br><span class="line">221</span><br><span class="line">222</span><br><span class="line">223</span><br><span class="line">224</span><br><span class="line">225</span><br><span class="line">226</span><br><span class="line">227</span><br><span class="line">228</span><br><span class="line">229</span><br><span class="line">230</span><br><span class="line">231</span><br><span class="line">232</span><br><span class="line">233</span><br><span class="line">234</span><br><span class="line">235</span><br><span class="line">236</span><br><span class="line">237</span><br><span class="line">238</span><br><span class="line">239</span><br><span class="line">240</span><br><span class="line">241</span><br><span class="line">242</span><br><span class="line">243</span><br><span class="line">244</span><br><span class="line">245</span><br><span class="line">246</span><br><span class="line">247</span><br><span class="line">248</span><br><span class="line">249</span><br><span class="line">250</span><br><span class="line">251</span><br><span class="line">252</span><br><span class="line">253</span><br><span class="line">254</span><br><span class="line">255</span><br><span class="line">256</span><br><span class="line">257</span><br><span class="line">258</span><br><span class="line">259</span><br><span class="line">260</span><br><span class="line">261</span><br><span class="line">262</span><br><span class="line">263</span><br><span class="line">264</span><br><span class="line">265</span><br><span class="line">266</span><br><span class="line">267</span><br><span class="line">268</span><br><span class="line">269</span><br><span class="line">270</span><br><span class="line">271</span><br><span class="line">272</span><br><span class="line">273</span><br><span class="line">274</span><br><span class="line">275</span><br><span class="line">276</span><br><span class="line">277</span><br><span class="line">278</span><br><span class="line">279</span><br><span class="line">280</span><br><span class="line">281</span><br><span class="line">282</span><br><span class="line">283</span><br><span class="line">284</span><br><span class="line">285</span><br><span class="line">286</span><br><span class="line">287</span><br><span class="line">288</span><br><span class="line">289</span><br><span class="line">290</span><br><span class="line">291</span><br><span class="line">292</span><br><span class="line">293</span><br><span class="line">294</span><br><span class="line">295</span><br><span class="line">296</span><br><span class="line">297</span><br><span class="line">298</span><br><span class="line">299</span><br><span class="line">300</span><br><span class="line">301</span><br><span class="line">302</span><br><span class="line">303</span><br><span class="line">304</span><br><span class="line">305</span><br><span class="line">306</span><br><span class="line">307</span><br><span class="line">308</span><br><span class="line">309</span><br><span class="line">310</span><br><span class="line">311</span><br><span class="line">312</span><br><span class="line">313</span><br><span class="line">314</span><br><span class="line">315</span><br><span class="line">316</span><br><span class="line">317</span><br><span class="line">318</span><br><span class="line">319</span><br><span class="line">320</span><br><span class="line">321</span><br><span class="line">322</span><br><span class="line">323</span><br><span class="line">324</span><br><span class="line">325</span><br><span class="line">326</span><br><span class="line">327</span><br><span class="line">328</span><br><span class="line">329</span><br><span class="line">330</span><br><span class="line">331</span><br><span class="line">332</span><br><span class="line">333</span><br><span class="line">334</span><br><span class="line">335</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># -*- coding:utf-8 -*-</span></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> numpy.random <span class="keyword">as</span> npr</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> ..fast_rcnn.config <span class="keyword">import</span> cfg</span><br><span class="line"><span class="keyword">from</span> bbox <span class="keyword">import</span> bbox_overlaps, bbox_intersections</span><br><span class="line"></span><br><span class="line">DEBUG = <span class="keyword">False</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成基础anchor box</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">generate_basic_anchors</span><span class="params">(sizes, base_size=<span class="number">16</span>)</span>:</span></span><br><span class="line">    base_anchor = np.array([<span class="number">0</span>, <span class="number">0</span>, base_size - <span class="number">1</span>, base_size - <span class="number">1</span>], np.int32)</span><br><span class="line">    anchors = np.zeros((len(sizes), <span class="number">4</span>), np.int32)</span><br><span class="line">    index = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> h, w <span class="keyword">in</span> sizes:</span><br><span class="line">        anchors[index] = scale_anchor(base_anchor, h, w)</span><br><span class="line">        index += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> anchors</span><br><span class="line"></span><br><span class="line"><span class="comment"># 根据baseanchor和设定的anchor的高度和宽度进行设定的anchor生成</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">scale_anchor</span><span class="params">(anchor, h, w)</span>:</span></span><br><span class="line">    x_ctr = (anchor[<span class="number">0</span>] + anchor[<span class="number">2</span>]) * <span class="number">0.5</span></span><br><span class="line">    y_ctr = (anchor[<span class="number">1</span>] + anchor[<span class="number">3</span>]) * <span class="number">0.5</span></span><br><span class="line">    scaled_anchor = anchor.copy()</span><br><span class="line">    scaled_anchor[<span class="number">0</span>] = x_ctr - w / <span class="number">2</span>  <span class="comment"># xmin</span></span><br><span class="line">    scaled_anchor[<span class="number">2</span>] = x_ctr + w / <span class="number">2</span>  <span class="comment"># xmax</span></span><br><span class="line">    scaled_anchor[<span class="number">1</span>] = y_ctr - h / <span class="number">2</span>  <span class="comment"># ymin</span></span><br><span class="line">    scaled_anchor[<span class="number">3</span>] = y_ctr + h / <span class="number">2</span>  <span class="comment"># ymax</span></span><br><span class="line">    <span class="keyword">return</span> scaled_anchor</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成anchor box</span></span><br><span class="line"><span class="comment"># 此处使用的是宽度固定，高度不同的anchor设置</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">generate_anchors</span><span class="params">(base_size=<span class="number">16</span>, ratios=[<span class="number">0.5</span>, <span class="number">1</span>, <span class="number">2</span>],</span></span></span><br><span class="line"><span class="function"><span class="params">                     scales=<span class="number">2</span> ** np.arange<span class="params">(<span class="number">3</span>, <span class="number">6</span>)</span>)</span>:</span></span><br><span class="line">    heights = [<span class="number">11</span>, <span class="number">16</span>, <span class="number">23</span>, <span class="number">33</span>, <span class="number">48</span>, <span class="number">68</span>, <span class="number">97</span>, <span class="number">139</span>, <span class="number">198</span>, <span class="number">283</span>]</span><br><span class="line">    widths = [<span class="number">16</span>]</span><br><span class="line">    sizes = []</span><br><span class="line">    <span class="keyword">for</span> h <span class="keyword">in</span> heights:</span><br><span class="line">        <span class="keyword">for</span> w <span class="keyword">in</span> widths:</span><br><span class="line">            sizes.append((h, w))</span><br><span class="line">    <span class="keyword">return</span> generate_basic_anchors(sizes)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成的anchor和groundtruth之间进行转换，转换方式和论文一致</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">bbox_transform</span><span class="params">(ex_rois, gt_rois)</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    computes the distance from ground-truth boxes to the given boxes, normed by their size</span></span><br><span class="line"><span class="string">    :param ex_rois: n * 4 numpy array, anchor boxes</span></span><br><span class="line"><span class="string">    :param gt_rois: n * 4 numpy array, ground-truth boxes</span></span><br><span class="line"><span class="string">    :return: deltas: n * 4 numpy array, ground-truth boxes</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    ex_widths = ex_rois[:, <span class="number">2</span>] - ex_rois[:, <span class="number">0</span>] + <span class="number">1.0</span> <span class="comment"># anchor width </span></span><br><span class="line">    ex_heights = ex_rois[:, <span class="number">3</span>] - ex_rois[:, <span class="number">1</span>] + <span class="number">1.0</span> <span class="comment"># anchor height</span></span><br><span class="line">    ex_ctr_x = ex_rois[:, <span class="number">0</span>] + <span class="number">0.5</span> * ex_widths <span class="comment"># anchor center x</span></span><br><span class="line">    ex_ctr_y = ex_rois[:, <span class="number">1</span>] + <span class="number">0.5</span> * ex_heights <span class="comment"># anchor center y</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">assert</span> np.min(ex_widths) &gt; <span class="number">0.1</span> <span class="keyword">and</span> np.min(ex_heights) &gt; <span class="number">0.1</span>, \</span><br><span class="line">        <span class="string">'Invalid boxes found: &#123;&#125; &#123;&#125;'</span>. \</span><br><span class="line">        format(ex_rois[np.argmin(ex_widths), :], ex_rois[np.argmin(ex_heights), :])</span><br><span class="line"></span><br><span class="line">    gt_widths = gt_rois[:, <span class="number">2</span>] - gt_rois[:, <span class="number">0</span>] + <span class="number">1.0</span> <span class="comment"># gt_box width</span></span><br><span class="line">    gt_heights = gt_rois[:, <span class="number">3</span>] - gt_rois[:, <span class="number">1</span>] + <span class="number">1.0</span> <span class="comment"># gt_box height</span></span><br><span class="line">    gt_ctr_x = gt_rois[:, <span class="number">0</span>] + <span class="number">0.5</span> * gt_widths <span class="comment"># gt_box center x</span></span><br><span class="line">    gt_ctr_y = gt_rois[:, <span class="number">1</span>] + <span class="number">0.5</span> * gt_heights <span class="comment"># gt_box center y</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># warnings.catch_warnings()</span></span><br><span class="line">    <span class="comment"># warnings.filterwarnings('error')</span></span><br><span class="line">    targets_dx = (gt_ctr_x - ex_ctr_x) / ex_widths  <span class="comment"># (gt_c_x-a_c_x)</span></span><br><span class="line">    targets_dy = (gt_ctr_y - ex_ctr_y) / ex_heights</span><br><span class="line">    targets_dw = np.log(gt_widths / ex_widths)</span><br><span class="line">    targets_dh = np.log(gt_heights / ex_heights)</span><br><span class="line"></span><br><span class="line">    targets = np.vstack(</span><br><span class="line">        (targets_dx, targets_dy, targets_dw, targets_dh)).transpose()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> targets</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成anchors</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">anchor_target_layer</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">        rpn_cls_score, gt_boxes, gt_ishard, dontcare_areas, im_info, _feat_stride=[<span class="number">16</span>, ],</span></span></span><br><span class="line"><span class="function"><span class="params">        anchor_scales=[<span class="number">16</span>, ])</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    Assign anchors to ground-truth targets. Produces anchor classification</span></span><br><span class="line"><span class="string">    labels and bounding-box regression targets.</span></span><br><span class="line"><span class="string">    Parameters</span></span><br><span class="line"><span class="string">    ----------</span></span><br><span class="line"><span class="string">    rpn_cls_score: (1, H, W, Ax2) bg/fg scores of previous conv layer</span></span><br><span class="line"><span class="string">    gt_boxes: (G, 5) vstack of [x1, y1, x2, y2, class]</span></span><br><span class="line"><span class="string">    gt_ishard: (G, 1), 1 or 0 indicates difficult or not</span></span><br><span class="line"><span class="string">    dontcare_areas: (D, 4), some areas may contains small objs but no labelling. D may be 0</span></span><br><span class="line"><span class="string">    im_info: a list of [image_height, image_width, scale_ratios]</span></span><br><span class="line"><span class="string">    _feat_stride: the downsampling ratio of feature map to the original input image</span></span><br><span class="line"><span class="string">    anchor_scales: the scales to the basic_anchor (basic anchor is [16, 16])</span></span><br><span class="line"><span class="string">    ----------</span></span><br><span class="line"><span class="string">    Returns</span></span><br><span class="line"><span class="string">    ----------</span></span><br><span class="line"><span class="string">    rpn_labels : (HxWxA, 1), for each anchor, 0 denotes bg, 1 fg, -1 dontcare</span></span><br><span class="line"><span class="string">    rpn_bbox_targets: (HxWxA, 4), distances of the anchors to the gt_boxes(may contains some transform)</span></span><br><span class="line"><span class="string">                            that are the regression objectives</span></span><br><span class="line"><span class="string">    rpn_bbox_inside_weights: (HxWxA, 4) weights of each boxes, mainly accepts hyper param in cfg</span></span><br><span class="line"><span class="string">    rpn_bbox_outside_weights: (HxWxA, 4) used to balance the fg/bg,</span></span><br><span class="line"><span class="string">                            beacuse the numbers of bgs and fgs mays significiantly different</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="comment"># anchors is the [x_min,y_min,x_max,y_max]</span></span><br><span class="line">    <span class="comment"># 生成基本的anchor,一共10个</span></span><br><span class="line">    _anchors = generate_anchors(scales=np.array(anchor_scales))  </span><br><span class="line">    _num_anchors = _anchors.shape[<span class="number">0</span>]  <span class="comment"># 10个anchor</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># allow boxes to sit over the edge by a small amount</span></span><br><span class="line">    _allowed_border = <span class="number">0</span></span><br><span class="line">    <span class="comment"># 原始图像的信息，图像的高宽及通道数</span></span><br><span class="line">    im_info = im_info[<span class="number">0</span>]  </span><br><span class="line"></span><br><span class="line">    <span class="comment"># 在feature-map上定位anchor，并加上delta，得到在实际图像中anchor的真实坐标</span></span><br><span class="line">    <span class="string">""" </span></span><br><span class="line"><span class="string">    Algorithm:</span></span><br><span class="line"><span class="string">        for each (H, W) location i</span></span><br><span class="line"><span class="string">            generate 9 anchor boxes centered on cell i</span></span><br><span class="line"><span class="string">            apply predicted bbox deltas at cell i to each of the 9 anchors</span></span><br><span class="line"><span class="string">            filter out-of-image anchors</span></span><br><span class="line"><span class="string">        measure GT overlap </span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="keyword">assert</span> rpn_cls_score.shape[<span class="number">0</span>] == <span class="number">1</span>, \</span><br><span class="line">        <span class="string">'Only single item batches are supported'</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># map of shape (..., H, W)</span></span><br><span class="line">    height, width = rpn_cls_score.shape[<span class="number">1</span>:<span class="number">3</span>]  <span class="comment"># feature-map的高宽</span></span><br><span class="line">    <span class="comment"># 1. Generate proposals from bbox deltas and shifted anchors</span></span><br><span class="line">    shift_x = np.arange(<span class="number">0</span>, width) * _feat_stride</span><br><span class="line">    shift_y = np.arange(<span class="number">0</span>, height) * _feat_stride</span><br><span class="line">    shift_x, shift_y = np.meshgrid(shift_x, shift_y)  <span class="comment"># in W H order</span></span><br><span class="line">    <span class="comment"># 生成feature-map和真实图像上anchor之间的偏移量</span></span><br><span class="line">    <span class="comment"># shifts构建网格结构，shape [height*width,4]</span></span><br><span class="line">    shifts = np.vstack((shift_x.ravel(), shift_y.ravel(),</span><br><span class="line">                        shift_x.ravel(), shift_y.ravel())).transpose()  </span><br><span class="line">    A = _num_anchors  <span class="comment"># 10个anchor</span></span><br><span class="line">    K = shifts.shape[<span class="number">0</span>]  <span class="comment"># feature-map的宽乘高的大小</span></span><br><span class="line">    <span class="comment"># 为当前的featuremap每个点生成A个anchor，shape is [K,A,4]</span></span><br><span class="line">    all_anchors = (_anchors.reshape((<span class="number">1</span>, A, <span class="number">4</span>)) +</span><br><span class="line">                   shifts.reshape((<span class="number">1</span>, K, <span class="number">4</span>)).transpose((<span class="number">1</span>, <span class="number">0</span>, <span class="number">2</span>)))  </span><br><span class="line">    all_anchors = all_anchors.reshape((K * A, <span class="number">4</span>))  <span class="comment"># shape is (K*A,4)</span></span><br><span class="line">    <span class="comment"># 在featuremap上每个点生成A个anchor</span></span><br><span class="line">    total_anchors = int(K * A)</span><br><span class="line">    <span class="comment"># only keep anchors inside the image</span></span><br><span class="line">    <span class="comment"># 因为生成的anchor尺寸有大有小，因此在边缘处生成的anchor有可能会超过原始图像的边界，</span></span><br><span class="line">    <span class="comment"># 将这些超过边界的anchor去掉,得到的是这些anchor的在all_anchors中的索引</span></span><br><span class="line">    <span class="comment"># 仅保留那些还在图像内部的anchor，超出图像的都删掉</span></span><br><span class="line">    <span class="comment"># anchors[:]=[x_min,y_min,x_max,y_max]</span></span><br><span class="line">    inds_inside = np.where(</span><br><span class="line">        (all_anchors[:, <span class="number">0</span>] &gt;= -_allowed_border) &amp;</span><br><span class="line">        (all_anchors[:, <span class="number">1</span>] &gt;= -_allowed_border) &amp;</span><br><span class="line">        (all_anchors[:, <span class="number">2</span>] &lt; im_info[<span class="number">1</span>] + _allowed_border) &amp;  <span class="comment"># width</span></span><br><span class="line">        (all_anchors[:, <span class="number">3</span>] &lt; im_info[<span class="number">0</span>] + _allowed_border)  <span class="comment"># height</span></span><br><span class="line">    )[<span class="number">0</span>]</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># keep only inside anchors</span></span><br><span class="line">    anchors = all_anchors[inds_inside, :]  <span class="comment"># 保留那些在图像内的anchor</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 至此，anchor准备好了</span></span><br><span class="line">    <span class="comment"># --------------------------------------------------------------</span></span><br><span class="line">    <span class="comment"># label: 1 is positive, 0 is negative, -1 is dont care</span></span><br><span class="line">    <span class="comment"># (A)</span></span><br><span class="line">    labels = np.empty((len(inds_inside),), dtype=np.float32)</span><br><span class="line">    labels.fill(<span class="number">-1</span>)  <span class="comment"># 初始化label，均为-1</span></span><br><span class="line">    <span class="comment"># overlaps between the anchors and the gt boxes</span></span><br><span class="line">    <span class="comment"># overlaps (ex, gt), shape is A x G</span></span><br><span class="line">    <span class="comment"># 计算anchor和gt-box的overlap，用来给anchor上标签</span></span><br><span class="line">    <span class="comment"># anchor box and groundtruth box 交集面积/并集面积</span></span><br><span class="line">    <span class="comment"># 通过IOU的得分来确定anchor为正样本与否</span></span><br><span class="line">    <span class="comment"># overlaps shape is [anchor.shape[0],gt_box.shape[0]]</span></span><br><span class="line">    overlaps = bbox_overlaps(</span><br><span class="line">        np.ascontiguousarray(anchors, dtype=np.float),</span><br><span class="line">        np.ascontiguousarray(gt_boxes, dtype=np.float))  </span><br><span class="line">    <span class="comment"># 存放每一个anchor和每一个gtbox之间的overlap</span></span><br><span class="line">    <span class="comment"># 找到和每一个gtbox，overlap最大的那个anchor</span></span><br><span class="line">    argmax_overlaps = overlaps.argmax(axis=<span class="number">1</span>) </span><br><span class="line">    max_overlaps = overlaps[np.arange(len(inds_inside)), argmax_overlaps]</span><br><span class="line">    <span class="comment"># 找到每个位置上10个anchor中与gtbox，overlap最大的那个</span></span><br><span class="line">    gt_argmax_overlaps = overlaps.argmax(axis=<span class="number">0</span>)  </span><br><span class="line">    gt_max_overlaps = overlaps[gt_argmax_overlaps,</span><br><span class="line">                               np.arange(overlaps.shape[<span class="number">1</span>])]</span><br><span class="line">    gt_argmax_overlaps = np.where(overlaps == gt_max_overlaps)[<span class="number">0</span>]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> cfg.TRAIN.RPN_CLOBBER_POSITIVES:</span><br><span class="line">        <span class="comment"># assign bg labels first so that positive labels can clobber them</span></span><br><span class="line">        <span class="comment"># 先给背景上标签，小于0.3overlap的为负样本label为0</span></span><br><span class="line">        labels[max_overlaps &lt; cfg.TRAIN.RPN_NEGATIVE_OVERLAP] = <span class="number">0</span>  </span><br><span class="line"></span><br><span class="line">    <span class="comment"># -----------------------------------#</span></span><br><span class="line">    <span class="comment"># 正样本的确定，iou得分大于0.7和每个位置上具有最大IOU得分的anchor</span></span><br><span class="line">    <span class="comment"># fg label: for each gt, anchor with highest overlap</span></span><br><span class="line">    <span class="comment"># 每个位置上的10个个anchor中overlap最大的认为是前景</span></span><br><span class="line">    labels[gt_argmax_overlaps] = <span class="number">1</span>  </span><br><span class="line">    <span class="comment"># fg label: above threshold IOU</span></span><br><span class="line">    <span class="comment"># overlap大于0.7的认为是前景</span></span><br><span class="line">    labels[max_overlaps &gt;= cfg.TRAIN.RPN_POSITIVE_OVERLAP] = <span class="number">1</span>  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> cfg.TRAIN.RPN_CLOBBER_POSITIVES:</span><br><span class="line">        <span class="comment"># assign bg labels last so that negative labels can clobber positives</span></span><br><span class="line">        labels[max_overlaps &lt; cfg.TRAIN.RPN_NEGATIVE_OVERLAP] = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># preclude dontcare areas</span></span><br><span class="line">    <span class="comment"># 这里我们暂时不考虑有doncare_area的存在</span></span><br><span class="line">    <span class="keyword">if</span> dontcare_areas <span class="keyword">is</span> <span class="keyword">not</span> <span class="keyword">None</span> <span class="keyword">and</span> dontcare_areas.shape[<span class="number">0</span>] &gt; <span class="number">0</span>:  </span><br><span class="line">        <span class="comment"># intersec shape is D x A</span></span><br><span class="line">        intersecs = bbox_intersections(</span><br><span class="line">            np.ascontiguousarray(dontcare_areas, dtype=np.float),  <span class="comment"># D x 4</span></span><br><span class="line">            np.ascontiguousarray(anchors, dtype=np.float)  <span class="comment"># A x 4</span></span><br><span class="line">        )</span><br><span class="line">        intersecs_ = intersecs.sum(axis=<span class="number">0</span>)  <span class="comment"># A x 1</span></span><br><span class="line">        labels[intersecs_ &gt; cfg.TRAIN.DONTCARE_AREA_INTERSECTION_HI] = <span class="number">-1</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 这里我们暂时不考虑难样本的问题</span></span><br><span class="line">    <span class="comment"># preclude hard samples that are highly occlusioned, truncated or difficult to see</span></span><br><span class="line">    <span class="keyword">if</span> cfg.TRAIN.PRECLUDE_HARD_SAMPLES <span class="keyword">and</span> gt_ishard <span class="keyword">is</span> <span class="keyword">not</span> <span class="keyword">None</span> <span class="keyword">and</span> gt_ishard.shape[<span class="number">0</span>] &gt; <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">assert</span> gt_ishard.shape[<span class="number">0</span>] == gt_boxes.shape[<span class="number">0</span>]</span><br><span class="line">        gt_ishard = gt_ishard.astype(int)</span><br><span class="line">        gt_hardboxes = gt_boxes[gt_ishard == <span class="number">1</span>, :]</span><br><span class="line">        <span class="keyword">if</span> gt_hardboxes.shape[<span class="number">0</span>] &gt; <span class="number">0</span>:</span><br><span class="line">            <span class="comment"># H x A</span></span><br><span class="line">            hard_overlaps = bbox_overlaps(</span><br><span class="line">                np.ascontiguousarray(gt_hardboxes, dtype=np.float),  <span class="comment"># H x 4</span></span><br><span class="line">                np.ascontiguousarray(anchors, dtype=np.float))  <span class="comment"># A x 4</span></span><br><span class="line">            hard_max_overlaps = hard_overlaps.max(axis=<span class="number">0</span>)  <span class="comment"># (A)</span></span><br><span class="line">            labels[hard_max_overlaps &gt;= cfg.TRAIN.RPN_POSITIVE_OVERLAP] = <span class="number">-1</span></span><br><span class="line">            max_intersec_label_inds = hard_overlaps.argmax(axis=<span class="number">1</span>)  <span class="comment"># H x 1</span></span><br><span class="line">            labels[max_intersec_label_inds] = <span class="number">-1</span>  <span class="comment">#</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># subsample positive labels if we have too many</span></span><br><span class="line">    <span class="comment"># 对正样本进行采样，如果正样本的数量太多的话</span></span><br><span class="line">    <span class="comment"># 限制正样本的数量不超过128个，排除的置位dont_Care类</span></span><br><span class="line">    <span class="comment"># TODO 这个后期可能还需要修改，毕竟如果使用的是字符的片段，那个正样本的数量是很多的。</span></span><br><span class="line">    num_fg = int(cfg.TRAIN.RPN_FG_FRACTION * cfg.TRAIN.RPN_BATCHSIZE)</span><br><span class="line">    fg_inds = np.where(labels == <span class="number">1</span>)[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">if</span> len(fg_inds) &gt; num_fg:</span><br><span class="line">        disable_inds = npr.choice(</span><br><span class="line">            fg_inds, size=(len(fg_inds) - num_fg), replace=<span class="keyword">False</span>)  <span class="comment"># 随机去除掉一些正样本</span></span><br><span class="line">        labels[disable_inds] = <span class="number">-1</span>  <span class="comment"># 变为-1</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># subsample negative labels if we have too many</span></span><br><span class="line">    <span class="comment"># 对负样本进行采样，如果负样本的数量太多的话</span></span><br><span class="line">    <span class="comment"># 正负样本总数是256，限制正样本数目最多128，</span></span><br><span class="line">    <span class="comment"># 如果正样本数量小于128，差的那些就用负样本补上，凑齐256个样本</span></span><br><span class="line">    num_bg = cfg.TRAIN.RPN_BATCHSIZE - np.sum(labels == <span class="number">1</span>)</span><br><span class="line">    bg_inds = np.where(labels == <span class="number">0</span>)[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">if</span> len(bg_inds) &gt; num_bg:</span><br><span class="line">        disable_inds = npr.choice(</span><br><span class="line">            bg_inds, size=(len(bg_inds) - num_bg), replace=<span class="keyword">False</span>)</span><br><span class="line">        labels[disable_inds] = <span class="number">-1</span></span><br><span class="line">        <span class="comment"># print "was %s inds, disabling %s, now %s inds" % (</span></span><br><span class="line">        <span class="comment"># len(bg_inds), len(disable_inds), np.sum(labels == 0))</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 至此， 上好标签，开始计算rpn-box的真值</span></span><br><span class="line">    <span class="comment"># --------------------------------------------------------------</span></span><br><span class="line">    bbox_targets = np.zeros((len(inds_inside), <span class="number">4</span>), dtype=np.float32)</span><br><span class="line">    <span class="comment"># 根据anchor和gtbox计算得真值（anchor和gtbox之间的偏差）</span></span><br><span class="line">    bbox_targets = _compute_targets(anchors, gt_boxes[argmax_overlaps, :])</span><br><span class="line"><span class="comment"># 内部权重，前景就给1，其他是0</span></span><br><span class="line">    bbox_inside_weights = np.zeros((len(inds_inside), <span class="number">4</span>), dtype=np.float32)</span><br><span class="line">    bbox_inside_weights[labels == <span class="number">1</span>, :] = np.array(</span><br><span class="line">        cfg.TRAIN.RPN_BBOX_INSIDE_WEIGHTS)  </span><br><span class="line"></span><br><span class="line">    bbox_outside_weights = np.zeros((len(inds_inside), <span class="number">4</span>), dtype=np.float32)</span><br><span class="line">    <span class="keyword">if</span> cfg.TRAIN.RPN_POSITIVE_WEIGHT &lt; <span class="number">0</span>: </span><br><span class="line">        <span class="comment"># 此处使用uniform权重，也就是正样本是1，负样本是0</span></span><br><span class="line">        <span class="comment"># uniform weighting of examples (given non-uniform sampling)</span></span><br><span class="line">        <span class="comment"># num_examples = np.sum(labels &gt;= 0) + 1</span></span><br><span class="line">        <span class="comment"># positive_weights = np.ones((1, 4)) * 1.0 / num_examples</span></span><br><span class="line">        <span class="comment"># negative_weights = np.ones((1, 4)) * 1.0 / num_examples</span></span><br><span class="line">        positive_weights = np.ones((<span class="number">1</span>, <span class="number">4</span>))  <span class="comment"># 前景为1</span></span><br><span class="line">        negative_weights = np.zeros((<span class="number">1</span>, <span class="number">4</span>))  <span class="comment"># 背景为0</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">assert</span> ((cfg.TRAIN.RPN_POSITIVE_WEIGHT &gt; <span class="number">0</span>) &amp;</span><br><span class="line">                (cfg.TRAIN.RPN_POSITIVE_WEIGHT &lt; <span class="number">1</span>))</span><br><span class="line">        positive_weights = (cfg.TRAIN.RPN_POSITIVE_WEIGHT /</span><br><span class="line">                            (np.sum(labels == <span class="number">1</span>)) + <span class="number">1</span>)</span><br><span class="line">        negative_weights = ((<span class="number">1.0</span> - cfg.TRAIN.RPN_POSITIVE_WEIGHT) /</span><br><span class="line">                            (np.sum(labels == <span class="number">0</span>)) + <span class="number">1</span>)</span><br><span class="line">    <span class="comment"># 外部权重，前景是1，背景是0</span></span><br><span class="line">    <span class="comment"># bbox_outside_weights初始化为0，将label中为0的位置赋值bbox_outside_weights为0,labels为1的位置赋值为1</span></span><br><span class="line">    bbox_outside_weights[labels == <span class="number">1</span>, :] = positive_weights</span><br><span class="line">    bbox_outside_weights[labels == <span class="number">0</span>, :] = negative_weights</span><br><span class="line"></span><br><span class="line">    <span class="comment"># map up to original set of anchors</span></span><br><span class="line">    <span class="comment"># 一开始是将超出图像范围的anchor直接丢掉的，现在在加回来</span></span><br><span class="line">    <span class="comment"># inds_inside 是原始anchor中的索引</span></span><br><span class="line">    labels = _unmap(labels, total_anchors, inds_inside, fill=<span class="number">-1</span>)  <span class="comment"># 这些anchor的label是-1，也即dontcare</span></span><br><span class="line">    bbox_targets = _unmap(bbox_targets, total_anchors, inds_inside, fill=<span class="number">0</span>)  <span class="comment"># 这些anchor的真值是0，也即没有值</span></span><br><span class="line">    bbox_inside_weights = _unmap(bbox_inside_weights, total_anchors,</span><br><span class="line">                                 inds_inside, fill=<span class="number">0</span>)  <span class="comment"># 内部权重以0填充</span></span><br><span class="line">    bbox_outside_weights = _unmap(bbox_outside_weights, total_anchors,</span><br><span class="line">                                  inds_inside, fill=<span class="number">0</span>)  <span class="comment"># 外部权重以0填充</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># labels</span></span><br><span class="line">    labels = labels.reshape((<span class="number">1</span>, height, width, A))  <span class="comment"># reshap一下label</span></span><br><span class="line">    rpn_labels = labels</span><br><span class="line"></span><br><span class="line">    <span class="comment"># bbox_targets</span></span><br><span class="line">    bbox_targets = bbox_targets.reshape((<span class="number">1</span>, height, width, A * <span class="number">4</span>))  <span class="comment"># reshape</span></span><br><span class="line">    rpn_bbox_targets = bbox_targets</span><br><span class="line"></span><br><span class="line">    <span class="comment"># bbox_inside_weights</span></span><br><span class="line">    bbox_inside_weights = bbox_inside_weights.reshape((<span class="number">1</span>, height, width, A * <span class="number">4</span>))</span><br><span class="line">    rpn_bbox_inside_weights = bbox_inside_weights</span><br><span class="line"></span><br><span class="line">    <span class="comment"># bbox_outside_weights</span></span><br><span class="line">    bbox_outside_weights = bbox_outside_weights.reshape((<span class="number">1</span>, height, width, A * <span class="number">4</span>))</span><br><span class="line">    rpn_bbox_outside_weights = bbox_outside_weights</span><br><span class="line">    </span><br><span class="line">rpn_data=(rpn_labels, rpn_bbox_targets, rpn_bbox_inside_weights, rpn_bbox_outside_weights)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> rpn_data</span><br><span class="line"></span><br><span class="line"><span class="comment"># 将排除掉边界之外的anchors之后的anchor补全回来</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_unmap</span><span class="params">(data, count, inds, fill=<span class="number">0</span>)</span>:</span></span><br><span class="line">    <span class="string">""" Unmap a subset of item (data) back to the original set of items (of</span></span><br><span class="line"><span class="string">    size count) """</span></span><br><span class="line">    <span class="keyword">if</span> len(data.shape) == <span class="number">1</span>:</span><br><span class="line">        ret = np.empty((count,), dtype=np.float32)</span><br><span class="line">        ret.fill(fill)</span><br><span class="line">        ret[inds] = data</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        ret = np.empty((count,) + data.shape[<span class="number">1</span>:], dtype=np.float32)</span><br><span class="line">        ret.fill(fill)</span><br><span class="line">        ret[inds, :] = data</span><br><span class="line">    <span class="keyword">return</span> ret</span><br><span class="line"></span><br><span class="line"><span class="comment"># 计算anchor和gt之间的矩形框的偏差</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_compute_targets</span><span class="params">(ex_rois, gt_rois)</span>:</span></span><br><span class="line">    <span class="string">"""Compute bounding-box regression targets for an image."""</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">assert</span> ex_rois.shape[<span class="number">0</span>] == gt_rois.shape[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">assert</span> ex_rois.shape[<span class="number">1</span>] == <span class="number">4</span></span><br><span class="line">    <span class="keyword">assert</span> gt_rois.shape[<span class="number">1</span>] == <span class="number">5</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> bbox_transform(ex_rois, gt_rois[:, :<span class="number">4</span>]).astype(np.float32, copy=<span class="keyword">False</span>)</span><br></pre></td></tr></table></figure><p>对于bbox使用cpython写成(.pyx文件)</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line">cimport numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">DTYPE = np.float</span><br><span class="line">ctypedef np.float_t DTYPE_t</span><br><span class="line"></span><br><span class="line"><span class="comment"># 计算IOU</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">bbox_overlaps</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">        np.ndarray[DTYPE_t, ndim=<span class="number">2</span>] boxes,</span></span></span><br><span class="line"><span class="function"><span class="params">        np.ndarray[DTYPE_t, ndim=<span class="number">2</span>] query_boxes)</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    Parameters</span></span><br><span class="line"><span class="string">    ----------</span></span><br><span class="line"><span class="string">    boxes: (N, 4) ndarray of float, anchor box nums</span></span><br><span class="line"><span class="string">    query_boxes: (K, 4) ndarray of float, groud_truth object nums,[x_min,y_min,x_max,y_max,class]</span></span><br><span class="line"><span class="string">    Returns</span></span><br><span class="line"><span class="string">    -------</span></span><br><span class="line"><span class="string">    overlaps: (N, K) ndarray of overlap between boxes and query_boxes</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    cdef unsigned int N = boxes.shape[<span class="number">0</span>]</span><br><span class="line">    cdef unsigned int K = query_boxes.shape[<span class="number">0</span>]</span><br><span class="line">    cdef np.ndarray[DTYPE_t, ndim=<span class="number">2</span>] overlaps = np.zeros((N, K), dtype=DTYPE)</span><br><span class="line">    cdef DTYPE_t iw, ih, box_area</span><br><span class="line">    cdef DTYPE_t ua</span><br><span class="line">    cdef unsigned int k, n</span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> range(K):</span><br><span class="line">        box_area = (</span><br><span class="line">            (query_boxes[k, <span class="number">2</span>] - query_boxes[k, <span class="number">0</span>] + <span class="number">1</span>) *</span><br><span class="line">            (query_boxes[k, <span class="number">3</span>] - query_boxes[k, <span class="number">1</span>] + <span class="number">1</span>)</span><br><span class="line">        )</span><br><span class="line">        <span class="keyword">for</span> n <span class="keyword">in</span> range(N):</span><br><span class="line">            <span class="comment"># 水平方向上的交集，如果存在那么iw为正</span></span><br><span class="line">            iw = (</span><br><span class="line">                min(boxes[n, <span class="number">2</span>], query_boxes[k, <span class="number">2</span>]) -</span><br><span class="line">                max(boxes[n, <span class="number">0</span>], query_boxes[k, <span class="number">0</span>]) + <span class="number">1</span></span><br><span class="line">            )</span><br><span class="line">            <span class="keyword">if</span> iw &gt; <span class="number">0</span>:</span><br><span class="line">                <span class="comment"># 竖直方向上的交集</span></span><br><span class="line">                ih = (</span><br><span class="line">                    min(boxes[n, <span class="number">3</span>], query_boxes[k, <span class="number">3</span>]) -</span><br><span class="line">                    max(boxes[n, <span class="number">1</span>], query_boxes[k, <span class="number">1</span>]) + <span class="number">1</span></span><br><span class="line">                )</span><br><span class="line">                <span class="keyword">if</span> ih &gt; <span class="number">0</span>:</span><br><span class="line">                    <span class="comment"># 如果存在交集，计算并集的面积</span></span><br><span class="line">                    <span class="comment"># union area</span></span><br><span class="line">                    ua = float(</span><br><span class="line">                        (boxes[n, <span class="number">2</span>] - boxes[n, <span class="number">0</span>] + <span class="number">1</span>) *</span><br><span class="line">                        (boxes[n, <span class="number">3</span>] - boxes[n, <span class="number">1</span>] + <span class="number">1</span>) +</span><br><span class="line">                        box_area - iw * ih</span><br><span class="line">                    )</span><br><span class="line">                    <span class="comment"># 交集面积/并集面积</span></span><br><span class="line">                    overlaps[n, k] = iw * ih / ua</span><br><span class="line">    <span class="keyword">return</span> overlaps</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># anchor与gt交集面积相对于gt面积的比例</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">bbox_intersections</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">        np.ndarray[DTYPE_t, ndim=<span class="number">2</span>] boxes,</span></span></span><br><span class="line"><span class="function"><span class="params">        np.ndarray[DTYPE_t, ndim=<span class="number">2</span>] query_boxes)</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    For each query box compute the intersection ratio covered by boxes</span></span><br><span class="line"><span class="string">    ----------</span></span><br><span class="line"><span class="string">    Parameters</span></span><br><span class="line"><span class="string">    ----------</span></span><br><span class="line"><span class="string">    boxes: (N, 4) ndarray of float</span></span><br><span class="line"><span class="string">    query_boxes: (K, 4) ndarray of float</span></span><br><span class="line"><span class="string">    Returns</span></span><br><span class="line"><span class="string">    -------</span></span><br><span class="line"><span class="string">    overlaps: (N, K) ndarray of intersec between boxes and query_boxes</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    cdef unsigned int N = boxes.shape[<span class="number">0</span>]</span><br><span class="line">    cdef unsigned int K = query_boxes.shape[<span class="number">0</span>]</span><br><span class="line">    cdef np.ndarray[DTYPE_t, ndim=<span class="number">2</span>] intersec = np.zeros((N, K), dtype=DTYPE)</span><br><span class="line">    cdef DTYPE_t iw, ih, box_area</span><br><span class="line">    cdef DTYPE_t ua</span><br><span class="line">    cdef unsigned int k, n</span><br><span class="line">    <span class="keyword">for</span> k <span class="keyword">in</span> range(K):</span><br><span class="line">        box_area = (</span><br><span class="line">            (query_boxes[k, <span class="number">2</span>] - query_boxes[k, <span class="number">0</span>] + <span class="number">1</span>) *</span><br><span class="line">            (query_boxes[k, <span class="number">3</span>] - query_boxes[k, <span class="number">1</span>] + <span class="number">1</span>)</span><br><span class="line">        )</span><br><span class="line">        <span class="keyword">for</span> n <span class="keyword">in</span> range(N):</span><br><span class="line">            iw = (</span><br><span class="line">                min(boxes[n, <span class="number">2</span>], query_boxes[k, <span class="number">2</span>]) -</span><br><span class="line">                max(boxes[n, <span class="number">0</span>], query_boxes[k, <span class="number">0</span>]) + <span class="number">1</span></span><br><span class="line">            )</span><br><span class="line">            <span class="keyword">if</span> iw &gt; <span class="number">0</span>:</span><br><span class="line">                ih = (</span><br><span class="line">                    min(boxes[n, <span class="number">3</span>], query_boxes[k, <span class="number">3</span>]) -</span><br><span class="line">                    max(boxes[n, <span class="number">1</span>], query_boxes[k, <span class="number">1</span>]) + <span class="number">1</span></span><br><span class="line">                )</span><br><span class="line">                <span class="keyword">if</span> ih &gt; <span class="number">0</span>:</span><br><span class="line">                    intersec[n, k] = iw * ih / box_area</span><br><span class="line">    <span class="keyword">return</span> intersec</span><br></pre></td></tr></table></figure><p>代码中的注释已经写得明明白白了。anchor生成函数为<code>anchor_target_layer.py</code></p><img src="/2019/01/23/深度学习-TextDetection/ctpn_gen_anchor.png" title="Anchors"><p>首先根据设定的anchor高度和宽度在特征图上每个cell生成A个anchors，这些anchors有的会超过原始图像的边界，如上图所示，将这些超出边界的anchors先删除，并记录保留的anchor在原始所有anchors中的索引值，使用内部的anchor和groundtruth进行IOU计算(anchor和gt之间如果存在交集，则使用交集面积和二者并集的面积进行IOU计算)，使用两个原则进行anchor正样本的认定：如果anchor和gt之间的IOU大于设定的阈值0.7则认定该anchor为正样本；将具有和任意gt最大的IOU的anchor为正样本，也就是和gt最大的几个anchor最为正样本，这一步选择的anchor数量和gt的数量相同。至此就确定了正样本的anchor和剩余的负样本anchor，使用设定的正负样本数量，来控制正负样本的数量，将正负样本和和gt之间计算偏移量并作为目标框的label。对于anchor和gt之间的偏移量计算如下图所示</p><img src="/2019/01/23/深度学习-TextDetection/ctpn_anchor_gt.png" title="Anchor_groudtruth"><p>图中红色表示groundtruth，黑色表示anchor box，首先计算两个矩形框的中心坐标和宽度高度，计算公式为</p><script type="math/tex; mode=display">\begin{align}target_{x} &=(GT_x-AN_x)/AN_{width} \\traget_y &=(GT_y-an_y)/AN_{height} \\traget_w &= \log (GT_{width}/AN_{width}) \\traget_h &= \log (GT_{height}/AN_{height})\end{align}</script><p>整个流程如下图所示</p><img src="/2019/01/23/深度学习-TextDetection/ctpn_anchor_gen.png" title="ctpn_anchor_gen"><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>至此，对CTPN网络结构结合代码进行了一些跟人理解的解读，该模型与2016年提出，可以看到收到很多的fastercnn的影响，可以看到CTPN具有如下的一些特点</p><ul><li>基础VGG网络的使用，因此一般需要ImageNet数据集的预训练权重会使得训练更快速和平稳</li><li>Bilstm的使用使得模型无法向CNN那样并行运算，影响了模型的速度</li><li>Anchor的设定为等宽度变高度，因此这种anchor只能适用于水平方向文本的检测，也可以通过更改anchor使得anchor兼容竖直方向的文本检测</li><li>模型中anchor的宽度为15，因此模型的检测粒度收到该设置的影响，有可能存在边界不明确的状况</li><li>因为使用的是和fasterrcnn相同的anchor生成及预测方法，因此在inference阶段需要对预测的值进行反向变换得到目标框</li></ul><h2 id="EAST"><a href="#EAST" class="headerlink" title="EAST"></a>EAST</h2><h3 id="论文关键idea"><a href="#论文关键idea" class="headerlink" title="论文关键idea"></a>论文关键idea</h3><ul><li>提出了两段式的文本检测方法，FCN+NMS，消除多过程造成的中间误差累计，减少了检测时间</li><li>模型可以进行单词级别检测，又可以进行文本行检测，检测的形状可以是任意形状的四边形也可以是普通的四边形</li><li>采用了<code>Locality-Aware NMS</code>的预测框过滤</li></ul><p>网络结构如下所示</p><img src="/2019/01/23/深度学习-TextDetection/east_model.png" title="EAST Model"><hr><h3 id="Pipeline"><a href="#Pipeline" class="headerlink" title="Pipeline"></a>Pipeline</h3><ul><li><p>先用一个通用的网络(论文中采用的是PVAnet，实际在使用的时候可以采用VGG16，Resnet等)作为base net ，用于特征提取</p><blockquote><p>此处对PAVnet进行一些说明，PAVnet主要是对VGG进行了改进并应用于目标检测任务，主要针对FasterRcnn的基础网络进行了改进，包含<code>mCReLU,Inception,Hyper-feature</code>各个结构</p><img src="/2019/01/23/深度学习-TextDetection/PVAnet.png" title="PVAnet"><p>在论文总的基础网络用的是<code>PVAnet</code>的基础网络，具体参数如下所示</p><img src="/2019/01/23/深度学习-TextDetection/PVAnetParam.png" title="PVAnetParam"><p>对于mCReLU结构和Inception结构如下所示</p><img src="/2019/01/23/深度学习-TextDetection/PVAnet_crelu_inception.png" title="PVAnet mCReLU Inception"></blockquote></li><li><p>基于上述主干特征提取网络，抽取不同层的<code>featuremap</code>（它们的尺寸分别是inuput-image的$\frac{1}{32},\frac{1}{16},\frac{1}{8},\frac{1}{4}$，这样可以得到不同尺度的特征图，这样做的目的是解决文本行尺度变换剧烈的问题，ealy-stage可用于预测小的文本行(较大的特征图)，late-stage可用于预测大的文本行(较小的特征图)。</p></li><li><p>特征合并层，将抽取的特征进行merge．这里合并的规则采用了Unet的方法，合并规则：从特征提取网络的顶部特征按照相应的规则向上进行合并，不断增大featuremap的尺寸。</p></li><li><p>网络输出层，包含文本得分和文本形状．根据不同文本形状(可分为RBOX和QUAD，对于RROX预测的是当前点距离gtbox的四个边的距离以及gtbox的相对图像的x正方向的角度$\theta$，也就是总共为5个值分别对应着$(d_1,d_2,d_3,d_4,\theta)$，而对于QUAD来说预测对应的gtbox的四个交点的坐标，一共8个值)，对于RBOX对应的示意图如下所示</p><img src="/2019/01/23/深度学习-TextDetection/EAST_RRBX.png" title="EAST_RBOX"><p>图中的$d_{i}$对应的是当前点到gt的距离，知道了一个固定点到矩形的四条边的距离，就可以的知道这个矩形所在的位置和大小，即确定这个矩形。</p><img src="/2019/01/23/深度学习-TextDetection/east_rbox_quad.png" title="EAST_RBOX_QUAD"><p>可以看出，对于RBOX输出5个预测值，而QUAD输出8个预测值。</p></li></ul><p>对于层g和h的计算方式如图中公式所示。</p><ul><li>对于g为uppooling层，每次操作将featuremap放大到原来的2倍，主要进行特征图的上采样，论文中采取的双线性插值的方法进行上采样，没有使用反卷积的方式，减少了模型的计算量但是有可能降低模型的表达能力</li><li>上采样之后的featuremap和下采样同样尺寸的f层进行merge并使用conv1x1降低合并后的模型的通道数</li><li>之后使用conv3x3卷积，输出该阶段的featuremap</li><li>上述操作重复3次最终模型输出的通道数为32</li></ul><p>进行特征图合并之后进行预测输出，也就是针对不同的box形式输出5个或者8个预测值。</p><h3 id="Loss计算"><a href="#Loss计算" class="headerlink" title="Loss计算"></a>Loss计算</h3><p>总的损失包含分类损失和回归损失，即</p><script type="math/tex; mode=display">L=L_S+\lambda_gL_g</script><p>分类损失论文中使用的是平衡交叉熵损失</p><script type="math/tex; mode=display">\begin{align}L_S&=\ {balanced-xent}(\dot Y,Y)\\&=-\beta Y \log \dot Y -(1-\beta)(1-\dot Y)(\log (1-\dot Y)) \\\\& where \:\:\:\:\: \beta=1-\frac{\sum_{ y \in  Y}y }{|Y|}\end{align}</script><p>其中$\dot Y$为预测值，$Y$为label值。相比普通的交叉熵损失，平衡交叉熵损失对正负样本进行了平衡。</p><p>对于$L_g$损失，由于在对于RBOX信息中包含的是5个预测值即$(d_1,d_2,d_3,d_4,\theta)$，那么就可以得到损失为</p><script type="math/tex; mode=display">\begin{align}& L_g=L_{AABB}+\lambda_{\theta}L_{\theta} \\where \:\:\:\:\:\:& L_{AABB}=-\log IoU (\dot R,R^*)=-\log \frac{|\dot R \cap R^* |}{|\dot R \cup R^*|} \\& L_{\theta}=1-\cos (\dot {\theta}-\theta^*)\end{align}</script><p>对于IOU损失的计算是，论文中对交集区域面积的计算方式为</p><script type="math/tex; mode=display">\begin{align} &w_i=\min(\dot d_2,d_2^*)+\min(\dot d_4,d_4^*) \\&h_i=\min(\dot d_1,d_1^*)+\min(\dot d_3,d_3^*)\end{align}</script><p><strong>实际上这种计算方式是存在问题的</strong>，分析如下</p><img src="/2019/01/23/深度学习-TextDetection/east_iou.png" title="east_iou"><p>如上图所示，红色对应gt，蓝色对应predict，如果不考虑角度，那么按照公式所述是正确的，但是考虑角度信息之后就会发现iou的交集面积计算公式存在错误。</p><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul><li></li><li><p>综述</p><p><a href="https://zhuanlan.zhihu.com/p/38655369" target="_blank" rel="noopener">自然场景文本检测识别技术综述</a></p><p><a href="https://www.jiqizhixin.com/articles/2018-06-20-16" target="_blank" rel="noopener">白翔:：图像OCR年度进展|VALSE2018之十一</a></p><p><a href="https://zhuanlan.zhihu.com/p/29549641" target="_blank" rel="noopener">白翔：趣谈“捕文捉字”— 场景文字检测 | VALSE2017之十</a></p><p><a href="https://blog.csdn.net/u013250416/article/details/79591263" target="_blank" rel="noopener">基于深度学习的目标检测及场景文字检测研究进展</a></p><p><a href="https://zhuanlan.zhihu.com/p/52335619" target="_blank" rel="noopener">知乎文本检测综述</a></p><p><a href="https://www.cnblogs.com/lillylin/" target="_blank" rel="noopener">优秀论文解读博客</a></p><p><a href="https://zhuanlan.zhihu.com/burness-DL" target="_blank" rel="noopener">知乎专栏:小石头的码疯窝</a></p><p><a href="https://www.cnblogs.com/skyfsm/category/1123384.html" target="_blank" rel="noopener">OCR_Overview_冠军试炼</a></p></li><li><p>文本检测</p><ul><li><p>CTPN</p><p><a href="https://zhuanlan.zhihu.com/p/34757009" target="_blank" rel="noopener">场景文字检测—CTPN原理与实现</a></p><p><a href="https://github.com/eragonruan/text-detection-ctpn" target="_blank" rel="noopener">CTPN: Tensorflow</a></p></li><li><p>EAST</p><p><a href="https://www.cnblogs.com/lillylin/p/9954981.html" target="_blank" rel="noopener">Bolg: EAST</a></p><p><a href="https://zhuanlan.zhihu.com/p/37504120" target="_blank" rel="noopener">知乎：文本检测之EAST</a></p><p><a href="https://github.com/argman/EAST" target="_blank" rel="noopener">EAST：tensorflow</a></p><p><a href="https://github.com/kurapan/EAST" target="_blank" rel="noopener">EAST: Keras</a></p><p><a href="https://github.com/huoyijie/AdvancedEAST" target="_blank" rel="noopener">EAST: Advanced keras</a></p></li><li><p>SegLink</p><p><a href="https://www.cnblogs.com/lillylin/p/6596731.html" target="_blank" rel="noopener">SegLink_Blog</a></p><p><a href="https://zhuanlan.zhihu.com/p/37781277" target="_blank" rel="noopener">文本检测之SegLink</a></p></li><li><p>PixelLink</p><p><a href="https://zhuanlan.zhihu.com/p/38171172" target="_blank" rel="noopener">文本检测之PixelLink</a></p><p><a href="https://github.com/ZJULearning/pixel_link" target="_blank" rel="noopener">Github: PixelLink </a></p></li><li><p>TextBoxes</p><p><a href="https://zhuanlan.zhihu.com/p/33723456" target="_blank" rel="noopener">论文笔记：TextBoxes++: A Single-Shot Oriented Scene Text Detector</a></p><p><a href="https://github.com/MhLiao/TextBoxes_plusplus" target="_blank" rel="noopener">Github: TextBoxes++</a></p></li><li><p>角定位</p></li></ul><p><a href="https://www.cnblogs.com/lillylin/p/8495124.html" target="_blank" rel="noopener">基于角定位于区域分割</a></p></li><li><p>文本识别</p><ul><li><p>ASTER</p><p><a href="https://github.com/bgshih/aster" target="_blank" rel="noopener">Github: ASTER</a></p></li></ul></li><li><p>TextSpotter</p><ul><li><p>Mask TextSpotter</p><p><a href="https://www.jiqizhixin.com/articles/2018-08-24-12" target="_blank" rel="noopener">华科白翔教授团队ECCV2018 OCR论文：Mask TextSpotter</a></p></li></ul></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文主要对常用的文本检测模型算法进行总结及分析，有的模型笔者切实run过，有的是通过论文及相关代码的分析，如有错误，请不吝指正。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/CV/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/tags/CV/"/>
    
      <category term="OCR" scheme="http://xiaofengshi.com/tags/OCR/"/>
    
  </entry>
  
  <entry>
    <title>深度学习-OCR_Overview</title>
    <link href="http://xiaofengshi.com/2019/01/05/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-OCR_Overview/"/>
    <id>http://xiaofengshi.com/2019/01/05/深度学习-OCR_Overview/</id>
    <published>2019-01-05T10:07:30.000Z</published>
    <updated>2019-01-23T07:41:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>本篇涉及使用深度学习的方法实现字符识别的任务，该任务与计算机视觉领域内的图像描述任务(<code>ImageCaption</code>)相似，对于图像描述任务，输入为图像，输出为对该图像的描述；而对于<code>OCR</code>任务，输入的图像为包含文字或字符的图片，而输出为这张图片中的文字或字符。</p><a id="more"></a><img src="/2019/01/05/深度学习-OCR_Overview/imagecaption.png" title="ImageCaption"><p>如图所示，对于<code>ImageCaption</code>任务来说，输入一张图片，输出对该图片的描述，简而言之就是看图说话。</p><hr><img src="/2019/01/05/深度学习-OCR_Overview/ocr.jpg" title="OCR"><p>如图所示，对于<code>OCR</code>任务，输入的是一张身份张照片，输出的就是该照片中包含的文字内容。</p><hr><p>下面结合自己的开发经验对<code>OCR</code>项目的一些相关技术思路进行一些记录说明。</p><h2 id="技术方案"><a href="#技术方案" class="headerlink" title="技术方案"></a>技术方案</h2><h3 id="图像预处理"><a href="#图像预处理" class="headerlink" title="图像预处理"></a><strong>图像预处理</strong></h3><p>假如输入时一些扫描或相机拍摄的文档，首先第一点肯定是对图像进行一些预处理，比如灰度处理，二值化处理，仿射变换矫正等对图像进行预处理，使图像统一成设置好的规格。</p><h3 id="定位待识别图像"><a href="#定位待识别图像" class="headerlink" title="定位待识别图像"></a><strong>定位待识别图像</strong></h3><p>对图像进行预处理之后，接下来就是要找到图像中包含待识别文本的图像，一般分为两种思路，单字符提取和文本行提取</p><h4 id="单字符提取"><a href="#单字符提取" class="headerlink" title="单字符提取"></a><strong>单字符提取</strong></h4><p>这算是模式识别中比较常用的传统的方法，主要针对的印刷体文档图片，并且英文相比中文的提取效果很好(中文中包含很多左右文字结构，而不像英文都是使用空格进行各个字符分割)。具体的实现方法有如下几种：</p><ul><li>对图像进行二值化处理，仿射变换等预处理；对图像进行竖直方向投影，提取出文本行；对文本行在水平方向投影，提取出单个字符，以来设置的阈值进行字符提取，这里就可以看出对于中文来说，进行水平方向投影时，如果阈值设置的不合适，会存在将一个左右结构的汉字拆分成两个单独字符的风险，比如“从”可能会被提取成“人”“人”；</li><li>假定字符本身是具有连通性的，然后通过连通区域的检测方法找到文字字符的候选。</li><li>通过最大稳定极值区域（<code>MSER-Maximally Stable Extremal Regions</code>）得到字符的候选，并将这些字符候选看作连通图(graph)的顶点，此时就可以将文本行的寻找过程视为聚类（<code>clustering</code>）的过程，因为来自相同文本行的文本通常具有相同的方向、颜色、字体以及形状。最后使用一个文本分类器滤除非文本部分。</li><li>北科大殷绪成教授研究组的一个工作对文本的信息进行了更加全面的考虑，使用了文本的颜色、笔画宽度、字符方向（<code>orientation</code>）以及投影的特征。</li></ul><h4 id="文本行提取"><a href="#文本行提取" class="headerlink" title="文本行提取"></a><strong>文本行提取</strong></h4><p>由于外部因素和内部因素，场景文本检测具有一定的挑战性。外部因素源自环境，例如噪声、模糊和遮挡，它们也是一般目标检测中存在的主要问题。内部因素是由场景文本的属性和变化引起的。与一般目标检测相比，场景文本检测更加复杂，因为：</p><ol><li><p>场景文本可能以任意方向存在于自然图像中，因此边界框可能是旋转的矩形或者四边形；</p></li><li><p>场景文本边界框的长宽比变化比较大；</p></li><li><p>因为场景文本的形式可能是字符、单词或者文本行的形式，所以在定位边界的时候算法可能会发生混淆。</p></li></ol><p>基于一般目标检测和语义分割模型，几个精心设计的模型使得文本检测能够更加准确地进行。使用最广泛的是基于<code>RegionProposal</code>的方法，其次是基于图像分割的方法。文本检测模型的目标是从图片中尽可能准确地找出文字所在区域。</p><p>一般目标检测器（<code>SSD</code>，<code>YOLO</code> 和 <code>DenseBox</code> ）为基础，例如 <code>TextBoxes</code>，<code>FCRN</code> 以及 <code>EAST</code>，<code>SegLink</code>等，它们直接预测候选的边界框。</p><p>以语义分割为基础，例如<code>PixelLink</code>和<code>FTSN</code>，它们生成分割映射，然后通过后处理生成最终的文本边界框。</p><p>视觉领域常规物体检测方法(<code>SSD</code>, <code>YOLO</code>, <code>FasterRCNN</code>等)直接套用于文字检测任务效果并不理想， 主要原因如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">·相比于常规物体，文字行长度、长宽比例变化范围很大。</span><br><span class="line">·文本行是有方向性的。常规物体边框BBox的四元组描述方式信息量不充足。</span><br><span class="line">·自然场景中某些物体局部图像与字母形状相似，如果不参考图像全局信息将有误报。</span><br><span class="line">·有些艺术字体使用了弯曲的文本行，而手写字体变化模式也很多。</span><br><span class="line">·由于丰富的背景图像干扰，手工设计特征在自然场景文本识别任务中不够鲁棒。</span><br></pre></td></tr></table></figure><p>针对上述问题根因，近年来出现了各种基于深度学习的技术解决方案。它们从特征提取、区域建议网络(RPN)、多目标协同训练、Loss改进、非极大值抑制（NMS）、半监督学习等角度对常规物体检测方法进行改造，极大提升了自然场景图像中文本检测的准确率。例如：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">·CTPN方案中，用BLSTM模块提取字符所在图像上下文特征，以提高文本块识别精度。</span><br><span class="line">·RRPN等方案中，文本框标注采用BBOX +方向角度值的形式，模型中产生出可旋转的文字区域候选框，并在边框回归计算过程中找到待测文本行的倾斜角度。</span><br><span class="line">·DMPNet等方案中，使用四边形（非矩形）标注文本框，来更紧凑的包围文本区域。</span><br><span class="line">·SegLink将单词切割为更易检测的小文字块，再预测邻近连接将小文字块连成词。</span><br><span class="line">·TextBoxes等方案中，调整了文字区域参考框的长宽比例，并将特征层卷积核调整为长方形，从而更适合检测出细长型的文本行。</span><br><span class="line">·FTSN方案中，作者使用Mask-NMS代替传统BBOX的NMS算法来过滤候选框。</span><br><span class="line">·WordSup方案中，采用半监督学习策略，用单词级标注数据来训练字符级文本检测模型。</span><br></pre></td></tr></table></figure><h5 id="CTPN-2016年"><a href="#CTPN-2016年" class="headerlink" title="CTPN(2016年)"></a><code>CTPN</code>(2016年)</h5><p><a href="https://arxiv.org/abs/1609.03605" target="_blank" rel="noopener">论文链接Connectionist Text Proposal Network</a></p><p>该算法由华南理工大学金连文老师研究组提出，该算法脱胎于<code>FasterRcnn</code>，<code>CTPN</code>是目前流传最广、影响最大的开源文本检测模型，可以检测水平或微斜的文本行。在<code>FasterRcnn</code>基础上去掉了<code>ROI</code>层，引入了不同的<code>anchor</code>设置，<code>anchor</code>设置为固定宽度不同高度，来模拟不同高度的文本行，加入了<code>RNN</code>网络，使用<code>RNN</code>对目标的位置偏移和置信度得分的计算，具体的实现路径会在接下来进行详细的分析。该算法虽然很准，但是由于使用了<code>RNN</code>网络，拖慢了网络速度。</p><p>在<code>CTPN</code>中文本行可以被看成一个序列<code>sequence</code>，而不是一般物体检测中单个独立的目标。同一文本行上各个字符图像间可以互为上下文，在训练阶段让检测模型学习图像中蕴含的这种上下文统计规律，可以使得预测阶段有效提升文本块预测准确率。<code>CTPN</code>模型的图像预测流程中，basenet为<code>VGG16</code>做基础网络来提取各字符的局部图像特征，之后使用<code>BLSTM</code>层提取字符序列上下文特征，并根据<code>BLSTM</code>得到的feature为输入进行全连接输出，得到每个anchor的目标预测概率和预测的box，后续的处理部分和fasterRCNN网络的RPN网络相同，经过预测分支输出各个文字块的坐标值和分类结果概率值。在数据后处理阶段，将合并相邻的小文字块为文本行。</p><h5 id="RRPN-ECCV2017"><a href="#RRPN-ECCV2017" class="headerlink" title="RRPN(ECCV2017)"></a><code>RRPN</code>(ECCV2017)</h5><p><a href="https://arxiv.org/abs/1703.01086" target="_blank" rel="noopener">Arbitrary-Oriented Scene Text Detection via Rotation Proposals</a></p><p><strong>这是第一个在场景文字检测中使用RNN的方法，但其主要用于水平文字的场景</strong>。基于旋转区域候选网络（<code>RRPN, Rotation Region Proposal Networks</code>）的方案，将旋转因素并入经典区域候选网络（如<code>FasterRCNN</code>）。这种方案中，一个文本区域的<code>ground truth</code>被表示为具有$5$元组$(x,y,h,w,\theta)$的旋转边框, 坐标$(x,y)$表示边框的几何中心, 高度$h$设定为边框的短边，宽度$w$为长边，方向是长边的方向。训练时，首先生成含有文本方向角的倾斜候选框，然后在边框回归过程中学习文本方向角。</p><p><code>RRPN</code>中方案中提出了旋转感兴趣区域（<code>RRoI，Rotation Region-of-Interest</code>）池化层，将任意方向的区域建议先划分成子区域，然后对这些子区域分别做<code>max pooling</code>、并将结果投影到具有固定空间尺寸小特征图上。</p><img src="/2019/01/05/深度学习-OCR_Overview/RRPN.png" title="RRPN"><hr><h5 id="FTSN-2018"><a href="#FTSN-2018" class="headerlink" title="FTSN(2018)"></a><code>FTSN</code>(2018)</h5><p><a href="https://arxiv.org/pdf/1709.03272.pdf" target="_blank" rel="noopener">Fused Text Segmentation Networks for Multi-oriented Scene Text Detection</a></p><p><code>FTSN（Fused Text Segmentation Networks）</code>模型使用<strong>分割网络支持倾斜文本检测</strong>。它使用<code>Resnet-101</code>做基础网络，使用了多尺度融合的特征图。标注数据包括文本实例的像素掩码和边框，使用像素预测与边框检测多目标联合训练。</p><p>基于文本实例间像素级重合度的<code>Mask-NMS</code>， 替代了传统基于水平边框间重合度的<code>NMS</code>算法。下图左边子图是传统<code>NMS</code>算法执行结果，中间白色边框被错误地抑制掉了。下图右边子图是<code>Mask-NMS</code>算法执行结果， 三个边框都被成功保留下来。</p><img src="/2019/01/05/深度学习-OCR_Overview/FTSN.png" title="FSTN"><hr><h5 id="DMPNet-2017"><a href="#DMPNet-2017" class="headerlink" title="DMPNet(2017)"></a><code>DMPNet</code>(2017)</h5><p><a href="https://arxiv.org/pdf/1703.01425.pdf" target="_blank" rel="noopener">Deep Matching Prior Network: Toward Tighter Multi-oriented Text Detection</a></p><p><code>DMPNet</code>（<code>Deep Matching Prior Network</code>）中，使用四边形（非矩形）来更紧凑地标注文本区域边界，其训练出的模型对倾斜文本块检测效果更好。</p><p>如下图所示，它使用滑动窗口在特征图上获取文本区域候选框，候选框既有正方形的、也有倾斜四边形的。接着，使用基于像素点采样的<code>Monte-Carlo</code>方法，来快速计算四边形候选框与标注框间的面积重合度。然后，计算四个顶点坐标到四边形中心点的距离，将它们与标注值相比计算出目标<code>Loss</code>。文章中推荐用<code>LnLoss</code>来取代<code>L1</code>、<code>L2Loss</code>，从而对大小文本框都有较快的训练回归速度。</p><img src="/2019/01/05/深度学习-OCR_Overview/DMPNet_anchors.png" title="DMPNet_anchors"><hr><h5 id="EAST-2017"><a href="#EAST-2017" class="headerlink" title="EAST(2017)"></a><code>EAST</code>(2017)</h5><p><a href="https://arxiv.org/pdf/1704.03155.pdf" target="_blank" rel="noopener">An Efficient and Accurate Scene Text Detector</a></p><p><code>EAST（An Efficient and Accurate Scene Text Detector）</code>模型中，首先使用全卷积网络（<code>FCN</code>）生成多尺度融合的特征图，作者使用了<code>PVANet</code>作为特征提取网络，然后在此基础上直接进行像素级的文本块预测。<strong>该模型中，支持旋转矩形框、任意四边形两种文本区域标注形式。</strong></p><ul><li>对应于四边形标注，模型执行时会对特征图中每个像素预测其到四个顶点的坐标差值。</li><li>对应于旋转矩形框标注，模型执行时会对特征图中每个像素预测其到矩形框四边的距离、以及矩形框的方向角。</li></ul><p>上述过程中，省略了其他模型中常见的区域建议、单词分割、子块合并等步骤，因此<strong>该模型的执行速度很快。</strong></p><img src="/2019/01/05/深度学习-OCR_Overview/EAST.png" title="EAST"><hr><h5 id="Textboxes-2016"><a href="#Textboxes-2016" class="headerlink" title="Textboxes(2016)"></a><code>Textboxes(2016)</code></h5><p><a href="https://arxiv.org/pdf/1611.06779.pdf" target="_blank" rel="noopener">TextBoxes: A Fast Text Detector with a Single Deep Neural Network</a></p><p><a href="https://zhuanlan.zhihu.com/p/43545190" target="_blank" rel="noopener">文本检测之TextBoxes</a></p><p><code>Textboxes</code>由华中科技大学的白翔组$2016$年提出。该模型是基于<code>SSD</code>框架的图文检测模型，训练方式是端到端的，运行速度也较快。在SSD基础上针对文字的形状做出了如下的改进，模型结构如下图所示</p><ul><li>为了适应文字行细长型的特点，候选框的长宽比增加了$1,2,3,5,7,10$这样初始值；</li><li>为了适应文本行细长型特点，特征层也用<strong>长条形卷积核</strong>代替了其他模型中常见的正方形($1 \times 1, 3\times 3$)卷积核；</li><li>为了防止漏检文本行，还在垂直方向增加了候选框数量；</li><li>为了检测大小不同的字符块，在多个尺度的特征图上并行预测文本框， 然后对预测结果做<code>NMS</code>过滤；</li><li>使用识别模型对文字进行过滤和判断，提出了一个实用的 “检测+识别”的框架。</li></ul><img src="/2019/01/05/深度学习-OCR_Overview/TextBoxes.png" title="TextBoxes"><hr><h5 id="SegLink-2017"><a href="#SegLink-2017" class="headerlink" title="SegLink(2017)"></a><code>SegLink</code>(2017)</h5><p><a href="https://arxiv.org/pdf/1703.06520.pdf" target="_blank" rel="noopener">论文链接 Detecting Oriented Text in Natural Images by Linking Segments</a></p><p><a href="https://zhuanlan.zhihu.com/p/37781277" target="_blank" rel="noopener">文本检测之SegLink</a></p><p><a href="https://www.cnblogs.com/lillylin/p/6596731.html" target="_blank" rel="noopener">白翔论文分析</a></p><p><code>SegLink</code>模型同样由白翔组提出，由<code>SSD</code>改进得来，<code>SegLink</code>模型的标注数据中，先将每个单词切割为更易检测的有方向的小文字块（$segment=(x, y, w, h, \theta)$），然后用邻近连接（<code>link</code> ）将各个小文字块连接成单词。这种方案方便于识别长度变化范围很大的、带方向的单词和文本行，它不会像<code>FasterRCNN</code>等方案因为候选框长宽比例原因检测不出长文本行。<strong>相比于<code>CTPN</code>等文本检测模型，<code>SegLink</code>的图片处理速度快很多。</strong></p><ol><li>首先给定一个含有文本边界框（<code>bounding box</code>）的图片，先使用<code>CNN</code>提取图像的特征；</li><li>然后用<code>BLSTM</code>学习文字的空间上下文信息；</li><li>最后对特征进行编码并得到最终的预测结果。</li></ol><blockquote><p>与<code>CTPN</code>方法相比，<code>SegLink</code>引入了带方向的<code>bbox</code>(即文中说的<code>segment</code>$(x, y, w, h, \theta)$)，<strong>它可以检测任意方向的文本行，而<code>CTPN</code>主要用于检测水平的文本行，当然如果将垂直<code>anchor</code>改成水平<code>anchor</code>，也可以检测垂直方向的文本行</strong>；</p><p>与<code>EAST</code>方法相比，<code>SegLink</code>利用不同的<code>feature map</code>分别进行预测，而<code>EAST</code>是将不同的<code>feature map</code>层先进行合并再预测；</p><p>不能检测很大的文本，这是因为<code>link</code>主要是用于连接相邻的<code>segments</code>，而不能用于检测相距较远的文本行；</p><p>不能检测形变或者曲线文本，这是因为<code>segments combining</code>算法在合并的时候采用的是直线拟合．这里可以通过修改合并算法，来检测变形或曲线文本</p></blockquote><p>整个过程可以端到端（<code>end-to-end</code>）完成。<strong>提出的定位和识别模型结合之后能得到目前端到端模型中最好的文字检测结果。</strong></p><img src="/2019/01/05/深度学习-OCR_Overview/SegLink.png" title="SegLink"><hr><h5 id="TextBoxes-2018"><a href="#TextBoxes-2018" class="headerlink" title="TextBoxes++(2018)"></a><code>TextBoxes++</code>(2018)</h5><p><a href="https://arxiv.org/pdf/1801.02765.pdf" target="_blank" rel="noopener">TextBoxes++: A Single-Shot Oriented Scene Text Detector</a></p><p><a href="https://zhuanlan.zhihu.com/p/33723456" target="_blank" rel="noopener">论文笔记：TextBoxes++</a></p><p><a href="https://github.com/MhLiao/TextBoxes_plusplus" target="_blank" rel="noopener">Github: TextBoxes++</a></p><p><code>Textboxes++</code>是<code>Textboxes</code>的升级版本，目的是增加对倾斜文本的支持。为此，将标注数据改为了旋转矩形框和不规则四边形的格式；对候选框的长宽比例、特征图层卷积核的形状都作了相应调整。·</p><img src="/2019/01/05/深度学习-OCR_Overview/TextBoxes++.png" title="TextBoxes++"><hr><h5 id="PixelLink-2018"><a href="#PixelLink-2018" class="headerlink" title="PixelLink(2018)"></a><code>PixelLink</code>(2018)</h5><p><a href="https://arxiv.org/pdf/1801.01315.pdf" target="_blank" rel="noopener">论文链接 Detecting Scene Text via Instance Segmentation</a></p><p>自然场景图像中一组文字块经常紧挨在一起，通过语义分割方法很难将它们识别开来，所以阿里开发了<code>PixelLink</code>模型尝试用<strong>实例分割</strong>方法解决这个问题。</p><p>该模型的特征提取部分，为<code>VGG16</code>基础上构建的<code>FCN</code>网络。模型执行流程如下图所示。首先，借助于<code>CNN</code> 模块执行两个像素级预测：一个文本二分类预测，一个链接二分类预测。接着，用正链接去连接邻居正文本像素，得到文字块实例分割结果。然后，由分割结果直接就获得文字块边框， 而且允许生成倾斜边框。</p><p>上述过程中，<strong>省掉了其他模型中常见的边框回归步骤，因此训练收敛速度更快些</strong>。训练阶段，使用了平衡策略，使得每个文字块在总<code>LOSS</code>中的权值相同。训练过程中，<strong>通过预处理增加了各种方向角度的文字块实例。</strong></p><blockquote><ul><li><p>与CTPN，EAST，SegLink相比，PixelLink放弃了边框回归方法来检测文本行的bbox，而是采用实例分割方法，直接从分割的文本行区域得到文本行的bbox．PixelLink可以以更少额数据和更快地速度进行训练。</p></li><li><p>假设提取特征的主干网络结构采用VGG16(当然也可以采用其它主干网络结构)，PixelLink不需要在imagenet预训练的模型上进行fine-tuned（即直接从头开始训练），而CTPN，EAST，SegLink都需要在imagenet预训练的模型上进行fine-tuned；、</p></li><li>与CTPN，EAST，SegLink相比，PixelLink对感受野的要求更少，因为每个神经元值只负责预测自己及其邻域内的状态</li><li>与SegLink一样，不能检测很大的文本，这是因为link主要是用于连接相邻的segments，而不能用于检测相距较远的文本行</li></ul></blockquote><img src="/2019/01/05/深度学习-OCR_Overview/PixelLink.png" title="PixelLink"><hr><h5 id="WordSup-2017"><a href="#WordSup-2017" class="headerlink" title="WordSup(2017)"></a><code>WordSup</code>(2017)</h5><p><a href="https://arxiv.org/abs/1708.06720" target="_blank" rel="noopener">WordSup: Exploiting Word Annotations for Character based Text Detection</a></p><p>百度提出，在数学公式图文识别、不规则形变文本行识别等应用中，字符级检测模型是一个关键基础模块。由于字符级自然场景图文标注成本很高、相关公开数据集稀少，导致现在多数图文检测模型只能在文本行、单词级标注数据上做训练。<code>WordSup</code>提出了一种弱监督的训练框架， 可以文本行、单词级标注数据集上训练出字符级检测模型。</p><p><code>WordSup</code>弱监督训练框架中，两个训练步骤被交替执行：给定当前字符检测模型，并结合单词级标注数据，计算出字符中心点掩码图； 给定字符中心点掩码图，有监督地训练字符级检测模型。</p><p>训练好字符检测器后，可以在数据流水线中加入合适的文本结构分析模块，以输出符合应用场景格式要求的文本内容。该文作者例举了多种文本结构分析模块的实现方法。</p><img src="/2019/01/05/深度学习-OCR_Overview/WordSup.png" title="WordSup"><hr><h5 id="基于角定位与区域分割-2018"><a href="#基于角定位与区域分割-2018" class="headerlink" title="基于角定位与区域分割(2018)"></a>基于角定位与区域分割(2018)</h5><p><a href="https://arxiv.org/pdf/1802.08948.pdf" target="_blank" rel="noopener">Multi-Oriented Scene Text Detection via Corner Localization and Region Segmentation</a></p><p><strong>主要是为了解决场景文本多方向，长宽比变化较大等场景文本检测中的难点问题。</strong></p><p>该方法用一个端到端网络完成文字检测整个过程——除了基础卷积网络（<code>backbone</code>）外，包括两个并行分支和一个后处理。第一个分支是通过一个DSSD网络进行角点检测来提取候选文字区域，第二个分支是利用类似于<code>RFCN</code>进行网格划分的方式来做<code>position-sensitive</code>的<code>segmentation</code>。后处理是利用<code>segmentation</code>的<code>score map</code>的综合得分，过滤角点检测得到的候选区域中的噪声。</p><p><strong>文章亮点</strong></p><ul><li>检测不是用一般的<code>object detection</code>的框架来做，而是用<code>corner point detection</code>来做。（可以更好解决文字方向任意、文字长宽比很大的文本，不会受到感受野的影响）</li><li>分割用的是<code>position sensitive segmentation</code>，仿照<code>RFCN</code>划分网格的思路，把位置信息融合进去（对于检测单词这种细粒度的更有利）</li><li>把检测+分割两大类的方法整合起来，进行综合打分的<code>pipeline</code>（可以使得检测精度更高）</li></ul><img src="/2019/01/05/深度学习-OCR_Overview/corner_overview.png" title="Overview of our method. Given an image, the network outputs corner points and segmentation maps by corner detection and position-sensitive segmentation. Then candidate boxes are generated by sampling and grouping corner points. Finally, those candidate boxes are scored by segmentation maps and suppressed by NMS"><ul><li><strong>backbone</strong>：<strong>基础网络，用来特征提取（不同分支特征共享）</strong></li><li><strong>corner detection：</strong>用来生成候选检测框，是一个独立的检测模块，类似于RPN的功能</li><li><strong>Position Sensitive Segmentation：</strong>整张图逐像素的打分，和一般分割不同的是输出4个score map，分别对应左上、左下、右上、右下不同位置的得分</li><li><strong>Scoring + NMS：</strong>综合打分，利用（2）的框和（3）的score map再综合打分，去掉非文字框，最后再接一个NMS</li></ul><img src="/2019/01/05/深度学习-OCR_Overview/corner.png" title="Network Architecture"><ul><li><code>Backbone</code>取自DSSD = VGG16(pool5) + conv6(fc6) + conv7(fc7) + 4conv + 6 deconv (with 6 residual block)</li><li><code>Corner Point Detection</code>是类似于<code>SSD</code>，从多个deconv的feature map上单独做detection得到候选框，然后多层的检测结果串起来nms后为最后的结果</li></ul><hr><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><ul><li>多个数据集中不同模型的表现</li></ul><img src="/2019/01/05/深度学习-OCR_Overview/overview.png" title="overview"><ul><li>ICDAR2013数据集表现</li></ul><img src="/2019/01/05/深度学习-OCR_Overview/ICDAR2013.png" title="ICDAR2013"><ul><li>ICDAR2015数据集表现</li></ul><img src="/2019/01/05/深度学习-OCR_Overview/ICDAR2015.png" title="ICDAR2015"><p>图中蓝色为召回率(实际为真的样本中预测为真的概率，衡量检索的覆盖率)，绿色为精确率(预测为真的样本中实际为真的概率，衡量检测的信噪比)，橙色为Fscore，图中显示$0.00$的数据表示论文中没有给出相应得分。具体可以参考另一篇文章<a href="http://xiaofengshi.com/2018/11/20/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0-%E5%B8%B8%E7%94%A8%E7%9F%A5%E8%AF%86%E7%82%B9/">机器学习-常用知识点</a>[可以直观的看到各个模型的评价指标值，在IC2015数据集上，<code>PixelLink_vgg2</code>对应的模型效果是最好的</p><h3 id="文本识别"><a href="#文本识别" class="headerlink" title="文本识别"></a><strong>文本识别</strong></h3><p>提取待识别图像之后就可以对图像进行识别了，和定位待识别图像相对应，同样是两种类型识别任务</p><h4 id="单字符识别"><a href="#单字符识别" class="headerlink" title="单字符识别"></a><strong>单字符识别</strong></h4><h5 id="k近邻"><a href="#k近邻" class="headerlink" title="k近邻"></a><strong>k近邻</strong></h5><p>该方法很好理解，数据库中保存着已经标注好label的不同的字符的图像，对于新的图像，识别时，比较待识别的图像和数据库中图像的距离，选择距离最小的类别对应的label为当前图像的识别结果，但是该方法存在一个问题，那就是计算量太大了，要想提高识别的准确性，就需要数据库中存储的数据要足够多，与此同时，在识别时计算量也会增加，识别一个图像要跟整个数据库中的所有样本进行距离计算</p><h5 id="图像分类模型"><a href="#图像分类模型" class="headerlink" title="图像分类模型"></a><strong>图像分类模型</strong></h5><p>借着深度学习的东风，使用图像识别算法进行单字符识别效果简直不要太好，相关的技术方法有很多，包括<code>VggNet</code>，<code>ResNet</code>，<code>InceptionNet</code>，<code>DenseNet</code>等，具体的可以参考本人的另一篇文字<a href="http://xiaofengshi.com/2018/11/23/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0-%E5%9B%BE%E5%83%8F%E8%AF%86%E5%88%AB/">深度学习-图像识别</a>。</p><h4 id="不定长字符识别"><a href="#不定长字符识别" class="headerlink" title="不定长字符识别"></a><strong>不定长字符识别</strong></h4><p>目前基本都是使用的<code>CTC</code>策略(稍后详解)，对于特征提取的基础网络目前主要使用的是<code>CRNN</code>和<code>Densenet</code></p><h5 id="CRNN-CTC-2015"><a href="#CRNN-CTC-2015" class="headerlink" title="CRNN+CTC(2015)"></a><code>CRNN+CTC</code>(2015)</h5><p><a href="https://arxiv.org/pdf/1507.05717.pdf" target="_blank" rel="noopener">An End-to-End Trainable Neural Network for Image-based Sequence Recognition and Its Application to Scene Text Recognition</a></p><p><strong><code>CRNN</code>(<code>Convolutional Recurrent Neural Network</code>）是目前较为流行的图文识别模型，可识别较长的文本序列。</strong></p><ul><li><p>包含<code>CNN</code>特征提取层和<code>BLSTM</code>序列特征提取层，能够进行端到端的联合训练。 </p></li><li><p>利用<code>BLSTM</code>和<code>CTC</code>部件学习字符图像中的上下文关系， 从而有效提升文本识别准确率，使得模型更加鲁棒。</p></li></ul><p>预测过程中，前端使用标准的<code>CNN</code>网络提取文本图像的特征，利用<code>BLSTM</code>将特征向量进行融合以提取字符序列的上下文特征，然后得到每列特征的概率分布，最后通过转录层(<code>CTC rule</code>)进行预测得到文本序列。</p><img src="/2019/01/05/深度学习-OCR_Overview/CRNN.png" title="CRNN"><h5 id="DenseNet-CTC"><a href="#DenseNet-CTC" class="headerlink" title="DenseNet+CTC"></a><code>DenseNet+CTC</code></h5><p>该框架和<code>CRNN</code>相似，使用<code>DenseNet</code>替换了原来的<code>CNN+BLSM</code>，提升了模型的运行速度，但是DenseNet由于block层对特征图的重复利用会非常占显存。</p><h5 id="RARE-2016"><a href="#RARE-2016" class="headerlink" title="RARE(2016)"></a><code>RARE</code>(2016)</h5><p><a href="https://arxiv.org/pdf/1603.03915.pdf" target="_blank" rel="noopener">Robust Scene Text Recognition with Automatic Rectification</a></p><p><strong><code>RARE</code>模型在识别变形的图像文本时效果很好。</strong>模型预测过程中，输入图像首先要被送到一个空间变换网络(<code>Spatial Transformer Network</code>)中做处理，矫正过的图像然后被送入序列识别网络中得到文本预测结果。空间变换网络如下图所示。网络模型有<code>STN+SRN</code>组成。</p><img src="/2019/01/05/深度学习-OCR_Overview/RARE_STN.png" title="STNet"><p><strong>空间变换网络内部包含定位网络(Localization Network)、网格生成器(Grid Generator)、采样器(Sampler)三个部件</strong>。经过训练后，它可以根据输入图像的特征图动态地产生空间变换网格，然后采样器根据变换网格核函数从原始图像中采样获得一个矩形的文本图像。<code>RARE</code>中支持一种称为<code>TPS（thin-plate splines）</code>的空间变换，从而能够比较准确地识别透视变换过的文本、以及弯曲的文本。</p><p>经过矫正之后的图像送入序列识别网络(<code>Sequence Recognition Network</code>)，网络结构如下图所示，为一个<code>Encoder_Decoder</code>模型，结构为<code>CNN+BLSTM+ATT_GRU+Softmax</code>，使用极小化对数似然估计的方法，优化方法为<code>ADADELTA</code></p><img src="/2019/01/05/深度学习-OCR_Overview/RARE_SRN.png" title="SRN"><hr><h5 id="FocusingAttention-2017"><a href="#FocusingAttention-2017" class="headerlink" title="FocusingAttention(2017)"></a><code>FocusingAttention</code>(2017)</h5><p><a href="https://arxiv.org/pdf/1709.02054.pdf" target="_blank" rel="noopener">Focusing Attention: Towards Accurate Text Recognition in Natural Images</a></p><p><strong>利用<code>attention model</code>去做序列文字识别，可能会因为图像分辨率较低、遮挡、文字间间隔较大等问题而导致<code>attention</code>位置并不是很准，从而造成字符的错误识别</strong>。<strong>海康威视</strong>在ICCV2017上提出使用字符像素级别的监督信息使<code>attention</code>更加准确地聚焦在文字区域，从而使识别变得更精准。他们用了部分像素级别的标注，有了类别信息以后做多任务，结果较为精准。并且只要部分字符的标注就可以带来网络性能的一定提升。</p><img src="/2019/01/05/深度学习-OCR_Overview/FocusingAttention.png" title="FocusingAttention"><hr><h5 id="AON-2018"><a href="#AON-2018" class="headerlink" title="AON (2018)"></a>AON (2018)</h5><p><a href="https://arxiv.org/pdf/1711.04226.pdf" target="_blank" rel="noopener">AON: Towards Arbitrarily-Oriented Text Recognition</a></p><p><strong>针对有形变或者任意方向文字的识别问题</strong>，Cheng等人在CVPR2018上提出了该模型。他们在水平方向之外加了一个竖直方向的双向LSTM，这样的话就有从上到下，从下到上，从左到右，从右到左四个方向序列的特征建模。接下来引入一个权重，该权重用来表示来自不同方向的特征在识别任务中发挥作用的重要性。这对性能有一定提升，尤其是对任意排列的文字识别。</p><img src="/2019/01/05/深度学习-OCR_Overview/AON.png" title="AON"><h5 id="ASTER-2018"><a href="#ASTER-2018" class="headerlink" title="ASTER(2018)"></a>ASTER(2018)</h5><p><a href="https://www.researchgate.net/publication/325993414_ASTER_An_Attentional_Scene_Text_Recognizer_with_Flexible_Rectification" target="_blank" rel="noopener">ASTER_An Attentional Scene Text Recognizer with Flexible Rectification</a></p><p>主要解决<strong>不规则排列文字</strong>的<strong>文字识别</strong>问题，论文为之前一篇<code>RARE</code>的改进版（journal版）</p><p><strong>主要思路</strong>：</p><ul><li>针对不规则文字，先矫正成正常线性排列的文字，再识别；</li><li>整合矫正网络和识别网络成为一个端到端网络来训练；</li><li>矫正网络使用STN，识别网络用经典的<code>seq2seq+attention</code></li></ul><p><strong>和<code>STN</code>的不同点</strong></p><p>本文在输入网络前将原图resize成小的图，然后在该小图上预测control point，而输入到Grid Generator或Sample计算的时候又映射回原图大小。这样的目的是<strong>为了减小网络参数，降低计算量</strong>（但有没有可能小图对于control point的prediction会不准？对于识别来讲，每个word的patch块本身就比较小了，而且小图映射回大图的点位置这个误差比例就会放大？）</p><p><strong>和<code>RARE</code>的不同点</strong></p><p>网络最后fc层的激活函数不是用tanh，而是直接对值进行clipping（具体怎么clip论文没说），这样做的目的是<strong>为了解决采样点可能落到图外面的问题，以及加快了网络训练的收敛速度</strong>，论文中对此没有解释本质原因，只是说明实验证明如此。</p><p>按照论文所述，ASTER和RARE之间的网络结构基本没有大幅度的变化，仍然是先矫正后识别，并且矫正模型和识别模型基本相同，但是效果相比RARE好了太多，不知为何。ASTER的网络参数配置如下所示</p><img src="/2019/01/05/深度学习-OCR_Overview/ASTER_PARAMS.png" title="ASTER网络参数"><hr><h4 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h4><p>目前主要的<code>OCR</code>项目使用的是如上的这些技术，比如<code>CTPN+CRNN</code>，<code>CTPN+DenseNet</code>，<code>YOLO+DenseNet</code>，<code>PixelLink+RARE</code>等，接下来针对上面这些技术点结合论文及代码进行一些个人的浅薄分析。</p><h3 id="End2End"><a href="#End2End" class="headerlink" title="End2End"></a><code>End2End</code></h3><p>端对端模型，直接从图片中定位并识别出包含的文本内容</p><h4 id="FOTS-2018"><a href="#FOTS-2018" class="headerlink" title="FOTS(2018)"></a><code>FOTS</code>(2018)</h4><p><a href="https://arxiv.org/pdf/1801.01671.pdf" target="_blank" rel="noopener">FOTS: Fast Oriented Text Spotting with a Unified Network</a></p><p><code>FOTS</code>是图像文本检测与识别同步训练、端到端可学习的网络模型。检测和识别任务共享卷积特征层，既节省了计算时间，也比两阶段训练方式学习到更多图像特征。引入了旋转感兴趣区域（<code>RoIRotate</code>）, 可以从卷积特征图中产生出定向的文本区域，从而支持倾斜文本的识别。</p><img src="/2019/01/05/深度学习-OCR_Overview/FOTS.png" title="FOTS"><hr><h4 id="STN-OCR-2017"><a href="#STN-OCR-2017" class="headerlink" title="STN-OCR(2017)"></a><code>STN-OCR</code>(2017)</h4><p><a href="https://arxiv.org/pdf/1707.08831.pdf" target="_blank" rel="noopener">STN-OCR: A single Neural Network for Text Detection and Text Recognition</a></p><p><code>STN-OCR</code>是集成了了图文检测和识别功能的端到端可学习模型。在它的检测部分嵌入了一个空间变换网络（<code>STN</code>）来对原始输入图像进行仿射（<code>affine</code>）变换，类似于<code>RARE</code>中的<code>STN</code>网络。利用这个空间变换网络，可以对检测到的多个文本块分别执行旋转、缩放和倾斜等图形矫正动作，从而在后续文本识别阶段得到更好的识别精度。在训练上<code>STN-OCR</code>属于<strong>半监督学习方法，只需要提供文本内容标注，而不要求文本定位信息</strong>。作者也提到，<strong>如果从头开始训练则网络收敛速度较慢，因此建议渐进地增加训练难度</strong>。<code>STN-OCR</code>已经开放了工程源代码和预训练模型。</p><img src="/2019/01/05/深度学习-OCR_Overview/STN-OCR.png" title="STN-OCR"><hr><h4 id="Mask-TextSpotter-2018"><a href="#Mask-TextSpotter-2018" class="headerlink" title="Mask TextSpotter(2018)"></a>Mask TextSpotter(2018)</h4><p>该文受到<code>MaskRCNN</code>的启发提出了一种用于场景text spotting的可端到端训练的神经网络模型：M<code>ask TextSpotter</code>。与以前使用端到端可训练深度神经网络完成text spotting的方法不同，Mask TextSpotter利用简单且平滑的端到端学习过程，通过语义分割获得精确的文本检测和识别。此外，它在处理不规则形状的文本实例（例如，弯曲文本）方面优于之前的方法。</p><h3 id="数据集"><a href="#数据集" class="headerlink" title="数据集"></a>数据集</h3><h2 id="训练数据集"><a href="#训练数据集" class="headerlink" title="训练数据集"></a><strong>训练数据集</strong></h2><p>本章将列举可用于文本检测和识别领域模型训练的一些大型公开数据集， 不涉及仅用于模型fine-tune任务的小型数据集。</p><h3 id="Chinese-Text-in-the-Wild-CTW"><a href="#Chinese-Text-in-the-Wild-CTW" class="headerlink" title="Chinese Text in the Wild(CTW)"></a><strong>Chinese Text in the Wild(CTW)</strong></h3><p>该数据集包含32285张图像，1018402个中文字符(来自于腾讯街景), 包含平面文本，凸起文本，城市文本，农村文本，低亮度文本，远处文本，部分遮挡文本。图像大小2048*2048，数据集大小为31GB。以(8:1:1)的比例将数据集分为训练集(25887张图像，812872个汉字)，测试集(3269张图像，103519个汉字)，验证集(3129张图像，103519个汉字)。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">文献链接：https://arxiv.org/pdf/1803.00085.pdf </span><br><span class="line">数据集下载地址：https://ctwdataset.github.io/</span><br></pre></td></tr></table></figure><h3 id="Reading-Chinese-Text-in-the-Wild-RCTW-17"><a href="#Reading-Chinese-Text-in-the-Wild-RCTW-17" class="headerlink" title="Reading Chinese Text in the Wild(RCTW-17)"></a><strong>Reading Chinese Text in the Wild(RCTW-17)</strong></h3><p>该数据集包含12263张图像，训练集8034张，测试集4229张，共11.4GB。大部分图像由手机相机拍摄，含有少量的屏幕截图，图像中包含中文文本与少量英文文本。图像分辨率大小不等。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">下载地址http://mclab.eic.hust.edu.cn/icdar2017chinese/dataset.html</span><br><span class="line">文献：http://arxiv.org/pdf/1708.09585v2</span><br></pre></td></tr></table></figure><h3 id="ICPR-MWI-2018-挑战赛"><a href="#ICPR-MWI-2018-挑战赛" class="headerlink" title="ICPR MWI 2018 挑战赛"></a>ICPR MWI 2018 挑战赛</h3><p>大赛提供20000张图像作为数据集，其中50%作为训练集，50%作为测试集。主要由合成图像，产品描述，网络广告构成。该数据集数据量充分，中英文混合，涵盖数十种字体，字体大小不一，多种版式，背景复杂。文件大小为2GB。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">下载地址：</span><br><span class="line">https://tianchi.aliyun.com/competition/information.htm?raceId=231651&amp;_is_login_redirect=true&amp;accounttraceid=595a06c3-7530-4b8a-ad3d-40165e22dbfe</span><br></pre></td></tr></table></figure><h3 id="Total-Text"><a href="#Total-Text" class="headerlink" title="Total-Text"></a><strong>Total-Text</strong></h3><p>该数据集共1555张图像，11459文本行，包含水平文本，倾斜文本，弯曲文本。文件大小441MB。大部分为英文文本，少量中文文本。训练集：1255张 测试集：300</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">下载地址：http://www.cs-chan.com/source/ICDAR2017/totaltext.zip</span><br><span class="line">文献：http:// arxiv.org/pdf/1710.10400v</span><br></pre></td></tr></table></figure><h3 id="Google-FSNS-谷歌街景文本数据集"><a href="#Google-FSNS-谷歌街景文本数据集" class="headerlink" title="Google FSNS(谷歌街景文本数据集)"></a>Google FSNS(谷歌街景文本数据集)</h3><p>该数据集是从谷歌法国街景图片上获得的一百多万张街道名字标志，每一张包含同一街道标志牌的不同视角，图像大小为600*150，训练集1044868张，验证集16150张，测试集20404张。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">下载地址：http://rrc.cvc.uab.es/?ch=6&amp;com=downloads</span><br><span class="line">文献：http:// arxiv.org/pdf/1702.03970v1</span><br></pre></td></tr></table></figure><h3 id="COCO-TEXT"><a href="#COCO-TEXT" class="headerlink" title="COCO-TEXT"></a><strong>COCO-TEXT</strong></h3><p>该数据集，包括63686幅图像，173589个文本实例，包括手写版和打印版，清晰版和非清晰版。文件大小12.58GB，训练集：43686张，测试集：10000张，验证集：10000张</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">文献: http://arxiv.org/pdf/1601.07140v2</span><br><span class="line">下载地址：https://vision.cornell.edu/se3/coco-text-2/</span><br></pre></td></tr></table></figure><h3 id="Synthetic-Data-for-Text-Localisation"><a href="#Synthetic-Data-for-Text-Localisation" class="headerlink" title="Synthetic Data for Text Localisation"></a><strong>Synthetic Data for Text Localisation</strong></h3><p>在复杂背景下人工合成的自然场景文本数据。包含858750张图像，共7266866个单词实例，28971487个字符，文件大小为41GB。该合成算法，不需要人工标注就可知道文字的label信息和位置信息，可得到大量自然场景文本标注数据。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">下载地址：http://www.robots.ox.ac.uk/~vgg/data/scenetext/</span><br><span class="line">文献：http://www.robots.ox.ac.uk/~ankush/textloc.pdf</span><br><span class="line">Code: https://github.com/ankush-me/SynthText (英文版)</span><br><span class="line">Code https://github.com/wang-tf/Chinese_OCR_synthetic_data(中文版)</span><br></pre></td></tr></table></figure><h3 id="Synthetic-Word-Dataset"><a href="#Synthetic-Word-Dataset" class="headerlink" title="Synthetic Word Dataset"></a><strong>Synthetic Word Dataset</strong></h3><p>合成文本识别数据集，包含9百万张图像，涵盖了9万个英语单词。文件大小为10GB</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">下载地址：http://www.robots.ox.ac.uk/~vgg/data/text/</span><br></pre></td></tr></table></figure><h3 id="Caffe-ocr中文合成数据"><a href="#Caffe-ocr中文合成数据" class="headerlink" title="Caffe-ocr中文合成数据"></a><strong>Caffe-ocr中文合成数据</strong></h3><p>数据利用中文语料库，通过字体、大小、灰度、模糊、透视、拉伸等变化随机生成，共360万张图片，图像分辨率为280x32，涵盖了汉字、标点、英文、数字共5990个字符。文件大小约为8.6GB</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">下载地址：https://pan.baidu.com/s/1dFda6R3</span><br></pre></td></tr></table></figure><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul><li><p>综述</p><p><a href="https://zhuanlan.zhihu.com/p/38655369" target="_blank" rel="noopener">自然场景文本检测识别技术综述</a></p><p><a href="https://www.jiqizhixin.com/articles/2018-06-20-16" target="_blank" rel="noopener">白翔:：图像OCR年度进展|VALSE2018之十一</a></p><p><a href="https://zhuanlan.zhihu.com/p/29549641" target="_blank" rel="noopener">白翔：趣谈“捕文捉字”— 场景文字检测 | VALSE2017之十</a></p><p><a href="https://blog.csdn.net/u013250416/article/details/79591263" target="_blank" rel="noopener">基于深度学习的目标检测及场景文字检测研究进展</a></p><p><a href="https://zhuanlan.zhihu.com/p/52335619" target="_blank" rel="noopener">知乎文本检测综述</a></p><p><a href="https://www.cnblogs.com/lillylin/" target="_blank" rel="noopener">优秀论文解读博客</a></p><p><a href="https://zhuanlan.zhihu.com/burness-DL" target="_blank" rel="noopener">知乎专栏:小石头的码疯窝</a></p></li><li><p>文本检测</p><ul><li><p>CTPN</p><p><a href="https://zhuanlan.zhihu.com/p/34757009" target="_blank" rel="noopener">场景文字检测—CTPN原理与实现</a></p><p><a href="https://github.com/eragonruan/text-detection-ctpn" target="_blank" rel="noopener">CTPN: Tensorflow</a></p></li><li><p>EAST</p><p><a href="https://www.cnblogs.com/lillylin/p/9954981.html" target="_blank" rel="noopener">Bolg: EAST</a></p><p><a href="https://zhuanlan.zhihu.com/p/37504120" target="_blank" rel="noopener">知乎：文本检测之EAST</a></p><p><a href="https://github.com/argman/EAST" target="_blank" rel="noopener">EAST：tensorflow</a></p><p><a href="https://github.com/kurapan/EAST" target="_blank" rel="noopener">EAST: Keras</a></p><p><a href="https://github.com/huoyijie/AdvancedEAST" target="_blank" rel="noopener">EAST: Advanced keras</a></p></li><li><p>SegLink</p><p><a href="https://www.cnblogs.com/lillylin/p/6596731.html" target="_blank" rel="noopener">SegLink_Blog</a></p><p><a href="https://zhuanlan.zhihu.com/p/37781277" target="_blank" rel="noopener">文本检测之SegLink</a></p></li><li><p>PixelLink</p><p><a href="https://zhuanlan.zhihu.com/p/38171172" target="_blank" rel="noopener">文本检测之PixelLink</a></p><p><a href="https://github.com/ZJULearning/pixel_link" target="_blank" rel="noopener">Github: PixelLink </a></p></li><li><p>TextBoxes</p><p><a href="https://zhuanlan.zhihu.com/p/33723456" target="_blank" rel="noopener">论文笔记：TextBoxes++: A Single-Shot Oriented Scene Text Detector</a></p><p><a href="https://github.com/MhLiao/TextBoxes_plusplus" target="_blank" rel="noopener">Github: TextBoxes++</a></p></li><li><p>角定位</p></li></ul><p><a href="https://www.cnblogs.com/lillylin/p/8495124.html" target="_blank" rel="noopener">基于角定位于区域分割</a></p></li><li><p>文本识别</p><ul><li><p>ASTER</p><p><a href="https://github.com/bgshih/aster" target="_blank" rel="noopener">Github: ASTER</a></p></li></ul></li><li><p>TextSpotter</p><ul><li><p>Mask TextSpotter</p><p><a href="https://www.jiqizhixin.com/articles/2018-08-24-12" target="_blank" rel="noopener">华科白翔教授团队ECCV2018 OCR论文：Mask TextSpotter</a></p></li></ul></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本篇涉及使用深度学习的方法实现字符识别的任务，该任务与计算机视觉领域内的图像描述任务(&lt;code&gt;ImageCaption&lt;/code&gt;)相似，对于图像描述任务，输入为图像，输出为对该图像的描述；而对于&lt;code&gt;OCR&lt;/code&gt;任务，输入的图像为包含文字或字符的图片，而输出为这张图片中的文字或字符。&lt;/p&gt;
    
    </summary>
    
      <category term="深度学习" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/CV/"/>
    
    
      <category term="深度学习" scheme="http://xiaofengshi.com/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="CV" scheme="http://xiaofengshi.com/tags/CV/"/>
    
      <category term="OCR" scheme="http://xiaofengshi.com/tags/OCR/"/>
    
  </entry>
  
</feed>
